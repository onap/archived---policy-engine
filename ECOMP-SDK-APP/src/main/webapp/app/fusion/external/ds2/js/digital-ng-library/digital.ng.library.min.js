/*! digital-design-library - v1.5.4 - Last updated: 2016-08-09. Copyright (c) 2016 AT&T Services, Inc. */ 
angular.module("ddh.att.tpls", ['template/calendar/datepicker-popup.html', 'template/calendar/datepicker.html', 'template/creditCard/creditCardImage.html', 'template/creditCard/cvcSecurityImg.html', 'template/creditCard/tooltipCardImage.html', 'template/dropdownVariations/ddhDropdownVariations.html', 'template/dropdowns/ddhDropdownDesktop.html', 'template/dropdowns/ddhDropdownGroupDesktop.html', 'template/dropdowns/ddhDropdownListDesktop.html', 'template/expandCollapse/expandCollapse-group.html', 'template/expandCollapse/expandCollapse-table.html', 'template/expandCollapse/expandCollapse.html', 'template/filmstrip/ddhFilmstrip.html', 'template/filmstrip/ddhFilmstripContent.html', 'template/leftNavigation/leftNavigation.html', 'template/loaderAnimation/modalLoader.html', 'template/modalsAndAlerts/backdrop.html', 'template/modalsAndAlerts/window.html', 'template/pagination/pagination.html', 'template/searchField/searchField.html', 'template/socialShareThis/socialShareThis.html', 'template/stepIndicator/stepIndicator.html', 'template/switches/switches-spanish.html', 'template/switches/switches.html', 'template/table/ddhTable.html', 'template/table/ddhTableBody.html', 'template/table/ddhTableHeader.html', 'template/tabs/ddhTab.html', 'template/tabs/ddhTabset.html', 'template/usageBar/usageBar.html']);angular.module("ddh.att", ["ddh.att.tpls", 'ddh.att.addressInput','ddh.att.arrows','ddh.att.badgesForAlerts','ddh.att.bellybandLinks','ddh.att.bodyCopy','ddh.att.breadcrumbs','ddh.att.buttonGroups','ddh.att.buttons','ddh.att.calendar','ddh.att.checkboxes','ddh.att.colorAndDataCapacity','ddh.att.confirmationProcess','ddh.att.creditCard','ddh.att.dropdownVariations','ddh.att.dropdowns','ddh.att.emphasizedTextField','ddh.att.expandCollapse','ddh.att.filmstrip','ddh.att.headings','ddh.att.leftNavigation','ddh.att.legalCopy','ddh.att.links','ddh.att.loaderAnimation','ddh.att.modalsAndAlerts','ddh.att.notesMessagesAndErrors','ddh.att.pagination','ddh.att.pivotLinks','ddh.att.pricing','ddh.att.promoCards','ddh.att.radioProductSelect','ddh.att.radios','ddh.att.searchField','ddh.att.socialFollowUs','ddh.att.socialShareThis','ddh.att.stepIndicator','ddh.att.switches','ddh.att.tables','ddh.att.tabs','ddh.att.termsAndConditions','ddh.att.textArea','ddh.att.tooltipsForForms','ddh.att.usageBar',]);angular.module('ddh.att.utilities', [])

.constant('ddhUtilitiesConfig', {
    prev: '37',
    up: '38',
    next: '39',
    down: '40',
    type: 'list',
    columns: 1,
    enableSearch: false,
    searchTimer: 200,
    circularTraversal: false
})

// All breakpoints ranges from >= min and < max
.constant('ddhAwdBreakpoints', {
    breakpoints: {
        mobile: {
            min: 1,
            max: 768
        },
        tablet: {
            min: 768,
            max: 1025
        },
        desktop: {
            min: 1025,
            max: 1920
        }
    }
})

.filter('ddhUnsafe', ['$sce', function ($sce) {
    return function (text) {
        return $sce.trustAsHtml(text);
    };
}])

.filter('ddhHighlight', function () {
    function escapeRegexp(queryToEscape) {
        return queryToEscape.replace(/([.?*+^$[\]\\(){}|-])/g, '\\$1');
    }
    return function (matchItem, query, className) {
        return query && matchItem ? matchItem.replace(new RegExp(escapeRegexp(query), 'gi'), '<span class=\"' + className + '\">$&</span>') : matchItem;
    };
})

.directive('ddhKey', ['ddhUtilitiesConfig', '$timeout', 'keymap', function (ddhUtilitiesConfig, $timeout, keymap) {
    return {
        restrict: 'EA',
        controller: ['$scope', '$attrs', function ($scope, attr) {
            this.childElements = [];
            this.disableNodes = {};
            this.enableSearch = attr.enableSearch !== undefined ? true : ddhUtilitiesConfig.enableSearch;
            this.circularTraversal = attr.circularTraversal !== undefined ? true : ddhUtilitiesConfig.circularTraversal;
            this.counter = -1;
            if (this.enableSearch) {
                this.searchKeys = [];
            }
            var searchString = '';
            this.registerElement = function (childElement, searchKey) {
                this.childElements.push(childElement);
                if (this.enableSearch) {
                    this.searchKeys.push(searchKey);
                }
                var count = this.childElements.length - 1;
                this.maxLength = count + 1;
                return count;
            };
            this.toggleDisable = function (count, state) {
                this.disableNodes[count] = state;
            };
            this.searchElement = function (searchExp) {
                var regex = new RegExp("\\b" + searchExp, "gi");
                var position = this.searchKeys.regexIndexOf(regex, this.counter + 1, true);
                if (position > -1) {
                    this.counter = position;
                    this.moveFocus(this.counter);
                }
            };
            this.startTimer = function (time) {
                if (searchString === '') {
                    $timeout(function () {
                        searchString = '';
                    }, time);
                }
            };
            this.resetCounter = function (count) {
                this.counter = count;
            };
            this.moveNext = function (count) {
                this.counter = (this.counter + count) < this.maxLength ? this.counter + count : (this.circularTraversal ? 0 : this.counter);
                if (this.disableNodes[this.counter]) {
                    if ((this.counter + count) < this.maxLength) {
                        this.moveNext(count);
                    }
                } else {
                    this.moveFocus(this.counter);
                }
            };
            this.movePrev = function (count) {
                this.counter = (this.counter - count) > -1 ? this.counter - count : (this.circularTraversal ? this.maxLength-1 : this.counter);
                if (this.disableNodes[this.counter]) {
                    if ((this.counter - count) > -1) {
                        this.movePrev(count);
                    }
                } else {
                    this.moveFocus(this.counter);
                }
            };
            this.moveFocus = function (index) {
                this.childElements[index][0].focus();
            };

            this.keyDownHandler = function (ev, count) {
                if (angular.isDefined(count) && !isNaN(count) && count !== this.counter) {
                    this.resetCounter(count);
                }
                if (!ev.keyCode) {
                    if (ev.which) {
                        ev.keyCode = ev.which;
                    } else if (ev.charCode) {
                        ev.keyCode = ev.charCode;
                    }
                }
                if (ev.keyCode) {
                    if (this.prev && this.prev.indexOf(ev.keyCode.toString()) > -1) {
                        this.movePrev(1);
                        ev.preventDefault();
                        ev.stopPropagation();
                    } else if (this.next && this.next.indexOf(ev.keyCode.toString()) > -1) {
                        this.moveNext(1);
                        ev.preventDefault();
                        ev.stopPropagation();
                    } else if (this.up && this.up.indexOf(ev.keyCode.toString()) > -1) {
                        if (this.type === 'table') {
                            this.movePrev(this.columns);
                            ev.preventDefault();
                            ev.stopPropagation();
                        }
                    } else if (this.down && this.down.indexOf(ev.keyCode.toString()) > -1) {
                        if (this.type === 'table') {
                            this.moveNext(this.columns);
                            ev.preventDefault();
                            ev.stopPropagation();
                        }
                    } else if (ev.keyCode >= 48 && ev.keyCode <= 105) {
                        if (this.enableSearch) {
                            this.startTimer(ddhUtilitiesConfig.searchTimer);
                            searchString = searchString + (keymap.MAP[ev.keyCode] || '');
                            this.searchElement(searchString);
                            ev.preventDefault();
                            ev.stopPropagation();
                        }
                    }
                }
            };
        }],
        link: function (scope, elem, attr, ctrl) {
            ctrl.prev = attr.prev ? attr.prev.split(',') : ddhUtilitiesConfig.prev.split(',');
            ctrl.next = attr.next ? attr.next.split(',') : ddhUtilitiesConfig.next.split(',');
            ctrl.type = attr.type ? attr.type : ddhUtilitiesConfig.type;
            if (ctrl.type === 'table') {
                ctrl.up = attr.up ? attr.up.split(',') : ddhUtilitiesConfig.up.split(',');
                ctrl.down = attr.down ? attr.down.split(',') : ddhUtilitiesConfig.down.split(',');
                ctrl.columns = attr.columns ? parseInt(attr.columns, 10) : ddhUtilitiesConfig.columns;
            }

            elem.bind('keydown', function (ev) {
                ctrl.keyDownHandler(ev);
            });
        }
    };
}])

.directive('ddhKeyItem', [function () {
    return {
        restrict: 'EA',
        link: function (scope, elem, attr, ctrl) {
            var parentCtrl = (elem.parent() && elem.parent().controller('ddhKey')) || undefined;
            if (angular.isDefined(parentCtrl)) {
                var count = parentCtrl.registerElement(elem, attr.searchKey);
                elem.bind('keydown', function (ev) {
                    parentCtrl.keyDownHandler(ev, count);
                });
                scope.$watch(attr.ddhKeyItem, function (value) {
                    value = value === undefined ? true : value;
                    parentCtrl.toggleDisable(count, !value);
                });
            }
        }
    };
}])

.directive('ddhAccessibilityClick', [function () {
    return {
        restrict: 'A',
        link: function (scope, elem, attr, ctrl) {
            var keyCode = [];
            attr.$observe('ddhAccessibilityClick', function (value) {
                if (value) {
                    keyCode = value.split(',');
                }
            });
            elem.bind('keydown keypress', function (ev) {
                var keyCodeCondition = function () {
                    var flag = false;
                    if (!(ev.keyCode)) {
                        if (ev.which) {
                            ev.keyCode = ev.which;
                        } else if (ev.charCode) {
                            ev.keyCode = ev.charCode;
                        }
                    }
                    if ((ev.keyCode && keyCode.indexOf(ev.keyCode.toString()) > -1)) {
                        flag = true;
                    }
                    return flag;
                };
                if (keyCode.length > 0 && keyCodeCondition()) {
                    elem[0].click();
                    ev.preventDefault();
                }
            });
        }
    };
}])

.factory('$documentBind', ['$document', '$timeout', function ($document, $timeout) {
    var _click = function (flag, callbackFunc, scope) {
        scope.$watch(flag, function (val) {
            $timeout(function () {
                if (val) {
                    $document.bind('click', callbackFunc);
                } else {
                    $document.unbind('click', callbackFunc);
                }
            });
        });
    };

    var _scroll = function (flag, callbackFunc, scope) {
        scope.$watch(flag, function (val) {
            $timeout(function () {
                if (val) {
                    $document.bind('scroll', callbackFunc);
                } else {
                    $document.unbind('scroll', callbackFunc);
                }
            });
        });
    };

    var _event = function (event, flag, callbackFunc, scope, timeoutFlag, timeoutValue) {
        if (timeoutFlag) {
            if (!(timeoutValue)) {
                timeoutValue = 0;
            }
            scope.$watch(flag, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    $timeout(function () {
                        if (newVal) {
                            $document.bind(event, callbackFunc);
                        } else {
                            $document.unbind(event, callbackFunc);
                        }
                    }, timeoutValue);
                }
            });
        } else {
            scope.$watch(flag, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    if (newVal) {
                        $document.bind(event, callbackFunc);
                    } else {
                        $document.unbind(event, callbackFunc);
                    }
                }
            });
        }
    };

    return {
        click: _click,
        scroll: _scroll,
        event: _event
    };
}])

.factory('$isElement', [function () {
    var isElement = function (currentElem, targetElem, alternateElem) {
        if (currentElem[0] === targetElem[0]) {
            return true;
        } else if (currentElem[0] === alternateElem[0]) {
            return false;
        } else {
            return isElement((currentElem.parent()[0] && currentElem.parent()) || targetElem, targetElem, alternateElem);
        }
    };

    return isElement;
}])

.factory('userAgent', [function () {
    var _isMobile = function () {
        return /Android|webOS|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
    };
    var _notMobile = function () {
        return !/Android|webOS|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
    };

    return {
        isMobile: _isMobile,
        notMobile: _notMobile
    };
}])

.factory('keymap', function () {
    return {
        KEY: {
            TAB: 9,
            ENTER: 13,
            ESC: 27,
            SPACE: 32,
            LEFT: 37,
            UP: 38,
            RIGHT: 39,
            DOWN: 40,
            SHIFT: 16,
            CTRL: 17,
            ALT: 18,
            PAGE_UP: 33,
            PAGE_DOWN: 34,
            HOME: 36,
            END: 35,
            BACKSPACE: 8,
            DELETE: 46,
            COMMAND: 91
        },
        MAP: { 91 : "COMMAND", 8 : "BACKSPACE" , 9 : "TAB" , 13 : "ENTER" , 16 : "SHIFT" , 17 : "CTRL" , 18 : "ALT" , 19 : "PAUSEBREAK" , 20 : "CAPSLOCK" , 27 : "ESC" , 32 : "SPACE" , 33 : "PAGE_UP", 34 : "PAGE_DOWN" , 35 : "END" , 36 : "HOME" , 37 : "LEFT" , 38 : "UP" , 39 : "RIGHT" , 40 : "DOWN" , 43 : "+" , 44 : "PRINTSCREEN" , 45 : "INSERT" , 46 : "DELETE", 48 : "0" , 49 : "1" , 50 : "2" , 51 : "3" , 52 : "4" , 53 : "5" , 54 : "6" , 55 : "7" , 56 : "8" , 57 : "9" , 59 : ";", 61 : "=" , 65 : "A" , 66 : "B" , 67 : "C" , 68 : "D" , 69 : "E" , 70 : "F" , 71 : "G" , 72 : "H" , 73 : "I" , 74 : "J" , 75 : "K" , 76 : "L", 77 : "M" , 78 : "N" , 79 : "O" , 80 : "P" , 81 : "Q" , 82 : "R" , 83 : "S" , 84 : "T" , 85 : "U" , 86 : "V" , 87 : "W" , 88 : "X" , 89 : "Y" , 90 : "Z", 96 : "0" , 97 : "1" , 98 : "2" , 99 : "3" , 100 : "4" , 101 : "5" , 102 : "6" , 103 : "7" , 104 : "8" , 105 : "9", 106 : "*" , 107 : "+" , 109 : "-" , 110 : "." , 111 : "/", 112 : "F1" , 113 : "F2" , 114 : "F3" , 115 : "F4" , 116 : "F5" , 117 : "F6" , 118 : "F7" , 119 : "F8" , 120 : "F9" , 121 : "F10" , 122 : "F11" , 123 : "F12", 144 : "NUMLOCK" , 145 : "SCROLLLOCK" , 186 : ";" , 187 : "=" , 188 : "," , 189 : "-" , 190 : "." , 191 : "/" , 192 : "`" , 219 : "[" , 220 : "\\" , 221 : "]" , 222 : "'"
        },
        isControl: function (e) {
            var k = e.keyCode;
            switch (k) {
            case this.KEY.COMMAND:
            case this.KEY.SHIFT:
            case this.KEY.CTRL:
            case this.KEY.ALT:
                return true;
            default:;
            }

            if (e.metaKey) {
                return true;
            }

            return false;
        },
        isFunctionKey: function (k) {
            k = k.keyCode ? k.keyCode : k;
            return k >= 112 && k <= 123;
        },
        isVerticalMovement: function (k) {
            return ~[this.KEY.UP, this.KEY.DOWN].indexOf(k);
        },
        isHorizontalMovement: function (k) {
            return ~[this.KEY.LEFT, this.KEY.RIGHT, this.KEY.BACKSPACE, this.KEY.DELETE].indexOf(k);
        },
        isAllowedKey: function (k) {
            return (~[this.KEY.SPACE, this.KEY.ESC, this.KEY.ENTER].indexOf(k)) || this.isHorizontalMovement(k) || this.isVerticalMovement(k);
        },
        isNumericKey: function (e) {
            var k = e.keyCode;
            if ((k >= 48 && k <= 57) || (k >= 96 && k <= 105)) {
                return true;
            } else {
                return false;
            }
        },
        isAlphaNumericKey: function (e) {
            var k = e.keyCode;
            if ((k >= 48 && k <= 57) || (k >= 96 && k <= 105) || (k >= 65 && k <= 90)) {
                return true;
            } else {
                return false;
            }
        }
    };
})

.factory('windowOrientation', ['$window', function ($window) {
    var _isPotrait = function () {
        if ($window.innerHeight > $window.innerWidth) {
            return true;
        } else {
            return false;
        }
    };
    var _isLandscape = function () {
        if ($window.innerHeight < $window.innerWidth) {
            return true;
        } else {
            return false;
        }
    };

    return {
        isPotrait: _isPotrait,
        isLandscape: _isLandscape
    };
}])

.directive('ddhReset', ['$compile', function ($compile) {
    return {
        restrict: 'A',
        require: ['?ngModel', 'ddhReset'],
        controller: ['$scope', function ($scope) {
            var resetButton = angular.element('<button type="button" class="reset-field" tabindex="-1" aria-label="Click to reset" aria-hidden="true" role="button"></button>');

            this.getResetButton = function () {
                return resetButton;
            };
        }],
        link: function (scope, element, attrs, ctrls) {

            var ngModelCtrl = ctrls[0];
            var ctrl = ctrls[1];

            var resetButton = ctrl.getResetButton();


            resetButton.on('click', function () {
                element[0].value = '';

                if (ngModelCtrl) {
                    if (attrs.ddhReset) {
                        ngModelCtrl.$setViewValue(attrs.ddhReset);
                    } else {
                        ngModelCtrl.$setViewValue('');
                    }
                    element[0].value = ngModelCtrl.$viewValue;
                    ngModelCtrl.$render();
                    scope.$digest();
                }
                element[0].focus();
                element[0].select();
            });

            var addResetButton = function () {
                element.after(resetButton);
                element.unbind('focus input', addResetButton);
            };

            element.bind('focus input', addResetButton);
        }
    };
}])

.factory('events', function () {
    var _stopPropagation = function (evt) {
        if (evt.stopPropagation) {
            evt.stopPropagation();
        } else {
            evt.returnValue = false;
        }
    };
    var _preventDefault = function (evt) {
        if (evt.preventDefault) {
            evt.preventDefault();
        } else {
            evt.returnValue = false;
        }
    }
    return {
        stopPropagation: _stopPropagation,
        preventDefault: _preventDefault
    };
})

.factory('DOMHelper', function () {

    var _isTabable = function (node) {
        var element = angular.element(node);
        var tagName = element[0].tagName.toUpperCase();

        if (isHidden(element)) {
            return false;
        }
        if (element.attr('tabindex') !== undefined) {
            return (parseInt(element.attr('tabindex'), 10) >= 0);
        }
        if (tagName === 'A' || tagName === 'AREA' || tagName === 'BUTTON' || tagName === 'INPUT' || tagName === 'TEXTAREA' || tagName === 'SELECT') {
            if (tagName === 'A' || tagName === 'AREA') {
                // anchors/areas without href are not focusable
                return (element[0].href !== '');
            }
            return !(element[0].disabled || element[0].readOnly);
        }
        return false;
    };

    function isValidChild(child) {
        return child.nodeType == 1 && child.nodeName != 'SCRIPT' && child.nodeName != 'STYLE';
    }

    function isHidden(obj) {
        var elem = angular.element(obj);
        return elem.hasClass('ng-hide') || elem.css('display') === 'none';
    }

    function traverse(obj, fromTop) {
        var obj = obj || document.getElementsByTagName('body')[0];
        if (isValidChild(obj) && _isTabable(obj)) {
            return obj;
        }
        // If object is hidden, skip it's children
        if (isValidChild(obj) && isHidden(obj)) {
            return undefined;
        }
        // If object is hidden, skip it's children
        if (angular.element(obj).hasClass('ng-hide')) {
            return undefined;
        }
        if (obj.hasChildNodes()) {
            var child;
            if (fromTop) {
                child = obj.firstChild;
            } else {
                child = obj.lastChild;
            }
            while (child) {
                var res = traverse(child, fromTop);
                if (res) {
                    return res;
                } else {
                    if (fromTop) {
                        child = child.nextSibling;
                    } else {
                        child = child.previousSibling;
                    }
                }
            }
        } else {
            return undefined;
        }
    }

    var _lastTabableElement = function (el) {
        /* This will return the last tabable element from the parent el */
        var elem = el;
        if (el.hasOwnProperty('length')) {
            elem = el[0];
        }

        return traverse(elem, false);
    };

    var _firstTabableElement = function (el) {
        /* This will return the first tabable element from the parent el */
        var elem = el;
        if (el.hasOwnProperty('length')) {
            elem = el[0];
        }

        return traverse(elem, true);
    };

    return {
        firstTabableElement: _firstTabableElement,
        lastTabableElement: _lastTabableElement,
        isTabable: _isTabable
    };
})

.factory('trapFocusInElement', ['$document', '$isElement', function ($document, $isElement) {
    var elementStack = [];
    var stackHead = undefined;
    var trapFocusInElement = function (flag) {
        var bodyElements = $document.find('body').children();
        var trapKeyboardFocusInElement = function (ev) {
            if (!$isElement(angular.element(ev.target), stackHead, $document)) {
                stackHead[0].focus();
            }
        };
        if (flag) {
            for (var i = 0; i < bodyElements.length; i++) {
                if (bodyElements[i] !== stackHead[0]) {
                    bodyElements.eq(i).attr('aria-hidden', true);
                }
            }
            $document.bind('focusin', trapKeyboardFocusInElement);
        } else {
            for (var j = 0; j < bodyElements.length; j++) {
                if (bodyElements[j] !== stackHead[0]) {
                    bodyElements.eq(j).removeAttr('aria-hidden');
                }
            }
            $document.unbind('focusin', trapKeyboardFocusInElement);
        }
    };
    var toggleTrapFocusInElement = function (flag, element) {
        if (angular.isDefined(flag) && angular.isDefined(element)) {
            if (angular.isUndefined(stackHead)) {
                stackHead = element;
                trapFocusInElement(flag);
            } else {
                if (flag) {
                    trapFocusInElement(false);
                    elementStack.push(stackHead);
                    stackHead = element;
                    trapFocusInElement(true);
                } else {
                    if (stackHead === element) {
                        trapFocusInElement(false);
                        stackHead = elementStack.pop();
                        if (angular.isDefined(stackHead)) {
                            trapFocusInElement(true);
                        }
                    }
                }
            }
        }
    };

    return toggleTrapFocusInElement;
}])

.directive('ddhElementFocus', [function () {
    return {
        restrict: 'A',
        link: function (scope, elem, attr, ctrl) {
            scope.$watch(attr.ddhElementFocus, function (value) {
                if (value === true) {
                    elem[0].focus();
                }
            });
        }
    };
}])

.directive('ddhAppendElement', [function () {
    return {
        restrict: 'A',
        link: function (scope, elem, attr, ctrl) {
            elem.append(scope.$eval(attr.ddhAppendElement));
        }
    };
}])

.directive('ddhOnlyDigits', ['keymap', function (keymap) {
        return {
            restrict: 'A',  
            scope:{ msize:'='},
            link: function (scope, elm, attrs, ctrl) {              
                if(attrs.maxlength)
                {
                    scope.msize = attrs.maxlength;
                }               
                elm.on('keydown', function (event) {
                    if (elm.val().length > scope.msize) {
                        if ((event.which === 37) 
                             || (event.which === 38)
                             || (event.which === 39) 
                             || (event.which === 40) 
                             || (event.which === 46) 
                             || (event.which === 8) 
                             || (event.which === 9)) {
                            return true;
                        } else {
                            event.preventDefault();
                            return false;
                        }
                    }
                    if (event.shiftKey) {
                        event.preventDefault();
                        return false;
                    }
                    if ((keymap.isNumericKey(event) === true) 
                             ||(event.which === 37) 
                             || (event.which === 38)
                             || (event.which === 39) 
                             || (event.which === 40) 
                             || (event.which == 46) 
                             || (event.which === 8) 
                             || (event.which === 9)) {
                        return true;
                    } else {
                        event.preventDefault();
                        return false;
                    }
                });
            }
        }
    }])
	
.factory('isHighContrast', function () {
    var _isHighContrast = function (idval) {
        var objDiv, objImage, strColor, strWidth, strReady;
        var strImageID = idval; // ID of image on the page

        // Create a test div
        objDiv = document.createElement('div');

        //Set its color style to something unusual
        objDiv.style.color = 'rgb(31, 41, 59)';

        // Attach to body so we can inspect it
        document.body.appendChild(objDiv);

        // Read computed color value
        strColor = document.defaultView ? document.defaultView.getComputedStyle(objDiv, null).color : objDiv.currentStyle.color;
        strColor = strColor.replace(/ /g, '');

        // Delete the test DIV
        document.body.removeChild(objDiv);

        // Check if we get the color back that we set. If not, we're in
        // high contrast mode.
        if (strColor !== 'rgb(31,41,59)') {
            return true;
        } else {
            return false;
        }
    };

    return _isHighContrast;
})

.run(['isHighContrast', '$document', function (isHighContrast, $document) {
    var html = $document.find('html').eq(0);
    if (isHighContrast()) {
        html.addClass('ds2-no-colors');
    } else {
        html.removeClass('ds2-no-colors');
    }
}]);	

(function () {
    String.prototype.toSnakeCase = function () {
        return this.replace(/([A-Z])/g, function ($1) {
            return "-" + $1.toLowerCase();
        });
    };
    var concat = function (character, times) {
        character = character || '';
        times = (!isNaN(times) && times) || 0;
        var finalChar = '';
        for (var i = 0; i < times; i++) {
            finalChar += character;
        }
        return finalChar;
    };

    // direction: true for left and false for right
    var pad = function (actualString, width, character, direction) {
        actualString = actualString || '';
        width = (!isNaN(width) && width) || 0;
        character = character || '';
        if (width > actualString.length) {
            if (direction) {
                return concat(character, (width - actualString.length)) + actualString;
            } else {
                return actualString + concat(character, (width - actualString.length));
            }
        }
        return actualString;
    };

    String.prototype.lPad = function (width, character) {
        return pad(this, width, character, true);
    };

    String.prototype.rPad = function (width, character) {
        return pad(this, width, character, false);
    };

    if (!Array.prototype.indexOf) {
        Array.prototype.indexOf = function (val) {
            for (var index = 0; index < this.length; index++) {
                if (this[index] === val) {
                    return index;
                }
            }
            return -1;
        };
    }

    if (!Array.prototype.regexIndexOf) {
        Object.defineProperty(Array.prototype, 'regexIndexOf', {
            enumerable: false,
            value: function (regex, startIndex, loop) {
                startIndex = startIndex && startIndex > -1 ? startIndex : 0;
                for (var index = startIndex; index < this.length; index++) {
                    if (this[index].toString().match(regex)) {
                        return index;
                    }
                }
                if (loop) {
                    for (var index = 0; index < startIndex; index++) {
                        if (this[index].toString().match(regex)) {
                            return index;
                        }
                    }
                }
                return -1;
            }
        })
    }
})();
/**
 * @ngdoc directive
 * @name Forms.att:dropdowns
 *
 * @description
 *  <file src="src/dropdowns/docs/readme.md" />
 * @usage
 *  <select name="dropdown1" ddh-dropdown ng-model="dropdownValue1.value">
        <option ddh-dropdown-list option-repeat="d in dropdownOptions1" value="{{d.value}}">{{d.text}}</option>
    </select>
 *
 * @example
   <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
     <file src="src/dropdowns/docs/demo.html" />
     <file src="src/dropdowns/docs/demo.js" />
    </example>
   </section>
 */
angular.module('ddh.att.dropdowns', ['ddh.att.utilities', 'ngSanitize'])

.constant('ddhDropdownConfig', {
    prev: '37,38',
    next: '39,40'
})

.directive("ddhDropdown", ['$timeout', '$compile', '$templateCache', 'userAgent', function ($timeout, $compile, $templateCache, userAgent) {
    return {
        restrict: 'A',
        scope: true,
        require: 'ngModel',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            if (userAgent.notMobile()) {
                var formCtrl = elem.controller('form');
                scope.setNgModelController = function (name, ngModelCtrl) {
                    if (name && formCtrl && ngModelCtrl) {
                        formCtrl[name] = ngModelCtrl;
                    }
                };
                var innerHtml = angular.element('<div></div>').append(elem.html());
                innerHtml = ($compile(innerHtml)(scope)).html();
                var template = angular.element($templateCache.get('template/dropdowns/ddhDropdownDesktop.html'));
                template.find('ul').append(innerHtml);
                template = $compile(template)(scope);
                elem.replaceWith(template);
            } else if (userAgent.isMobile()) {
                elem.css({
                    'opacity': '0',
                    'filter': 'alpha(opacity=0)'
                });
                elem.addClass('awd-select isWrapped');
                elem.wrap('<span class="selectWrap"></span>');
                var cover = angular.element('<span aria-hidden="true"><i class="cssIcon-chevron-d-w"></i></span>');
                elem.parent().append(cover);
                var set = function () {
                    var sel = elem[0] ? elem[0] : elem;
                    var selectedText = "";
                    var selIndex = sel.selectedIndex;
                    if (typeof selIndex !== 'undefined') {
                        selectedText = sel.options[selIndex].text;
                    }
                    cover.text(selectedText).append('<i class="cssIcon-chevron-d-w"></i>');
                };
                var update = function (value) {
                    $timeout(set, 100);
                };

                if (attr.ngModel) {
                    scope.$watch(attr.ngModel, function (newVal, oldVal) {
                        update();
                    });
                }
                elem.bind('keyup', function () {
                    set();
                });
            }
        }],
        link: function (scope, elem, attr, ctrl) {
            if (userAgent.notMobile()) {
                if (attr.type === "large") {
                    scope.stype = "large";
                } else {
                    scope.stype = "";
                }

                scope.updateModel = function () {
                    ctrl.$setViewValue(scope.currentSelected.value);
                };
                ctrl.$render = function () {
                    $timeout(function () {
                        scope.dropdownLists[ctrl.$viewValue] && scope.dropdownLists[ctrl.$viewValue][0].updateDropdownValue();
                    }, 100);
                };
                scope.disabled = false;
                scope.dropdownName = attr.name;
                scope.dropdownId = attr.id;
                scope.ariaDescribedbyId = attr.ariaDescribedby;
				scope.labelText = attr.labelText;
                if (attr.required) {
                    scope.dropdownRequired = true;
                } else {
                    scope.dropdownRequired = false;
                }
                elem.removeAttr('name');
                elem.removeAttr('id');
                scope.$parent.$watch(attr.ngDisabled, function (val) {
                    scope.disabled = val;
                });
            }
        }
    };
}])

.directive("ddhDropdownButton", ['$document', '$documentBind', '$isElement', 'ddhDropdownConfig', 'keymap', 'ddhUtilitiesConfig', '$timeout', function ($document, $documentBind, $isElement, ddhDropdownConfig, keymap, ddhUtilitiesConfig, $timeout) {
    return {
        restrict: 'A',
        require: '?^ddhKey',
        link: function (scope, elem, attr, ctrl) {
            scope.toggleFlag = false;
            scope.dropdownLists = {};
            scope.dropdownListValues = [];
            scope.dropdown = {
                totalIndex: -1
            };
            scope.currentSelected = {
                value: '',
                text: '',
                label: '',
                index: -1
            };
            var searchString = '';
            var searchElement = function (searchExp) {
                var regex = new RegExp("\\b" + searchExp, "gi");
                var position = scope.dropdownListValues.regexIndexOf(regex, scope.currentSelected.index + 1, true);
                if (position > -1) {
                    return position;
                }
                return undefined;
            };
            var startTimer = function (time) {
                if (searchString === '') {
                    $timeout(function () {
                        searchString = '';
                    }, time);
                }
            };
            scope.toggleDropdown = function (toggleFlag) {
                if (!scope.disabled) {
                    if (angular.isDefined(toggleFlag)) {
                        scope.toggleFlag = toggleFlag;
                    } else {
                        scope.toggleFlag = !scope.toggleFlag;
                    }
                    if (!scope.toggleFlag) {
                        elem[0].focus();
                    } else {
                        if (ctrl && ctrl.enableSearch) {
							if (angular.isDefined(scope.dropdownLists[scope.currentSelected.value])) {
								ctrl.resetCounter(scope.dropdownLists[scope.currentSelected.value][2]);
							}
                        }
                    }
                }
            };
            elem.bind('keydown', function (ev) {
                if (!ev.keyCode) {
                    if (ev.which) {
                        ev.keyCode = ev.which;
                    } else if (ev.charCode) {
                        ev.keyCode = ev.charCode;
                    }
                }
                if (!scope.toggleFlag) {
                    if (ev.keyCode) {
                        var currentIndex = scope.currentSelected.index;
                        if (ddhDropdownConfig.prev.split(',').indexOf(ev.keyCode.toString()) > -1) {
                            angular.isDefined(scope.dropdownListValues[currentIndex - 1]) ? scope.dropdownLists[scope.dropdownListValues[currentIndex - 1]][0].updateDropdownValue() : angular.noop();
                            ev.preventDefault();
                            ev.stopPropagation();
                        } else if (ddhDropdownConfig.next.split(',').indexOf(ev.keyCode.toString()) > -1) {
                            angular.isDefined(scope.dropdownListValues[currentIndex + 1]) ? scope.dropdownLists[scope.dropdownListValues[currentIndex + 1]][0].updateDropdownValue() : angular.noop();
                            ev.preventDefault();
                            ev.stopPropagation();
                        } else if (ev.keyCode >= 48 && ev.keyCode <= 105) {
                            startTimer(ddhUtilitiesConfig.searchTimer);
                            searchString = searchString + (keymap.MAP[ev.keyCode] || '');
                            var position = searchElement(searchString);
                            angular.isDefined(scope.dropdownListValues[position]) ? scope.dropdownLists[scope.dropdownListValues[position]][0].updateDropdownValue() : angular.noop();
                            ev.preventDefault();
                            ev.stopPropagation();
                        }
                    }
                } else {
                    if (ev.keyCode === keymap.KEY.TAB || ev.keyCode === keymap.KEY.ESC) {
                        scope.toggleDropdown(false);
                        ev.preventDefault();
                        ev.stopPropagation();
                    }
                }
                scope.$apply();
            });
            var outsideClick = function (e) {
                var isElement = $isElement(angular.element(e.target), elem.parent(), $document);
                if (!isElement) {
                    scope.toggleDropdown(false);
                    scope.$apply();
                }
            };
            $documentBind.click('toggleFlag', outsideClick, scope);
        }
    };
}])

.directive("ddhDropdownGroup", ['$compile', '$templateCache', 'userAgent', function ($compile, $templateCache, userAgent) {
    return {
        restrict: 'A',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            if (userAgent.notMobile()) {
                var innerHtml = angular.element('<div></div>').append(elem.html());
                innerHtml = ($compile(innerHtml)(scope)).html();
                var template = angular.element($templateCache.get('template/dropdowns/ddhDropdownGroupDesktop.html'));
                template.attr('ng-repeat', attr.optGroupRepeat);
                template.attr('label', elem.attr('label'));
                template.find('ul').append(innerHtml);
                elem.replaceWith(template);
            } else if (userAgent.isMobile()) {
                var template = angular.element(elem.prop('outerHTML'));
                template.attr('ng-repeat', attr.optGroupRepeat);
                template.removeAttr('ddh-dropdown-group');
                template.removeAttr('opt-group-repeat');
                template = $compile(template)(scope);
                elem.replaceWith(template);
            }
        }]
    };
}])

.directive("ddhDropdownGroupDesktop", [function () {
    return {
        restrict: 'A',
        scope: true,
        link: function (scope, elem, attr, ctrl) {
            scope.groupHeader = attr.label;
        }
    };
}])

.directive("ddhDropdownList", ['$compile', '$templateCache', 'userAgent', function ($compile, $templateCache, userAgent) {
    return {
        restrict: 'A',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            if (userAgent.notMobile()) {
                var innerHtml = angular.element('<div></div>').append(elem.html());
                innerHtml = ($compile(innerHtml)(scope)).html();
                var template = angular.element($templateCache.get('template/dropdowns/ddhDropdownListDesktop.html'));
                template.attr('ng-repeat', attr.optionRepeat);
                template.attr('value', elem.attr('value'));
                template.attr('search-key', elem.attr('value'));
                if (elem.attr('imgsrc')) {
                    template.append('<img ng-src="' + elem.attr('imgsrc') + '"/>');
                };
                template.append(innerHtml);
                elem.replaceWith(template);
            } else if (userAgent.isMobile()) {
                var template = angular.element(elem.prop('outerHTML'));
                template.attr('ng-repeat', attr.optionRepeat);
                template.removeAttr('ddh-dropdown-list');
                template.removeAttr('option-repeat');
                template = $compile(template)(scope);
                elem.replaceWith(template);
            }
        }]
    };
}])

.directive("ddhDropdownListDesktop", ['$sce', function ($sce) {
    return {
        restrict: 'A',
        scope: true,
        link: function (scope, elem, attr, ctrl) {
            var dropdownListValue = scope.dropdownListValue = attr.value;
            scope.dropdown.totalIndex++;
            var dropdownListIndex = scope.dropdown.totalIndex;
            scope.dropdownListValues.push(dropdownListValue);
            scope.dropdownLists[dropdownListValue] = [];
            scope.dropdownLists[dropdownListValue][0] = scope;
            scope.dropdownLists[dropdownListValue][1] = elem;
            scope.dropdownLists[dropdownListValue][2] = dropdownListIndex;
            scope.updateDropdownValue = function () {
                scope.currentSelected.value = dropdownListValue;
                scope.currentSelected.text = $sce.trustAsHtml(elem.html());
                scope.currentSelected.label = elem.text();
                scope.currentSelected.index = dropdownListIndex;
                scope.updateModel();
            };
            scope.selectDropdownItem = function () {
                scope.setDirty();
                scope.updateDropdownValue();
                scope.toggleDropdown(false);
            };
        }
    };
}])

.directive("ddhDropdownRepeat", ['$compile', 'userAgent', function ($compile, userAgent) {
    return {
        restrict: 'A',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            if (userAgent.notMobile()) {
                var innerHtml = angular.element('<div></div>').append(elem.html());
                innerHtml = ($compile(innerHtml)(scope)).html();
                var template = angular.element('<div></div>');
                template.attr('ng-repeat', attr.ddhDropdownRepeat);
                template.append(innerHtml);
                elem.replaceWith(template);
            } else if (userAgent.isMobile()) {
                angular.noop();
            }
        }]
    };
}])

.directive("ddhDropdownValidation", ['$timeout', function ($timeout) {
    return {
        restrict: 'A',
        require: 'ngModel',
        link: function (scope, elem, attr, ctrl) {
            $timeout(function () {
                scope.setNgModelController(attr.name, ctrl);
            }, 100);
            scope.setDirty = function () {
                if (ctrl.$dirty === false) {
                    ctrl.$dirty = true;
                    ctrl.$pristine = false;
                }
            };
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Forms.att:addressInput
 *
 * @description
 *  <file src="src/addressInput/docs/readme.md" />
 *
 * @usage

 *
 * @example
 *  <section id="code">   
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/addressInput/docs/demo.html" />
 <file src="src/addressInput/docs/demo.js" />
 </example>
 </section>
 *
 */
angular.module('ddh.att.addressInput', ['ngMessages', 'ddh.att.dropdowns','ddh.att.utilities']);
     
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:arrows
 *
 * @description
 *  <file src="src/arrows/docs/readme.md" />
 *
 * @usage
 *   Please refer demo.html tab in Example section below.
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/arrows/docs/demo.html" />
            <file src="src/arrows/docs/demo.js" />
       </example>
    </section>
 *
 */
angular.module('ddh.att.arrows', ['ddh.att.utilities']);
/**
 * @ngdoc directive
 * @name Messages, modals & alerts.att:badgesForAlerts
 *
 * @description
 *  <file src="src/badgesForAlerts/docs/readme.md" />
 *
 * @usage
 *  Single digit badge
    <a href="#"><span class="badge badge-urgent">2</span>Alerts</a>
    Double digit badge
    <a href="#"><span class="badge badge-urgent">10</span>Alerts</a>
    Maximum digit (99+) badge
    <a href="#"><span class="badge badge-urgent">+99</span>Alerts</a>
    Showing zero alerts
    <a href="#"><span class="badge">0</span>Alerts</a>
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/badgesForAlerts/docs/demo.html" />
            <file src="src/badgesForAlerts/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.badgesForAlerts', []);
/**
 * @ngdoc directive
 * @name Navigation.att:bellybandLinks
 *
 * @description
 *  <file src="src/bellybandLinks/docs/readme.md" />
 *
 * @usage
 *   <div class="bellyband-group autoSize" bellyband-autosize-what=".bellyband-link">
                  <div class="bellyband-link">
                     <a href="patterns.html">
                        <img class="visible-desktop" src="../style/images/svg/master/misc/puzzleL.svg" alt="">
                                                          
                        <img class="hidden-desktop" src="../style/images/svg/master/misc/puzzle.svg" alt="">
                        <span>Pattern library</span>
                        <p>Review our digital design standards and patterns for creating AT&amp;T digital assets. </p>
                     </a>
                  </div>
                  <div class="bellyband-link">
                     <a href="support-development-overview.html">
                        <img class="visible-desktop" src="../style/images/svg/master/datanetwork/cloudL.svg" alt="">
                        <img class="hidden-desktop" src="../style/images/svg/master/datanetwork/cloud.svg" alt="">
                        <span>Development overview</span>
                        <p>Set up your environment, access the code snippets, and follow our standards for creating a digital code base.</p>
                     </a>
                  </div>
     </div>   
 
 * @example
 *  <section id="code">   
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/bellybandLinks/docs/demo.html" />
 <file src="src/bellybandLinks/docs/demo.js" />
 </example>
 </section>
 *
 */
angular.module('ddh.att.bellybandLinks', [])
        .directive('bellybandAutosizeWhat', ['$window', function($window) {
                return {
                    restrict: 'EA',
                    link: function(scope, ele, attr) {
                        var sizeWhat = attr.bellybandAutosizeWhat;
                        var bellyBandCssCalculation = function() {
                            angular.forEach(ele[0].querySelectorAll('.bellyband-link'), function(value, key) {
                                var newElement = angular.element(value);
                                if ($window.innerWidth <= '767') {
                                    var marginRightVal = getComputedStyle(value.querySelector('.hidden-desktop'), null).marginRight.split("px");
                                    var imgWidth = parseInt(value.querySelector('.hidden-desktop').offsetWidth) + parseInt(marginRightVal[0]) + 'px';
                                    newElement.find('p').css({
                                        'padding-left': (parseInt(imgWidth)) + 'px'
                                    });
                                }

                                if ($window.innerWidth >= '480' && $window.innerWidth <= '767') {
                                    var lnkHeight = getComputedStyle(value, null).height.split("px");
                                    lnkMar = getComputedStyle(value, null).marginBottom.split("px");
                                    if (ele[0].querySelectorAll('.bellyband-link').length === 2) {
                                        bblink = ((parseInt(lnkHeight[0]) + parseInt(lnkMar[0])) * 1) + 'px';
                                    } else if (ele[0].querySelectorAll('.bellyband-link').length >= 3 && ele[0].querySelectorAll('.bellyband-link').length <= 4) {
                                        bblink = ((parseInt(lnkHeight[0]) + parseInt(lnkMar[0])) * 2) + 'px';
                                    } else if (ele[0].querySelectorAll('.bellyband-link').length >= 5) {
                                        bblink = ((parseInt(lnkHeight[0]) + parseInt(lnkMar[0])) * 3) + 'px';
                                    }

                                    ele.css({
                                        'max-height': bblink
                                    });

                                } else if ($window.innerWidth >= '768') {

                                    ele.css({
                                        'max-height': 'inherit'
                                    });
                                }

                                if ($window.innerWidth >= '480') {
                                    if (ele[0].querySelectorAll('.bellyband-link').length >= '2') {
                                        angular.element(value).css({
                                            'min-width': '192px'
                                        });
                                    }
                                    if (ele[0].querySelectorAll('.bellyband-link').length == '4') {
                                        angular.element(value).css({
                                            'min-width': '23.9437%'
                                        });
                                    }
                                    if (ele[0].querySelectorAll('.bellyband-link').length >= '5') {
                                        angular.element(value).css({
                                            'min-width': '32.2751%'
                                        });
                                    }
                                }
                            });
                        };

                        function tallestSize() {

                            var maxInnerHeight = -1;
                            angular.forEach(ele[0].querySelectorAll(sizeWhat), function(value, key) {
                                if (maxInnerHeight < value.clientHeight) {
                                    maxInnerHeight = value.clientHeight;
                                }
                            });
                            return maxInnerHeight;
                        }
                        var setautoSize = function() {
                            autoSize2 = tallestSize() + "px";
                            angular.forEach(ele[0].querySelectorAll(sizeWhat), function(value, key) {
                                var newElement = angular.element(value);
                                if ($window.innerWidth < '768') {
                                    newElement.css({
                                        'height': autoSize2
                                    });
                                }
                            });
                        };
                        setautoSize();
                        bellyBandCssCalculation();

                        angular.element($window).bind('resize', function() {
                            angular.element(ele[0].querySelectorAll('.bellyband-link')).removeAttr('style');
                            setautoSize();
                            bellyBandCssCalculation();
                        });
                    }
                };
            }]);
/**
 * @ngdoc directive
 * @name Headings & copy.att:Body Copy
 *
 * @description
 *  <file src="src/bodyCopy/docs/readme.md" />
 * @usage
 *      <!-- See below examples for link implementation -->
 *      
 * @example
       <section id="code">              
           <b>HTML + AngularJS</b>
           <example module="ddh.att">
           <file src="src/bodyCopy/docs/demo.html" />
            <file src="src/bodyCopy/docs/demo.js" />            
          </example>          
        </section>
 */
angular.module('ddh.att.bodyCopy', []);
/**
 * @ngdoc directive
 * @name Navigation.att:breadcrumbs
 *
 * @description
 *  <file src="src/breadcrumbs/docs/readme.md" />
 * @usage
    <ul class="breadcrumb">
        <li ng-repeat="link in breadCrumbsLink"><a tabindex="{{(idx==$index)?-1:0}}" href='javascript:void(0)' ng-click="clickActive($index)" ng-class="{'active':idx==$index, '': idx!=$index}">{{link.title}}</a></li>
    </ul>
 * @example
 <example module="ddh.att">
 <file src="src/breadcrumbs/docs/demo.html" />
 <file src="src/breadcrumbs/docs/demo.js" />
 </example>
 */
angular.module('ddh.att.breadcrumbs',[])
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:buttonGroups
 *
 * @description
 *  <file src="src/buttonGroups/docs/readme.md" />
 *
 * @usage
<h2>Radio Aproach</h2>
<div class="btn-group" ddh-key prev="37,38" next="39,40" circular-traversal role="radiogroup">
    <button type="button" class="btn btn-secondary" ddh-key-item ng-focus="radioModel='Button 1'" ng-model="radioModel" ddh-btn-radio="'Button 1'" tabindex="{{(!radioModel || 'Button 1'===radioModel)?0:-1}}">Button 1</button>
    <button type="button" class="btn btn-secondary" ddh-key-item ng-focus="radioModel='Button 2'" ng-model="radioModel" ddh-btn-radio="'Button 2'" tabindex="{{(!radioModel || 'Button 2'===radioModel)?0:-1}}">Button 2</button>
    <button type="button" class="btn btn-secondary" ddh-key-item ng-focus="radioModel='Button 3'" ng-model="radioModel" ddh-btn-radio="'Button 3'" tabindex="{{(!radioModel || 'Button 3'===radioModel)?0:-1}}">Button 3</button>
</div>

<h2>Checkbox Aproach</h2>
<span ddh-button-group class="btn-group btn-fullwidth" role="group" max-select="3" ng-model="checkModel1">
    <button type="button" class="btn btn-secondary" ng-model="checkModel1.Button1" ddh-btn-checkbox>Button1</button>
    <button type="button" class="btn btn-secondary" ng-model="checkModel1.Button2" ddh-btn-checkbox>Button2</button>
    <button type="button" class="btn btn-secondary" ng-model="checkModel1.Button3" ddh-btn-checkbox>Button3</button>
    <button type="button" class="btn btn-secondary" ng-model="checkModel1.Button4" ddh-btn-checkbox>Button4</button>
    <button type="button" class="btn btn-secondary" ng-model="checkModel1.Button5" ddh-btn-checkbox>Button5</button>
</span>
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/buttonGroups/docs/demo.html" />
            <file src="src/buttonGroups/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.buttonGroups', ['ddh.att.utilities'])
    .constant('buttonConfig', {
        activeClass: 'active',
        toggleEvent: 'click'
    })
    .directive('ddhBtnRadio', ['buttonConfig', function (buttonConfig) {
        var activeClass = buttonConfig.activeClass || 'active';
        var toggleEvent = buttonConfig.toggleEvent || 'click';

        return {
            require: 'ngModel',
            link: function (scope, element, attrs, ngModelCtrl) {
                element.attr('role', 'radio');

                //model -> UI
                ngModelCtrl.$render = function () {
                    element.toggleClass(activeClass, angular.equals(ngModelCtrl.$modelValue, scope.$eval(attrs.ddhBtnRadio)));
                    if (angular.equals(ngModelCtrl.$modelValue, scope.$eval(attrs.ddhBtnRadio))) {
                        element.attr("aria-checked", true);
                    } else {
                        element.attr("aria-checked", false);
                    }
                };

                //ui->model
                element.bind(toggleEvent, function () {
                    if (!element.hasClass(activeClass)) {
                        scope.$apply(function () {
                            ngModelCtrl.$setViewValue(scope.$eval(attrs.ddhBtnRadio));
                            ngModelCtrl.$render();
                        });
                    }
                });
            }
        };
    }])
    .directive('ddhBtnCheckbox', ['buttonConfig', function (buttonConfig) {
        var activeClass = buttonConfig.activeClass || 'active';
        var toggleEvent = buttonConfig.toggleEvent || 'click';

        return {
            require: ['ngModel', '^^ddhButtonGroup'],
            link: function (scope, element, attrs, ctrls) {

                var ngModelCtrl = ctrls[0];
                var parentCtrl = ctrls[1];

                element.attr('role', 'checkbox');
                element.attr('aria-describedby', parentCtrl.getStateDescriptionElemId());

                function getTrueValue() {
                    var trueValue = scope.$eval(attrs.ddhBtnCheckboxTrue);
                    return angular.isDefined(trueValue) ? trueValue : true;
                }

                function getFalseValue() {
                    var falseValue = scope.$eval(attrs.ddhBtnCheckboxFalse);
                    return angular.isDefined(falseValue) ? falseValue : false;
                }

                //model -> UI
                ngModelCtrl.$render = function () {
                    element.toggleClass(activeClass, angular.equals(ngModelCtrl.$modelValue, getTrueValue()));
                    if ((angular.equals(ngModelCtrl.$modelValue, getTrueValue()))) {
                        element.attr("aria-checked", true);
                    } else {
                        element.attr("aria-checked", false);
                    }
                };

                //ui->model
                element.bind(toggleEvent, function () {
                    scope.$apply(function () {
                        ngModelCtrl.$setViewValue(element.hasClass(activeClass) ? getFalseValue() : getTrueValue());
                        ngModelCtrl.$render();
                    });
                });
            }
        };
    }])
    .directive('ddhButtonGroup', ['$timeout', '$compile', function ($timeout, $compile) {
        return {
            restrict: 'A',
            scope: {
                maxSelect: "=",
                ngModelButtonState: '=ngModel'
            },
            controller: ['$scope', '$element', function ($scope, $element) {
                $scope.nSel = 0;

                var stateDescriptionElem = angular.element('<span id="ddh_button_group_' + $scope.$id + '" class="hide" aria-hidden="true">{{nSel}} of {{maxSelect}} options selected.</span>');
                $compile(stateDescriptionElem)($scope);
                $element.after(stateDescriptionElem);

                this.getStateDescriptionElemId = function () {
                    return stateDescriptionElem.attr('id');
                };
            }],
            link: function (scope, element) {


                var executeFxn = function () {
                    scope.nSel = 0;
                    angular.forEach(scope.ngModelButtonState, function (value, key) {
                        if (value === true) {
                            scope.nSel += 1;
                        }
                    });

                    if (scope.nSel >= scope.maxSelect) {
                        angular.forEach(element.children(), function (chd) {
                            if (chd.className.indexOf('active') < 0) {
                                chd.disabled = true;
                                chd.setAttribute('aria-disabled', true);
                            }
                        });
                    } else {
                        angular.forEach(element.children(), function (chd) {
                            chd.disabled = false;
                            chd.setAttribute('aria-disabled', false);
                        });
                    }
                    scope.$digest();
                };

                $timeout(function () {
                    executeFxn();
                });
                element.bind('click', executeFxn);
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:buttons
 * @element input
 * @function
 *
 * @description
 *  <file src="src/buttons/docs/readme.md" />
 * @usage
 *
Button shape
<button class="btn" type="button">Button</button> button.btn (button shape only)
<button aria-label="Custom aria label" class="btn" type="button">Button</button> button.btn (button shape only) with custom aria label
<button aria-label="Click on button/Press enter" class="btn" type="button" onclick="javascript:alert('It works!');">Click on button/Press enter</button> button.btn with click functionality
<a href="javascript:void(0)" class="btn" role="button">Button</a> a.btn (button shape only)
<button class="btn btn-primary">Button</button> .btn-primary
<a href="javascript:void(0)" class="btn btn-primary" role="button">Button</a> a.btn-primary

5 Button colors
<button class="btn btn-secondary">Button</button> .btn-secondary
<a href="javascript:void(0)" class="btn btn-secondary" role="button">Button</a> a.btn-secondary
<button class="btn btn-alt">Button</button> .btn-alt
<a href="javascript:void(0)" class="btn btn-alt" role="button">Button</a> a.btn-alt
<button class="btn btn-specialty">Button</button> .btn-specialty
<a href="javascript:void(0)" class="btn btn-specialty" role="button">Button</a> a.btn-specialty
<button class="btn btn-specialty" disabled="">Button</button> disabled="disabled"
<a aria-disabled="true" href="javascript:void(0)" class="btn btn-primary disabled" role="button">Button</a> a.disabled

3 button heights
<button class="btn btn-secondary">Button</button> .btn is default and 46px height
<button class="btn btn-secondary btn-medium">Button</button> .btn-medium is 42px
<button class="btn btn-secondary btn-small">Button</button> .btn-small is 36px

.row-nowrap 2 up buttons
<div class="row-nowrap">
    <button class="btn btn-secondary btn-fullwidth" type="button">Cancel</button>
    <button class="btn btn-primary btn-fullwidth" type="button">Continue</button>
</div>

.row 2 up buttons (desktop) stacked (mobile) (different order)
<div class="row cta-button-group">
    <button class="span btn btn-secondary btn-fullwidth hidden-phone" type="button">Cancel</button>
    <button class="span btn btn-primary btn-fullwidth" type="button">Continue</button>
    <button class="span btn btn-secondary btn-fullwidth visible-phone" type="button">Cancel</button>
</div>

 * @example
 *  <section id="code">
               <b>HTML + AngularJS</b>
 *              <example module="ddh.att">
 *              <file src="src/buttons/docs/demo.html" />
                 <file src="src/buttons/docs/demo.js" />
 *              </example>
            </section>
 *
 */
angular.module('ddh.att.buttons', []);
angular.module('ddh.att.position', [])

.factory('$position', ['$document', '$window', function ($document, $window) {
    function getStyle(el, cssprop) {
        if (el.currentStyle) { //IE
            return el.currentStyle[cssprop];
        } else if ($window.getComputedStyle) {
            return $window.getComputedStyle(el)[cssprop];
        }
        // finally try and get inline style
        return el.style[cssprop];
    }

    /**
     * Checks if a given element is statically positioned
     * @param element - raw DOM element
     */
    function isStaticPositioned(element) {
        return (getStyle(element, "position") || 'static') === 'static';
    }

    /**
     * returns the closest, non-statically positioned parentOffset of a given element
     * @param element
     */
    var parentOffsetEl = function (element) {
        var docDomEl = $document[0];
        var offsetParent = element.offsetParent || docDomEl;
        while (offsetParent && offsetParent !== docDomEl && isStaticPositioned(offsetParent)) {
            offsetParent = offsetParent.offsetParent;
        }
        return offsetParent || docDomEl;
    };

    return {
        /**
         * Provides read-only equivalent of jQuery's position function:
         * http://api.jquery.com/position/
         */
        position: function (element) {
            var elBCR = this.offset(element);
            var offsetParentBCR = {
                top: 0,
                left: 0
            };
            var offsetParentEl = parentOffsetEl(element[0]);
            if (offsetParentEl !== $document[0]) {
                offsetParentBCR = this.offset(angular.element(offsetParentEl));
                offsetParentBCR.top += offsetParentEl.clientTop - offsetParentEl.scrollTop;
                offsetParentBCR.left += offsetParentEl.clientLeft - offsetParentEl.scrollLeft;
            }

            return {
                width: element.prop('offsetWidth'),
                height: element.prop('offsetHeight'),
                top: elBCR.top - offsetParentBCR.top,
                left: elBCR.left - offsetParentBCR.left
            };
        },

        /**
         * Provides read-only equivalent of jQuery's offset function:
         * http://api.jquery.com/offset/
         */
        offset: function (element) {
            var boundingClientRect = element[0].getBoundingClientRect();
            return {
                width: element.prop('offsetWidth'),
                height: element.prop('offsetHeight'),
                top: boundingClientRect.top + ($window.pageYOffset || $document[0].body.scrollTop || $document[0].documentElement.scrollTop),
                left: boundingClientRect.left + ($window.pageXOffset || $document[0].body.scrollLeft || $document[0].documentElement.scrollLeft)
            };
        },
        
         /**
         * Provides functionality to check whether an element is in view port.
         */
        isElementInViewport: function (element) {
            if (element) {
                var rect = element[0].getBoundingClientRect();
                return (
                    rect.top >= 0 &&
                    rect.left >= 0 &&
                    rect.bottom <= ($window.innerHeight || $document[0].documentElement.clientHeight) &&
                    rect.right <= ($window.innerWidth || $document[0].documentElement.clientWidth)
                );
            } else {
                return false;
            }
        }
    };
}])

.factory('$isElement', [function () {
    var isElement = function (currentElem, targetElem, alternateElem) {
        if (currentElem[0] === targetElem[0]) {
            return true;
        } else if (currentElem[0] === alternateElem[0]) {
            return false;
        } else {
            return isElement((currentElem.parent()[0] && currentElem.parent()) || targetElem, targetElem, alternateElem);
        }
    };

    return isElement;
}])

.directive('attPosition', ['$position', function ($position) {
    return {
        restrict: 'A',
        link: function (scope, elem, attr) {
            scope.$watchCollection(function () {
                return $position.position(elem);
            }, function (value) {
                scope[attr.attPosition] = value;
            });
        }
    };
}]);

/**
 * @ngdoc directive
 * @name Forms.att:calendar
 *
 * @description
 *  <file src="src/calendar/docs/readme.md" />
 * @usage
 *  <input type="text" ng-model="dt" ddh-datepicker>
 *
 * @example
   <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
     <file src="src/calendar/docs/demo.html" />
     <file src="src/calendar/docs/demo.js" />
    </example>
   </section>
 */
angular.module('ddh.att.calendar', ['ddh.att.position', 'ddh.att.utilities'])

.constant('ddhDatepickerConfig', {
    dateFormat: 'MM/dd/yyyy',
    dayFormat: 'd',
    monthFormat: 'MMMM',
    yearFormat: 'yyyy',
    dayHeaderFormat: 'EEEE',
    dayTitleFormat: 'MMMM yyyy',
    disableWeekend: false,
    disableSunday: false,
    disableDates: null,
    onSelectClose: null,
    startingDay: 0,
    minDate: null,
    maxDate: null,
    dueDate: null,
    fromDate: null,
    legendIcon: null,
    legendMessage: null,
    calendarDisabled: false,
    collapseWait: 0,
    orientation: 'left',
    inline: false,
    helperText: 'The date you selected is $date. Double tap to open calendar. Select a date to close the calendar.',
    datepickerEvalAttributes: ['dateFormat', 'dayFormat', 'monthFormat', 'yearFormat', 'dayHeaderFormat', 'dayTitleFormat', 'disableWeekend', 'disableSunday', 'startingDay', 'collapseWait', 'orientation'],
    datepickerWatchAttributes: ['min', 'max', 'due', 'from', 'legendIcon', 'legendMessage', 'ngDisabled'],
    datepickerFunctionAttributes: ['disableDates', 'onSelectClose']
})

.factory('ddhDatepickerService', ['ddhDatepickerConfig', 'dateFilter', function (ddhDatepickerConfig, dateFilter) {
    var setAttributes = function (attr, elem) {
        if (angular.isDefined(attr) && attr !== null && angular.isDefined(elem) && elem !== null) {
            var attributes = ddhDatepickerConfig.datepickerEvalAttributes.concat(ddhDatepickerConfig.datepickerWatchAttributes, ddhDatepickerConfig.datepickerFunctionAttributes);
            for (var key in attr) {
                var val = attr[key];
                if (attributes.indexOf(key) !== -1 && angular.isDefined(val)) {
                    elem.attr(key.toSnakeCase(), key);
                }
            }
        }
    };

    var bindScope = function (attr, scope) {
        if (angular.isDefined(attr) && attr !== null && angular.isDefined(scope) && scope !== null) {
            var evalFunction = function (key, val) {
                scope[key] = scope.$parent.$eval(val);
            };

            var watchFunction = function (key, val) {
                scope.$parent.$watch(val, function (value) {
                    scope[key] = value;
                });
                scope.$watch(key, function (value) {
                    scope.$parent[val] = value;
                });
            };

            var evalAttributes = ddhDatepickerConfig.datepickerEvalAttributes;
            var watchAttributes = ddhDatepickerConfig.datepickerWatchAttributes;
            for (var key in attr) {
                var val = attr[key];
                if (evalAttributes.indexOf(key) !== -1 && angular.isDefined(val)) {
                    evalFunction(key, val);
                } else if (watchAttributes.indexOf(key) !== -1 && angular.isDefined(val)) {
                    watchFunction(key, val);
                }
            }
        }
    };

    return {
        setAttributes: setAttributes,
        bindScope: bindScope
    };
}])

.controller('ddhDatepickerController', ['$scope', '$attrs', 'dateFilter', '$element', '$position', 'ddhDatepickerConfig', function ($scope, $attrs, dateFilter, $element, $position, dtConfig) {
    var format = {
            date: getValue($attrs.dateFormat, dtConfig.dateFormat),
            day: getValue($attrs.dayFormat, dtConfig.dayFormat),
            month: getValue($attrs.monthFormat, dtConfig.monthFormat),
            year: getValue($attrs.yearFormat, dtConfig.yearFormat),
            dayHeader: getValue($attrs.dayHeaderFormat, dtConfig.dayHeaderFormat),
            dayTitle: getValue($attrs.dayTitleFormat, dtConfig.dayTitleFormat),
            disableWeekend: getValue($attrs.disableWeekend, dtConfig.disableWeekend),
            disableSunday: getValue($attrs.disableSunday, dtConfig.disableSunday),
            disableDates: getValue($attrs.disableDates, dtConfig.disableDates)
        },
        startingDay = getValue($attrs.startingDay, dtConfig.startingDay);

    $scope.minDate = dtConfig.minDate ? $scope.resetTime(dtConfig.minDate) : null;
    $scope.maxDate = dtConfig.maxDate ? $scope.resetTime(dtConfig.maxDate) : null;
    $scope.dueDate = dtConfig.dueDate ? $scope.resetTime(dtConfig.dueDate) : null;
    $scope.fromDate = dtConfig.fromDate ? $scope.resetTime(dtConfig.fromDate) : null;
    $scope.legendIcon = dtConfig.legendIcon ? dtConfig.legendIcon : null;
    $scope.legendMessage = dtConfig.legendMessage ? dtConfig.legendMessage : null;
    $scope.ngDisabled = dtConfig.calendarDisabled ? dtConfig.calendarDisabled : null;
    $scope.collapseWait = getValue($attrs.collapseWait, dtConfig.collapseWait);
    $scope.orientation = getValue($attrs.orientation, dtConfig.orientation);
    $scope.onSelectClose = getValue($attrs.onSelectClose, dtConfig.onSelectClose);

    $scope.inline = $attrs.inline === 'true' ? true : dtConfig.inline;

    function getValue(value, defaultValue) {
        return angular.isDefined(value) ? $scope.$parent.$eval(value) : defaultValue;
    }

    function getDaysInMonth(year, month) {
        return new Date(year, month, 0).getDate();
    }

    function getDates(startDate, n) {
        var dates = new Array(n);
        var current = startDate,
            i = 0;
        while (i < n) {
            dates[i++] = new Date(current);
            current.setDate(current.getDate() + 1);
        }
        return dates;
    }

    this.updatePosition = function (ddhDatepickerPopupTemplate) {
        $scope.position = $position.offset($element);
        $scope.position.top = $scope.position.top + $element.find('input').prop('offsetHeight');
        if ($scope.orientation === 'right') {
            $scope.position.left = $scope.position.left - (((ddhDatepickerPopupTemplate && ddhDatepickerPopupTemplate.prop('offsetWidth')) || 290) - $element.find('input').prop('offsetWidth'));
        }
    };

    function isSelected(dt) {
        if (dt && angular.isDate($scope.currentDate) && compare(dt, $scope.currentDate) === 0) {
            return true;
        }
        return false;
    }

    function isFromDate(dt) {
        if (dt && angular.isDate($scope.fromDate) && compare(dt, $scope.fromDate) === 0) {
            return true;
        }
        return false;
    }

    function isDateRange(dt) {
        if (dt && $scope.fromDate && angular.isDate($scope.currentDate) && (compare(dt, $scope.fromDate) >= 0) && (compare(dt, $scope.currentDate) <= 0)) {
            return true;
        } else if (dt && $scope.fromDate && compare(dt, $scope.fromDate) === 0) {
            return true;
        }
        return false;
    }

    function isOld(date, currentMonthDate) {
        if (date && currentMonthDate && (new Date(date.getFullYear(), date.getMonth(), 1, 0, 0, 0).getTime() < new Date(currentMonthDate.getFullYear(), currentMonthDate.getMonth(), 1, 0, 0, 0).getTime())) {
            return true;
        } else {
            return false;
        }
    }

    function isNew(date, currentMonthDate) {
        if (date && currentMonthDate && (new Date(date.getFullYear(), date.getMonth(), 1, 0, 0, 0).getTime() > new Date(currentMonthDate.getFullYear(), currentMonthDate.getMonth(), 1, 0, 0, 0).getTime())) {
            return true;
        } else {
            return false;
        }
    }

    function isPastDue(dt) {
        if ($scope.dueDate) {
            return (dt > $scope.dueDate);
        }
        return false;
    }

    function isDueDate(dt) {
        if ($scope.dueDate) {
            return (dt.getTime() === $scope.dueDate.getTime());
        }
        return false;
    }

    var isDisabled = function (date, currentMonthDate) {
        if ($attrs.from && !angular.isDate($scope.fromDate)) {
            return true;
        }
        if (format.disableWeekend === true && (dateFilter(date, format.dayHeader) === "Saturday" || dateFilter(date, format.dayHeader) === "Sunday")) {
            return true;
        }
        if (format.disableSunday === true && (dateFilter(date, format.dayHeader) === "Sunday")) {
            return true;
        }
        if (isSelected(date) || isOld(date, currentMonthDate) || isNew(date, currentMonthDate)) {
            return true;
        }
        return (($scope.minDate && compare(date, $scope.minDate) < 0) || ($scope.maxDate && compare(date, $scope.maxDate) > 0) || (format.disableDates && format.disableDates({
            date: date
        })));
    };

    var compare = function (date1, date2) {
        return (new Date(date1.getFullYear(), date1.getMonth(), date1.getDate()) - new Date(date2.getFullYear(), date2.getMonth(), date2.getDate()));
    };

    function isMinDateAvailable(startDate, endDate) {
        if (($scope.minDate && $scope.minDate.getTime() >= startDate.getTime()) && ($scope.minDate.getTime() <= endDate.getTime())) {
            $scope.disablePrev = true;
            $scope.visibilityPrev = "hidden";
        } else {
            $scope.disablePrev = false;
            $scope.visibilityPrev = "visible";
        }
    }

    function isMaxDateAvailable(startDate, endDate) {
        if (($scope.maxDate && $scope.maxDate.getTime() >= startDate.getTime()) && ($scope.maxDate.getTime() <= endDate.getTime())) {
            $scope.disableNext = true;
            $scope.visibilityNext = "hidden";
        } else {
            $scope.disableNext = false;
            $scope.visibilityNext = "visible";
        }
    }

    function getLabel(label) {
        if (label) {
            var labelObj = {
                pre: label.substr(0, 1).toUpperCase(),
                post: label
            };
            return labelObj;
        }
        return;
    }

    function makeDate(date, dayFormat, dayHeaderFormat, isSelected, isFromDate, isDateRange, isOld, isNew, isDisabled, dueDate, pastDue) {
        return {
            date: date,
            label: dateFilter(date, dayFormat),
            header: dateFilter(date, dayHeaderFormat),
            selected: !!isSelected,
            fromDate: !!isFromDate,
            dateRange: !!isDateRange,
            oldMonth: !!isOld,
            nextMonth: !!isNew,
            disabled: !!isDisabled,
            dueDate: !!dueDate,
            pastDue: !!pastDue,
            focusable: !((isDisabled && !(isSelected || isDateRange)) || (isOld || isNew))
        };
    }

    this.modes = [
        {
            name: 'day',
            getVisibleDates: function (date) {
                var year = date.getFullYear(),
                    month = date.getMonth(),
                    firstDayOfMonth = new Date(year, month, 1),
                    lastDayOfMonth = new Date(year, month + 1, 0);
                var difference = startingDay - firstDayOfMonth.getDay(),
                    numDisplayedFromPreviousMonth = (difference > 0) ? 7 - difference : -difference,
                    firstDate = new Date(firstDayOfMonth),
                    numDates = 0;

                if (numDisplayedFromPreviousMonth > 0) {
                    firstDate.setDate(-numDisplayedFromPreviousMonth + 1);
                    numDates += numDisplayedFromPreviousMonth; // Previous
                }
                numDates += getDaysInMonth(year, month + 1); // Current
                numDates += (7 - numDates % 7) % 7; // Next

                var days = getDates(firstDate, numDates),
                    labels = new Array(7);
                for (var i = 0; i < numDates; i++) {
                    var dt = new Date(days[i]);
                    days[i] = makeDate(dt,
                        format.day,
                        format.dayHeader,
                        isSelected(dt),
                        isFromDate(dt),
                        isDateRange(dt),
                        isOld(dt, date),
                        isNew(dt, date),
                        isDisabled(dt, date),
                        isDueDate(dt),
                        isPastDue(dt));
                }
                for (var j = 0; j < 7; j++) {
                    labels[j] = getLabel(dateFilter(days[j].date, format.dayHeader));
                }
                isMinDateAvailable(firstDayOfMonth, lastDayOfMonth);
                isMaxDateAvailable(firstDayOfMonth, lastDayOfMonth);
                return {
                    objects: days,
                    title: dateFilter(date, format.dayTitle),
                    labels: labels
                };
            },
            split: 7,
            step: {
                months: 1
            }
        }
    ];
}])

.directive('ddhDatepickerPopup', ['$parse', '$log', '$timeout', '$document', '$documentBind', '$isElement', '$templateCache', '$compile', 'trapFocusInElement', function ($parse, $log, $timeout, $document, $documentBind, $isElement, $templateCache, $compile, trapFocusInElement) {
    return {
        restrict: 'EA',
        replace: true,
        transclude: true,
        templateUrl: function (elem, attr) {
            if (attr.inline === 'true') {
                return 'template/calendar/datepicker-popup.html';
            } else {
                return 'template/calendar/datepicker.html';
            }
        },
        scope: {},
        require: ['ddhDatepickerPopup', 'ngModel', '?^ddhDatepickerGroup'],
        controller: 'ddhDatepickerController',
        link: function (scope, element, attrs, ctrls) {
            var datepickerCtrl = ctrls[0],
                ngModel = ctrls[1],
                ddhDatepickerGroupCtrl = ctrls[2];
            var ddhDatepickerPopupTemplate;

            if (!ngModel) {
                $log.error("ng-model is required.");
                return; // do nothing if no ng-model
            }

            // Configuration parameters
            var mode = 0,
                selected;
            scope.isOpen = false;

            scope.headers = [];
            scope.footers = [];

            if (ddhDatepickerGroupCtrl) {
                ddhDatepickerGroupCtrl.registerDatepickerScope(scope);
            }

            element.find('button').bind('click', function () {
                element.find('input')[0].click();
            });

            element.find('input').bind('click', function () {
                if (!scope.ngDisabled) {
                    scope.isOpen = !scope.isOpen;
                    toggleCalendar(scope.isOpen);
                    scope.$apply();
                    datepickerCtrl.updatePosition(ddhDatepickerPopupTemplate);
                }
            });

            var toggleCalendar = function (flag) {
                if (!scope.inline) {
                    if (flag) {
                        ddhDatepickerPopupTemplate = angular.element($templateCache.get('template/calendar/datepicker-popup.html'));
                        ddhDatepickerPopupTemplate = $compile(ddhDatepickerPopupTemplate)(scope);
                        $document.find('body').append(ddhDatepickerPopupTemplate);
                        $timeout(function () {
                            scope.getFocus = true;
                            trapFocusInElement(flag, ddhDatepickerPopupTemplate);
                            scope.$apply();
                            $timeout(function () {
                                scope.getFocus = false;
                                scope.$apply();
                            }, 100);
                        });
                    } else {
                        ddhDatepickerPopupTemplate.remove();
                        element.find('button')[0].focus();
                        scope.getFocus = false;
                        trapFocusInElement(flag, ddhDatepickerPopupTemplate);
                    }
                }
            };

            var outsideClick = function (e) {
                var isElement = $isElement(angular.element(e.target), element, $document);
                var isDdhDatepickerPopupTemplate = $isElement(angular.element(e.target), ddhDatepickerPopupTemplate, $document);
                if (!(isElement || isDdhDatepickerPopupTemplate)) {
                    scope.isOpen = false;
                    toggleCalendar(scope.isOpen);
                    scope.$apply();
                }
            };

            $documentBind.click('isOpen', outsideClick, scope);

            scope.$on('$destroy', function () {
                if (scope.isOpen) {
                    scope.isOpen = false;
                    toggleCalendar(scope.isOpen);
                }
            });

            scope.resetTime = function (date) {
                if (typeof date === 'string') {
                    date = date + 'T12:00:00';
                }
                var dt;
                if (!isNaN(new Date(date))) {
                    dt = new Date(date);
                } else {
                    return null;
                }
                return new Date(dt.getFullYear(), dt.getMonth(), dt.getDate());
            };

            if (attrs.min) {
                scope.$parent.$watch($parse(attrs.min), function (value) {
                    scope.minDate = value ? scope.resetTime(value) : null;
                    refill();
                });
            }
            if (attrs.max) {
                scope.$parent.$watch($parse(attrs.max), function (value) {
                    scope.maxDate = value ? scope.resetTime(value) : null;
                    refill();
                });
            }
            if (attrs.due) {
                scope.$parent.$watch($parse(attrs.due), function (value) {
                    scope.dueDate = value ? scope.resetTime(value) : null;
                    refill();
                });
            }
            if (attrs.from) {
                scope.$parent.$watch($parse(attrs.from), function (value) {
                    scope.fromDate = value ? scope.resetTime(value) : null;
                    refill();
                });
            }

            if (attrs.legendIcon) {
                scope.$parent.$watch(attrs.legendIcon, function (value) {
                    scope.legendIcon = value ? value : null;
                    refill();
                });
            }
            if (attrs.legendMessage) {
                scope.$parent.$watch(attrs.legendMessage, function (value) {
                    scope.legendMessage = value ? value : null;
                    refill();
                });
            }
            if (attrs.ngDisabled) {
                scope.$parent.$watch(attrs.ngDisabled, function (value) {
                    scope.ngDisabled = value ? value : null;
                });
            }

            // Split array into smaller arrays
            function split(arr, size) {
                var arrays = [];
                while (arr.length > 0) {
                    arrays.push(arr.splice(0, size));
                }
                return arrays;
            }

            function refill(date) {
                if (angular.isDate(date) && !isNaN(date)) {
                    selected = new Date(date);
                } else {
                    if (!selected) {
                        selected = new Date();
                    }
                }

                if (selected) {
                    var currentMode = datepickerCtrl.modes[mode],
                        data = currentMode.getVisibleDates(selected);

                    scope.rows = split(data.objects, currentMode.split);
                    scope.labels = data.labels || [];
                    scope.title = data.title;

                    datepickerCtrl.updatePosition(ddhDatepickerPopupTemplate);
                }
            }

            scope.select = function (date) {
                var dt = new Date(date.getFullYear(), date.getMonth(), date.getDate());
                scope.currentDate = dt;
                if (!scope.onSelectClose || (scope.onSelectClose && scope.onSelectClose({
                        date: dt
                    }) !== false)) {
                    if (angular.isNumber(scope.collapseWait)) {
                        $timeout(function () {
                            scope.isOpen = false;
                            toggleCalendar(scope.isOpen);
                        }, scope.collapseWait);
                    } else {
                        scope.isOpen = false;
                        toggleCalendar(scope.isOpen);
                    }
                }
            };

            scope.move = function (direction) {
                var step = datepickerCtrl.modes[mode].step;
                selected.setDate(1);
                selected.setMonth(selected.getMonth() + direction * (step.months || 0));
                selected.setFullYear(selected.getFullYear() + direction * (step.years || 0));
                refill();
            };

            scope.$watch('currentDate', function (value) {
                if (angular.isDefined(value) && value !== null) {
                    refill(value);
                } else {
                    refill();
                }
                ngModel.$setViewValue(value);
            });

            ngModel.$render = function () {
                scope.currentDate = ngModel.$viewValue;
            };

            var stringToDate = function (value) {
                if (!isNaN(new Date(value))) {
                    value = new Date(value);
                }
                return value;
            };
            ngModel.$formatters.unshift(stringToDate);
        }
    };
}])

.directive('ddhDatepicker', ['$compile', '$log', 'ddhDatepickerConfig', 'ddhDatepickerService', function ($compile, $log, ddhDatepickerConfig, ddhDatepickerService) {
    return {
        restrict: 'A',
        scope: {
            disableDates: '&',
            onSelectClose: '&'
        },
        require: 'ngModel',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            var dateFormatString = angular.isDefined(attr.dateFormat) ? scope.$parent.$eval(attr.dateFormat) : ddhDatepickerConfig.dateFormat;
            var helperText = angular.isDefined(attr.helperText) ? scope.$parent.$eval(attr.helperText) : ddhDatepickerConfig.helperText;
            helperText = helperText.replace('$date', '{{dt | date : \'' + dateFormatString + '\'}}');

            var inline = false;
            if (elem.prop('nodeName') !== 'INPUT') {
                inline = true;
            }

            var selectedDateMessage = '<button type="button" class="span12 faux-input" ng-disabled="ngDisabled"><span class="hidden-spoken" aria-live="assertive" aria-atomic="false">' + helperText + '</span></button>';

            elem.removeAttr('ddh-datepicker');
            elem.removeAttr('ng-model');
            elem.removeAttr('ng-disabled');
            elem.addClass('datepicker-input');
            elem.attr('ng-model', 'dt');
            elem.attr('aria-describedby', 'datepicker');
            elem.attr('aria-hidden', 'true');
            elem.attr('tabindex', '-1');
            elem.attr('readonly', 'true');
            elem.attr('ng-disabled', 'ngDisabled');
            elem.attr('ddh-format-date', dateFormatString);

            var wrapperElement = angular.element('<div></div>');
            wrapperElement.attr('ddh-datepicker-popup', '');
            wrapperElement.attr('ng-model', 'dt');
            if (inline) {
                wrapperElement.attr('inline', inline);
            }

            ddhDatepickerService.setAttributes(attr, wrapperElement);
            ddhDatepickerService.bindScope(attr, scope);

            wrapperElement.html('');
            wrapperElement.append(selectedDateMessage);
            wrapperElement.append(elem.prop('outerHTML'));

            var elm = wrapperElement.prop('outerHTML');
            elm = $compile(elm)(scope);
            elem.replaceWith(elm);
        }],
        link: function (scope, elem, attr, ctrl) {
            if (!ctrl) {
                $log.error("ng-model is required.");
                return; // do nothing if no ng-model
            }

            scope.$watch('dt', function (value) {
                ctrl.$setViewValue(value);
            });
            ctrl.$render = function () {
                scope.dt = ctrl.$viewValue;
            };
        }
    };
}])

.directive('ddhDatepickerGroup', [function () {
    return {
        restrict: 'EA',
        controller: ['$scope', '$element', '$attrs', function (scope, elem, attr) {
            this.$$headers = [];
            this.$$footers = [];
            this.registerDatepickerScope = function (datepickerScope) {
                datepickerScope.headers = this.$$headers;
                datepickerScope.footers = this.$$footers;
            };
        }],
        link: function (scope, elem, attr, ctrl) {}
    };
}])

.directive('ddhFormatDate', ['dateFilter', function (dateFilter) {
    return {
        restrict: 'A',
        require: 'ngModel',
        link: function (scope, elem, attr, ctrl) {
            var ddhFormatDate = "";
            attr.$observe('ddhFormatDate', function (value) {
                ddhFormatDate = value;
            });
            var dateToString = function (value) {
                if (!isNaN(new Date(value))) {
                    return dateFilter(new Date(value), ddhFormatDate);
                }
                return value;
            };
            ctrl.$formatters.unshift(dateToString);
        }
    };
}])

.directive('ddhDatepickerHeader', [function () {
    return {
        restrict: 'EA',
        require: '^ddhDatepickerGroup',
        transclude: true,
        replace: true,
        template: '',
        compile: function (elem, attr, transclude) {
            return function link(scope, elem, attr, ctrl) {
                if (ctrl) {
                    ctrl.$$headers.push(transclude(scope, function () {}));
                }
                elem.remove();
            };
        }
    };
}])

.directive('ddhDatepickerFooter', [function () {
    return {
        restrict: 'EA',
        require: '^ddhDatepickerGroup',
        transclude: true,
        replace: true,
        template: '',
        compile: function (elem, attr, transclude) {
            return function link(scope, elem, attr, ctrl) {
                if (ctrl) {
                    ctrl.$$footers.push(transclude(scope, function () {}));
                }
                elem.remove();
            };
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Forms.att:checkboxes
 *
 * @description
 *  <file src="src/checkboxes/docs/readme.md" />
 * @usage
 <!-- Normal CheckBoxes-->
 <label for="checkbox1" class="checkbox">
 <input id="checkbox1" ddh-accessibility-click="13" type="checkbox" role="checkbox" /><i class="skin"></i><span>Normal checkbox</span></label>

 <label for="checkbox4" class="checkbox">
 <input id="checkbox4" type="checkbox" disabled /><i class="skin"></i><span>Disabled checkbox</span></label>
<div class="form-row error">
 <label for="checkbox6" class="checkbox">
<input id="checkbox6" accessibility-click="13" type="checkbox" role="checkbox" required /><i class="skin"></i><span>Required Checkbox</span></label>
</div>
<!-- Checkbox Group -->
    <label for="checkbox7" class="checkbox">
        <input id="checkbox7" ddh-accessibility-click="13" type="checkbox" role="checkbox" ng-model="cbGroupOption"  select-group checkboxes="ListOfItems" /><i class="skin"></i><span>Select All/Deselect All</span></label>
    <div ng-repeat="item in ListOfItems">
        <label class="checkbox child-checkbox">
        <input type="checkbox" role="checkbox" ddh-accessibility-click="13" ng-model="item.isSelected" /><i class="skin"></i><span>{{item.desc}}</span></label>
    </div>
 * @example
 <example module="ddh.att">
 <file src="src/checkboxes/docs/demo.html" />
 <file src="src/checkboxes/docs/demo.js" />
 </example>
 */
angular.module('ddh.att.checkboxes', ['ddh.att.utilities']).directive('ddhSelectGroup', [function (){
        return {
            restrict: 'A',
            require: 'ngModel',
            scope: {
                checkboxes: "="
            },
            link: function (scope, elem, attr, ctrl) {
                elem.bind('change', function () {
                    var isChecked = elem.prop('checked');
                    angular.forEach(scope.checkboxes, function (item) {
                        item.isSelected = isChecked;
                    });
                    scope.$apply();
                });
                scope.$watch('checkboxes', function () {
                    var allSet = true, allClear = true;
                    angular.forEach(scope.checkboxes, function (item) {
                        if (item.isSelected) {
                            allClear = false;
                        } else {
                            allSet = false;
                        }
                    });
                    elem.prop('indeterminate', false);
                    if (allSet) {
                        ctrl.$setViewValue(true);
                    } else if (allClear) {
                       ctrl.$setViewValue(false);
                    } else {
                        ctrl.$setViewValue(false);
                        elem.prop('indeterminate', true);
                    }
                    ctrl.$render();
                }, true);
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Product selection.att:colorAndDataCapacity
 *
 * @description
 *  <file src="src/colorAndDataCapacity/docs/readme.md" />
 *
 * @usage
 *   Please refer demo.html tab in Example section below.
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/colorAndDataCapacity/docs/demo.html" />
            <file src="src/colorAndDataCapacity/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.colorAndDataCapacity', [])
    .directive('ddhColorAndDataCapacity', function () {
        return {
            restrict: 'A',
            scope: {
                ddhColorAndDataCapacity: '='
            },
            controller: ['$scope', function ($scope) {
                this.setItemIdSelected = function (item) {
                    $scope.ddhColorAndDataCapacity = item.id;
                };
            }]
        };
    })
    .directive('ddhColorAndDataCapacityItem', [function () {
        return {
            restrict: 'A',
            require: '^ddhColorAndDataCapacity',
            scope: {
                ddhColorAndDataCapacityItem: "="
            },
            link: function (scope, element, attr, ddhColorAndDataCapacityCtrl) {

                element.bind('click', function () {
                    scope.$apply(function () {
                        ddhColorAndDataCapacityCtrl.setItemIdSelected(scope.ddhColorAndDataCapacityItem);
                    });
                });
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Progress & usage indicators.att:stepIndicator
 *
 * @description
 *  <file src="src/stepIndicator/docs/readme.md" />
 *
 * @usage
 * <div ng-controller="stepIndicatorController">
    <section id="overview" class="bottom-space">
        <div class="span12">
            <div ddh-step-indicator steps-items-object="stepsObject" current-index="currentStepIndex" step-indicator-heading="stepHeading">
                <div class="row-nowrap step-confirmation" ng-show="{{currentStepIndex === stepsObject.length}}">
                    <i class="icon-primary-approval"></i>
                    <div>
                        <h3><strong>Lorem success!</strong></h3>
                        <p>Lorem message of confirmation that process has been complete.</p>
                    </div>
                </div>
            </div>
        </div>
        <hr>
         <div class="span12">
            <div ddh-step-indicator steps-items-object="stepsObject2" current-index="currentStepIndex2" step-indicator-heading='stepHeading2'>
                <div class="row-nowrap step-confirmation" ng-show="{{currentStepIndex2 === stepsObject2.length}}">
                    <i class="icon-primary-approval"></i>
                    <div>
                        <h3><strong>success!</strong></h3>
                        <p>You've created your AT&T Access ID.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

 * @example
 *  <section id="code">   
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/stepIndicator/docs/demo.html" />
 <file src="src/stepIndicator/docs/demo.js" />
 </example>
 </section>
 *
 */
angular.module('ddh.att.stepIndicator', ['ddh.att.utilities']).
        directive('ddhStepIndicator', [function() {
                return {
                    restrict: 'EA',
                    transclude: true,
                    scope:{
                        stepsItemsObject:"=",
                        currentIndex:"=",
                        stepIndicatorHeading:"="
                        
                    },
                    templateUrl: 'template/stepIndicator/stepIndicator.html',
                    link: function(scope, ele, attr) {
                       
                    }

                };

            }]);
     
/**
 * @ngdoc directive
 * @name Headings & copy.att:Confirmation with in Process
 *
 * @description
 *  <file src="src/confirmationProcess/docs/readme.md" />
 *
 * @usage
 * <h2 class="ds2 heading-group">Confirmation with in Process</h2>
 * <p>
 * .....
 * </p> 
 *
 * @example
 <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
        <file src="src/confirmationProcess/docs/demo.html" />
        <file src="src/confirmationProcess/docs/demo.js" />
    </example>
</section>
 */

var ddhLegalCopy = angular.module('ddh.att.confirmationProcess', ['ddh.att.stepIndicator']);
/**
 * @ngdoc directive
 * @name Forms.att:creditCard
 *
 * @description
 *  <file src="src/creditCard/docs/readme.md" />
 *
 * @usage
 * <form name="ccForm" ng-controller="ddhCreditCardFormController">
 *  <div data-credit-card-image="ccForm.card.$card.type"></div>
 *  <div security-code-image="ccForm.card.$card.cvcSecurityImg"></div>
 * </form>
 * @example
 *  <section id="code">
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/creditCard/docs/demo.html" />
 <file src="src/creditCard/docs/demo.js" />
 </example>
 </section>
 *
 */

angular.module('ddh.att.creditCard', ['ddh.att.dropdowns', 'ddh.att.utilities'])
.factory('Cards', [function () {
    var defaultFormat = /(\d{1,4})/g;
    var defaultInputFormat = /(?:^|\s)(\d{4})$/;
    var cards = [
        {
            type: 'discover',
            pattern: /^(6011|65|64[4-9]|622)/,
            format: defaultFormat,
            inputFormat: defaultInputFormat,
            length: [16],
            cvcLength: [3],
            cvcSecurityImg: 'visaI',
            zipLength: [5],
            luhn: true
        },
        {
            type: 'mc',
            pattern: /^5[1-5]/,
            format: defaultFormat,
            inputFormat: defaultInputFormat,
            length: [16],
            cvcLength: [3],
            cvcSecurityImg: 'visaI',
            zipLength: [5],
            luhn: true
        },
        {
            type: 'amex',
            pattern: /^3[47]/,
            format: /(\d{1,4})(\d{1,6})?(\d{1,5})?/,
            inputFormat: /^(\d{4}|\d{4}\s\d{6})$/,
            length: [15],
            cvcLength: [4],
            cvcSecurityImg: 'amexI',
            zipLength: [5],
            luhn: true
        },
        {
            type: 'visa',
            pattern: /^4/,
            format: defaultFormat,
            inputFormat: defaultInputFormat,
            length: [16],
            cvcLength: [3],
            cvcSecurityImg: 'visaI',
            zipLength: [5],
            luhn: true
        }
    ];

    var _fromNumber = function (num) {
        var card, i, len;

        num = (num + '').replace(/\D/g, '');

        for (i = 0, len = cards.length; i < len; i++) {

            card = cards[i];

            if (card.pattern.test(num)) {
                return card;
            }

        }
    };

    var _fromType = function (type) {
        var card, i, len;

        for (i = 0, len = cards.length; i < len; i++) {

            card = cards[i];

            if (card.type === type) {
                return card;
            }

        }
    };

    return {
        fromNumber: function (val) {
            return _fromNumber(val);
        },
        fromType: function (val) {
            return _fromType(val);
        },
        defaultFormat: function () {
            return defaultFormat;
        },
        defaultInputFormat: function () {
            return defaultInputFormat;
        }
    };

}])
.factory('_Validate', ['Cards', '$parse', function (Cards, $parse) {
    var __indexOf = [].indexOf || function (item) {
        for (var i = 0, l = this.length; i < l; i++) {
            if (i in this && this[i] === item) {
                return i;
            }
        }
        return -1;
    };

    var _luhnCheck = function (num) {
        var digit, digits, odd, sum, i, len;

        odd = true;
        sum = 0;
        digits = (num + '').split('').reverse();

        for (i = 0, len = digits.length; i < len; i++) {

            digit = digits[i];
            digit = parseInt(digit, 10);

            if ((odd = !odd)) {
                digit *= 2;
            }

            if (digit > 9) {
                digit -= 9;
            }

            sum += digit;

        }

        return sum % 10 === 0;
    };

    var _validators = {};

    _validators['cvc'] = function (cvc, ctrl, scope, attr) {
        var ref, ref1;

        // valid if empty - let ng-required handle empty
        if ((angular.isUndefined(cvc)) || (cvc === null) || (cvc.length === 0)) {
            return true;
        }

        if (!/^\d+$/.test(cvc)) {
            return false;
        }

        var type;
        if (attr.paymentsTypeModel) {
            var typeModel = $parse(attr.paymentsTypeModel);
            type = typeModel(scope);
        }

        if (type) {
            ref1 = Cards.fromType(type);
            return (ref = cvc.length, __indexOf.call((ref1 !== null) ? ref1.cvcLength : void 0, ref)) >= 0;
        } else {
            return cvc.length >= 3 && cvc.length <= 4;
        }
    };
    _validators['zip'] = function (zip, ctrl, scope, attr) {
        var ref, ref1;

        // valid if empty - let ng-required handle empty
        if ((angular.isUndefined(zip)) || (zip === null) || (zip.length === 0)) {
            return true;
        }

        if (!/^\d+$/.test(zip)) {
            return false;
        }

        var type;
        if (attr.paymentsTypeModel) {
            var typeModel = $parse(attr.paymentsTypeModel);
            type = typeModel(scope);
        }
        if (type) {
            ref1 = Cards.fromType(type);
            return (ref = zip.length, __indexOf.call(ref1 !== null ? ref1.zipLength : void 0, ref)) >= 0;
        } else {
            return zip.length < 6;
        }
    };
    _validators['card'] = function (num, ctrl, scope, attr) {
        var card, ref, typeModel;

        if (attr.paymentsTypeModel) {
            typeModel = $parse(attr.paymentsTypeModel);
        }

        var clearCard = function () {
            if (typeModel) {
                typeModel.assign(scope, null);
            }
            ctrl.$card = null;
        };

        // valid if empty - let ng-required handle empty
        if ((angular.isUndefined(num)) || (num === null) || (num.length === 0)) {
            clearCard();
            return true;
        }

        num = (num + '').replace(/\s+|-/g, '');

        if (!/^\d+$/.test(num)) {
            clearCard();
            return false;
        }

        card = Cards.fromNumber(num);
        if (!card) {
            clearCard();
            return false;
        }
        ctrl.$card = angular.copy(card);

        if (typeModel) {
            typeModel.assign(scope, card.type);
        }

        ret = (ref = num.length, __indexOf.call(card.length, ref) >= 0) && (card.luhn === false || _luhnCheck(num));
        return ret;
    };
    return function (type, val, ctrl, scope, attr) {
        if (!_validators[type]) {

            types = Object.keys(_validators);

            errstr = 'Unknown type for validation: "' + type + '". ';
            errstr += 'Should be one of: "' + types.join('", "') + '"';

            throw errstr;
        }
        return _validators[type](val, ctrl, scope, attr);
    };
}])
.factory('_ValidateWatch', ['_Validate', function (_Validate) {

    var _validatorWatches = {};

    _validatorWatches['cvc'] = function (type, ctrl, scope, attr) {
        if (attr.paymentsTypeModel) {
            scope.$watch(attr.paymentsTypeModel, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    var valid = _Validate(type, ctrl.$modelValue, ctrl, scope, attr);
                    ctrl.$setValidity(type, valid);
                }
            });
        }
    };
    _validatorWatches['zip'] = function (type, ctrl, scope, attr) {
        if (attr.paymentsTypeModel) {
            scope.$watch(attr.paymentsTypeModel, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    var valid = _Validate(type, ctrl.$modelValue, ctrl, scope, attr);
                    ctrl.$setValidity(type, valid);
                }
            });
        }
    };
    return function (type, ctrl, scope, attr) {
        if (_validatorWatches[type]) {
            return _validatorWatches[type](type, ctrl, scope, attr);
        }
    };
}])
.directive('validateCard', ['$window', '_Validate', '_ValidateWatch', function ($window, _Validate, _ValidateWatch) {
    return {
        restrict: 'A',
        require: 'ngModel',
        link: function (scope, elem, attr, ctrl) {

            var type = attr.validateCard;
            _ValidateWatch(type, ctrl, scope, attr);
            var validateFn = function (val) {
                var valid = _Validate(type, val, ctrl, scope, attr);
                ctrl.$setValidity(type, valid);
                if (type === 'card') {
                    if (ctrl.$card === null) {
                        if ((val == null) || (val === "") || (val === '')) {
                            scope.invalidCardError = '';
                            scope.invalidCard = "";
                        } else if (val.length > 1) {
                            scope.invalidCardError = 'error';
                            scope.invalidCard = "The number entered is not a recognized credit card number.";
                        }
                    } else {
                        if (!valid) {
                            if (ctrl.$card.length.indexOf(val.length) >= 0) {
                                scope.invalidCardError = 'error';
                                scope.invalidCard = "The number entered is not a recognized credit card number.";
                            } else {
                                scope.invalidCardError = '';
                                scope.invalidCard = "";
                            }
                        } else {
                            scope.invalidCardError = '';
                            scope.invalidCard = "";
                        }
                    }
                    elem.bind("blur", function () {
                        if ((!valid) || (ctrl.$card === null)) {
                            scope.invalidCardError = 'error';
                            scope.invalidCard = "The number entered is not a recognized credit card number.";
                        } else {
                            scope.invalidCardError = '';
                            scope.invalidCard = "";
                        }
                    });
                }
                return valid ? val : undefined;
            };
            ctrl.$formatters.push(validateFn);
            ctrl.$parsers.push(validateFn);
        }
    };
}])
.directive('creditCardImage', function () {
    return {
        templateUrl: 'template/creditCard/creditCardImage.html',
        replace: false,
        transclude: false,
        link: function (scope, element, attr) {
            scope.$watch(attr.creditCardImage, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    scope.cvc = '';
                    if (!angular.isUndefined(newVal) && newVal !== null) {
                        scope.newValCCI = 'show-' + newVal;
                    }
                    if (newVal === null || newVal === "" || angular.isUndefined(newVal)) {
                        scope.newValCCI = '';
                    }
                }
            });
        }
    };
})
.directive('securityCodeImage', ['$document', function ($document) {
    return {
        templateUrl: 'template/creditCard/cvcSecurityImg.html',
        replace: false,
        transclude: false,
        link: function (scope, element, attr) {
            scope.$watch(attr.securityCodeImage, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    if (!angular.isUndefined(newVal) && newVal !== null) {
                        if (newVal === 'amexI') {
                            scope.newValI = 'ccv2-security-amex';
                            scope.newValIAlt = "The 4 digit CVC security code is on the front of the card.";
                            scope.cvcPlaceholder = "4 digits";
                            scope.cvcMaxlength = 4;
                        } else if (newVal === 'visaI') {
                            scope.newValI = 'ccv2-security';
                            scope.newValIAlt = "The CVC security code is on the back of your card right after the credit card number.";
                            scope.cvcPlaceholder = "3 digits";
                            scope.cvcMaxlength = 3;
                        }
                    }
                    if (newVal === null) {
                        scope.newValI = 'ccv2-security';
                        scope.cvcPlaceholder = "3 digits";
                        scope.cvcMaxlength = 3;
                        scope.newValIAlt = "The CVC security code is on the back of your card right after the credit card number.";
                    }

                }
            });

            var window = angular.element($document);
            window.bind("click", function (ev) {
                var targetClassname = ev.target.className;
                if ((targetClassname !== "btn btn-alt btn-tooltip active")) {
                    if (element.find("button").hasClass("active")) {
                        element.find("button").removeClass("active");
                    }
                }

            });
        }
    };
}])
.directive('tooltipCardImage', ['$document', function ($document) {
    return {
        templateUrl: 'template/creditCard/tooltipCardImage.html',
        replace: false,
        transclude: false,
        link: function (scope, element, attr) {
            var window = angular.element($document);
            window.bind("click", function (ev) {
                var targetClassname = ev.target.className;
                if ((targetClassname !== "btn btn-alt btn-tooltip active")) {
                    if (element.find("button").hasClass("active")) {
                        element.find("button").removeClass("active");
                    }
                }

            });
        }
    };
}])
.directive('ddhAlphaNumeric', ['keymap', function (keymap) {
    return {
        restrict: 'A',
        scope: {
            msize: '='
        },
        link: function (scope, elm, attrs, ctrl) {
            if (attrs.maxlength) {
                scope.msize = attrs.maxlength;
            }
            elm.on('keydown', function (event) {
                if (elm.val().length > scope.msize) {
                    if ((event.which == 37) || (event.which == 38) || (event.which == 39) || (event.which == 40) || (event.which == 46) || (event.which == 8) || (event.which == 9)) {
                        return true;
                    } else {
                        event.preventDefault();
                        return false;
                    }
                }
                if (event.shiftKey && event.keyCode !== 9) {
                    event.preventDefault();
                    return false;
                }
                if ((keymap.isAlphaNumericKey(event) == true) || (event.which == 37) || (event.which == 38) || (event.which == 39) || (event.which == 40) || (event.which == 46) || (event.which == 8) || (event.which == 9)) {
                    return true;
                } else {
                    event.preventDefault();
                    return false;
                }
            });
        }
    }
}])
.directive('ddhOnlyDigitsShift', ['keymap', function (keymap) {
    return {
        restrict: 'A',
        scope: {
            msize: '='
        },
        link: function (scope, elm, attrs, ctrl) {
            if (attrs.maxlength) {
                scope.msize = attrs.maxlength;
            }
            elm.on('keydown', function (event) {
                if (elm.val().length > scope.msize) {
                    if ((event.which === 37) || (event.which === 38) || (event.which === 39) || (event.which === 40) || (event.which === 46) || (event.which === 8) || (event.which === 9)) {
                        return true;
                    } else {
                        event.preventDefault();
                        return false;
                    }
                }
                if (event.shiftKey && event.keyCode !== 9) {
                    event.preventDefault();
                    return false;
                }
                if ((keymap.isNumericKey(event) === true) || (event.which === 37) || (event.which === 38) || (event.which === 39) || (event.which === 40) || (event.which === 46) || (event.which === 8) || (event.which === 9)) {
                    return true;
                } else {
                    event.preventDefault();
                    return false;
                }
            });
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Forms.att:dropdownVariations
 *
 * @description
 *  <file src="src/dropdownVariations/docs/readme.md" />
 *
 * @usage
 * <select name="myNameBig" type="large" ddh-dropdown ng-model="Controller Variable here">
 *   <option ddh-dropdown-list option-repeat="option data here" imgsrc="image path" value="value">List Text</option>
 * </select>
 *
 * <select name="myNameBig" type="large" ddh-dropdown ng-model="Controller Variable here">
 * <option ddh-dropdown-list option-repeat="option data here" imgsrc="image path" value="value">List Text</option>
 * </select>
 *
 * <select name="myNameBig" ddh-dropdown ng-model="Controller Variable here">
 *   <optgroup ddh-dropdown-group label="Group Label here">
 *     <option ddh-dropdown-list option-repeat="option data here" imgsrc="image path" value="value">List Text</option>
 *   </optgroup>
 * </select>
 *
 *  @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/dropdownVariations/docs/demo.html" />
            <file src="src/dropdownVariations/docs/demo.js" />
       </example>
    </section>
 *
 */
angular.module('ddh.att.dropdownVariations', ['ddh.att.dropdowns']);
/**
 * @ngdoc directive
 * @name Forms.att:emphasizedTextField
 *
 * @description
 *  <file src="src/emphasizedTextField/docs/readme.md" />
 *
 * @usage
 *  <input type="text" id="fieldId" placeholder="placholder text here" class="span12 input-enhanced" name="fieldName">
 *
 * @example
 <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
        <file src="src/emphasizedTextField/docs/demo.html" />
        <file src="src/emphasizedTextField/docs/demo.js" />
    </example>
 </section>
 */
angular.module('ddh.att.emphasizedTextField', ['ddh.att.utilities']);

/*!
 * VERSION: 1.7.3
 * DATE: 2014-01-14
 * UPDATES AND DOCS AT: http://www.greensock.com
 *
 * @license Copyright (c) 2008-2014, GreenSock. All rights reserved.
 * This work is subject to the terms at http://www.greensock.com/terms_of_use.html or for
 * Club GreenSock members, the software agreement that was issued with your membership.
 * 
 * @author: Jack Doyle, jack@greensock.com
 **/
(window._gsQueue || (window._gsQueue = [])).push( function() {

    "use strict";

    var _doc = document.documentElement,
        _window = window,
        _max = function(element, axis) {
            var dim = (axis === "x") ? "Width" : "Height",
                scroll = "scroll" + dim,
                client = "client" + dim,
                body = document.body;
            return (element === _window || element === _doc || element === body) ? Math.max(_doc[scroll], body[scroll]) - (_window["inner" + dim] || Math.max(_doc[client], body[client])) : element[scroll] - element["offset" + dim];
        },

        ScrollToPlugin = window._gsDefine.plugin({
            propName: "scrollTo",
            API: 2,
            version:"1.7.3",

            //called when the tween renders for the first time. This is where initial values should be recorded and any setup routines should run.
            init: function(target, value, tween) {
                this._wdw = (target === _window);
                this._target = target;
                this._tween = tween;
                if (typeof(value) !== "object") {
                    value = {y:value}; //if we don't receive an object as the parameter, assume the user intends "y".
                }
                this._autoKill = (value.autoKill !== false);
                this.x = this.xPrev = this.getX();
                this.y = this.yPrev = this.getY();
                if (value.x != null) {
                    this._addTween(this, "x", this.x, (value.x === "max") ? _max(target, "x") : value.x, "scrollTo_x", true);
                    this._overwriteProps.push("scrollTo_x");
                } else {
                    this.skipX = true;
                }
                if (value.y != null) {
                    this._addTween(this, "y", this.y, (value.y === "max") ? _max(target, "y") : value.y, "scrollTo_y", true);
                    this._overwriteProps.push("scrollTo_y");
                } else {
                    this.skipY = true;
                }
                return true;
            },

            //called each time the values should be updated, and the ratio gets passed as the only parameter (typically it's a value between 0 and 1, but it can exceed those when using an ease like Elastic.easeOut or Back.easeOut, etc.)
            set: function(v) {
                this._super.setRatio.call(this, v);

                var x = (this._wdw || !this.skipX) ? this.getX() : this.xPrev,
                    y = (this._wdw || !this.skipY) ? this.getY() : this.yPrev,
                    yDif = y - this.yPrev,
                    xDif = x - this.xPrev;

                if (this._autoKill) {
                    //note: iOS has a bug that throws off the scroll by several pixels, so we need to check if it's within 7 pixels of the previous one that we set instead of just looking for an exact match.
                    if (!this.skipX && (xDif > 7 || xDif < -7) && x < _max(this._target, "x")) {
                        this.skipX = true; //if the user scrolls separately, we should stop tweening!
                    }
                    if (!this.skipY && (yDif > 7 || yDif < -7) && y < _max(this._target, "y")) {
                        this.skipY = true; //if the user scrolls separately, we should stop tweening!
                    }
                    if (this.skipX && this.skipY) {
                        this._tween.kill();
                    }
                }
                if (this._wdw) {
                    _window.scrollTo((!this.skipX) ? this.x : x, (!this.skipY) ? this.y : y);
                } else {
                    if (!this.skipY) {
                        this._target.scrollTop = this.y;
                    }
                    if (!this.skipX) {
                        this._target.scrollLeft = this.x;
                    }
                }
                this.xPrev = this.x;
                this.yPrev = this.y;
            }

        }),
        p = ScrollToPlugin.prototype;

    ScrollToPlugin.max = _max;

    p.getX = function() {
        return (!this._wdw) ? this._target.scrollLeft : (_window.pageXOffset != null) ? _window.pageXOffset : (_doc.scrollLeft != null) ? _doc.scrollLeft : document.body.scrollLeft;
    };

    p.getY = function() {
        return (!this._wdw) ? this._target.scrollTop : (_window.pageYOffset != null) ? _window.pageYOffset : (_doc.scrollTop != null) ? _doc.scrollTop : document.body.scrollTop;
    };

    p._kill = function(lookup) {
        if (lookup.scrollTo_x) {
            this.skipX = true;
        }
        if (lookup.scrollTo_y) {
            this.skipY = true;
        }
        return this._super._kill.call(this, lookup);
    };

}); if (window._gsDefine) { window._gsQueue.pop()(); }
/*!
 * VERSION: 1.12.1
 * DATE: 2014-06-26
 * UPDATES AND DOCS AT: http://www.greensock.com
 * 
 * Includes all of the following: TweenLite, TweenMax, TimelineLite, TimelineMax, EasePack, CSSPlugin, RoundPropsPlugin, BezierPlugin, AttrPlugin, DirectionalRotationPlugin
 *
 * @license Copyright (c) 2008-2014, GreenSock. All rights reserved.
 * This work is subject to the terms at http://www.greensock.com/terms_of_use.html or for
 * Club GreenSock members, the software agreement that was issued with your membership.
 * 
 * @author: Jack Doyle, jack@greensock.com
 **/

(window._gsQueue || (window._gsQueue = [])).push( function() {

    "use strict";

    window._gsDefine("TweenMax", ["core.Animation","core.SimpleTimeline","TweenLite"], function(Animation, SimpleTimeline, TweenLite) {

        var _slice = [].slice,
            TweenMax = function(target, duration, vars) {
                TweenLite.call(this, target, duration, vars);
                this._cycle = 0;
                this._yoyo = (this.vars.yoyo === true);
                this._repeat = this.vars.repeat || 0;
                this._repeatDelay = this.vars.repeatDelay || 0;
                this._dirty = true; //ensures that if there is any repeat, the totalDuration will get recalculated to accurately report it.
                this.render = TweenMax.prototype.render; //speed optimization (avoid prototype lookup on this "hot" method)
            },
            _tinyNum = 0.0000000001,
            TweenLiteInternals = TweenLite._internals,
            _isSelector = TweenLiteInternals.isSelector,
            _isArray = TweenLiteInternals.isArray,
            p = TweenMax.prototype = TweenLite.to({}, 0.1, {}),
            _blankArray = [];

        TweenMax.version = "1.12.1";
        p.constructor = TweenMax;
        p.kill()._gc = false;
        TweenMax.killTweensOf = TweenMax.killDelayedCallsTo = TweenLite.killTweensOf;
        TweenMax.getTweensOf = TweenLite.getTweensOf;
        TweenMax.lagSmoothing = TweenLite.lagSmoothing;
        TweenMax.ticker = TweenLite.ticker;
        TweenMax.render = TweenLite.render;

        p.invalidate = function() {
            this._yoyo = (this.vars.yoyo === true);
            this._repeat = this.vars.repeat || 0;
            this._repeatDelay = this.vars.repeatDelay || 0;
            this._uncache(true);
            return TweenLite.prototype.invalidate.call(this);
        };
        
        p.updateTo = function(vars, resetDuration) {
            var curRatio = this.ratio, p;
            if (resetDuration && this._startTime < this._timeline._time) {
                this._startTime = this._timeline._time;
                this._uncache(false);
                if (this._gc) {
                    this._enabled(true, false);
                } else {
                    this._timeline.insert(this, this._startTime - this._delay); //ensures that any necessary re-sequencing of Animations in the timeline occurs to make sure the rendering order is correct.
                }
            }
            for (p in vars) {
                this.vars[p] = vars[p];
            }
            if (this._initted) {
                if (resetDuration) {
                    this._initted = false;
                } else {
                    if (this._gc) {
                        this._enabled(true, false);
                    }
                    if (this._notifyPluginsOfEnabled && this._firstPT) {
                        TweenLite._onPluginEvent("_onDisable", this); //in case a plugin like MotionBlur must perform some cleanup tasks
                    }
                    if (this._time / this._duration > 0.998) { //if the tween has finished (or come extremely close to finishing), we just need to rewind it to 0 and then render it again at the end which forces it to re-initialize (parsing the new vars). We allow tweens that are close to finishing (but haven't quite finished) to work this way too because otherwise, the values are so small when determining where to project the starting values that binary math issues creep in and can make the tween appear to render incorrectly when run backwards. 
                        var prevTime = this._time;
                        this.render(0, true, false);
                        this._initted = false;
                        this.render(prevTime, true, false);
                    } else if (this._time > 0) {
                        this._initted = false;
                        this._init();
                        var inv = 1 / (1 - curRatio),
                            pt = this._firstPT, endValue;
                        while (pt) {
                            endValue = pt.s + pt.c; 
                            pt.c *= inv;
                            pt.s = endValue - pt.c;
                            pt = pt._next;
                        }
                    }
                }
            }
            return this;
        };
                
        p.render = function(time, suppressEvents, force) {
            if (!this._initted) if (this._duration === 0 && this.vars.repeat) { //zero duration tweens that render immediately have render() called from TweenLite's constructor, before TweenMax's constructor has finished setting _repeat, _repeatDelay, and _yoyo which are critical in determining totalDuration() so we need to call invalidate() which is a low-kb way to get those set properly.
                this.invalidate();
            }
            var totalDur = (!this._dirty) ? this._totalDuration : this.totalDuration(),
                prevTime = this._time,
                prevTotalTime = this._totalTime, 
                prevCycle = this._cycle,
                duration = this._duration,
                prevRawPrevTime = this._rawPrevTime,
                isComplete, callback, pt, cycleDuration, r, type, pow, rawPrevTime, i;
            if (time >= totalDur) {
                this._totalTime = totalDur;
                this._cycle = this._repeat;
                if (this._yoyo && (this._cycle & 1) !== 0) {
                    this._time = 0;
                    this.ratio = this._ease._calcEnd ? this._ease.getRatio(0) : 0;
                } else {
                    this._time = duration;
                    this.ratio = this._ease._calcEnd ? this._ease.getRatio(1) : 1;
                }
                if (!this._reversed) {
                    isComplete = true;
                    callback = "onComplete";
                }
                if (duration === 0) if (this._initted || !this.vars.lazy || force) { //zero-duration tweens are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                    if (this._startTime === this._timeline._duration) { //if a zero-duration tween is at the VERY end of a timeline and that timeline renders at its end, it will typically add a tiny bit of cushion to the render time to prevent rounding errors from getting in the way of tweens rendering their VERY end. If we then reverse() that timeline, the zero-duration tween will trigger its onReverseComplete even though technically the playhead didn't pass over it again. It's a very specific edge case we must accommodate.
                        time = 0;
                    }
                    if (time === 0 || prevRawPrevTime < 0 || prevRawPrevTime === _tinyNum) if (prevRawPrevTime !== time) {
                        force = true;
                        if (prevRawPrevTime > _tinyNum) {
                            callback = "onReverseComplete";
                        }
                    }
                    this._rawPrevTime = rawPrevTime = (!suppressEvents || time || prevRawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                }
                
            } else if (time < 0.0000001) { //to work around occasional floating point math artifacts, round super small values to 0.
                this._totalTime = this._time = this._cycle = 0;
                this.ratio = this._ease._calcEnd ? this._ease.getRatio(0) : 0;
                if (prevTotalTime !== 0 || (duration === 0 && prevRawPrevTime > 0 && prevRawPrevTime !== _tinyNum)) {
                    callback = "onReverseComplete";
                    isComplete = this._reversed;
                }
                if (time < 0) {
                    this._active = false;
                    if (duration === 0) if (this._initted || !this.vars.lazy || force) { //zero-duration tweens are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                        if (prevRawPrevTime >= 0) {
                            force = true;
                        }
                        this._rawPrevTime = rawPrevTime = (!suppressEvents || time || prevRawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                    }
                } else if (!this._initted) { //if we render the very beginning (time == 0) of a fromTo(), we must force the render (normal tweens wouldn't need to render at a time of 0 when the prevTime was also 0). This is also mandatory to make sure overwriting kicks in immediately.
                    force = true;
                }
            } else {
                this._totalTime = this._time = time;
                
                if (this._repeat !== 0) {
                    cycleDuration = duration + this._repeatDelay;
                    this._cycle = (this._totalTime / cycleDuration) >> 0; //originally _totalTime % cycleDuration but floating point errors caused problems, so I normalized it. (4 % 0.8 should be 0 but Flash reports it as 0.79999999!)
                    if (this._cycle !== 0) if (this._cycle === this._totalTime / cycleDuration) {
                        this._cycle--; //otherwise when rendered exactly at the end time, it will act as though it is repeating (at the beginning)
                    }
                    this._time = this._totalTime - (this._cycle * cycleDuration);
                    if (this._yoyo) if ((this._cycle & 1) !== 0) {
                        this._time = duration - this._time;
                    }
                    if (this._time > duration) {
                        this._time = duration;
                    } else if (this._time < 0) {
                        this._time = 0;
                    }
                }

                if (this._easeType) {
                    r = this._time / duration;
                    type = this._easeType;
                    pow = this._easePower;
                    if (type === 1 || (type === 3 && r >= 0.5)) {
                        r = 1 - r;
                    }
                    if (type === 3) {
                        r *= 2;
                    }
                    if (pow === 1) {
                        r *= r;
                    } else if (pow === 2) {
                        r *= r * r;
                    } else if (pow === 3) {
                        r *= r * r * r;
                    } else if (pow === 4) {
                        r *= r * r * r * r;
                    }

                    if (type === 1) {
                        this.ratio = 1 - r;
                    } else if (type === 2) {
                        this.ratio = r;
                    } else if (this._time / duration < 0.5) {
                        this.ratio = r / 2;
                    } else {
                        this.ratio = 1 - (r / 2);
                    }

                } else {
                    this.ratio = this._ease.getRatio(this._time / duration);
                }
                
            }
                
            if (prevTime === this._time && !force && prevCycle === this._cycle) {
                if (prevTotalTime !== this._totalTime) if (this._onUpdate) if (!suppressEvents) { //so that onUpdate fires even during the repeatDelay - as long as the totalTime changed, we should trigger onUpdate.
                    this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
                }
                return;
            } else if (!this._initted) {
                this._init();
                if (!this._initted || this._gc) { //immediateRender tweens typically won't initialize until the playhead advances (_time is greater than 0) in order to ensure that overwriting occurs properly. Also, if all of the tweening properties have been overwritten (which would cause _gc to be true, as set in _init()), we shouldn't continue otherwise an onStart callback could be called for example.
                    return;
                } else if (!force && this._firstPT && ((this.vars.lazy !== false && this._duration) || (this.vars.lazy && !this._duration))) { //we stick it in the queue for rendering at the very end of the tick - this is a performance optimization because browsers invalidate styles and force a recalculation if you read, write, and then read style data (so it's better to read/read/read/write/write/write than read/write/read/write/read/write). The down side, of course, is that usually you WANT things to render immediately because you may have code running right after that which depends on the change. Like imagine running TweenLite.set(...) and then immediately after that, creating a nother tween that animates the same property to another value; the starting values of that 2nd tween wouldn't be accurate if lazy is true.
                    this._time = prevTime;
                    this._totalTime = prevTotalTime;
                    this._rawPrevTime = prevRawPrevTime;
                    this._cycle = prevCycle;
                    TweenLiteInternals.lazyTweens.push(this);
                    this._lazy = time;
                    return;
                }
                //_ease is initially set to defaultEase, so now that init() has run, _ease is set properly and we need to recalculate the ratio. Overall this is faster than using conditional logic earlier in the method to avoid having to set ratio twice because we only init() once but renderTime() gets called VERY frequently.
                if (this._time && !isComplete) {
                    this.ratio = this._ease.getRatio(this._time / duration);
                } else if (isComplete && this._ease._calcEnd) {
                    this.ratio = this._ease.getRatio((this._time === 0) ? 0 : 1);
                }
            }
            if (this._lazy !== false) {
                this._lazy = false;
            }

            if (!this._active) if (!this._paused && this._time !== prevTime && time >= 0) {
                this._active = true; //so that if the user renders a tween (as opposed to the timeline rendering it), the timeline is forced to re-render and align it with the proper time/frame on the next rendering cycle. Maybe the tween already finished but the user manually re-renders it as halfway done.
            }
            if (prevTotalTime === 0) {
                if (this._initted === 2 && time > 0) {
                    //this.invalidate();
                    this._init(); //will just apply overwriting since _initted of (2) means it was a from() tween that had immediateRender:true
                }
                if (this._startAt) {
                    if (time >= 0) {
                        this._startAt.render(time, suppressEvents, force);
                    } else if (!callback) {
                        callback = "_dummyGS"; //if no callback is defined, use a dummy value just so that the condition at the end evaluates as true because _startAt should render AFTER the normal render loop when the time is negative. We could handle this in a more intuitive way, of course, but the render loop is the MOST important thing to optimize, so this technique allows us to avoid adding extra conditional logic in a high-frequency area.
                    }
                }
                if (this.vars.onStart) if (this._totalTime !== 0 || duration === 0) if (!suppressEvents) {
                    this.vars.onStart.apply(this.vars.onStartScope || this, this.vars.onStartParams || _blankArray);
                }
            }
            
            pt = this._firstPT;
            while (pt) {
                if (pt.f) {
                    pt.t[pt.p](pt.c * this.ratio + pt.s);
                } else {
                    pt.t[pt.p] = pt.c * this.ratio + pt.s;
                }
                pt = pt._next;
            }
            
            if (this._onUpdate) {
                if (time < 0) if (this._startAt && this._startTime) { //if the tween is positioned at the VERY beginning (_startTime 0) of its parent timeline, it's illegal for the playhead to go back further, so we should not render the recorded startAt values.
                    this._startAt.render(time, suppressEvents, force); //note: for performance reasons, we tuck this conditional logic inside less traveled areas (most tweens don't have an onUpdate). We'd just have it at the end before the onComplete, but the values should be updated before any onUpdate is called, so we ALSO put it here and then if it's not called, we do so later near the onComplete.
                }
                if (!suppressEvents) if (this._totalTime !== prevTotalTime || isComplete) {
                    this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
                }
            }
            if (this._cycle !== prevCycle) if (!suppressEvents) if (!this._gc) if (this.vars.onRepeat) {
                this.vars.onRepeat.apply(this.vars.onRepeatScope || this, this.vars.onRepeatParams || _blankArray);
            }
            if (callback) if (!this._gc) { //check gc because there's a chance that kill() could be called in an onUpdate
                if (time < 0 && this._startAt && !this._onUpdate && this._startTime) { //if the tween is positioned at the VERY beginning (_startTime 0) of its parent timeline, it's illegal for the playhead to go back further, so we should not render the recorded startAt values.
                    this._startAt.render(time, suppressEvents, force);
                }
                if (isComplete) {
                    if (this._timeline.autoRemoveChildren) {
                        this._enabled(false, false);
                    }
                    this._active = false;
                }
                if (!suppressEvents && this.vars[callback]) {
                    this.vars[callback].apply(this.vars[callback + "Scope"] || this, this.vars[callback + "Params"] || _blankArray);
                }
                if (duration === 0 && this._rawPrevTime === _tinyNum && rawPrevTime !== _tinyNum) { //the onComplete or onReverseComplete could trigger movement of the playhead and for zero-duration tweens (which must discern direction) that land directly back on their start time, we don't want to fire again on the next render. Think of several addPause()'s in a timeline that forces the playhead to a certain spot, but what if it's already paused and another tween is tweening the "time" of the timeline? Each time it moves [forward] past that spot, it would move back, and since suppressEvents is true, it'd reset _rawPrevTime to _tinyNum so that when it begins again, the callback would fire (so ultimately it could bounce back and forth during that tween). Again, this is a very uncommon scenario, but possible nonetheless.
                    this._rawPrevTime = 0;
                }
            }
        };
        
//---- STATIC FUNCTIONS -----------------------------------------------------------------------------------------------------------
        
        TweenMax.to = function(target, duration, vars) {
            return new TweenMax(target, duration, vars);
        };
        
        TweenMax.from = function(target, duration, vars) {
            vars.runBackwards = true;
            vars.immediateRender = (vars.immediateRender != false);
            return new TweenMax(target, duration, vars);
        };
        
        TweenMax.fromTo = function(target, duration, fromVars, toVars) {
            toVars.startAt = fromVars;
            toVars.immediateRender = (toVars.immediateRender != false && fromVars.immediateRender != false);
            return new TweenMax(target, duration, toVars);
        };
        
        TweenMax.staggerTo = TweenMax.allTo = function(targets, duration, vars, stagger, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            stagger = stagger || 0;
            var delay = vars.delay || 0,
                a = [],
                finalComplete = function() {
                    if (vars.onComplete) {
                        vars.onComplete.apply(vars.onCompleteScope || this, arguments);
                    }
                    onCompleteAll.apply(onCompleteAllScope || this, onCompleteAllParams || _blankArray);
                },
                l, copy, i, p;
            if (!_isArray(targets)) {
                if (typeof(targets) === "string") {
                    targets = TweenLite.selector(targets) || targets;
                }
                if (_isSelector(targets)) {
                    targets = _slice.call(targets, 0);
                }
            }
            l = targets.length;
            for (i = 0; i < l; i++) {
                copy = {};
                for (p in vars) {
                    copy[p] = vars[p];
                }
                copy.delay = delay;
                if (i === l - 1 && onCompleteAll) {
                    copy.onComplete = finalComplete;
                }
                a[i] = new TweenMax(targets[i], duration, copy);
                delay += stagger;
            }
            return a;
        };
        
        TweenMax.staggerFrom = TweenMax.allFrom = function(targets, duration, vars, stagger, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            vars.runBackwards = true;
            vars.immediateRender = (vars.immediateRender != false);
            return TweenMax.staggerTo(targets, duration, vars, stagger, onCompleteAll, onCompleteAllParams, onCompleteAllScope);
        };
        
        TweenMax.staggerFromTo = TweenMax.allFromTo = function(targets, duration, fromVars, toVars, stagger, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            toVars.startAt = fromVars;
            toVars.immediateRender = (toVars.immediateRender != false && fromVars.immediateRender != false);
            return TweenMax.staggerTo(targets, duration, toVars, stagger, onCompleteAll, onCompleteAllParams, onCompleteAllScope);
        };
                
        TweenMax.delayedCall = function(delay, callback, params, scope, useFrames) {
            return new TweenMax(callback, 0, {delay:delay, onComplete:callback, onCompleteParams:params, onCompleteScope:scope, onReverseComplete:callback, onReverseCompleteParams:params, onReverseCompleteScope:scope, immediateRender:false, useFrames:useFrames, overwrite:0});
        };
        
        TweenMax.set = function(target, vars) {
            return new TweenMax(target, 0, vars);
        };
        
        TweenMax.isTweening = function(target) {
            return (TweenLite.getTweensOf(target, true).length > 0);
        };
        
        var _getChildrenOf = function(timeline, includeTimelines) {
                var a = [],
                    cnt = 0,
                    tween = timeline._first;
                while (tween) {
                    if (tween instanceof TweenLite) {
                        a[cnt++] = tween;
                    } else {
                        if (includeTimelines) {
                            a[cnt++] = tween;
                        }
                        a = a.concat(_getChildrenOf(tween, includeTimelines));
                        cnt = a.length;
                    }
                    tween = tween._next;
                }
                return a;
            }, 
            getAllTweens = TweenMax.getAllTweens = function(includeTimelines) {
                return _getChildrenOf(Animation._rootTimeline, includeTimelines).concat( _getChildrenOf(Animation._rootFramesTimeline, includeTimelines) );
            };
        
        TweenMax.killAll = function(complete, tweens, delayedCalls, timelines) {
            if (tweens == null) {
                tweens = true;
            }
            if (delayedCalls == null) {
                delayedCalls = true;
            }
            var a = getAllTweens((timelines != false)),
                l = a.length,
                allTrue = (tweens && delayedCalls && timelines),
                isDC, tween, i;
            for (i = 0; i < l; i++) {
                tween = a[i];
                if (allTrue || (tween instanceof SimpleTimeline) || ((isDC = (tween.target === tween.vars.onComplete)) && delayedCalls) || (tweens && !isDC)) {
                    if (complete) {
                        tween.totalTime(tween._reversed ? 0 : tween.totalDuration());
                    } else {
                        tween._enabled(false, false);
                    }
                }
            }
        };
        
        TweenMax.killChildTweensOf = function(parent, complete) {
            if (parent == null) {
                return;
            }
            var tl = TweenLiteInternals.tweenLookup,
                a, curParent, p, i, l;
            if (typeof(parent) === "string") {
                parent = TweenLite.selector(parent) || parent;
            }
            if (_isSelector(parent)) {
                parent = _slice.call(parent, 0);
            }
            if (_isArray(parent)) {
                i = parent.length;
                while (--i > -1) {
                    TweenMax.killChildTweensOf(parent[i], complete);
                }
                return;
            }
            a = [];
            for (p in tl) {
                curParent = tl[p].target.parentNode;
                while (curParent) {
                    if (curParent === parent) {
                        a = a.concat(tl[p].tweens);
                    }
                    curParent = curParent.parentNode;
                }
            }
            l = a.length;
            for (i = 0; i < l; i++) {
                if (complete) {
                    a[i].totalTime(a[i].totalDuration());
                }
                a[i]._enabled(false, false);
            }
        };

        var _changePause = function(pause, tweens, delayedCalls, timelines) {
            tweens = (tweens !== false);
            delayedCalls = (delayedCalls !== false);
            timelines = (timelines !== false);
            var a = getAllTweens(timelines),
                allTrue = (tweens && delayedCalls && timelines),
                i = a.length,
                isDC, tween;
            while (--i > -1) {
                tween = a[i];
                if (allTrue || (tween instanceof SimpleTimeline) || ((isDC = (tween.target === tween.vars.onComplete)) && delayedCalls) || (tweens && !isDC)) {
                    tween.paused(pause);
                }
            }
        };
        
        TweenMax.pauseAll = function(tweens, delayedCalls, timelines) {
            _changePause(true, tweens, delayedCalls, timelines);
        };
        
        TweenMax.resumeAll = function(tweens, delayedCalls, timelines) {
            _changePause(false, tweens, delayedCalls, timelines);
        };

        TweenMax.globalTimeScale = function(value) {
            var tl = Animation._rootTimeline,
                t = TweenLite.ticker.time;
            if (!arguments.length) {
                return tl._timeScale;
            }
            value = value || _tinyNum; //can't allow zero because it'll throw the math off
            tl._startTime = t - ((t - tl._startTime) * tl._timeScale / value);
            tl = Animation._rootFramesTimeline;
            t = TweenLite.ticker.frame;
            tl._startTime = t - ((t - tl._startTime) * tl._timeScale / value);
            tl._timeScale = Animation._rootTimeline._timeScale = value;
            return value;
        };
        
    
//---- GETTERS / SETTERS ----------------------------------------------------------------------------------------------------------
        
        p.progress = function(value) {
            return (!arguments.length) ? this._time / this.duration() : this.totalTime( this.duration() * ((this._yoyo && (this._cycle & 1) !== 0) ? 1 - value : value) + (this._cycle * (this._duration + this._repeatDelay)), false);
        };
        
        p.totalProgress = function(value) {
            return (!arguments.length) ? this._totalTime / this.totalDuration() : this.totalTime( this.totalDuration() * value, false);
        };
        
        p.time = function(value, suppressEvents) {
            if (!arguments.length) {
                return this._time;
            }
            if (this._dirty) {
                this.totalDuration();
            }
            if (value > this._duration) {
                value = this._duration;
            }
            if (this._yoyo && (this._cycle & 1) !== 0) {
                value = (this._duration - value) + (this._cycle * (this._duration + this._repeatDelay));
            } else if (this._repeat !== 0) {
                value += this._cycle * (this._duration + this._repeatDelay);
            }
            return this.totalTime(value, suppressEvents);
        };

        p.duration = function(value) {
            if (!arguments.length) {
                return this._duration; //don't set _dirty = false because there could be repeats that haven't been factored into the _totalDuration yet. Otherwise, if you create a repeated TweenMax and then immediately check its duration(), it would cache the value and the totalDuration would not be correct, thus repeats wouldn't take effect.
            }
            return Animation.prototype.duration.call(this, value);
        };

        p.totalDuration = function(value) {
            if (!arguments.length) {
                if (this._dirty) {
                    //instead of Infinity, we use 999999999999 so that we can accommodate reverses
                    this._totalDuration = (this._repeat === -1) ? 999999999999 : this._duration * (this._repeat + 1) + (this._repeatDelay * this._repeat);
                    this._dirty = false;
                }
                return this._totalDuration;
            }
            return (this._repeat === -1) ? this : this.duration( (value - (this._repeat * this._repeatDelay)) / (this._repeat + 1) );
        };
        
        p.repeat = function(value) {
            if (!arguments.length) {
                return this._repeat;
            }
            this._repeat = value;
            return this._uncache(true);
        };
        
        p.repeatDelay = function(value) {
            if (!arguments.length) {
                return this._repeatDelay;
            }
            this._repeatDelay = value;
            return this._uncache(true);
        };
        
        p.yoyo = function(value) {
            if (!arguments.length) {
                return this._yoyo;
            }
            this._yoyo = value;
            return this;
        };
        
        
        return TweenMax;
        
    }, true);








/*
 * ----------------------------------------------------------------
 * TimelineLite
 * ----------------------------------------------------------------
 */
    window._gsDefine("TimelineLite", ["core.Animation","core.SimpleTimeline","TweenLite"], function(Animation, SimpleTimeline, TweenLite) {

        var TimelineLite = function(vars) {
                SimpleTimeline.call(this, vars);
                this._labels = {};
                this.autoRemoveChildren = (this.vars.autoRemoveChildren === true);
                this.smoothChildTiming = (this.vars.smoothChildTiming === true);
                this._sortChildren = true;
                this._onUpdate = this.vars.onUpdate;
                var v = this.vars,
                    val, p;
                for (p in v) {
                    val = v[p];
                    if (_isArray(val)) if (val.join("").indexOf("{self}") !== -1) {
                        v[p] = this._swapSelfInParams(val);
                    }
                }
                if (_isArray(v.tweens)) {
                    this.add(v.tweens, 0, v.align, v.stagger);
                }
            },
            _tinyNum = 0.0000000001,
            _isSelector = TweenLite._internals.isSelector,
            _isArray = TweenLite._internals.isArray,
            _blankArray = [],
            _globals = window._gsDefine.globals,
            _copy = function(vars) {
                var copy = {}, p;
                for (p in vars) {
                    copy[p] = vars[p];
                }
                return copy;
            },
            _pauseCallback = function(tween, callback, params, scope) {
                tween._timeline.pause(tween._startTime);
                if (callback) {
                    callback.apply(scope || tween._timeline, params || _blankArray);
                }
            },
            _slice = _blankArray.slice,
            p = TimelineLite.prototype = new SimpleTimeline();

        TimelineLite.version = "1.12.1";
        p.constructor = TimelineLite;
        p.kill()._gc = false;

        p.to = function(target, duration, vars, position) {
            var Engine = (vars.repeat && _globals.TweenMax) || TweenLite;
            return duration ? this.add( new Engine(target, duration, vars), position) : this.set(target, vars, position);
        };

        p.from = function(target, duration, vars, position) {
            return this.add( ((vars.repeat && _globals.TweenMax) || TweenLite).from(target, duration, vars), position);
        };

        p.fromTo = function(target, duration, fromVars, toVars, position) {
            var Engine = (toVars.repeat && _globals.TweenMax) || TweenLite;
            return duration ? this.add( Engine.fromTo(target, duration, fromVars, toVars), position) : this.set(target, toVars, position);
        };

        p.staggerTo = function(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            var tl = new TimelineLite({onComplete:onCompleteAll, onCompleteParams:onCompleteAllParams, onCompleteScope:onCompleteAllScope, smoothChildTiming:this.smoothChildTiming}),
                i;
            if (typeof(targets) === "string") {
                targets = TweenLite.selector(targets) || targets;
            }
            if (_isSelector(targets)) { //senses if the targets object is a selector. If it is, we should translate it into an array.
                targets = _slice.call(targets, 0);
            }
            stagger = stagger || 0;
            for (i = 0; i < targets.length; i++) {
                if (vars.startAt) {
                    vars.startAt = _copy(vars.startAt);
                }
                tl.to(targets[i], duration, _copy(vars), i * stagger);
            }
            return this.add(tl, position);
        };

        p.staggerFrom = function(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            vars.immediateRender = (vars.immediateRender != false);
            vars.runBackwards = true;
            return this.staggerTo(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams, onCompleteAllScope);
        };

        p.staggerFromTo = function(targets, duration, fromVars, toVars, stagger, position, onCompleteAll, onCompleteAllParams, onCompleteAllScope) {
            toVars.startAt = fromVars;
            toVars.immediateRender = (toVars.immediateRender != false && fromVars.immediateRender != false);
            return this.staggerTo(targets, duration, toVars, stagger, position, onCompleteAll, onCompleteAllParams, onCompleteAllScope);
        };

        p.call = function(callback, params, scope, position) {
            return this.add( TweenLite.delayedCall(0, callback, params, scope), position);
        };

        p.set = function(target, vars, position) {
            position = this._parseTimeOrLabel(position, 0, true);
            if (vars.immediateRender == null) {
                vars.immediateRender = (position === this._time && !this._paused);
            }
            return this.add( new TweenLite(target, 0, vars), position);
        };

        TimelineLite.exportRoot = function(vars, ignoreDelayedCalls) {
            vars = vars || {};
            if (vars.smoothChildTiming == null) {
                vars.smoothChildTiming = true;
            }
            var tl = new TimelineLite(vars),
                root = tl._timeline,
                tween, next;
            if (ignoreDelayedCalls == null) {
                ignoreDelayedCalls = true;
            }
            root._remove(tl, true);
            tl._startTime = 0;
            tl._rawPrevTime = tl._time = tl._totalTime = root._time;
            tween = root._first;
            while (tween) {
                next = tween._next;
                if (!ignoreDelayedCalls || !(tween instanceof TweenLite && tween.target === tween.vars.onComplete)) {
                    tl.add(tween, tween._startTime - tween._delay);
                }
                tween = next;
            }
            root.add(tl, 0);
            return tl;
        };

        p.add = function(value, position, align, stagger) {
            var curTime, l, i, child, tl, beforeRawTime;
            if (typeof(position) !== "number") {
                position = this._parseTimeOrLabel(position, 0, true, value);
            }
            if (!(value instanceof Animation)) {
                if ((value instanceof Array) || (value && value.push && _isArray(value))) {
                    align = align || "normal";
                    stagger = stagger || 0;
                    curTime = position;
                    l = value.length;
                    for (i = 0; i < l; i++) {
                        if (_isArray(child = value[i])) {
                            child = new TimelineLite({tweens:child});
                        }
                        this.add(child, curTime);
                        if (typeof(child) !== "string" && typeof(child) !== "function") {
                            if (align === "sequence") {
                                curTime = child._startTime + (child.totalDuration() / child._timeScale);
                            } else if (align === "start") {
                                child._startTime -= child.delay();
                            }
                        }
                        curTime += stagger;
                    }
                    return this._uncache(true);
                } else if (typeof(value) === "string") {
                    return this.addLabel(value, position);
                } else if (typeof(value) === "function") {
                    value = TweenLite.delayedCall(0, value);
                } else {
                    throw("Cannot add " + value + " into the timeline; it is not a tween, timeline, function, or string.");
                }
            }

            SimpleTimeline.prototype.add.call(this, value, position);

            //if the timeline has already ended but the inserted tween/timeline extends the duration, we should enable this timeline again so that it renders properly. We should also align the playhead with the parent timeline's when appropriate.
            if (this._gc || this._time === this._duration) if (!this._paused) if (this._duration < this.duration()) {
                //in case any of the ancestors had completed but should now be enabled...
                tl = this;
                beforeRawTime = (tl.rawTime() > value._startTime); //if the tween is placed on the timeline so that it starts BEFORE the current rawTime, we should align the playhead (move the timeline). This is because sometimes users will create a timeline, let it finish, and much later append a tween and expect it to run instead of jumping to its end state. While technically one could argue that it should jump to its end state, that's not what users intuitively expect.
                while (tl._timeline) {
                    if (beforeRawTime && tl._timeline.smoothChildTiming) {
                        tl.totalTime(tl._totalTime, true); //moves the timeline (shifts its startTime) if necessary, and also enables it.
                    } else if (tl._gc) {
                        tl._enabled(true, false);
                    }
                    tl = tl._timeline;
                }
            }

            return this;
        };

        p.remove = function(value) {
            if (value instanceof Animation) {
                return this._remove(value, false);
            } else if (value instanceof Array || (value && value.push && _isArray(value))) {
                var i = value.length;
                while (--i > -1) {
                    this.remove(value[i]);
                }
                return this;
            } else if (typeof(value) === "string") {
                return this.removeLabel(value);
            }
            return this.kill(null, value);
        };

        p._remove = function(tween, skipDisable) {
            SimpleTimeline.prototype._remove.call(this, tween, skipDisable);
            var last = this._last;
            if (!last) {
                this._time = this._totalTime = this._duration = this._totalDuration = 0;
            } else if (this._time > last._startTime + last._totalDuration / last._timeScale) {
                this._time = this.duration();
                this._totalTime = this._totalDuration;
            }
            return this;
        };

        p.append = function(value, offsetOrLabel) {
            return this.add(value, this._parseTimeOrLabel(null, offsetOrLabel, true, value));
        };

        p.insert = p.insertMultiple = function(value, position, align, stagger) {
            return this.add(value, position || 0, align, stagger);
        };

        p.appendMultiple = function(tweens, offsetOrLabel, align, stagger) {
            return this.add(tweens, this._parseTimeOrLabel(null, offsetOrLabel, true, tweens), align, stagger);
        };

        p.addLabel = function(label, position) {
            this._labels[label] = this._parseTimeOrLabel(position);
            return this;
        };

        p.addPause = function(position, callback, params, scope) {
            return this.call(_pauseCallback, ["{self}", callback, params, scope], this, position);
        };

        p.removeLabel = function(label) {
            delete this._labels[label];
            return this;
        };

        p.getLabelTime = function(label) {
            return (this._labels[label] != null) ? this._labels[label] : -1;
        };

        p._parseTimeOrLabel = function(timeOrLabel, offsetOrLabel, appendIfAbsent, ignore) {
            var i;
            //if we're about to add a tween/timeline (or an array of them) that's already a child of this timeline, we should remove it first so that it doesn't contaminate the duration().
            if (ignore instanceof Animation && ignore.timeline === this) {
                this.remove(ignore);
            } else if (ignore && ((ignore instanceof Array) || (ignore.push && _isArray(ignore)))) {
                i = ignore.length;
                while (--i > -1) {
                    if (ignore[i] instanceof Animation && ignore[i].timeline === this) {
                        this.remove(ignore[i]);
                    }
                }
            }
            if (typeof(offsetOrLabel) === "string") {
                return this._parseTimeOrLabel(offsetOrLabel, (appendIfAbsent && typeof(timeOrLabel) === "number" && this._labels[offsetOrLabel] == null) ? timeOrLabel - this.duration() : 0, appendIfAbsent);
            }
            offsetOrLabel = offsetOrLabel || 0;
            if (typeof(timeOrLabel) === "string" && (isNaN(timeOrLabel) || this._labels[timeOrLabel] != null)) { //if the string is a number like "1", check to see if there's a label with that name, otherwise interpret it as a number (absolute value).
                i = timeOrLabel.indexOf("=");
                if (i === -1) {
                    if (this._labels[timeOrLabel] == null) {
                        return appendIfAbsent ? (this._labels[timeOrLabel] = this.duration() + offsetOrLabel) : offsetOrLabel;
                    }
                    return this._labels[timeOrLabel] + offsetOrLabel;
                }
                offsetOrLabel = parseInt(timeOrLabel.charAt(i-1) + "1", 10) * Number(timeOrLabel.substr(i+1));
                timeOrLabel = (i > 1) ? this._parseTimeOrLabel(timeOrLabel.substr(0, i-1), 0, appendIfAbsent) : this.duration();
            } else if (timeOrLabel == null) {
                timeOrLabel = this.duration();
            }
            return Number(timeOrLabel) + offsetOrLabel;
        };

        p.seek = function(position, suppressEvents) {
            return this.totalTime((typeof(position) === "number") ? position : this._parseTimeOrLabel(position), (suppressEvents !== false));
        };

        p.stop = function() {
            return this.paused(true);
        };

        p.gotoAndPlay = function(position, suppressEvents) {
            return this.play(position, suppressEvents);
        };

        p.gotoAndStop = function(position, suppressEvents) {
            return this.pause(position, suppressEvents);
        };

        p.render = function(time, suppressEvents, force) {
            if (this._gc) {
                this._enabled(true, false);
            }
            var totalDur = (!this._dirty) ? this._totalDuration : this.totalDuration(),
                prevTime = this._time,
                prevStart = this._startTime,
                prevTimeScale = this._timeScale,
                prevPaused = this._paused,
                tween, isComplete, next, callback, internalForce;
            if (time >= totalDur) {
                this._totalTime = this._time = totalDur;
                if (!this._reversed) if (!this._hasPausedChild()) {
                    isComplete = true;
                    callback = "onComplete";
                    if (this._duration === 0) if (time === 0 || this._rawPrevTime < 0 || this._rawPrevTime === _tinyNum) if (this._rawPrevTime !== time && this._first) {
                        internalForce = true;
                        if (this._rawPrevTime > _tinyNum) {
                            callback = "onReverseComplete";
                        }
                    }
                }
                this._rawPrevTime = (this._duration || !suppressEvents || time || this._rawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration timeline or tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                time = totalDur + 0.0001; //to avoid occasional floating point rounding errors - sometimes child tweens/timelines were not being fully completed (their progress might be 0.999999999999998 instead of 1 because when _time - tween._startTime is performed, floating point errors would return a value that was SLIGHTLY off). Try (999999999999.7 - 999999999999) * 1 = 0.699951171875 instead of 0.7.

            } else if (time < 0.0000001) { //to work around occasional floating point math artifacts, round super small values to 0.
                this._totalTime = this._time = 0;
                if (prevTime !== 0 || (this._duration === 0 && this._rawPrevTime !== _tinyNum && (this._rawPrevTime > 0 || (time < 0 && this._rawPrevTime >= 0)))) {
                    callback = "onReverseComplete";
                    isComplete = this._reversed;
                }
                if (time < 0) {
                    this._active = false;
                    if (this._duration === 0) if (this._rawPrevTime >= 0 && this._first) { //zero-duration timelines are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                        internalForce = true;
                    }
                    this._rawPrevTime = time;
                } else {
                    this._rawPrevTime = (this._duration || !suppressEvents || time || this._rawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration timeline or tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.

                    time = 0; //to avoid occasional floating point rounding errors (could cause problems especially with zero-duration tweens at the very beginning of the timeline)
                    if (!this._initted) {
                        internalForce = true;
                    }
                }

            } else {
                this._totalTime = this._time = this._rawPrevTime = time;
            }
            if ((this._time === prevTime || !this._first) && !force && !internalForce) {
                return;
            } else if (!this._initted) {
                this._initted = true;
            }

            if (!this._active) if (!this._paused && this._time !== prevTime && time > 0) {
                this._active = true;  //so that if the user renders the timeline (as opposed to the parent timeline rendering it), it is forced to re-render and align it with the proper time/frame on the next rendering cycle. Maybe the timeline already finished but the user manually re-renders it as halfway done, for example.
            }

            if (prevTime === 0) if (this.vars.onStart) if (this._time !== 0) if (!suppressEvents) {
                this.vars.onStart.apply(this.vars.onStartScope || this, this.vars.onStartParams || _blankArray);
            }

            if (this._time >= prevTime) {
                tween = this._first;
                while (tween) {
                    next = tween._next; //record it here because the value could change after rendering...
                    if (this._paused && !prevPaused) { //in case a tween pauses the timeline when rendering
                        break;
                    } else if (tween._active || (tween._startTime <= this._time && !tween._paused && !tween._gc)) {
                        if (!tween._reversed) {
                            tween.render((time - tween._startTime) * tween._timeScale, suppressEvents, force);
                        } else {
                            tween.render(((!tween._dirty) ? tween._totalDuration : tween.totalDuration()) - ((time - tween._startTime) * tween._timeScale), suppressEvents, force);
                        }
                    }
                    tween = next;
                }
            } else {
                tween = this._last;
                while (tween) {
                    next = tween._prev; //record it here because the value could change after rendering...
                    if (this._paused && !prevPaused) { //in case a tween pauses the timeline when rendering
                        break;
                    } else if (tween._active || (tween._startTime <= prevTime && !tween._paused && !tween._gc)) {
                        if (!tween._reversed) {
                            tween.render((time - tween._startTime) * tween._timeScale, suppressEvents, force);
                        } else {
                            tween.render(((!tween._dirty) ? tween._totalDuration : tween.totalDuration()) - ((time - tween._startTime) * tween._timeScale), suppressEvents, force);
                        }
                    }
                    tween = next;
                }
            }

            if (this._onUpdate) if (!suppressEvents) {
                this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
            }

            if (callback) if (!this._gc) if (prevStart === this._startTime || prevTimeScale !== this._timeScale) if (this._time === 0 || totalDur >= this.totalDuration()) { //if one of the tweens that was rendered altered this timeline's startTime (like if an onComplete reversed the timeline), it probably isn't complete. If it is, don't worry, because whatever call altered the startTime would complete if it was necessary at the new time. The only exception is the timeScale property. Also check _gc because there's a chance that kill() could be called in an onUpdate
                if (isComplete) {
                    if (this._timeline.autoRemoveChildren) {
                        this._enabled(false, false);
                    }
                    this._active = false;
                }
                if (!suppressEvents && this.vars[callback]) {
                    this.vars[callback].apply(this.vars[callback + "Scope"] || this, this.vars[callback + "Params"] || _blankArray);
                }
            }
        };

        p._hasPausedChild = function() {
            var tween = this._first;
            while (tween) {
                if (tween._paused || ((tween instanceof TimelineLite) && tween._hasPausedChild())) {
                    return true;
                }
                tween = tween._next;
            }
            return false;
        };

        p.getChildren = function(nested, tweens, timelines, ignoreBeforeTime) {
            ignoreBeforeTime = ignoreBeforeTime || -9999999999;
            var a = [],
                tween = this._first,
                cnt = 0;
            while (tween) {
                if (tween._startTime < ignoreBeforeTime) {
                    //do nothing
                } else if (tween instanceof TweenLite) {
                    if (tweens !== false) {
                        a[cnt++] = tween;
                    }
                } else {
                    if (timelines !== false) {
                        a[cnt++] = tween;
                    }
                    if (nested !== false) {
                        a = a.concat(tween.getChildren(true, tweens, timelines));
                        cnt = a.length;
                    }
                }
                tween = tween._next;
            }
            return a;
        };

        p.getTweensOf = function(target, nested) {
            var disabled = this._gc,
                a = [],
                cnt = 0,
                tweens, i;
            if (disabled) {
                this._enabled(true, true); //getTweensOf() filters out disabled tweens, and we have to mark them as _gc = true when the timeline completes in order to allow clean garbage collection, so temporarily re-enable the timeline here.
            }
            tweens = TweenLite.getTweensOf(target);
            i = tweens.length;
            while (--i > -1) {
                if (tweens[i].timeline === this || (nested && this._contains(tweens[i]))) {
                    a[cnt++] = tweens[i];
                }
            }
            if (disabled) {
                this._enabled(false, true);
            }
            return a;
        };

        p._contains = function(tween) {
            var tl = tween.timeline;
            while (tl) {
                if (tl === this) {
                    return true;
                }
                tl = tl.timeline;
            }
            return false;
        };

        p.shiftChildren = function(amount, adjustLabels, ignoreBeforeTime) {
            ignoreBeforeTime = ignoreBeforeTime || 0;
            var tween = this._first,
                labels = this._labels,
                p;
            while (tween) {
                if (tween._startTime >= ignoreBeforeTime) {
                    tween._startTime += amount;
                }
                tween = tween._next;
            }
            if (adjustLabels) {
                for (p in labels) {
                    if (labels[p] >= ignoreBeforeTime) {
                        labels[p] += amount;
                    }
                }
            }
            return this._uncache(true);
        };

        p._kill = function(vars, target) {
            if (!vars && !target) {
                return this._enabled(false, false);
            }
            var tweens = (!target) ? this.getChildren(true, true, false) : this.getTweensOf(target),
                i = tweens.length,
                changed = false;
            while (--i > -1) {
                if (tweens[i]._kill(vars, target)) {
                    changed = true;
                }
            }
            return changed;
        };

        p.clear = function(labels) {
            var tweens = this.getChildren(false, true, true),
                i = tweens.length;
            this._time = this._totalTime = 0;
            while (--i > -1) {
                tweens[i]._enabled(false, false);
            }
            if (labels !== false) {
                this._labels = {};
            }
            return this._uncache(true);
        };

        p.invalidate = function() {
            var tween = this._first;
            while (tween) {
                tween.invalidate();
                tween = tween._next;
            }
            return this;
        };

        p._enabled = function(enabled, ignoreTimeline) {
            if (enabled === this._gc) {
                var tween = this._first;
                while (tween) {
                    tween._enabled(enabled, true);
                    tween = tween._next;
                }
            }
            return SimpleTimeline.prototype._enabled.call(this, enabled, ignoreTimeline);
        };

        p.duration = function(value) {
            if (!arguments.length) {
                if (this._dirty) {
                    this.totalDuration(); //just triggers recalculation
                }
                return this._duration;
            }
            if (this.duration() !== 0 && value !== 0) {
                this.timeScale(this._duration / value);
            }
            return this;
        };

        p.totalDuration = function(value) {
            if (!arguments.length) {
                if (this._dirty) {
                    var max = 0,
                        tween = this._last,
                        prevStart = 999999999999,
                        prev, end;
                    while (tween) {
                        prev = tween._prev; //record it here in case the tween changes position in the sequence...
                        if (tween._dirty) {
                            tween.totalDuration(); //could change the tween._startTime, so make sure the tween's cache is clean before analyzing it.
                        }
                        if (tween._startTime > prevStart && this._sortChildren && !tween._paused) { //in case one of the tweens shifted out of order, it needs to be re-inserted into the correct position in the sequence
                            this.add(tween, tween._startTime - tween._delay);
                        } else {
                            prevStart = tween._startTime;
                        }
                        if (tween._startTime < 0 && !tween._paused) { //children aren't allowed to have negative startTimes unless smoothChildTiming is true, so adjust here if one is found.
                            max -= tween._startTime;
                            if (this._timeline.smoothChildTiming) {
                                this._startTime += tween._startTime / this._timeScale;
                            }
                            this.shiftChildren(-tween._startTime, false, -9999999999);
                            prevStart = 0;
                        }
                        end = tween._startTime + (tween._totalDuration / tween._timeScale);
                        if (end > max) {
                            max = end;
                        }
                        tween = prev;
                    }
                    this._duration = this._totalDuration = max;
                    this._dirty = false;
                }
                return this._totalDuration;
            }
            if (this.totalDuration() !== 0) if (value !== 0) {
                this.timeScale(this._totalDuration / value);
            }
            return this;
        };

        p.usesFrames = function() {
            var tl = this._timeline;
            while (tl._timeline) {
                tl = tl._timeline;
            }
            return (tl === Animation._rootFramesTimeline);
        };

        p.rawTime = function() {
            return this._paused ? this._totalTime : (this._timeline.rawTime() - this._startTime) * this._timeScale;
        };

        return TimelineLite;

    }, true);
    







    
    
    
    
    
/*
 * ----------------------------------------------------------------
 * TimelineMax
 * ----------------------------------------------------------------
 */
    window._gsDefine("TimelineMax", ["TimelineLite","TweenLite","easing.Ease"], function(TimelineLite, TweenLite, Ease) {

        var TimelineMax = function(vars) {
                TimelineLite.call(this, vars);
                this._repeat = this.vars.repeat || 0;
                this._repeatDelay = this.vars.repeatDelay || 0;
                this._cycle = 0;
                this._yoyo = (this.vars.yoyo === true);
                this._dirty = true;
            },
            _tinyNum = 0.0000000001,
            _blankArray = [],
            _easeNone = new Ease(null, null, 1, 0),
            p = TimelineMax.prototype = new TimelineLite();

        p.constructor = TimelineMax;
        p.kill()._gc = false;
        TimelineMax.version = "1.12.1";

        p.invalidate = function() {
            this._yoyo = (this.vars.yoyo === true);
            this._repeat = this.vars.repeat || 0;
            this._repeatDelay = this.vars.repeatDelay || 0;
            this._uncache(true);
            return TimelineLite.prototype.invalidate.call(this);
        };

        p.addCallback = function(callback, position, params, scope) {
            return this.add( TweenLite.delayedCall(0, callback, params, scope), position);
        };

        p.removeCallback = function(callback, position) {
            if (callback) {
                if (position == null) {
                    this._kill(null, callback);
                } else {
                    var a = this.getTweensOf(callback, false),
                        i = a.length,
                        time = this._parseTimeOrLabel(position);
                    while (--i > -1) {
                        if (a[i]._startTime === time) {
                            a[i]._enabled(false, false);
                        }
                    }
                }
            }
            return this;
        };

        p.tweenTo = function(position, vars) {
            vars = vars || {};
            var copy = {ease:_easeNone, overwrite:(vars.delay ? 2 : 1), useFrames:this.usesFrames(), immediateRender:false},//note: set overwrite to 1 (true/all) by default unless there's a delay so that we avoid a racing situation that could happen if, for example, an onmousemove creates the same tweenTo() over and over again.
                duration, p, t;
            for (p in vars) {
                copy[p] = vars[p];
            }
            copy.time = this._parseTimeOrLabel(position);
            duration = (Math.abs(Number(copy.time) - this._time) / this._timeScale) || 0.001;
            t = new TweenLite(this, duration, copy);
            copy.onStart = function() {
                t.target.paused(true);
                if (t.vars.time !== t.target.time() && duration === t.duration()) { //don't make the duration zero - if it's supposed to be zero, don't worry because it's already initting the tween and will complete immediately, effectively making the duration zero anyway. If we make duration zero, the tween won't run at all.
                    t.duration( Math.abs( t.vars.time - t.target.time()) / t.target._timeScale );
                }
                if (vars.onStart) { //in case the user had an onStart in the vars - we don't want to overwrite it.
                    vars.onStart.apply(vars.onStartScope || t, vars.onStartParams || _blankArray);
                }
            };
            return t;
        };

        p.tweenFromTo = function(fromPosition, toPosition, vars) {
            vars = vars || {};
            fromPosition = this._parseTimeOrLabel(fromPosition);
            vars.startAt = {onComplete:this.seek, onCompleteParams:[fromPosition], onCompleteScope:this};
            vars.immediateRender = (vars.immediateRender !== false);
            var t = this.tweenTo(toPosition, vars);
            return t.duration((Math.abs( t.vars.time - fromPosition) / this._timeScale) || 0.001);
        };

        p.render = function(time, suppressEvents, force) {
            if (this._gc) {
                this._enabled(true, false);
            }
            var totalDur = (!this._dirty) ? this._totalDuration : this.totalDuration(),
                dur = this._duration,
                prevTime = this._time,
                prevTotalTime = this._totalTime,
                prevStart = this._startTime,
                prevTimeScale = this._timeScale,
                prevRawPrevTime = this._rawPrevTime,
                prevPaused = this._paused,
                prevCycle = this._cycle,
                tween, isComplete, next, callback, internalForce, cycleDuration;
            if (time >= totalDur) {
                if (!this._locked) {
                    this._totalTime = totalDur;
                    this._cycle = this._repeat;
                }
                if (!this._reversed) if (!this._hasPausedChild()) {
                    isComplete = true;
                    callback = "onComplete";
                    if (this._duration === 0) if (time === 0 || prevRawPrevTime < 0 || prevRawPrevTime === _tinyNum) if (prevRawPrevTime !== time && this._first) {
                        internalForce = true;
                        if (prevRawPrevTime > _tinyNum) {
                            callback = "onReverseComplete";
                        }
                    }
                }
                this._rawPrevTime = (this._duration || !suppressEvents || time || this._rawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration timeline or tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                if (this._yoyo && (this._cycle & 1) !== 0) {
                    this._time = time = 0;
                } else {
                    this._time = dur;
                    time = dur + 0.0001; //to avoid occasional floating point rounding errors - sometimes child tweens/timelines were not being fully completed (their progress might be 0.999999999999998 instead of 1 because when _time - tween._startTime is performed, floating point errors would return a value that was SLIGHTLY off). Try (999999999999.7 - 999999999999) * 1 = 0.699951171875 instead of 0.7. We cannot do less then 0.0001 because the same issue can occur when the duration is extremely large like 999999999999 in which case adding 0.00000001, for example, causes it to act like nothing was added.
                }

            } else if (time < 0.0000001) { //to work around occasional floating point math artifacts, round super small values to 0.
                if (!this._locked) {
                    this._totalTime = this._cycle = 0;
                }
                this._time = 0;
                if (prevTime !== 0 || (dur === 0 && prevRawPrevTime !== _tinyNum && (prevRawPrevTime > 0 || (time < 0 && prevRawPrevTime >= 0)) && !this._locked)) { //edge case for checking time < 0 && prevRawPrevTime >= 0: a zero-duration fromTo() tween inside a zero-duration timeline (yeah, very rare)
                    callback = "onReverseComplete";
                    isComplete = this._reversed;
                }
                if (time < 0) {
                    this._active = false;
                    if (dur === 0) if (prevRawPrevTime >= 0 && this._first) { //zero-duration timelines are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                        internalForce = true;
                    }
                    this._rawPrevTime = time;
                } else {
                    this._rawPrevTime = (dur || !suppressEvents || time || this._rawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration timeline or tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                    time = 0; //to avoid occasional floating point rounding errors (could cause problems especially with zero-duration tweens at the very beginning of the timeline)
                    if (!this._initted) {
                        internalForce = true;
                    }
                }

            } else {
                if (dur === 0 && prevRawPrevTime < 0) { //without this, zero-duration repeating timelines (like with a simple callback nested at the very beginning and a repeatDelay) wouldn't render the first time through.
                    internalForce = true;
                }
                this._time = this._rawPrevTime = time;
                if (!this._locked) {
                    this._totalTime = time;
                    if (this._repeat !== 0) {
                        cycleDuration = dur + this._repeatDelay;
                        this._cycle = (this._totalTime / cycleDuration) >> 0; //originally _totalTime % cycleDuration but floating point errors caused problems, so I normalized it. (4 % 0.8 should be 0 but it gets reported as 0.79999999!)
                        if (this._cycle !== 0) if (this._cycle === this._totalTime / cycleDuration) {
                            this._cycle--; //otherwise when rendered exactly at the end time, it will act as though it is repeating (at the beginning)
                        }
                        this._time = this._totalTime - (this._cycle * cycleDuration);
                        if (this._yoyo) if ((this._cycle & 1) !== 0) {
                            this._time = dur - this._time;
                        }
                        if (this._time > dur) {
                            this._time = dur;
                            time = dur + 0.0001; //to avoid occasional floating point rounding error
                        } else if (this._time < 0) {
                            this._time = time = 0;
                        } else {
                            time = this._time;
                        }
                    }
                }
            }

            if (this._cycle !== prevCycle) if (!this._locked) {
                /*
                make sure children at the end/beginning of the timeline are rendered properly. If, for example,
                a 3-second long timeline rendered at 2.9 seconds previously, and now renders at 3.2 seconds (which
                would get transated to 2.8 seconds if the timeline yoyos or 0.2 seconds if it just repeats), there
                could be a callback or a short tween that's at 2.95 or 3 seconds in which wouldn't render. So
                we need to push the timeline to the end (and/or beginning depending on its yoyo value). Also we must
                ensure that zero-duration tweens at the very beginning or end of the TimelineMax work.
                */
                var backwards = (this._yoyo && (prevCycle & 1) !== 0),
                    wrap = (backwards === (this._yoyo && (this._cycle & 1) !== 0)),
                    recTotalTime = this._totalTime,
                    recCycle = this._cycle,
                    recRawPrevTime = this._rawPrevTime,
                    recTime = this._time;

                this._totalTime = prevCycle * dur;
                if (this._cycle < prevCycle) {
                    backwards = !backwards;
                } else {
                    this._totalTime += dur;
                }
                this._time = prevTime; //temporarily revert _time so that render() renders the children in the correct order. Without this, tweens won't rewind correctly. We could arhictect things in a "cleaner" way by splitting out the rendering queue into a separate method but for performance reasons, we kept it all inside this method.

                this._rawPrevTime = (dur === 0) ? prevRawPrevTime - 0.0001 : prevRawPrevTime;
                this._cycle = prevCycle;
                this._locked = true; //prevents changes to totalTime and skips repeat/yoyo behavior when we recursively call render()
                prevTime = (backwards) ? 0 : dur;
                this.render(prevTime, suppressEvents, (dur === 0));
                if (!suppressEvents) if (!this._gc) {
                    if (this.vars.onRepeat) {
                        this.vars.onRepeat.apply(this.vars.onRepeatScope || this, this.vars.onRepeatParams || _blankArray);
                    }
                }
                if (wrap) {
                    prevTime = (backwards) ? dur + 0.0001 : -0.0001;
                    this.render(prevTime, true, false);
                }
                this._locked = false;
                if (this._paused && !prevPaused) { //if the render() triggered callback that paused this timeline, we should abort (very rare, but possible)
                    return;
                }
                this._time = recTime;
                this._totalTime = recTotalTime;
                this._cycle = recCycle;
                this._rawPrevTime = recRawPrevTime;
            }

            if ((this._time === prevTime || !this._first) && !force && !internalForce) {
                if (prevTotalTime !== this._totalTime) if (this._onUpdate) if (!suppressEvents) { //so that onUpdate fires even during the repeatDelay - as long as the totalTime changed, we should trigger onUpdate.
                    this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
                }
                return;
            } else if (!this._initted) {
                this._initted = true;
            }

            if (!this._active) if (!this._paused && this._totalTime !== prevTotalTime && time > 0) {
                this._active = true;  //so that if the user renders the timeline (as opposed to the parent timeline rendering it), it is forced to re-render and align it with the proper time/frame on the next rendering cycle. Maybe the timeline already finished but the user manually re-renders it as halfway done, for example.
            }

            if (prevTotalTime === 0) if (this.vars.onStart) if (this._totalTime !== 0) if (!suppressEvents) {
                this.vars.onStart.apply(this.vars.onStartScope || this, this.vars.onStartParams || _blankArray);
            }

            if (this._time >= prevTime) {
                tween = this._first;
                while (tween) {
                    next = tween._next; //record it here because the value could change after rendering...
                    if (this._paused && !prevPaused) { //in case a tween pauses the timeline when rendering
                        break;
                    } else if (tween._active || (tween._startTime <= this._time && !tween._paused && !tween._gc)) {
                        if (!tween._reversed) {
                            tween.render((time - tween._startTime) * tween._timeScale, suppressEvents, force);
                        } else {
                            tween.render(((!tween._dirty) ? tween._totalDuration : tween.totalDuration()) - ((time - tween._startTime) * tween._timeScale), suppressEvents, force);
                        }

                    }
                    tween = next;
                }
            } else {
                tween = this._last;
                while (tween) {
                    next = tween._prev; //record it here because the value could change after rendering...
                    if (this._paused && !prevPaused) { //in case a tween pauses the timeline when rendering
                        break;
                    } else if (tween._active || (tween._startTime <= prevTime && !tween._paused && !tween._gc)) {
                        if (!tween._reversed) {
                            tween.render((time - tween._startTime) * tween._timeScale, suppressEvents, force);
                        } else {
                            tween.render(((!tween._dirty) ? tween._totalDuration : tween.totalDuration()) - ((time - tween._startTime) * tween._timeScale), suppressEvents, force);
                        }
                    }
                    tween = next;
                }
            }

            if (this._onUpdate) if (!suppressEvents) {
                this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
            }
            if (callback) if (!this._locked) if (!this._gc) if (prevStart === this._startTime || prevTimeScale !== this._timeScale) if (this._time === 0 || totalDur >= this.totalDuration()) { //if one of the tweens that was rendered altered this timeline's startTime (like if an onComplete reversed the timeline), it probably isn't complete. If it is, don't worry, because whatever call altered the startTime would complete if it was necessary at the new time. The only exception is the timeScale property. Also check _gc because there's a chance that kill() could be called in an onUpdate
                if (isComplete) {
                    if (this._timeline.autoRemoveChildren) {
                        this._enabled(false, false);
                    }
                    this._active = false;
                }
                if (!suppressEvents && this.vars[callback]) {
                    this.vars[callback].apply(this.vars[callback + "Scope"] || this, this.vars[callback + "Params"] || _blankArray);
                }
            }
        };

        p.getActive = function(nested, tweens, timelines) {
            if (nested == null) {
                nested = true;
            }
            if (tweens == null) {
                tweens = true;
            }
            if (timelines == null) {
                timelines = false;
            }
            var a = [],
                all = this.getChildren(nested, tweens, timelines),
                cnt = 0,
                l = all.length,
                i, tween;
            for (i = 0; i < l; i++) {
                tween = all[i];
                if (tween.isActive()) {
                    a[cnt++] = tween;
                }
            }
            return a;
        };


        p.getLabelAfter = function(time) {
            if (!time) if (time !== 0) { //faster than isNan()
                time = this._time;
            }
            var labels = this.getLabelsArray(),
                l = labels.length,
                i;
            for (i = 0; i < l; i++) {
                if (labels[i].time > time) {
                    return labels[i].name;
                }
            }
            return null;
        };

        p.getLabelBefore = function(time) {
            if (time == null) {
                time = this._time;
            }
            var labels = this.getLabelsArray(),
                i = labels.length;
            while (--i > -1) {
                if (labels[i].time < time) {
                    return labels[i].name;
                }
            }
            return null;
        };

        p.getLabelsArray = function() {
            var a = [],
                cnt = 0,
                p;
            for (p in this._labels) {
                a[cnt++] = {time:this._labels[p], name:p};
            }
            a.sort(function(a,b) {
                return a.time - b.time;
            });
            return a;
        };


//---- GETTERS / SETTERS -------------------------------------------------------------------------------------------------------

        p.progress = function(value) {
            return (!arguments.length) ? this._time / this.duration() : this.totalTime( this.duration() * ((this._yoyo && (this._cycle & 1) !== 0) ? 1 - value : value) + (this._cycle * (this._duration + this._repeatDelay)), false);
        };

        p.totalProgress = function(value) {
            return (!arguments.length) ? this._totalTime / this.totalDuration() : this.totalTime( this.totalDuration() * value, false);
        };

        p.totalDuration = function(value) {
            if (!arguments.length) {
                if (this._dirty) {
                    TimelineLite.prototype.totalDuration.call(this); //just forces refresh
                    //Instead of Infinity, we use 999999999999 so that we can accommodate reverses.
                    this._totalDuration = (this._repeat === -1) ? 999999999999 : this._duration * (this._repeat + 1) + (this._repeatDelay * this._repeat);
                }
                return this._totalDuration;
            }
            return (this._repeat === -1) ? this : this.duration( (value - (this._repeat * this._repeatDelay)) / (this._repeat + 1) );
        };

        p.time = function(value, suppressEvents) {
            if (!arguments.length) {
                return this._time;
            }
            if (this._dirty) {
                this.totalDuration();
            }
            if (value > this._duration) {
                value = this._duration;
            }
            if (this._yoyo && (this._cycle & 1) !== 0) {
                value = (this._duration - value) + (this._cycle * (this._duration + this._repeatDelay));
            } else if (this._repeat !== 0) {
                value += this._cycle * (this._duration + this._repeatDelay);
            }
            return this.totalTime(value, suppressEvents);
        };

        p.repeat = function(value) {
            if (!arguments.length) {
                return this._repeat;
            }
            this._repeat = value;
            return this._uncache(true);
        };

        p.repeatDelay = function(value) {
            if (!arguments.length) {
                return this._repeatDelay;
            }
            this._repeatDelay = value;
            return this._uncache(true);
        };

        p.yoyo = function(value) {
            if (!arguments.length) {
                return this._yoyo;
            }
            this._yoyo = value;
            return this;
        };

        p.currentLabel = function(value) {
            if (!arguments.length) {
                return this.getLabelBefore(this._time + 0.00000001);
            }
            return this.seek(value, true);
        };

        return TimelineMax;

    }, true);
    




    
    
    
    
    
    
    
/*
 * ----------------------------------------------------------------
 * BezierPlugin
 * ----------------------------------------------------------------
 */
    (function() {

        var _RAD2DEG = 180 / Math.PI,
            _r1 = [],
            _r2 = [],
            _r3 = [],
            _corProps = {},
            Segment = function(a, b, c, d) {
                this.a = a;
                this.b = b;
                this.c = c;
                this.d = d;
                this.da = d - a;
                this.ca = c - a;
                this.ba = b - a;
            },
            _correlate = ",x,y,z,left,top,right,bottom,marginTop,marginLeft,marginRight,marginBottom,paddingLeft,paddingTop,paddingRight,paddingBottom,backgroundPosition,backgroundPosition_y,",
            cubicToQuadratic = function(a, b, c, d) {
                var q1 = {a:a},
                    q2 = {},
                    q3 = {},
                    q4 = {c:d},
                    mab = (a + b) / 2,
                    mbc = (b + c) / 2,
                    mcd = (c + d) / 2,
                    mabc = (mab + mbc) / 2,
                    mbcd = (mbc + mcd) / 2,
                    m8 = (mbcd - mabc) / 8;
                q1.b = mab + (a - mab) / 4;
                q2.b = mabc + m8;
                q1.c = q2.a = (q1.b + q2.b) / 2;
                q2.c = q3.a = (mabc + mbcd) / 2;
                q3.b = mbcd - m8;
                q4.b = mcd + (d - mcd) / 4;
                q3.c = q4.a = (q3.b + q4.b) / 2;
                return [q1, q2, q3, q4];
            },
            _calculateControlPoints = function(a, curviness, quad, basic, correlate) {
                var l = a.length - 1,
                    ii = 0,
                    cp1 = a[0].a,
                    i, p1, p2, p3, seg, m1, m2, mm, cp2, qb, r1, r2, tl;
                for (i = 0; i < l; i++) {
                    seg = a[ii];
                    p1 = seg.a;
                    p2 = seg.d;
                    p3 = a[ii+1].d;

                    if (correlate) {
                        r1 = _r1[i];
                        r2 = _r2[i];
                        tl = ((r2 + r1) * curviness * 0.25) / (basic ? 0.5 : _r3[i] || 0.5);
                        m1 = p2 - (p2 - p1) * (basic ? curviness * 0.5 : (r1 !== 0 ? tl / r1 : 0));
                        m2 = p2 + (p3 - p2) * (basic ? curviness * 0.5 : (r2 !== 0 ? tl / r2 : 0));
                        mm = p2 - (m1 + (((m2 - m1) * ((r1 * 3 / (r1 + r2)) + 0.5) / 4) || 0));
                    } else {
                        m1 = p2 - (p2 - p1) * curviness * 0.5;
                        m2 = p2 + (p3 - p2) * curviness * 0.5;
                        mm = p2 - (m1 + m2) / 2;
                    }
                    m1 += mm;
                    m2 += mm;

                    seg.c = cp2 = m1;
                    if (i !== 0) {
                        seg.b = cp1;
                    } else {
                        seg.b = cp1 = seg.a + (seg.c - seg.a) * 0.6; //instead of placing b on a exactly, we move it inline with c so that if the user specifies an ease like Back.easeIn or Elastic.easeIn which goes BEYOND the beginning, it will do so smoothly.
                    }

                    seg.da = p2 - p1;
                    seg.ca = cp2 - p1;
                    seg.ba = cp1 - p1;

                    if (quad) {
                        qb = cubicToQuadratic(p1, cp1, cp2, p2);
                        a.splice(ii, 1, qb[0], qb[1], qb[2], qb[3]);
                        ii += 4;
                    } else {
                        ii++;
                    }

                    cp1 = m2;
                }
                seg = a[ii];
                seg.b = cp1;
                seg.c = cp1 + (seg.d - cp1) * 0.4; //instead of placing c on d exactly, we move it inline with b so that if the user specifies an ease like Back.easeOut or Elastic.easeOut which goes BEYOND the end, it will do so smoothly.
                seg.da = seg.d - seg.a;
                seg.ca = seg.c - seg.a;
                seg.ba = cp1 - seg.a;
                if (quad) {
                    qb = cubicToQuadratic(seg.a, cp1, seg.c, seg.d);
                    a.splice(ii, 1, qb[0], qb[1], qb[2], qb[3]);
                }
            },
            _parseAnchors = function(values, p, correlate, prepend) {
                var a = [],
                    l, i, p1, p2, p3, tmp;
                if (prepend) {
                    values = [prepend].concat(values);
                    i = values.length;
                    while (--i > -1) {
                        if (typeof( (tmp = values[i][p]) ) === "string") if (tmp.charAt(1) === "=") {
                            values[i][p] = prepend[p] + Number(tmp.charAt(0) + tmp.substr(2)); //accommodate relative values. Do it inline instead of breaking it out into a function for speed reasons
                        }
                    }
                }
                l = values.length - 2;
                if (l < 0) {
                    a[0] = new Segment(values[0][p], 0, 0, values[(l < -1) ? 0 : 1][p]);
                    return a;
                }
                for (i = 0; i < l; i++) {
                    p1 = values[i][p];
                    p2 = values[i+1][p];
                    a[i] = new Segment(p1, 0, 0, p2);
                    if (correlate) {
                        p3 = values[i+2][p];
                        _r1[i] = (_r1[i] || 0) + (p2 - p1) * (p2 - p1);
                        _r2[i] = (_r2[i] || 0) + (p3 - p2) * (p3 - p2);
                    }
                }
                a[i] = new Segment(values[i][p], 0, 0, values[i+1][p]);
                return a;
            },
            bezierThrough = function(values, curviness, quadratic, basic, correlate, prepend) {
                var obj = {},
                    props = [],
                    first = prepend || values[0],
                    i, p, a, j, r, l, seamless, last;
                correlate = (typeof(correlate) === "string") ? ","+correlate+"," : _correlate;
                if (curviness == null) {
                    curviness = 1;
                }
                for (p in values[0]) {
                    props.push(p);
                }
                //check to see if the last and first values are identical (well, within 0.05). If so, make seamless by appending the second element to the very end of the values array and the 2nd-to-last element to the very beginning (we'll remove those segments later)
                if (values.length > 1) {
                    last = values[values.length - 1];
                    seamless = true;
                    i = props.length;
                    while (--i > -1) {
                        p = props[i];
                        if (Math.abs(first[p] - last[p]) > 0.05) { //build in a tolerance of +/-0.05 to accommodate rounding errors. For example, if you set an object's position to 4.945, Flash will make it 4.9
                            seamless = false;
                            break;
                        }
                    }
                    if (seamless) {
                        values = values.concat(); //duplicate the array to avoid contaminating the original which the user may be reusing for other tweens
                        if (prepend) {
                            values.unshift(prepend);
                        }
                        values.push(values[1]);
                        prepend = values[values.length - 3];
                    }
                }
                _r1.length = _r2.length = _r3.length = 0;
                i = props.length;
                while (--i > -1) {
                    p = props[i];
                    _corProps[p] = (correlate.indexOf(","+p+",") !== -1);
                    obj[p] = _parseAnchors(values, p, _corProps[p], prepend);
                }
                i = _r1.length;
                while (--i > -1) {
                    _r1[i] = Math.sqrt(_r1[i]);
                    _r2[i] = Math.sqrt(_r2[i]);
                }
                if (!basic) {
                    i = props.length;
                    while (--i > -1) {
                        if (_corProps[p]) {
                            a = obj[props[i]];
                            l = a.length - 1;
                            for (j = 0; j < l; j++) {
                                r = a[j+1].da / _r2[j] + a[j].da / _r1[j];
                                _r3[j] = (_r3[j] || 0) + r * r;
                            }
                        }
                    }
                    i = _r3.length;
                    while (--i > -1) {
                        _r3[i] = Math.sqrt(_r3[i]);
                    }
                }
                i = props.length;
                j = quadratic ? 4 : 1;
                while (--i > -1) {
                    p = props[i];
                    a = obj[p];
                    _calculateControlPoints(a, curviness, quadratic, basic, _corProps[p]); //this method requires that _parseAnchors() and _setSegmentRatios() ran first so that _r1, _r2, and _r3 values are populated for all properties
                    if (seamless) {
                        a.splice(0, j);
                        a.splice(a.length - j, j);
                    }
                }
                return obj;
            },
            _parseBezierData = function(values, type, prepend) {
                type = type || "soft";
                var obj = {},
                    inc = (type === "cubic") ? 3 : 2,
                    soft = (type === "soft"),
                    props = [],
                    a, b, c, d, cur, i, j, l, p, cnt, tmp;
                if (soft && prepend) {
                    values = [prepend].concat(values);
                }
                if (values == null || values.length < inc + 1) { throw "invalid Bezier data"; }
                for (p in values[0]) {
                    props.push(p);
                }
                i = props.length;
                while (--i > -1) {
                    p = props[i];
                    obj[p] = cur = [];
                    cnt = 0;
                    l = values.length;
                    for (j = 0; j < l; j++) {
                        a = (prepend == null) ? values[j][p] : (typeof( (tmp = values[j][p]) ) === "string" && tmp.charAt(1) === "=") ? prepend[p] + Number(tmp.charAt(0) + tmp.substr(2)) : Number(tmp);
                        if (soft) if (j > 1) if (j < l - 1) {
                            cur[cnt++] = (a + cur[cnt-2]) / 2;
                        }
                        cur[cnt++] = a;
                    }
                    l = cnt - inc + 1;
                    cnt = 0;
                    for (j = 0; j < l; j += inc) {
                        a = cur[j];
                        b = cur[j+1];
                        c = cur[j+2];
                        d = (inc === 2) ? 0 : cur[j+3];
                        cur[cnt++] = tmp = (inc === 3) ? new Segment(a, b, c, d) : new Segment(a, (2 * b + a) / 3, (2 * b + c) / 3, c);
                    }
                    cur.length = cnt;
                }
                return obj;
            },
            _addCubicLengths = function(a, steps, resolution) {
                var inc = 1 / resolution,
                    j = a.length,
                    d, d1, s, da, ca, ba, p, i, inv, bez, index;
                while (--j > -1) {
                    bez = a[j];
                    s = bez.a;
                    da = bez.d - s;
                    ca = bez.c - s;
                    ba = bez.b - s;
                    d = d1 = 0;
                    for (i = 1; i <= resolution; i++) {
                        p = inc * i;
                        inv = 1 - p;
                        d = d1 - (d1 = (p * p * da + 3 * inv * (p * ca + inv * ba)) * p);
                        index = j * resolution + i - 1;
                        steps[index] = (steps[index] || 0) + d * d;
                    }
                }
            },
            _parseLengthData = function(obj, resolution) {
                resolution = resolution >> 0 || 6;
                var a = [],
                    lengths = [],
                    d = 0,
                    total = 0,
                    threshold = resolution - 1,
                    segments = [],
                    curLS = [], //current length segments array
                    p, i, l, index;
                for (p in obj) {
                    _addCubicLengths(obj[p], a, resolution);
                }
                l = a.length;
                for (i = 0; i < l; i++) {
                    d += Math.sqrt(a[i]);
                    index = i % resolution;
                    curLS[index] = d;
                    if (index === threshold) {
                        total += d;
                        index = (i / resolution) >> 0;
                        segments[index] = curLS;
                        lengths[index] = total;
                        d = 0;
                        curLS = [];
                    }
                }
                return {length:total, lengths:lengths, segments:segments};
            },



            BezierPlugin = window._gsDefine.plugin({
                    propName: "bezier",
                    priority: -1,
                    version: "1.3.2",
                    API: 2,
                    global:true,

                    //gets called when the tween renders for the first time. This is where initial values should be recorded and any setup routines should run.
                    init: function(target, vars, tween) {
                        this._target = target;
                        if (vars instanceof Array) {
                            vars = {values:vars};
                        }
                        this._func = {};
                        this._round = {};
                        this._props = [];
                        this._timeRes = (vars.timeResolution == null) ? 6 : parseInt(vars.timeResolution, 10);
                        var values = vars.values || [],
                            first = {},
                            second = values[0],
                            autoRotate = vars.autoRotate || tween.vars.orientToBezier,
                            p, isFunc, i, j, prepend;

                        this._autoRotate = autoRotate ? (autoRotate instanceof Array) ? autoRotate : [["x","y","rotation",((autoRotate === true) ? 0 : Number(autoRotate) || 0)]] : null;
                        for (p in second) {
                            this._props.push(p);
                        }

                        i = this._props.length;
                        while (--i > -1) {
                            p = this._props[i];

                            this._overwriteProps.push(p);
                            isFunc = this._func[p] = (typeof(target[p]) === "function");
                            first[p] = (!isFunc) ? parseFloat(target[p]) : target[ ((p.indexOf("set") || typeof(target["get" + p.substr(3)]) !== "function") ? p : "get" + p.substr(3)) ]();
                            if (!prepend) if (first[p] !== values[0][p]) {
                                prepend = first;
                            }
                        }
                        this._beziers = (vars.type !== "cubic" && vars.type !== "quadratic" && vars.type !== "soft") ? bezierThrough(values, isNaN(vars.curviness) ? 1 : vars.curviness, false, (vars.type === "thruBasic"), vars.correlate, prepend) : _parseBezierData(values, vars.type, first);
                        this._segCount = this._beziers[p].length;

                        if (this._timeRes) {
                            var ld = _parseLengthData(this._beziers, this._timeRes);
                            this._length = ld.length;
                            this._lengths = ld.lengths;
                            this._segments = ld.segments;
                            this._l1 = this._li = this._s1 = this._si = 0;
                            this._l2 = this._lengths[0];
                            this._curSeg = this._segments[0];
                            this._s2 = this._curSeg[0];
                            this._prec = 1 / this._curSeg.length;
                        }

                        if ((autoRotate = this._autoRotate)) {
                            this._initialRotations = [];
                            if (!(autoRotate[0] instanceof Array)) {
                                this._autoRotate = autoRotate = [autoRotate];
                            }
                            i = autoRotate.length;
                            while (--i > -1) {
                                for (j = 0; j < 3; j++) {
                                    p = autoRotate[i][j];
                                    this._func[p] = (typeof(target[p]) === "function") ? target[ ((p.indexOf("set") || typeof(target["get" + p.substr(3)]) !== "function") ? p : "get" + p.substr(3)) ] : false;
                                }
                                p = autoRotate[i][2];
                                this._initialRotations[i] = this._func[p] ? this._func[p].call(this._target) : this._target[p];
                            }
                        }
                        this._startRatio = tween.vars.runBackwards ? 1 : 0; //we determine the starting ratio when the tween inits which is always 0 unless the tween has runBackwards:true (indicating it's a from() tween) in which case it's 1.
                        return true;
                    },

                    //called each time the values should be updated, and the ratio gets passed as the only parameter (typically it's a value between 0 and 1, but it can exceed those when using an ease like Elastic.easeOut or Back.easeOut, etc.)
                    set: function(v) {
                        var segments = this._segCount,
                            func = this._func,
                            target = this._target,
                            notStart = (v !== this._startRatio),
                            curIndex, inv, i, p, b, t, val, l, lengths, curSeg;
                        if (!this._timeRes) {
                            curIndex = (v < 0) ? 0 : (v >= 1) ? segments - 1 : (segments * v) >> 0;
                            t = (v - (curIndex * (1 / segments))) * segments;
                        } else {
                            lengths = this._lengths;
                            curSeg = this._curSeg;
                            v *= this._length;
                            i = this._li;
                            //find the appropriate segment (if the currently cached one isn't correct)
                            if (v > this._l2 && i < segments - 1) {
                                l = segments - 1;
                                while (i < l && (this._l2 = lengths[++i]) <= v) {   }
                                this._l1 = lengths[i-1];
                                this._li = i;
                                this._curSeg = curSeg = this._segments[i];
                                this._s2 = curSeg[(this._s1 = this._si = 0)];
                            } else if (v < this._l1 && i > 0) {
                                while (i > 0 && (this._l1 = lengths[--i]) >= v) { }
                                if (i === 0 && v < this._l1) {
                                    this._l1 = 0;
                                } else {
                                    i++;
                                }
                                this._l2 = lengths[i];
                                this._li = i;
                                this._curSeg = curSeg = this._segments[i];
                                this._s1 = curSeg[(this._si = curSeg.length - 1) - 1] || 0;
                                this._s2 = curSeg[this._si];
                            }
                            curIndex = i;
                            //now find the appropriate sub-segment (we split it into the number of pieces that was defined by "precision" and measured each one)
                            v -= this._l1;
                            i = this._si;
                            if (v > this._s2 && i < curSeg.length - 1) {
                                l = curSeg.length - 1;
                                while (i < l && (this._s2 = curSeg[++i]) <= v) {    }
                                this._s1 = curSeg[i-1];
                                this._si = i;
                            } else if (v < this._s1 && i > 0) {
                                while (i > 0 && (this._s1 = curSeg[--i]) >= v) {    }
                                if (i === 0 && v < this._s1) {
                                    this._s1 = 0;
                                } else {
                                    i++;
                                }
                                this._s2 = curSeg[i];
                                this._si = i;
                            }
                            t = (i + (v - this._s1) / (this._s2 - this._s1)) * this._prec;
                        }
                        inv = 1 - t;

                        i = this._props.length;
                        while (--i > -1) {
                            p = this._props[i];
                            b = this._beziers[p][curIndex];
                            val = (t * t * b.da + 3 * inv * (t * b.ca + inv * b.ba)) * t + b.a;
                            if (this._round[p]) {
                                val = Math.round(val);
                            }
                            if (func[p]) {
                                target[p](val);
                            } else {
                                target[p] = val;
                            }
                        }

                        if (this._autoRotate) {
                            var ar = this._autoRotate,
                                b2, x1, y1, x2, y2, add, conv;
                            i = ar.length;
                            while (--i > -1) {
                                p = ar[i][2];
                                add = ar[i][3] || 0;
                                conv = (ar[i][4] === true) ? 1 : _RAD2DEG;
                                b = this._beziers[ar[i][0]];
                                b2 = this._beziers[ar[i][1]];

                                if (b && b2) { //in case one of the properties got overwritten.
                                    b = b[curIndex];
                                    b2 = b2[curIndex];

                                    x1 = b.a + (b.b - b.a) * t;
                                    x2 = b.b + (b.c - b.b) * t;
                                    x1 += (x2 - x1) * t;
                                    x2 += ((b.c + (b.d - b.c) * t) - x2) * t;

                                    y1 = b2.a + (b2.b - b2.a) * t;
                                    y2 = b2.b + (b2.c - b2.b) * t;
                                    y1 += (y2 - y1) * t;
                                    y2 += ((b2.c + (b2.d - b2.c) * t) - y2) * t;

                                    val = notStart ? Math.atan2(y2 - y1, x2 - x1) * conv + add : this._initialRotations[i];

                                    if (func[p]) {
                                        target[p](val);
                                    } else {
                                        target[p] = val;
                                    }
                                }
                            }
                        }
                    }
            }),
            p = BezierPlugin.prototype;


        BezierPlugin.bezierThrough = bezierThrough;
        BezierPlugin.cubicToQuadratic = cubicToQuadratic;
        BezierPlugin._autoCSS = true; //indicates that this plugin can be inserted into the "css" object using the autoCSS feature of TweenLite
        BezierPlugin.quadraticToCubic = function(a, b, c) {
            return new Segment(a, (2 * b + a) / 3, (2 * b + c) / 3, c);
        };

        BezierPlugin._cssRegister = function() {
            var CSSPlugin = window._gsDefine.globals.CSSPlugin;
            if (!CSSPlugin) {
                return;
            }
            var _internals = CSSPlugin._internals,
                _parseToProxy = _internals._parseToProxy,
                _setPluginRatio = _internals._setPluginRatio,
                CSSPropTween = _internals.CSSPropTween;
            _internals._registerComplexSpecialProp("bezier", {parser:function(t, e, prop, cssp, pt, plugin) {
                if (e instanceof Array) {
                    e = {values:e};
                }
                plugin = new BezierPlugin();
                var values = e.values,
                    l = values.length - 1,
                    pluginValues = [],
                    v = {},
                    i, p, data;
                if (l < 0) {
                    return pt;
                }
                for (i = 0; i <= l; i++) {
                    data = _parseToProxy(t, values[i], cssp, pt, plugin, (l !== i));
                    pluginValues[i] = data.end;
                }
                for (p in e) {
                    v[p] = e[p]; //duplicate the vars object because we need to alter some things which would cause problems if the user plans to reuse the same vars object for another tween.
                }
                v.values = pluginValues;
                pt = new CSSPropTween(t, "bezier", 0, 0, data.pt, 2);
                pt.data = data;
                pt.plugin = plugin;
                pt.setRatio = _setPluginRatio;
                if (v.autoRotate === 0) {
                    v.autoRotate = true;
                }
                if (v.autoRotate && !(v.autoRotate instanceof Array)) {
                    i = (v.autoRotate === true) ? 0 : Number(v.autoRotate);
                    v.autoRotate = (data.end.left != null) ? [["left","top","rotation",i,false]] : (data.end.x != null) ? [["x","y","rotation",i,false]] : false;
                }
                if (v.autoRotate) {
                    if (!cssp._transform) {
                        cssp._enableTransforms(false);
                    }
                    data.autoRotate = cssp._target._gsTransform;
                }
                plugin._onInitTween(data.proxy, v, cssp._tween);
                return pt;
            }});
        };

        p._roundProps = function(lookup, value) {
            var op = this._overwriteProps,
                i = op.length;
            while (--i > -1) {
                if (lookup[op[i]] || lookup.bezier || lookup.bezierThrough) {
                    this._round[op[i]] = value;
                }
            }
        };

        p._kill = function(lookup) {
            var a = this._props,
                p, i;
            for (p in this._beziers) {
                if (p in lookup) {
                    delete this._beziers[p];
                    delete this._func[p];
                    i = a.length;
                    while (--i > -1) {
                        if (a[i] === p) {
                            a.splice(i, 1);
                        }
                    }
                }
            }
            return this._super._kill.call(this, lookup);
        };

    }());






    
    
    
    
    
    
    
    
/*
 * ----------------------------------------------------------------
 * CSSPlugin
 * ----------------------------------------------------------------
 */
    window._gsDefine("plugins.CSSPlugin", ["plugins.TweenPlugin","TweenLite"], function(TweenPlugin, TweenLite) {

        /** @constructor **/
        var CSSPlugin = function() {
                TweenPlugin.call(this, "css");
                this._overwriteProps.length = 0;
                this.setRatio = CSSPlugin.prototype.setRatio; //speed optimization (avoid prototype lookup on this "hot" method)
            },
            _hasPriority, //turns true whenever a CSSPropTween instance is created that has a priority other than 0. This helps us discern whether or not we should spend the time organizing the linked list or not after a CSSPlugin's _onInitTween() method is called.
            _suffixMap, //we set this in _onInitTween() each time as a way to have a persistent variable we can use in other methods like _parse() without having to pass it around as a parameter and we keep _parse() decoupled from a particular CSSPlugin instance
            _cs, //computed style (we store this in a shared variable to conserve memory and make minification tighter
            _overwriteProps, //alias to the currently instantiating CSSPlugin's _overwriteProps array. We use this closure in order to avoid having to pass a reference around from method to method and aid in minification.
            _specialProps = {},
            p = CSSPlugin.prototype = new TweenPlugin("css");

        p.constructor = CSSPlugin;
        CSSPlugin.version = "1.12.1";
        CSSPlugin.API = 2;
        CSSPlugin.defaultTransformPerspective = 0;
        CSSPlugin.defaultSkewType = "compensated";
        p = "px"; //we'll reuse the "p" variable to keep file size down
        CSSPlugin.suffixMap = {top:p, right:p, bottom:p, left:p, width:p, height:p, fontSize:p, padding:p, margin:p, perspective:p, lineHeight:""};


        var _numExp = /(?:\d|\-\d|\.\d|\-\.\d)+/g,
            _relNumExp = /(?:\d|\-\d|\.\d|\-\.\d|\+=\d|\-=\d|\+=.\d|\-=\.\d)+/g,
            _valuesExp = /(?:\+=|\-=|\-|\b)[\d\-\.]+[a-zA-Z0-9]*(?:%|\b)/gi, //finds all the values that begin with numbers or += or -= and then a number. Includes suffixes. We use this to split complex values apart like "1px 5px 20px rgb(255,102,51)"
            _NaNExp = /[^\d\-\.]/g,
            _suffixExp = /(?:\d|\-|\+|=|#|\.)*/g,
            _opacityExp = /opacity *= *([^)]*)/i,
            _opacityValExp = /opacity:([^;]*)/i,
            _alphaFilterExp = /alpha\(opacity *=.+?\)/i,
            _rgbhslExp = /^(rgb|hsl)/,
            _capsExp = /([A-Z])/g,
            _camelExp = /-([a-z])/gi,
            _urlExp = /(^(?:url\(\"|url\())|(?:(\"\))$|\)$)/gi, //for pulling out urls from url(...) or url("...") strings (some browsers wrap urls in quotes, some don't when reporting things like backgroundImage)
            _camelFunc = function(s, g) { return g.toUpperCase(); },
            _horizExp = /(?:Left|Right|Width)/i,
            _ieGetMatrixExp = /(M11|M12|M21|M22)=[\d\-\.e]+/gi,
            _ieSetMatrixExp = /progid\:DXImageTransform\.Microsoft\.Matrix\(.+?\)/i,
            _commasOutsideParenExp = /,(?=[^\)]*(?:\(|$))/gi, //finds any commas that are not within parenthesis
            _DEG2RAD = Math.PI / 180,
            _RAD2DEG = 180 / Math.PI,
            _forcePT = {},
            _doc = document,
            _tempDiv = _doc.createElement("div"),
            _tempImg = _doc.createElement("img"),
            _internals = CSSPlugin._internals = {_specialProps:_specialProps}, //provides a hook to a few internal methods that we need to access from inside other plugins
            _agent = navigator.userAgent,
            _autoRound,
            _reqSafariFix, //we won't apply the Safari transform fix until we actually come across a tween that affects a transform property (to maintain best performance).

            _isSafari,
            _isFirefox, //Firefox has a bug that causes 3D transformed elements to randomly disappear unless a repaint is forced after each update on each element.
            _isSafariLT6, //Safari (and Android 4 which uses a flavor of Safari) has a bug that prevents changes to "top" and "left" properties from rendering properly if changed on the same frame as a transform UNLESS we set the element's WebkitBackfaceVisibility to hidden (weird, I know). Doing this for Android 3 and earlier seems to actually cause other problems, though (fun!)
            _ieVers,
            _supportsOpacity = (function() { //we set _isSafari, _ieVers, _isFirefox, and _supportsOpacity all in one function here to reduce file size slightly, especially in the minified version.
                var i = _agent.indexOf("Android"),
                    d = _doc.createElement("div"), a;

                _isSafari = (_agent.indexOf("Safari") !== -1 && _agent.indexOf("Chrome") === -1 && (i === -1 || Number(_agent.substr(i+8, 1)) > 3));
                _isSafariLT6 = (_isSafari && (Number(_agent.substr(_agent.indexOf("Version/")+8, 1)) < 6));
                _isFirefox = (_agent.indexOf("Firefox") !== -1);

                if ((/MSIE ([0-9]{1,}[\.0-9]{0,})/).exec(_agent)) {
                    _ieVers = parseFloat( RegExp.$1 );
                }

                d.innerHTML = "<a title='' style='top:1px;opacity:.55;'>a</a>";
                a = d.getElementsByTagName("a")[0];
                return a ? /^0.55/.test(a.style.opacity) : false;
            }()),
            _getIEOpacity = function(v) {
                return (_opacityExp.test( ((typeof(v) === "string") ? v : (v.currentStyle ? v.currentStyle.filter : v.style.filter) || "") ) ? ( parseFloat( RegExp.$1 ) / 100 ) : 1);
            },
            _log = function(s) {//for logging messages, but in a way that won't throw errors in old versions of IE.
                if (window.console) {
                    //console.log(s);
                }
            },
            _prefixCSS = "", //the non-camelCase vendor prefix like "-o-", "-moz-", "-ms-", or "-webkit-"
            _prefix = "", //camelCase vendor prefix like "O", "ms", "Webkit", or "Moz".

            // @private feed in a camelCase property name like "transform" and it will check to see if it is valid as-is or if it needs a vendor prefix. It returns the corrected camelCase property name (i.e. "WebkitTransform" or "MozTransform" or "transform" or null if no such property is found, like if the browser is IE8 or before, "transform" won't be found at all)
            _checkPropPrefix = function(p, e) {
                e = e || _tempDiv;
                var s = e.style,
                    a, i;
                if (s[p] !== undefined) {
                    return p;
                }
                p = p.charAt(0).toUpperCase() + p.substr(1);
                a = ["O","Moz","ms","Ms","Webkit"];
                i = 5;
                while (--i > -1 && s[a[i]+p] === undefined) { }
                if (i >= 0) {
                    _prefix = (i === 3) ? "ms" : a[i];
                    _prefixCSS = "-" + _prefix.toLowerCase() + "-";
                    return _prefix + p;
                }
                return null;
            },

            _getComputedStyle = _doc.defaultView ? _doc.defaultView.getComputedStyle : function() {},

            /**
             * @private Returns the css style for a particular property of an element. For example, to get whatever the current "left" css value for an element with an ID of "myElement", you could do:
             * var currentLeft = CSSPlugin.getStyle( document.getElementById("myElement"), "left");
             *
             * @param {!Object} t Target element whose style property you want to query
             * @param {!string} p Property name (like "left" or "top" or "marginTop", etc.)
             * @param {Object=} cs Computed style object. This just provides a way to speed processing if you're going to get several properties on the same element in quick succession - you can reuse the result of the getComputedStyle() call.
             * @param {boolean=} calc If true, the value will not be read directly from the element's "style" property (if it exists there), but instead the getComputedStyle() result will be used. This can be useful when you want to ensure that the browser itself is interpreting the value.
             * @param {string=} dflt Default value that should be returned in the place of null, "none", "auto" or "auto auto".
             * @return {?string} The current property value
             */
            _getStyle = CSSPlugin.getStyle = function(t, p, cs, calc, dflt) {
                var rv;
                if (!_supportsOpacity) if (p === "opacity") { //several versions of IE don't use the standard "opacity" property - they use things like filter:alpha(opacity=50), so we parse that here.
                    return _getIEOpacity(t);
                }
                if (!calc && t.style[p]) {
                    rv = t.style[p];
                } else if ((cs = cs || _getComputedStyle(t))) {
                    rv = cs[p] || cs.getPropertyValue(p) || cs.getPropertyValue(p.replace(_capsExp, "-$1").toLowerCase());
                } else if (t.currentStyle) {
                    rv = t.currentStyle[p];
                }
                return (dflt != null && (!rv || rv === "none" || rv === "auto" || rv === "auto auto")) ? dflt : rv;
            },

            /**
             * @private Pass the target element, the property name, the numeric value, and the suffix (like "%", "em", "px", etc.) and it will spit back the equivalent pixel number.
             * @param {!Object} t Target element
             * @param {!string} p Property name (like "left", "top", "marginLeft", etc.)
             * @param {!number} v Value
             * @param {string=} sfx Suffix (like "px" or "%" or "em")
             * @param {boolean=} recurse If true, the call is a recursive one. In some browsers (like IE7/8), occasionally the value isn't accurately reported initially, but if we run the function again it will take effect.
             * @return {number} value in pixels
             */
            _convertToPixels = _internals.convertToPixels = function(t, p, v, sfx, recurse) {
                if (sfx === "px" || !sfx) { return v; }
                if (sfx === "auto" || !v) { return 0; }
                var horiz = _horizExp.test(p),
                    node = t,
                    style = _tempDiv.style,
                    neg = (v < 0),
                    pix, cache, time;
                if (neg) {
                    v = -v;
                }
                if (sfx === "%" && p.indexOf("border") !== -1) {
                    pix = (v / 100) * (horiz ? t.clientWidth : t.clientHeight);
                } else {
                    style.cssText = "border:0 solid red;position:" + _getStyle(t, "position") + ";line-height:0;";
                    if (sfx === "%" || !node.appendChild) {
                        node = t.parentNode || _doc.body;
                        cache = node._gsCache;
                        time = TweenLite.ticker.frame;
                        if (cache && horiz && cache.time === time) { //performance optimization: we record the width of elements along with the ticker frame so that we can quickly get it again on the same tick (seems relatively safe to assume it wouldn't change on the same tick)
                            return cache.width * v / 100;
                        }
                        style[(horiz ? "width" : "height")] = v + sfx;
                    } else {
                        style[(horiz ? "borderLeftWidth" : "borderTopWidth")] = v + sfx;
                    }
                    node.appendChild(_tempDiv);
                    pix = parseFloat(_tempDiv[(horiz ? "offsetWidth" : "offsetHeight")]);
                    node.removeChild(_tempDiv);
                    if (horiz && sfx === "%" && CSSPlugin.cacheWidths !== false) {
                        cache = node._gsCache = node._gsCache || {};
                        cache.time = time;
                        cache.width = pix / v * 100;
                    }
                    if (pix === 0 && !recurse) {
                        pix = _convertToPixels(t, p, v, sfx, true);
                    }
                }
                return neg ? -pix : pix;
            },
            _calculateOffset = _internals.calculateOffset = function(t, p, cs) { //for figuring out "top" or "left" in px when it's "auto". We need to factor in margin with the offsetLeft/offsetTop
                if (_getStyle(t, "position", cs) !== "absolute") { return 0; }
                var dim = ((p === "left") ? "Left" : "Top"),
                    v = _getStyle(t, "margin" + dim, cs);
                return t["offset" + dim] - (_convertToPixels(t, p, parseFloat(v), v.replace(_suffixExp, "")) || 0);
            },

            // @private returns at object containing ALL of the style properties in camelCase and their associated values.
            _getAllStyles = function(t, cs) {
                var s = {},
                    i, tr;
                if ((cs = cs || _getComputedStyle(t, null))) {
                    if ((i = cs.length)) {
                        while (--i > -1) {
                            s[cs[i].replace(_camelExp, _camelFunc)] = cs.getPropertyValue(cs[i]);
                        }
                    } else { //Opera behaves differently - cs.length is always 0, so we must do a for...in loop.
                        for (i in cs) {
                            s[i] = cs[i];
                        }
                    }
                } else if ((cs = t.currentStyle || t.style)) {
                    for (i in cs) {
                        if (typeof(i) === "string" && s[i] === undefined) {
                            s[i.replace(_camelExp, _camelFunc)] = cs[i];
                        }
                    }
                }
                if (!_supportsOpacity) {
                    s.opacity = _getIEOpacity(t);
                }
                tr = _getTransform(t, cs, false);
                s.rotation = tr.rotation;
                s.skewX = tr.skewX;
                s.scaleX = tr.scaleX;
                s.scaleY = tr.scaleY;
                s.x = tr.x;
                s.y = tr.y;
                if (_supports3D) {
                    s.z = tr.z;
                    s.rotationX = tr.rotationX;
                    s.rotationY = tr.rotationY;
                    s.scaleZ = tr.scaleZ;
                }
                if (s.filters) {
                    delete s.filters;
                }
                return s;
            },

            // @private analyzes two style objects (as returned by _getAllStyles()) and only looks for differences between them that contain tweenable values (like a number or color). It returns an object with a "difs" property which refers to an object containing only those isolated properties and values for tweening, and a "firstMPT" property which refers to the first MiniPropTween instance in a linked list that recorded all the starting values of the different properties so that we can revert to them at the end or beginning of the tween - we don't want the cascading to get messed up. The forceLookup parameter is an optional generic object with properties that should be forced into the results - this is necessary for className tweens that are overwriting others because imagine a scenario where a rollover/rollout adds/removes a class and the user swipes the mouse over the target SUPER fast, thus nothing actually changed yet and the subsequent comparison of the properties would indicate they match (especially when px rounding is taken into consideration), thus no tweening is necessary even though it SHOULD tween and remove those properties after the tween (otherwise the inline styles will contaminate things). See the className SpecialProp code for details.
            _cssDif = function(t, s1, s2, vars, forceLookup) {
                var difs = {},
                    style = t.style,
                    val, p, mpt;
                for (p in s2) {
                    if (p !== "cssText") if (p !== "length") if (isNaN(p)) if (s1[p] !== (val = s2[p]) || (forceLookup && forceLookup[p])) if (p.indexOf("Origin") === -1) if (typeof(val) === "number" || typeof(val) === "string") {
                        difs[p] = (val === "auto" && (p === "left" || p === "top")) ? _calculateOffset(t, p) : ((val === "" || val === "auto" || val === "none") && typeof(s1[p]) === "string" && s1[p].replace(_NaNExp, "") !== "") ? 0 : val; //if the ending value is defaulting ("" or "auto"), we check the starting value and if it can be parsed into a number (a string which could have a suffix too, like 700px), then we swap in 0 for "" or "auto" so that things actually tween.
                        if (style[p] !== undefined) { //for className tweens, we must remember which properties already existed inline - the ones that didn't should be removed when the tween isn't in progress because they were only introduced to facilitate the transition between classes.
                            mpt = new MiniPropTween(style, p, style[p], mpt);
                        }
                    }
                }
                if (vars) {
                    for (p in vars) { //copy properties (except className)
                        if (p !== "className") {
                            difs[p] = vars[p];
                        }
                    }
                }
                return {difs:difs, firstMPT:mpt};
            },
            _dimensions = {width:["Left","Right"], height:["Top","Bottom"]},
            _margins = ["marginLeft","marginRight","marginTop","marginBottom"],

            /**
             * @private Gets the width or height of an element
             * @param {!Object} t Target element
             * @param {!string} p Property name ("width" or "height")
             * @param {Object=} cs Computed style object (if one exists). Just a speed optimization.
             * @return {number} Dimension (in pixels)
             */
            _getDimension = function(t, p, cs) {
                var v = parseFloat((p === "width") ? t.offsetWidth : t.offsetHeight),
                    a = _dimensions[p],
                    i = a.length;
                cs = cs || _getComputedStyle(t, null);
                while (--i > -1) {
                    v -= parseFloat( _getStyle(t, "padding" + a[i], cs, true) ) || 0;
                    v -= parseFloat( _getStyle(t, "border" + a[i] + "Width", cs, true) ) || 0;
                }
                return v;
            },

            // @private Parses position-related complex strings like "top left" or "50px 10px" or "70% 20%", etc. which are used for things like transformOrigin or backgroundPosition. Optionally decorates a supplied object (recObj) with the following properties: "ox" (offsetX), "oy" (offsetY), "oxp" (if true, "ox" is a percentage not a pixel value), and "oxy" (if true, "oy" is a percentage not a pixel value)
            _parsePosition = function(v, recObj) {
                if (v == null || v === "" || v === "auto" || v === "auto auto") { //note: Firefox uses "auto auto" as default whereas Chrome uses "auto".
                    v = "0 0";
                }
                var a = v.split(" "),
                    x = (v.indexOf("left") !== -1) ? "0%" : (v.indexOf("right") !== -1) ? "100%" : a[0],
                    y = (v.indexOf("top") !== -1) ? "0%" : (v.indexOf("bottom") !== -1) ? "100%" : a[1];
                if (y == null) {
                    y = "0";
                } else if (y === "center") {
                    y = "50%";
                }
                if (x === "center" || (isNaN(parseFloat(x)) && (x + "").indexOf("=") === -1)) { //remember, the user could flip-flop the values and say "bottom center" or "center bottom", etc. "center" is ambiguous because it could be used to describe horizontal or vertical, hence the isNaN(). If there's an "=" sign in the value, it's relative.
                    x = "50%";
                }
                if (recObj) {
                    recObj.oxp = (x.indexOf("%") !== -1);
                    recObj.oyp = (y.indexOf("%") !== -1);
                    recObj.oxr = (x.charAt(1) === "=");
                    recObj.oyr = (y.charAt(1) === "=");
                    recObj.ox = parseFloat(x.replace(_NaNExp, ""));
                    recObj.oy = parseFloat(y.replace(_NaNExp, ""));
                }
                return x + " " + y + ((a.length > 2) ? " " + a[2] : "");
            },

            /**
             * @private Takes an ending value (typically a string, but can be a number) and a starting value and returns the change between the two, looking for relative value indicators like += and -= and it also ignores suffixes (but make sure the ending value starts with a number or +=/-= and that the starting value is a NUMBER!)
             * @param {(number|string)} e End value which is typically a string, but could be a number
             * @param {(number|string)} b Beginning value which is typically a string but could be a number
             * @return {number} Amount of change between the beginning and ending values (relative values that have a "+=" or "-=" are recognized)
             */
            _parseChange = function(e, b) {
                return (typeof(e) === "string" && e.charAt(1) === "=") ? parseInt(e.charAt(0) + "1", 10) * parseFloat(e.substr(2)) : parseFloat(e) - parseFloat(b);
            },

            /**
             * @private Takes a value and a default number, checks if the value is relative, null, or numeric and spits back a normalized number accordingly. Primarily used in the _parseTransform() function.
             * @param {Object} v Value to be parsed
             * @param {!number} d Default value (which is also used for relative calculations if "+=" or "-=" is found in the first parameter)
             * @return {number} Parsed value
             */
            _parseVal = function(v, d) {
                return (v == null) ? d : (typeof(v) === "string" && v.charAt(1) === "=") ? parseInt(v.charAt(0) + "1", 10) * Number(v.substr(2)) + d : parseFloat(v);
            },

            /**
             * @private Translates strings like "40deg" or "40" or 40rad" or "+=40deg" or "270_short" or "-90_cw" or "+=45_ccw" to a numeric radian angle. Of course a starting/default value must be fed in too so that relative values can be calculated properly.
             * @param {Object} v Value to be parsed
             * @param {!number} d Default value (which is also used for relative calculations if "+=" or "-=" is found in the first parameter)
             * @param {string=} p property name for directionalEnd (optional - only used when the parsed value is directional ("_short", "_cw", or "_ccw" suffix). We need a way to store the uncompensated value so that at the end of the tween, we set it to exactly what was requested with no directional compensation). Property name would be "rotation", "rotationX", or "rotationY"
             * @param {Object=} directionalEnd An object that will store the raw end values for directional angles ("_short", "_cw", or "_ccw" suffix). We need a way to store the uncompensated value so that at the end of the tween, we set it to exactly what was requested with no directional compensation.
             * @return {number} parsed angle in radians
             */
            _parseAngle = function(v, d, p, directionalEnd) {
                var min = 0.000001,
                    cap, split, dif, result;
                if (v == null) {
                    result = d;
                } else if (typeof(v) === "number") {
                    result = v;
                } else {
                    cap = 360;
                    split = v.split("_");
                    dif = Number(split[0].replace(_NaNExp, "")) * ((v.indexOf("rad") === -1) ? 1 : _RAD2DEG) - ((v.charAt(1) === "=") ? 0 : d);
                    if (split.length) {
                        if (directionalEnd) {
                            directionalEnd[p] = d + dif;
                        }
                        if (v.indexOf("short") !== -1) {
                            dif = dif % cap;
                            if (dif !== dif % (cap / 2)) {
                                dif = (dif < 0) ? dif + cap : dif - cap;
                            }
                        }
                        if (v.indexOf("_cw") !== -1 && dif < 0) {
                            dif = ((dif + cap * 9999999999) % cap) - ((dif / cap) | 0) * cap;
                        } else if (v.indexOf("ccw") !== -1 && dif > 0) {
                            dif = ((dif - cap * 9999999999) % cap) - ((dif / cap) | 0) * cap;
                        }
                    }
                    result = d + dif;
                }
                if (result < min && result > -min) {
                    result = 0;
                }
                return result;
            },

            _colorLookup = {aqua:[0,255,255],
                lime:[0,255,0],
                silver:[192,192,192],
                black:[0,0,0],
                maroon:[128,0,0],
                teal:[0,128,128],
                blue:[0,0,255],
                navy:[0,0,128],
                white:[255,255,255],
                fuchsia:[255,0,255],
                olive:[128,128,0],
                yellow:[255,255,0],
                orange:[255,165,0],
                gray:[128,128,128],
                purple:[128,0,128],
                green:[0,128,0],
                red:[255,0,0],
                pink:[255,192,203],
                cyan:[0,255,255],
                transparent:[255,255,255,0]},

            _hue = function(h, m1, m2) {
                h = (h < 0) ? h + 1 : (h > 1) ? h - 1 : h;
                return ((((h * 6 < 1) ? m1 + (m2 - m1) * h * 6 : (h < 0.5) ? m2 : (h * 3 < 2) ? m1 + (m2 - m1) * (2 / 3 - h) * 6 : m1) * 255) + 0.5) | 0;
            },

            /**
             * @private Parses a color (like #9F0, #FF9900, or rgb(255,51,153)) into an array with 3 elements for red, green, and blue. Also handles rgba() values (splits into array of 4 elements of course)
             * @param {(string|number)} v The value the should be parsed which could be a string like #9F0 or rgb(255,102,51) or rgba(255,0,0,0.5) or it could be a number like 0xFF00CC or even a named color like red, blue, purple, etc.
             * @return {Array.<number>} An array containing red, green, and blue (and optionally alpha) in that order.
             */
            _parseColor = function(v) {
                var c1, c2, c3, h, s, l;
                if (!v || v === "") {
                    return _colorLookup.black;
                }
                if (typeof(v) === "number") {
                    return [v >> 16, (v >> 8) & 255, v & 255];
                }
                if (v.charAt(v.length - 1) === ",") { //sometimes a trailing commma is included and we should chop it off (typically from a comma-delimited list of values like a textShadow:"2px 2px 2px blue, 5px 5px 5px rgb(255,0,0)" - in this example "blue," has a trailing comma. We could strip it out inside parseComplex() but we'd need to do it to the beginning and ending values plus it wouldn't provide protection from other potential scenarios like if the user passes in a similar value.
                    v = v.substr(0, v.length - 1);
                }
                if (_colorLookup[v]) {
                    return _colorLookup[v];
                }
                if (v.charAt(0) === "#") {
                    if (v.length === 4) { //for shorthand like #9F0
                        c1 = v.charAt(1),
                        c2 = v.charAt(2),
                        c3 = v.charAt(3);
                        v = "#" + c1 + c1 + c2 + c2 + c3 + c3;
                    }
                    v = parseInt(v.substr(1), 16);
                    return [v >> 16, (v >> 8) & 255, v & 255];
                }
                if (v.substr(0, 3) === "hsl") {
                    v = v.match(_numExp);
                    h = (Number(v[0]) % 360) / 360;
                    s = Number(v[1]) / 100;
                    l = Number(v[2]) / 100;
                    c2 = (l <= 0.5) ? l * (s + 1) : l + s - l * s;
                    c1 = l * 2 - c2;
                    if (v.length > 3) {
                        v[3] = Number(v[3]);
                    }
                    v[0] = _hue(h + 1 / 3, c1, c2);
                    v[1] = _hue(h, c1, c2);
                    v[2] = _hue(h - 1 / 3, c1, c2);
                    return v;
                }
                v = v.match(_numExp) || _colorLookup.transparent;
                v[0] = Number(v[0]);
                v[1] = Number(v[1]);
                v[2] = Number(v[2]);
                if (v.length > 3) {
                    v[3] = Number(v[3]);
                }
                return v;
            },
            _colorExp = "(?:\\b(?:(?:rgb|rgba|hsl|hsla)\\(.+?\\))|\\B#.+?\\b"; //we'll dynamically build this Regular Expression to conserve file size. After building it, it will be able to find rgb(), rgba(), # (hexadecimal), and named color values like red, blue, purple, etc.

        for (p in _colorLookup) {
            _colorExp += "|" + p + "\\b";
        }
        _colorExp = new RegExp(_colorExp+")", "gi");

        /**
         * @private Returns a formatter function that handles taking a string (or number in some cases) and returning a consistently formatted one in terms of delimiters, quantity of values, etc. For example, we may get boxShadow values defined as "0px red" or "0px 0px 10px rgb(255,0,0)" or "0px 0px 20px 20px #F00" and we need to ensure that what we get back is described with 4 numbers and a color. This allows us to feed it into the _parseComplex() method and split the values up appropriately. The neat thing about this _getFormatter() function is that the dflt defines a pattern as well as a default, so for example, _getFormatter("0px 0px 0px 0px #777", true) not only sets the default as 0px for all distances and #777 for the color, but also sets the pattern such that 4 numbers and a color will always get returned.
         * @param {!string} dflt The default value and pattern to follow. So "0px 0px 0px 0px #777" will ensure that 4 numbers and a color will always get returned.
         * @param {boolean=} clr If true, the values should be searched for color-related data. For example, boxShadow values typically contain a color whereas borderRadius don't.
         * @param {boolean=} collapsible If true, the value is a top/left/right/bottom style one that acts like margin or padding, where if only one value is received, it's used for all 4; if 2 are received, the first is duplicated for 3rd (bottom) and the 2nd is duplicated for the 4th spot (left), etc.
         * @return {Function} formatter function
         */
        var _getFormatter = function(dflt, clr, collapsible, multi) {
                if (dflt == null) {
                    return function(v) {return v;};
                }
                var dColor = clr ? (dflt.match(_colorExp) || [""])[0] : "",
                    dVals = dflt.split(dColor).join("").match(_valuesExp) || [],
                    pfx = dflt.substr(0, dflt.indexOf(dVals[0])),
                    sfx = (dflt.charAt(dflt.length - 1) === ")") ? ")" : "",
                    delim = (dflt.indexOf(" ") !== -1) ? " " : ",",
                    numVals = dVals.length,
                    dSfx = (numVals > 0) ? dVals[0].replace(_numExp, "") : "",
                    formatter;
                if (!numVals) {
                    return function(v) {return v;};
                }
                if (clr) {
                    formatter = function(v) {
                        var color, vals, i, a;
                        if (typeof(v) === "number") {
                            v += dSfx;
                        } else if (multi && _commasOutsideParenExp.test(v)) {
                            a = v.replace(_commasOutsideParenExp, "|").split("|");
                            for (i = 0; i < a.length; i++) {
                                a[i] = formatter(a[i]);
                            }
                            return a.join(",");
                        }
                        color = (v.match(_colorExp) || [dColor])[0];
                        vals = v.split(color).join("").match(_valuesExp) || [];
                        i = vals.length;
                        if (numVals > i--) {
                            while (++i < numVals) {
                                vals[i] = collapsible ? vals[(((i - 1) / 2) | 0)] : dVals[i];
                            }
                        }
                        return pfx + vals.join(delim) + delim + color + sfx + (v.indexOf("inset") !== -1 ? " inset" : "");
                    };
                    return formatter;

                }
                formatter = function(v) {
                    var vals, a, i;
                    if (typeof(v) === "number") {
                        v += dSfx;
                    } else if (multi && _commasOutsideParenExp.test(v)) {
                        a = v.replace(_commasOutsideParenExp, "|").split("|");
                        for (i = 0; i < a.length; i++) {
                            a[i] = formatter(a[i]);
                        }
                        return a.join(",");
                    }
                    vals = v.match(_valuesExp) || [];
                    i = vals.length;
                    if (numVals > i--) {
                        while (++i < numVals) {
                            vals[i] = collapsible ? vals[(((i - 1) / 2) | 0)] : dVals[i];
                        }
                    }
                    return pfx + vals.join(delim) + sfx;
                };
                return formatter;
            },

            /**
             * @private returns a formatter function that's used for edge-related values like marginTop, marginLeft, paddingBottom, paddingRight, etc. Just pass a comma-delimited list of property names related to the edges.
             * @param {!string} props a comma-delimited list of property names in order from top to left, like "marginTop,marginRight,marginBottom,marginLeft"
             * @return {Function} a formatter function
             */
            _getEdgeParser = function(props) {
                props = props.split(",");
                return function(t, e, p, cssp, pt, plugin, vars) {
                    var a = (e + "").split(" "),
                        i;
                    vars = {};
                    for (i = 0; i < 4; i++) {
                        vars[props[i]] = a[i] = a[i] || a[(((i - 1) / 2) >> 0)];
                    }
                    return cssp.parse(t, vars, pt, plugin);
                };
            },

            // @private used when other plugins must tween values first, like BezierPlugin or ThrowPropsPlugin, etc. That plugin's setRatio() gets called first so that the values are updated, and then we loop through the MiniPropTweens  which handle copying the values into their appropriate slots so that they can then be applied correctly in the main CSSPlugin setRatio() method. Remember, we typically create a proxy object that has a bunch of uniquely-named properties that we feed to the sub-plugin and it does its magic normally, and then we must interpret those values and apply them to the css because often numbers must get combined/concatenated, suffixes added, etc. to work with css, like boxShadow could have 4 values plus a color.
            _setPluginRatio = _internals._setPluginRatio = function(v) {
                this.plugin.setRatio(v);
                var d = this.data,
                    proxy = d.proxy,
                    mpt = d.firstMPT,
                    min = 0.000001,
                    val, pt, i, str;
                while (mpt) {
                    val = proxy[mpt.v];
                    if (mpt.r) {
                        val = Math.round(val);
                    } else if (val < min && val > -min) {
                        val = 0;
                    }
                    mpt.t[mpt.p] = val;
                    mpt = mpt._next;
                }
                if (d.autoRotate) {
                    d.autoRotate.rotation = proxy.rotation;
                }
                //at the end, we must set the CSSPropTween's "e" (end) value dynamically here because that's what is used in the final setRatio() method.
                if (v === 1) {
                    mpt = d.firstMPT;
                    while (mpt) {
                        pt = mpt.t;
                        if (!pt.type) {
                            pt.e = pt.s + pt.xs0;
                        } else if (pt.type === 1) {
                            str = pt.xs0 + pt.s + pt.xs1;
                            for (i = 1; i < pt.l; i++) {
                                str += pt["xn"+i] + pt["xs"+(i+1)];
                            }
                            pt.e = str;
                        }
                        mpt = mpt._next;
                    }
                }
            },

            /**
             * @private @constructor Used by a few SpecialProps to hold important values for proxies. For example, _parseToProxy() creates a MiniPropTween instance for each property that must get tweened on the proxy, and we record the original property name as well as the unique one we create for the proxy, plus whether or not the value needs to be rounded plus the original value.
             * @param {!Object} t target object whose property we're tweening (often a CSSPropTween)
             * @param {!string} p property name
             * @param {(number|string|object)} v value
             * @param {MiniPropTween=} next next MiniPropTween in the linked list
             * @param {boolean=} r if true, the tweened value should be rounded to the nearest integer
             */
            MiniPropTween = function(t, p, v, next, r) {
                this.t = t;
                this.p = p;
                this.v = v;
                this.r = r;
                if (next) {
                    next._prev = this;
                    this._next = next;
                }
            },

            /**
             * @private Most other plugins (like BezierPlugin and ThrowPropsPlugin and others) can only tween numeric values, but CSSPlugin must accommodate special values that have a bunch of extra data (like a suffix or strings between numeric values, etc.). For example, boxShadow has values like "10px 10px 20px 30px rgb(255,0,0)" which would utterly confuse other plugins. This method allows us to split that data apart and grab only the numeric data and attach it to uniquely-named properties of a generic proxy object ({}) so that we can feed that to virtually any plugin to have the numbers tweened. However, we must also keep track of which properties from the proxy go with which CSSPropTween values and instances. So we create a linked list of MiniPropTweens. Each one records a target (the original CSSPropTween), property (like "s" or "xn1" or "xn2") that we're tweening and the unique property name that was used for the proxy (like "boxShadow_xn1" and "boxShadow_xn2") and whether or not they need to be rounded. That way, in the _setPluginRatio() method we can simply copy the values over from the proxy to the CSSPropTween instance(s). Then, when the main CSSPlugin setRatio() method runs and applies the CSSPropTween values accordingly, they're updated nicely. So the external plugin tweens the numbers, _setPluginRatio() copies them over, and setRatio() acts normally, applying css-specific values to the element.
             * This method returns an object that has the following properties:
             *  - proxy: a generic object containing the starting values for all the properties that will be tweened by the external plugin.  This is what we feed to the external _onInitTween() as the target
             *  - end: a generic object containing the ending values for all the properties that will be tweened by the external plugin. This is what we feed to the external plugin's _onInitTween() as the destination values
             *  - firstMPT: the first MiniPropTween in the linked list
             *  - pt: the first CSSPropTween in the linked list that was created when parsing. If shallow is true, this linked list will NOT attach to the one passed into the _parseToProxy() as the "pt" (4th) parameter.
             * @param {!Object} t target object to be tweened
             * @param {!(Object|string)} vars the object containing the information about the tweening values (typically the end/destination values) that should be parsed
             * @param {!CSSPlugin} cssp The CSSPlugin instance
             * @param {CSSPropTween=} pt the next CSSPropTween in the linked list
             * @param {TweenPlugin=} plugin the external TweenPlugin instance that will be handling tweening the numeric values
             * @param {boolean=} shallow if true, the resulting linked list from the parse will NOT be attached to the CSSPropTween that was passed in as the "pt" (4th) parameter.
             * @return An object containing the following properties: proxy, end, firstMPT, and pt (see above for descriptions)
             */
            _parseToProxy = _internals._parseToProxy = function(t, vars, cssp, pt, plugin, shallow) {
                var bpt = pt,
                    start = {},
                    end = {},
                    transform = cssp._transform,
                    oldForce = _forcePT,
                    i, p, xp, mpt, firstPT;
                cssp._transform = null;
                _forcePT = vars;
                pt = firstPT = cssp.parse(t, vars, pt, plugin);
                _forcePT = oldForce;
                //break off from the linked list so the new ones are isolated.
                if (shallow) {
                    cssp._transform = transform;
                    if (bpt) {
                        bpt._prev = null;
                        if (bpt._prev) {
                            bpt._prev._next = null;
                        }
                    }
                }
                while (pt && pt !== bpt) {
                    if (pt.type <= 1) {
                        p = pt.p;
                        end[p] = pt.s + pt.c;
                        start[p] = pt.s;
                        if (!shallow) {
                            mpt = new MiniPropTween(pt, "s", p, mpt, pt.r);
                            pt.c = 0;
                        }
                        if (pt.type === 1) {
                            i = pt.l;
                            while (--i > 0) {
                                xp = "xn" + i;
                                p = pt.p + "_" + xp;
                                end[p] = pt.data[xp];
                                start[p] = pt[xp];
                                if (!shallow) {
                                    mpt = new MiniPropTween(pt, xp, p, mpt, pt.rxp[xp]);
                                }
                            }
                        }
                    }
                    pt = pt._next;
                }
                return {proxy:start, end:end, firstMPT:mpt, pt:firstPT};
            },



            /**
             * @constructor Each property that is tweened has at least one CSSPropTween associated with it. These instances store important information like the target, property, starting value, amount of change, etc. They can also optionally have a number of "extra" strings and numeric values named xs1, xn1, xs2, xn2, xs3, xn3, etc. where "s" indicates string and "n" indicates number. These can be pieced together in a complex-value tween (type:1) that has alternating types of data like a string, number, string, number, etc. For example, boxShadow could be "5px 5px 8px rgb(102, 102, 51)". In that value, there are 6 numbers that may need to tween and then pieced back together into a string again with spaces, suffixes, etc. xs0 is special in that it stores the suffix for standard (type:0) tweens, -OR- the first string (prefix) in a complex-value (type:1) CSSPropTween -OR- it can be the non-tweening value in a type:-1 CSSPropTween. We do this to conserve memory.
             * CSSPropTweens have the following optional properties as well (not defined through the constructor):
             *  - l: Length in terms of the number of extra properties that the CSSPropTween has (default: 0). For example, for a boxShadow we may need to tween 5 numbers in which case l would be 5; Keep in mind that the start/end values for the first number that's tweened are always stored in the s and c properties to conserve memory. All additional values thereafter are stored in xn1, xn2, etc.
             *  - xfirst: The first instance of any sub-CSSPropTweens that are tweening properties of this instance. For example, we may split up a boxShadow tween so that there's a main CSSPropTween of type:1 that has various xs* and xn* values associated with the h-shadow, v-shadow, blur, color, etc. Then we spawn a CSSPropTween for each of those that has a higher priority and runs BEFORE the main CSSPropTween so that the values are all set by the time it needs to re-assemble them. The xfirst gives us an easy way to identify the first one in that chain which typically ends at the main one (because they're all prepende to the linked list)
             *  - plugin: The TweenPlugin instance that will handle the tweening of any complex values. For example, sometimes we don't want to use normal subtweens (like xfirst refers to) to tween the values - we might want ThrowPropsPlugin or BezierPlugin some other plugin to do the actual tweening, so we create a plugin instance and store a reference here. We need this reference so that if we get a request to round values or disable a tween, we can pass along that request.
             *  - data: Arbitrary data that needs to be stored with the CSSPropTween. Typically if we're going to have a plugin handle the tweening of a complex-value tween, we create a generic object that stores the END values that we're tweening to and the CSSPropTween's xs1, xs2, etc. have the starting values. We store that object as data. That way, we can simply pass that object to the plugin and use the CSSPropTween as the target.
             *  - setRatio: Only used for type:2 tweens that require custom functionality. In this case, we call the CSSPropTween's setRatio() method and pass the ratio each time the tween updates. This isn't quite as efficient as doing things directly in the CSSPlugin's setRatio() method, but it's very convenient and flexible.
             * @param {!Object} t Target object whose property will be tweened. Often a DOM element, but not always. It could be anything.
             * @param {string} p Property to tween (name). For example, to tween element.width, p would be "width".
             * @param {number} s Starting numeric value
             * @param {number} c Change in numeric value over the course of the entire tween. For example, if element.width starts at 5 and should end at 100, c would be 95.
             * @param {CSSPropTween=} next The next CSSPropTween in the linked list. If one is defined, we will define its _prev as the new instance, and the new instance's _next will be pointed at it.
             * @param {number=} type The type of CSSPropTween where -1 = a non-tweening value, 0 = a standard simple tween, 1 = a complex value (like one that has multiple numbers in a comma- or space-delimited string like border:"1px solid red"), and 2 = one that uses a custom setRatio function that does all of the work of applying the values on each update.
             * @param {string=} n Name of the property that should be used for overwriting purposes which is typically the same as p but not always. For example, we may need to create a subtween for the 2nd part of a "clip:rect(...)" tween in which case "p" might be xs1 but "n" is still "clip"
             * @param {boolean=} r If true, the value(s) should be rounded
             * @param {number=} pr Priority in the linked list order. Higher priority CSSPropTweens will be updated before lower priority ones. The default priority is 0.
             * @param {string=} b Beginning value. We store this to ensure that it is EXACTLY what it was when the tween began without any risk of interpretation issues.
             * @param {string=} e Ending value. We store this to ensure that it is EXACTLY what the user defined at the end of the tween without any risk of interpretation issues.
             */
            CSSPropTween = _internals.CSSPropTween = function(t, p, s, c, next, type, n, r, pr, b, e) {
                this.t = t; //target
                this.p = p; //property
                this.s = s; //starting value
                this.c = c; //change value
                this.n = n || p; //name that this CSSPropTween should be associated to (usually the same as p, but not always - n is what overwriting looks at)
                if (!(t instanceof CSSPropTween)) {
                    _overwriteProps.push(this.n);
                }
                this.r = r; //round (boolean)
                this.type = type || 0; //0 = normal tween, -1 = non-tweening (in which case xs0 will be applied to the target's property, like tp.t[tp.p] = tp.xs0), 1 = complex-value SpecialProp, 2 = custom setRatio() that does all the work
                if (pr) {
                    this.pr = pr;
                    _hasPriority = true;
                }
                this.b = (b === undefined) ? s : b;
                this.e = (e === undefined) ? s + c : e;
                if (next) {
                    this._next = next;
                    next._prev = this;
                }
            },

            /**
             * Takes a target, the beginning value and ending value (as strings) and parses them into a CSSPropTween (possibly with child CSSPropTweens) that accommodates multiple numbers, colors, comma-delimited values, etc. For example:
             * sp.parseComplex(element, "boxShadow", "5px 10px 20px rgb(255,102,51)", "0px 0px 0px red", true, "0px 0px 0px rgb(0,0,0,0)", pt);
             * It will walk through the beginning and ending values (which should be in the same format with the same number and type of values) and figure out which parts are numbers, what strings separate the numeric/tweenable values, and then create the CSSPropTweens accordingly. If a plugin is defined, no child CSSPropTweens will be created. Instead, the ending values will be stored in the "data" property of the returned CSSPropTween like: {s:-5, xn1:-10, xn2:-20, xn3:255, xn4:0, xn5:0} so that it can be fed to any other plugin and it'll be plain numeric tweens but the recomposition of the complex value will be handled inside CSSPlugin's setRatio().
             * If a setRatio is defined, the type of the CSSPropTween will be set to 2 and recomposition of the values will be the responsibility of that method.
             *
             * @param {!Object} t Target whose property will be tweened
             * @param {!string} p Property that will be tweened (its name, like "left" or "backgroundColor" or "boxShadow")
             * @param {string} b Beginning value
             * @param {string} e Ending value
             * @param {boolean} clrs If true, the value could contain a color value like "rgb(255,0,0)" or "#F00" or "red". The default is false, so no colors will be recognized (a performance optimization)
             * @param {(string|number|Object)} dflt The default beginning value that should be used if no valid beginning value is defined or if the number of values inside the complex beginning and ending values don't match
             * @param {?CSSPropTween} pt CSSPropTween instance that is the current head of the linked list (we'll prepend to this).
             * @param {number=} pr Priority in the linked list order. Higher priority properties will be updated before lower priority ones. The default priority is 0.
             * @param {TweenPlugin=} plugin If a plugin should handle the tweening of extra properties, pass the plugin instance here. If one is defined, then NO subtweens will be created for any extra properties (the properties will be created - just not additional CSSPropTween instances to tween them) because the plugin is expected to do so. However, the end values WILL be populated in the "data" property, like {s:100, xn1:50, xn2:300}
             * @param {function(number)=} setRatio If values should be set in a custom function instead of being pieced together in a type:1 (complex-value) CSSPropTween, define that custom function here.
             * @return {CSSPropTween} The first CSSPropTween in the linked list which includes the new one(s) added by the parseComplex() call.
             */
            _parseComplex = CSSPlugin.parseComplex = function(t, p, b, e, clrs, dflt, pt, pr, plugin, setRatio) {
                //DEBUG: _log("parseComplex: "+p+", b: "+b+", e: "+e);
                b = b || dflt || "";
                pt = new CSSPropTween(t, p, 0, 0, pt, (setRatio ? 2 : 1), null, false, pr, b, e);
                e += ""; //ensures it's a string
                var ba = b.split(", ").join(",").split(" "), //beginning array
                    ea = e.split(", ").join(",").split(" "), //ending array
                    l = ba.length,
                    autoRound = (_autoRound !== false),
                    i, xi, ni, bv, ev, bnums, enums, bn, rgba, temp, cv, str;
                if (e.indexOf(",") !== -1 || b.indexOf(",") !== -1) {
                    ba = ba.join(" ").replace(_commasOutsideParenExp, ", ").split(" ");
                    ea = ea.join(" ").replace(_commasOutsideParenExp, ", ").split(" ");
                    l = ba.length;
                }
                if (l !== ea.length) {
                    //DEBUG: _log("mismatched formatting detected on " + p + " (" + b + " vs " + e + ")");
                    ba = (dflt || "").split(" ");
                    l = ba.length;
                }
                pt.plugin = plugin;
                pt.setRatio = setRatio;
                for (i = 0; i < l; i++) {
                    bv = ba[i];
                    ev = ea[i];
                    bn = parseFloat(bv);

                    //if the value begins with a number (most common). It's fine if it has a suffix like px
                    if (bn || bn === 0) {
                        pt.appendXtra("", bn, _parseChange(ev, bn), ev.replace(_relNumExp, ""), (autoRound && ev.indexOf("px") !== -1), true);

                    //if the value is a color
                    } else if (clrs && (bv.charAt(0) === "#" || _colorLookup[bv] || _rgbhslExp.test(bv))) {
                        str = ev.charAt(ev.length - 1) === "," ? ")," : ")"; //if there's a comma at the end, retain it.
                        bv = _parseColor(bv);
                        ev = _parseColor(ev);
                        rgba = (bv.length + ev.length > 6);
                        if (rgba && !_supportsOpacity && ev[3] === 0) { //older versions of IE don't support rgba(), so if the destination alpha is 0, just use "transparent" for the end color
                            pt["xs" + pt.l] += pt.l ? " transparent" : "transparent";
                            pt.e = pt.e.split(ea[i]).join("transparent");
                        } else {
                            if (!_supportsOpacity) { //old versions of IE don't support rgba().
                                rgba = false;
                            }
                            pt.appendXtra((rgba ? "rgba(" : "rgb("), bv[0], ev[0] - bv[0], ",", true, true)
                                .appendXtra("", bv[1], ev[1] - bv[1], ",", true)
                                .appendXtra("", bv[2], ev[2] - bv[2], (rgba ? "," : str), true);
                            if (rgba) {
                                bv = (bv.length < 4) ? 1 : bv[3];
                                pt.appendXtra("", bv, ((ev.length < 4) ? 1 : ev[3]) - bv, str, false);
                            }
                        }

                    } else {
                        bnums = bv.match(_numExp); //gets each group of numbers in the beginning value string and drops them into an array

                        //if no number is found, treat it as a non-tweening value and just append the string to the current xs.
                        if (!bnums) {
                            pt["xs" + pt.l] += pt.l ? " " + bv : bv;

                        //loop through all the numbers that are found and construct the extra values on the pt.
                        } else {
                            enums = ev.match(_relNumExp); //get each group of numbers in the end value string and drop them into an array. We allow relative values too, like +=50 or -=.5
                            if (!enums || enums.length !== bnums.length) {
                                //DEBUG: _log("mismatched formatting detected on " + p + " (" + b + " vs " + e + ")");
                                return pt;
                            }
                            ni = 0;
                            for (xi = 0; xi < bnums.length; xi++) {
                                cv = bnums[xi];
                                temp = bv.indexOf(cv, ni);
                                pt.appendXtra(bv.substr(ni, temp - ni), Number(cv), _parseChange(enums[xi], cv), "", (autoRound && bv.substr(temp + cv.length, 2) === "px"), (xi === 0));
                                ni = temp + cv.length;
                            }
                            pt["xs" + pt.l] += bv.substr(ni);
                        }
                    }
                }
                //if there are relative values ("+=" or "-=" prefix), we need to adjust the ending value to eliminate the prefixes and combine the values properly.
                if (e.indexOf("=") !== -1) if (pt.data) {
                    str = pt.xs0 + pt.data.s;
                    for (i = 1; i < pt.l; i++) {
                        str += pt["xs" + i] + pt.data["xn" + i];
                    }
                    pt.e = str + pt["xs" + i];
                }
                if (!pt.l) {
                    pt.type = -1;
                    pt.xs0 = pt.e;
                }
                return pt.xfirst || pt;
            },
            i = 9;


        p = CSSPropTween.prototype;
        p.l = p.pr = 0; //length (number of extra properties like xn1, xn2, xn3, etc.
        while (--i > 0) {
            p["xn" + i] = 0;
            p["xs" + i] = "";
        }
        p.xs0 = "";
        p._next = p._prev = p.xfirst = p.data = p.plugin = p.setRatio = p.rxp = null;


        /**
         * Appends and extra tweening value to a CSSPropTween and automatically manages any prefix and suffix strings. The first extra value is stored in the s and c of the main CSSPropTween instance, but thereafter any extras are stored in the xn1, xn2, xn3, etc. The prefixes and suffixes are stored in the xs0, xs1, xs2, etc. properties. For example, if I walk through a clip value like "rect(10px, 5px, 0px, 20px)", the values would be stored like this:
         * xs0:"rect(", s:10, xs1:"px, ", xn1:5, xs2:"px, ", xn2:0, xs3:"px, ", xn3:20, xn4:"px)"
         * And they'd all get joined together when the CSSPlugin renders (in the setRatio() method).
         * @param {string=} pfx Prefix (if any)
         * @param {!number} s Starting value
         * @param {!number} c Change in numeric value over the course of the entire tween. For example, if the start is 5 and the end is 100, the change would be 95.
         * @param {string=} sfx Suffix (if any)
         * @param {boolean=} r Round (if true).
         * @param {boolean=} pad If true, this extra value should be separated by the previous one by a space. If there is no previous extra and pad is true, it will automatically drop the space.
         * @return {CSSPropTween} returns itself so that multiple methods can be chained together.
         */
        p.appendXtra = function(pfx, s, c, sfx, r, pad) {
            var pt = this,
                l = pt.l;
            pt["xs" + l] += (pad && l) ? " " + pfx : pfx || "";
            if (!c) if (l !== 0 && !pt.plugin) { //typically we'll combine non-changing values right into the xs to optimize performance, but we don't combine them when there's a plugin that will be tweening the values because it may depend on the values being split apart, like for a bezier, if a value doesn't change between the first and second iteration but then it does on the 3rd, we'll run into trouble because there's no xn slot for that value!
                pt["xs" + l] += s + (sfx || "");
                return pt;
            }
            pt.l++;
            pt.type = pt.setRatio ? 2 : 1;
            pt["xs" + pt.l] = sfx || "";
            if (l > 0) {
                pt.data["xn" + l] = s + c;
                pt.rxp["xn" + l] = r; //round extra property (we need to tap into this in the _parseToProxy() method)
                pt["xn" + l] = s;
                if (!pt.plugin) {
                    pt.xfirst = new CSSPropTween(pt, "xn" + l, s, c, pt.xfirst || pt, 0, pt.n, r, pt.pr);
                    pt.xfirst.xs0 = 0; //just to ensure that the property stays numeric which helps modern browsers speed up processing. Remember, in the setRatio() method, we do pt.t[pt.p] = val + pt.xs0 so if pt.xs0 is "" (the default), it'll cast the end value as a string. When a property is a number sometimes and a string sometimes, it prevents the compiler from locking in the data type, slowing things down slightly.
                }
                return pt;
            }
            pt.data = {s:s + c};
            pt.rxp = {};
            pt.s = s;
            pt.c = c;
            pt.r = r;
            return pt;
        };

        /**
         * @constructor A SpecialProp is basically a css property that needs to be treated in a non-standard way, like if it may contain a complex value like boxShadow:"5px 10px 15px rgb(255, 102, 51)" or if it is associated with another plugin like ThrowPropsPlugin or BezierPlugin. Every SpecialProp is associated with a particular property name like "boxShadow" or "throwProps" or "bezier" and it will intercept those values in the vars object that's passed to the CSSPlugin and handle them accordingly.
         * @param {!string} p Property name (like "boxShadow" or "throwProps")
         * @param {Object=} options An object containing any of the following configuration options:
         *                      - defaultValue: the default value
         *                      - parser: A function that should be called when the associated property name is found in the vars. This function should return a CSSPropTween instance and it should ensure that it is properly inserted into the linked list. It will receive 4 paramters: 1) The target, 2) The value defined in the vars, 3) The CSSPlugin instance (whose _firstPT should be used for the linked list), and 4) A computed style object if one was calculated (this is a speed optimization that allows retrieval of starting values quicker)
         *                      - formatter: a function that formats any value received for this special property (for example, boxShadow could take "5px 5px red" and format it to "5px 5px 0px 0px red" so that both the beginning and ending values have a common order and quantity of values.)
         *                      - prefix: if true, we'll determine whether or not this property requires a vendor prefix (like Webkit or Moz or ms or O)
         *                      - color: set this to true if the value for this SpecialProp may contain color-related values like rgb(), rgba(), etc.
         *                      - priority: priority in the linked list order. Higher priority SpecialProps will be updated before lower priority ones. The default priority is 0.
         *                      - multi: if true, the formatter should accommodate a comma-delimited list of values, like boxShadow could have multiple boxShadows listed out.
         *                      - collapsible: if true, the formatter should treat the value like it's a top/right/bottom/left value that could be collapsed, like "5px" would apply to all, "5px, 10px" would use 5px for top/bottom and 10px for right/left, etc.
         *                      - keyword: a special keyword that can [optionally] be found inside the value (like "inset" for boxShadow). This allows us to validate beginning/ending values to make sure they match (if the keyword is found in one, it'll be added to the other for consistency by default).
         */
        var SpecialProp = function(p, options) {
                options = options || {};
                this.p = options.prefix ? _checkPropPrefix(p) || p : p;
                _specialProps[p] = _specialProps[this.p] = this;
                this.format = options.formatter || _getFormatter(options.defaultValue, options.color, options.collapsible, options.multi);
                if (options.parser) {
                    this.parse = options.parser;
                }
                this.clrs = options.color;
                this.multi = options.multi;
                this.keyword = options.keyword;
                this.dflt = options.defaultValue;
                this.pr = options.priority || 0;
            },

            //shortcut for creating a new SpecialProp that can accept multiple properties as a comma-delimited list (helps minification). dflt can be an array for multiple values (we don't do a comma-delimited list because the default value may contain commas, like rect(0px,0px,0px,0px)). We attach this method to the SpecialProp class/object instead of using a private _createSpecialProp() method so that we can tap into it externally if necessary, like from another plugin.
            _registerComplexSpecialProp = _internals._registerComplexSpecialProp = function(p, options, defaults) {
                if (typeof(options) !== "object") {
                    options = {parser:defaults}; //to make backwards compatible with older versions of BezierPlugin and ThrowPropsPlugin
                }
                var a = p.split(","),
                    d = options.defaultValue,
                    i, temp;
                defaults = defaults || [d];
                for (i = 0; i < a.length; i++) {
                    options.prefix = (i === 0 && options.prefix);
                    options.defaultValue = defaults[i] || d;
                    temp = new SpecialProp(a[i], options);
                }
            },

            //creates a placeholder special prop for a plugin so that the property gets caught the first time a tween of it is attempted, and at that time it makes the plugin register itself, thus taking over for all future tweens of that property. This allows us to not mandate that things load in a particular order and it also allows us to log() an error that informs the user when they attempt to tween an external plugin-related property without loading its .js file.
            _registerPluginProp = function(p) {
                if (!_specialProps[p]) {
                    var pluginName = p.charAt(0).toUpperCase() + p.substr(1) + "Plugin";
                    _registerComplexSpecialProp(p, {parser:function(t, e, p, cssp, pt, plugin, vars) {
                        var pluginClass = (window.GreenSockGlobals || window).com.greensock.plugins[pluginName];
                        if (!pluginClass) {
                            _log("Error: " + pluginName + " js file not loaded.");
                            return pt;
                        }
                        pluginClass._cssRegister();
                        return _specialProps[p].parse(t, e, p, cssp, pt, plugin, vars);
                    }});
                }
            };


        p = SpecialProp.prototype;

        /**
         * Alias for _parseComplex() that automatically plugs in certain values for this SpecialProp, like its property name, whether or not colors should be sensed, the default value, and priority. It also looks for any keyword that the SpecialProp defines (like "inset" for boxShadow) and ensures that the beginning and ending values have the same number of values for SpecialProps where multi is true (like boxShadow and textShadow can have a comma-delimited list)
         * @param {!Object} t target element
         * @param {(string|number|object)} b beginning value
         * @param {(string|number|object)} e ending (destination) value
         * @param {CSSPropTween=} pt next CSSPropTween in the linked list
         * @param {TweenPlugin=} plugin If another plugin will be tweening the complex value, that TweenPlugin instance goes here.
         * @param {function=} setRatio If a custom setRatio() method should be used to handle this complex value, that goes here.
         * @return {CSSPropTween=} First CSSPropTween in the linked list
         */
        p.parseComplex = function(t, b, e, pt, plugin, setRatio) {
            var kwd = this.keyword,
                i, ba, ea, l, bi, ei;
            //if this SpecialProp's value can contain a comma-delimited list of values (like boxShadow or textShadow), we must parse them in a special way, and look for a keyword (like "inset" for boxShadow) and ensure that the beginning and ending BOTH have it if the end defines it as such. We also must ensure that there are an equal number of values specified (we can't tween 1 boxShadow to 3 for example)
            if (this.multi) if (_commasOutsideParenExp.test(e) || _commasOutsideParenExp.test(b)) {
                ba = b.replace(_commasOutsideParenExp, "|").split("|");
                ea = e.replace(_commasOutsideParenExp, "|").split("|");
            } else if (kwd) {
                ba = [b];
                ea = [e];
            }
            if (ea) {
                l = (ea.length > ba.length) ? ea.length : ba.length;
                for (i = 0; i < l; i++) {
                    b = ba[i] = ba[i] || this.dflt;
                    e = ea[i] = ea[i] || this.dflt;
                    if (kwd) {
                        bi = b.indexOf(kwd);
                        ei = e.indexOf(kwd);
                        if (bi !== ei) {
                            e = (ei === -1) ? ea : ba;
                            e[i] += " " + kwd;
                        }
                    }
                }
                b = ba.join(", ");
                e = ea.join(", ");
            }
            return _parseComplex(t, this.p, b, e, this.clrs, this.dflt, pt, this.pr, plugin, setRatio);
        };

        /**
         * Accepts a target and end value and spits back a CSSPropTween that has been inserted into the CSSPlugin's linked list and conforms with all the conventions we use internally, like type:-1, 0, 1, or 2, setting up any extra property tweens, priority, etc. For example, if we have a boxShadow SpecialProp and call:
         * this._firstPT = sp.parse(element, "5px 10px 20px rgb(2550,102,51)", "boxShadow", this);
         * It should figure out the starting value of the element's boxShadow, compare it to the provided end value and create all the necessary CSSPropTweens of the appropriate types to tween the boxShadow. The CSSPropTween that gets spit back should already be inserted into the linked list (the 4th parameter is the current head, so prepend to that).
         * @param {!Object} t Target object whose property is being tweened
         * @param {Object} e End value as provided in the vars object (typically a string, but not always - like a throwProps would be an object).
         * @param {!string} p Property name
         * @param {!CSSPlugin} cssp The CSSPlugin instance that should be associated with this tween.
         * @param {?CSSPropTween} pt The CSSPropTween that is the current head of the linked list (we'll prepend to it)
         * @param {TweenPlugin=} plugin If a plugin will be used to tween the parsed value, this is the plugin instance.
         * @param {Object=} vars Original vars object that contains the data for parsing.
         * @return {CSSPropTween} The first CSSPropTween in the linked list which includes the new one(s) added by the parse() call.
         */
        p.parse = function(t, e, p, cssp, pt, plugin, vars) {
            return this.parseComplex(t.style, this.format(_getStyle(t, this.p, _cs, false, this.dflt)), this.format(e), pt, plugin);
        };

        /**
         * Registers a special property that should be intercepted from any "css" objects defined in tweens. This allows you to handle them however you want without CSSPlugin doing it for you. The 2nd parameter should be a function that accepts 3 parameters:
         *  1) Target object whose property should be tweened (typically a DOM element)
         *  2) The end/destination value (could be a string, number, object, or whatever you want)
         *  3) The tween instance (you probably don't need to worry about this, but it can be useful for looking up information like the duration)
         *
         * Then, your function should return a function which will be called each time the tween gets rendered, passing a numeric "ratio" parameter to your function that indicates the change factor (usually between 0 and 1). For example:
         *
         * CSSPlugin.registerSpecialProp("myCustomProp", function(target, value, tween) {
         *      var start = target.style.width;
         *      return function(ratio) {
         *              target.style.width = (start + value * ratio) + "px";
         *              console.log("set width to " + target.style.width);
         *          }
         * }, 0);
         *
         * Then, when I do this tween, it will trigger my special property:
         *
         * TweenLite.to(element, 1, {css:{myCustomProp:100}});
         *
         * In the example, of course, we're just changing the width, but you can do anything you want.
         *
         * @param {!string} name Property name (or comma-delimited list of property names) that should be intercepted and handled by your function. For example, if I define "myCustomProp", then it would handle that portion of the following tween: TweenLite.to(element, 1, {css:{myCustomProp:100}})
         * @param {!function(Object, Object, Object, string):function(number)} onInitTween The function that will be called when a tween of this special property is performed. The function will receive 4 parameters: 1) Target object that should be tweened, 2) Value that was passed to the tween, 3) The tween instance itself (rarely used), and 4) The property name that's being tweened. Your function should return a function that should be called on every update of the tween. That function will receive a single parameter that is a "change factor" value (typically between 0 and 1) indicating the amount of change as a ratio. You can use this to determine how to set the values appropriately in your function.
         * @param {number=} priority Priority that helps the engine determine the order in which to set the properties (default: 0). Higher priority properties will be updated before lower priority ones.
         */
        CSSPlugin.registerSpecialProp = function(name, onInitTween, priority) {
            _registerComplexSpecialProp(name, {parser:function(t, e, p, cssp, pt, plugin, vars) {
                var rv = new CSSPropTween(t, p, 0, 0, pt, 2, p, false, priority);
                rv.plugin = plugin;
                rv.setRatio = onInitTween(t, e, cssp._tween, p);
                return rv;
            }, priority:priority});
        };








        //transform-related methods and properties
        var _transformProps = ("scaleX,scaleY,scaleZ,x,y,z,skewX,skewY,rotation,rotationX,rotationY,perspective").split(","),
            _transformProp = _checkPropPrefix("transform"), //the Javascript (camelCase) transform property, like msTransform, WebkitTransform, MozTransform, or OTransform.
            _transformPropCSS = _prefixCSS + "transform",
            _transformOriginProp = _checkPropPrefix("transformOrigin"),
            _supports3D = (_checkPropPrefix("perspective") !== null),
            Transform = _internals.Transform = function() {
                this.skewY = 0;
            },

            /**
             * Parses the transform values for an element, returning an object with x, y, z, scaleX, scaleY, scaleZ, rotation, rotationX, rotationY, skewX, and skewY properties. Note: by default (for performance reasons), all skewing is combined into skewX and rotation but skewY still has a place in the transform object so that we can record how much of the skew is attributed to skewX vs skewY. Remember, a skewY of 10 looks the same as a rotation of 10 and skewX of -10.
             * @param {!Object} t target element
             * @param {Object=} cs computed style object (optional)
             * @param {boolean=} rec if true, the transform values will be recorded to the target element's _gsTransform object, like target._gsTransform = {x:0, y:0, z:0, scaleX:1...}
             * @param {boolean=} parse if true, we'll ignore any _gsTransform values that already exist on the element, and force a reparsing of the css (calculated style)
             * @return {object} object containing all of the transform properties/values like {x:0, y:0, z:0, scaleX:1...}
             */
            _getTransform = _internals.getTransform = function(t, cs, rec, parse) {
                if (t._gsTransform && rec && !parse) {
                    return t._gsTransform; //if the element already has a _gsTransform, use that. Note: some browsers don't accurately return the calculated style for the transform (particularly for SVG), so it's almost always safest to just use the values we've already applied rather than re-parsing things.
                }
                var tm = rec ? t._gsTransform || new Transform() : new Transform(),
                    invX = (tm.scaleX < 0), //in order to interpret things properly, we need to know if the user applied a negative scaleX previously so that we can adjust the rotation and skewX accordingly. Otherwise, if we always interpret a flipped matrix as affecting scaleY and the user only wants to tween the scaleX on multiple sequential tweens, it would keep the negative scaleY without that being the user's intent.
                    min = 0.00002,
                    rnd = 100000,
                    minAngle = 179.99,
                    minPI = minAngle * _DEG2RAD,
                    zOrigin = _supports3D ? parseFloat(_getStyle(t, _transformOriginProp, cs, false, "0 0 0").split(" ")[2]) || tm.zOrigin  || 0 : 0,
                    s, m, i, n, dec, scaleX, scaleY, rotation, skewX, difX, difY, difR, difS;
                if (_transformProp) {
                    s = _getStyle(t, _transformPropCSS, cs, true);
                } else if (t.currentStyle) {
                    //for older versions of IE, we need to interpret the filter portion that is in the format: progid:DXImageTransform.Microsoft.Matrix(M11=6.123233995736766e-17, M12=-1, M21=1, M22=6.123233995736766e-17, sizingMethod='auto expand') Notice that we need to swap b and c compared to a normal matrix.
                    s = t.currentStyle.filter.match(_ieGetMatrixExp);
                    s = (s && s.length === 4) ? [s[0].substr(4), Number(s[2].substr(4)), Number(s[1].substr(4)), s[3].substr(4), (tm.x || 0), (tm.y || 0)].join(",") : "";
                }
                //split the matrix values out into an array (m for matrix)
                m = (s || "").match(/(?:\-|\b)[\d\-\.e]+\b/gi) || [];
                i = m.length;
                while (--i > -1) {
                    n = Number(m[i]);
                    m[i] = (dec = n - (n |= 0)) ? ((dec * rnd + (dec < 0 ? -0.5 : 0.5)) | 0) / rnd + n : n; //convert strings to Numbers and round to 5 decimal places to avoid issues with tiny numbers. Roughly 20x faster than Number.toFixed(). We also must make sure to round before dividing so that values like 0.9999999999 become 1 to avoid glitches in browser rendering and interpretation of flipped/rotated 3D matrices. And don't just multiply the number by rnd, floor it, and then divide by rnd because the bitwise operations max out at a 32-bit signed integer, thus it could get clipped at a relatively low value (like 22,000.00000 for example).
                }
                if (m.length === 16) {

                    //we'll only look at these position-related 6 variables first because if x/y/z all match, it's relatively safe to assume we don't need to re-parse everything which risks losing important rotational information (like rotationX:180 plus rotationY:180 would look the same as rotation:180 - there's no way to know for sure which direction was taken based solely on the matrix3d() values)
                    var a13 = m[8], a23 = m[9], a33 = m[10],
                        a14 = m[12], a24 = m[13], a34 = m[14];

                    //we manually compensate for non-zero z component of transformOrigin to work around bugs in Safari
                    if (tm.zOrigin) {
                        a34 = -tm.zOrigin;
                        a14 = a13*a34-m[12];
                        a24 = a23*a34-m[13];
                        a34 = a33*a34+tm.zOrigin-m[14];
                    }

                    //only parse from the matrix if we MUST because not only is it usually unnecessary due to the fact that we store the values in the _gsTransform object, but also because it's impossible to accurately interpret rotationX, rotationY, rotationZ, scaleX, and scaleY if all are applied, so it's much better to rely on what we store. However, we must parse the first time that an object is tweened. We also assume that if the position has changed, the user must have done some styling changes outside of CSSPlugin, thus we force a parse in that scenario.
                    if (!rec || parse || tm.rotationX == null) {
                        var a11 = m[0], a21 = m[1], a31 = m[2], a41 = m[3],
                            a12 = m[4], a22 = m[5], a32 = m[6], a42 = m[7],
                            a43 = m[11],
                            angle = Math.atan2(a32, a33),
                            xFlip = (angle < -minPI || angle > minPI),
                            t1, t2, t3, cos, sin, yFlip, zFlip;
                        tm.rotationX = angle * _RAD2DEG;
                        //rotationX
                        if (angle) {
                            cos = Math.cos(-angle);
                            sin = Math.sin(-angle);
                            t1 = a12*cos+a13*sin;
                            t2 = a22*cos+a23*sin;
                            t3 = a32*cos+a33*sin;
                            a13 = a12*-sin+a13*cos;
                            a23 = a22*-sin+a23*cos;
                            a33 = a32*-sin+a33*cos;
                            a43 = a42*-sin+a43*cos;
                            a12 = t1;
                            a22 = t2;
                            a32 = t3;
                        }
                        //rotationY
                        angle = Math.atan2(a13, a11);
                        tm.rotationY = angle * _RAD2DEG;
                        if (angle) {
                            yFlip = (angle < -minPI || angle > minPI);
                            cos = Math.cos(-angle);
                            sin = Math.sin(-angle);
                            t1 = a11*cos-a13*sin;
                            t2 = a21*cos-a23*sin;
                            t3 = a31*cos-a33*sin;
                            a23 = a21*sin+a23*cos;
                            a33 = a31*sin+a33*cos;
                            a43 = a41*sin+a43*cos;
                            a11 = t1;
                            a21 = t2;
                            a31 = t3;
                        }
                        //rotationZ
                        angle = Math.atan2(a21, a22);
                        tm.rotation = angle * _RAD2DEG;
                        if (angle) {
                            zFlip = (angle < -minPI || angle > minPI);
                            cos = Math.cos(-angle);
                            sin = Math.sin(-angle);
                            a11 = a11*cos+a12*sin;
                            t2 = a21*cos+a22*sin;
                            a22 = a21*-sin+a22*cos;
                            a32 = a31*-sin+a32*cos;
                            a21 = t2;
                        }

                        if (zFlip && xFlip) {
                            tm.rotation = tm.rotationX = 0;
                        } else if (zFlip && yFlip) {
                            tm.rotation = tm.rotationY = 0;
                        } else if (yFlip && xFlip) {
                            tm.rotationY = tm.rotationX = 0;
                        }

                        tm.scaleX = ((Math.sqrt(a11 * a11 + a21 * a21) * rnd + 0.5) | 0) / rnd;
                        tm.scaleY = ((Math.sqrt(a22 * a22 + a23 * a23) * rnd + 0.5) | 0) / rnd;
                        tm.scaleZ = ((Math.sqrt(a32 * a32 + a33 * a33) * rnd + 0.5) | 0) / rnd;
                        tm.skewX = 0;
                        tm.perspective = a43 ? 1 / ((a43 < 0) ? -a43 : a43) : 0;
                        tm.x = a14;
                        tm.y = a24;
                        tm.z = a34;
                    }

                } else if ((!_supports3D || parse || !m.length || tm.x !== m[4] || tm.y !== m[5] || (!tm.rotationX && !tm.rotationY)) && !(tm.x !== undefined && _getStyle(t, "display", cs) === "none")) { //sometimes a 6-element matrix is returned even when we performed 3D transforms, like if rotationX and rotationY are 180. In cases like this, we still need to honor the 3D transforms. If we just rely on the 2D info, it could affect how the data is interpreted, like scaleY might get set to -1 or rotation could get offset by 180 degrees. For example, do a TweenLite.to(element, 1, {css:{rotationX:180, rotationY:180}}) and then later, TweenLite.to(element, 1, {css:{rotationX:0}}) and without this conditional logic in place, it'd jump to a state of being unrotated when the 2nd tween starts. Then again, we need to honor the fact that the user COULD alter the transforms outside of CSSPlugin, like by manually applying new css, so we try to sense that by looking at x and y because if those changed, we know the changes were made outside CSSPlugin and we force a reinterpretation of the matrix values. Also, in Webkit browsers, if the element's "display" is "none", its calculated style value will always return empty, so if we've already recorded the values in the _gsTransform object, we'll just rely on those.
                    var k = (m.length >= 6),
                        a = k ? m[0] : 1,
                        b = m[1] || 0,
                        c = m[2] || 0,
                        d = k ? m[3] : 1;
                    tm.x = m[4] || 0;
                    tm.y = m[5] || 0;
                    scaleX = Math.sqrt(a * a + b * b);
                    scaleY = Math.sqrt(d * d + c * c);
                    rotation = (a || b) ? Math.atan2(b, a) * _RAD2DEG : tm.rotation || 0; //note: if scaleX is 0, we cannot accurately measure rotation. Same for skewX with a scaleY of 0. Therefore, we default to the previously recorded value (or zero if that doesn't exist).
                    skewX = (c || d) ? Math.atan2(c, d) * _RAD2DEG + rotation : tm.skewX || 0;
                    difX = scaleX - Math.abs(tm.scaleX || 0);
                    difY = scaleY - Math.abs(tm.scaleY || 0);
                    if (Math.abs(skewX) > 90 && Math.abs(skewX) < 270) {
                        if (invX) {
                            scaleX *= -1;
                            skewX += (rotation <= 0) ? 180 : -180;
                            rotation += (rotation <= 0) ? 180 : -180;
                        } else {
                            scaleY *= -1;
                            skewX += (skewX <= 0) ? 180 : -180;
                        }
                    }
                    difR = (rotation - tm.rotation) % 180; //note: matching ranges would be very small (+/-0.0001) or very close to 180.
                    difS = (skewX - tm.skewX) % 180;
                    //if there's already a recorded _gsTransform in place for the target, we should leave those values in place unless we know things changed for sure (beyond a super small amount). This gets around ambiguous interpretations, like if scaleX and scaleY are both -1, the matrix would be the same as if the rotation was 180 with normal scaleX/scaleY. If the user tweened to particular values, those must be prioritized to ensure animation is consistent.
                    if (tm.skewX === undefined || difX > min || difX < -min || difY > min || difY < -min || (difR > -minAngle && difR < minAngle && (difR * rnd) | 0 !== 0) || (difS > -minAngle && difS < minAngle && (difS * rnd) | 0 !== 0)) {
                        tm.scaleX = scaleX;
                        tm.scaleY = scaleY;
                        tm.rotation = rotation;
                        tm.skewX = skewX;
                    }
                    if (_supports3D) {
                        tm.rotationX = tm.rotationY = tm.z = 0;
                        tm.perspective = parseFloat(CSSPlugin.defaultTransformPerspective) || 0;
                        tm.scaleZ = 1;
                    }
                }
                tm.zOrigin = zOrigin;

                //some browsers have a hard time with very small values like 2.4492935982947064e-16 (notice the "e-" towards the end) and would render the object slightly off. So we round to 0 in these cases. The conditional logic here is faster than calling Math.abs(). Also, browsers tend to render a SLIGHTLY rotated object in a fuzzy way, so we need to snap to exactly 0 when appropriate.
                for (i in tm) {
                    if (tm[i] < min) if (tm[i] > -min) {
                        tm[i] = 0;
                    }
                }
                //DEBUG: _log("parsed rotation: "+(tm.rotationX)+", "+(tm.rotationY)+", "+(tm.rotation)+", scale: "+tm.scaleX+", "+tm.scaleY+", "+tm.scaleZ+", position: "+tm.x+", "+tm.y+", "+tm.z+", perspective: "+tm.perspective);
                if (rec) {
                    t._gsTransform = tm; //record to the object's _gsTransform which we use so that tweens can control individual properties independently (we need all the properties to accurately recompose the matrix in the setRatio() method)
                }
                return tm;
            },

            //for setting 2D transforms in IE6, IE7, and IE8 (must use a "filter" to emulate the behavior of modern day browser transforms)
            _setIETransformRatio = function(v) {
                var t = this.data, //refers to the element's _gsTransform object
                    ang = -t.rotation * _DEG2RAD,
                    skew = ang + t.skewX * _DEG2RAD,
                    rnd = 100000,
                    a = ((Math.cos(ang) * t.scaleX * rnd) | 0) / rnd,
                    b = ((Math.sin(ang) * t.scaleX * rnd) | 0) / rnd,
                    c = ((Math.sin(skew) * -t.scaleY * rnd) | 0) / rnd,
                    d = ((Math.cos(skew) * t.scaleY * rnd) | 0) / rnd,
                    style = this.t.style,
                    cs = this.t.currentStyle,
                    filters, val;
                if (!cs) {
                    return;
                }
                val = b; //just for swapping the variables an inverting them (reused "val" to avoid creating another variable in memory). IE's filter matrix uses a non-standard matrix configuration (angle goes the opposite way, and b and c are reversed and inverted)
                b = -c;
                c = -val;
                filters = cs.filter;
                style.filter = ""; //remove filters so that we can accurately measure offsetWidth/offsetHeight
                var w = this.t.offsetWidth,
                    h = this.t.offsetHeight,
                    clip = (cs.position !== "absolute"),
                    m = "progid:DXImageTransform.Microsoft.Matrix(M11=" + a + ", M12=" + b + ", M21=" + c + ", M22=" + d,
                    ox = t.x,
                    oy = t.y,
                    dx, dy;

                //if transformOrigin is being used, adjust the offset x and y
                if (t.ox != null) {
                    dx = ((t.oxp) ? w * t.ox * 0.01 : t.ox) - w / 2;
                    dy = ((t.oyp) ? h * t.oy * 0.01 : t.oy) - h / 2;
                    ox += dx - (dx * a + dy * b);
                    oy += dy - (dx * c + dy * d);
                }

                if (!clip) {
                    m += ", sizingMethod='auto expand')";
                } else {
                    dx = (w / 2);
                    dy = (h / 2);
                    //translate to ensure that transformations occur around the correct origin (default is center).
                    m += ", Dx=" + (dx - (dx * a + dy * b) + ox) + ", Dy=" + (dy - (dx * c + dy * d) + oy) + ")";
                }
                if (filters.indexOf("DXImageTransform.Microsoft.Matrix(") !== -1) {
                    style.filter = filters.replace(_ieSetMatrixExp, m);
                } else {
                    style.filter = m + " " + filters; //we must always put the transform/matrix FIRST (before alpha(opacity=xx)) to avoid an IE bug that slices part of the object when rotation is applied with alpha.
                }

                //at the end or beginning of the tween, if the matrix is normal (1, 0, 0, 1) and opacity is 100 (or doesn't exist), remove the filter to improve browser performance.
                if (v === 0 || v === 1) if (a === 1) if (b === 0) if (c === 0) if (d === 1) if (!clip || m.indexOf("Dx=0, Dy=0") !== -1) if (!_opacityExp.test(filters) || parseFloat(RegExp.$1) === 100) if (filters.indexOf("gradient(" && filters.indexOf("Alpha")) === -1) {
                    style.removeAttribute("filter");
                }

                //we must set the margins AFTER applying the filter in order to avoid some bugs in IE8 that could (in rare scenarios) cause them to be ignored intermittently (vibration).
                if (!clip) {
                    var mult = (_ieVers < 8) ? 1 : -1, //in Internet Explorer 7 and before, the box model is broken, causing the browser to treat the width/height of the actual rotated filtered image as the width/height of the box itself, but Microsoft corrected that in IE8. We must use a negative offset in IE8 on the right/bottom
                        marg, prop, dif;
                    dx = t.ieOffsetX || 0;
                    dy = t.ieOffsetY || 0;
                    t.ieOffsetX = Math.round((w - ((a < 0 ? -a : a) * w + (b < 0 ? -b : b) * h)) / 2 + ox);
                    t.ieOffsetY = Math.round((h - ((d < 0 ? -d : d) * h + (c < 0 ? -c : c) * w)) / 2 + oy);
                    for (i = 0; i < 4; i++) {
                        prop = _margins[i];
                        marg = cs[prop];
                        //we need to get the current margin in case it is being tweened separately (we want to respect that tween's changes)
                        val = (marg.indexOf("px") !== -1) ? parseFloat(marg) : _convertToPixels(this.t, prop, parseFloat(marg), marg.replace(_suffixExp, "")) || 0;
                        if (val !== t[prop]) {
                            dif = (i < 2) ? -t.ieOffsetX : -t.ieOffsetY; //if another tween is controlling a margin, we cannot only apply the difference in the ieOffsets, so we essentially zero-out the dx and dy here in that case. We record the margin(s) later so that we can keep comparing them, making this code very flexible.
                        } else {
                            dif = (i < 2) ? dx - t.ieOffsetX : dy - t.ieOffsetY;
                        }
                        style[prop] = (t[prop] = Math.round( val - dif * ((i === 0 || i === 2) ? 1 : mult) )) + "px";
                    }
                }
            },

            _set3DTransformRatio = _internals.set3DTransformRatio = function(v) {
                var t = this.data, //refers to the element's _gsTransform object
                    style = this.t.style,
                    angle = t.rotation * _DEG2RAD,
                    sx = t.scaleX,
                    sy = t.scaleY,
                    sz = t.scaleZ,
                    perspective = t.perspective,
                    a11, a12, a13, a14, a21, a22, a23, a24, a31, a32, a33, a34, a41, a42, a43,
                    zOrigin, rnd, cos, sin, t1, t2, t3, t4;
                if (v === 1 || v === 0) if (t.force3D === "auto") if (!t.rotationY && !t.rotationX && sz === 1 && !perspective && !t.z) { //on the final render (which could be 0 for a from tween), if there are no 3D aspects, render in 2D to free up memory and improve performance especially on mobile devices
                    _set2DTransformRatio.call(this, v);
                    return;
                }
                if (_isFirefox) {
                    var n = 0.0001;
                    if (sx < n && sx > -n) { //Firefox has a bug (at least in v25) that causes it to render the transparent part of 32-bit PNG images as black when displayed inside an iframe and the 3D scale is very small and doesn't change sufficiently enough between renders (like if you use a Power4.easeInOut to scale from 0 to 1 where the beginning values only change a tiny amount to begin the tween before accelerating). In this case, we force the scale to be 0.00002 instead which is visually the same but works around the Firefox issue.
                        sx = sz = 0.00002;
                    }
                    if (sy < n && sy > -n) {
                        sy = sz = 0.00002;
                    }
                    if (perspective && !t.z && !t.rotationX && !t.rotationY) { //Firefox has a bug that causes elements to have an odd super-thin, broken/dotted black border on elements that have a perspective set but aren't utilizing 3D space (no rotationX, rotationY, or z).
                        perspective = 0;
                    }
                }
                if (angle || t.skewX) {
                    cos = Math.cos(angle);
                    sin = Math.sin(angle);
                    a11 = cos;
                    a21 = sin;
                    if (t.skewX) {
                        angle -= t.skewX * _DEG2RAD;
                        cos = Math.cos(angle);
                        sin = Math.sin(angle);
                        if (t.skewType === "simple") { //by default, we compensate skewing on the other axis to make it look more natural, but you can set the skewType to "simple" to use the uncompensated skewing that CSS does
                            t1 = Math.tan(t.skewX * _DEG2RAD);
                            t1 = Math.sqrt(1 + t1 * t1);
                            cos *= t1;
                            sin *= t1;
                        }
                    }
                    a12 = -sin;
                    a22 = cos;

                } else if (!t.rotationY && !t.rotationX && sz === 1 && !perspective) { //if we're only translating and/or 2D scaling, this is faster...
                    style[_transformProp] = "translate3d(" + t.x + "px," + t.y + "px," + t.z +"px)" + ((sx !== 1 || sy !== 1) ? " scale(" + sx + "," + sy + ")" : "");
                    return;
                } else {
                    a11 = a22 = 1;
                    a12 = a21 = 0;
                }
                a33 = 1;
                a13 = a14 = a23 = a24 = a31 = a32 = a34 = a41 = a42 = 0;
                a43 = (perspective) ? -1 / perspective : 0;
                zOrigin = t.zOrigin;
                rnd = 100000;
                angle = t.rotationY * _DEG2RAD;
                if (angle) {
                    cos = Math.cos(angle);
                    sin = Math.sin(angle);
                    a31 = a33*-sin;
                    a41 = a43*-sin;
                    a13 = a11*sin;
                    a23 = a21*sin;
                    a33 *= cos;
                    a43 *= cos;
                    a11 *= cos;
                    a21 *= cos;
                }
                angle = t.rotationX * _DEG2RAD;
                if (angle) {
                    cos = Math.cos(angle);
                    sin = Math.sin(angle);
                    t1 = a12*cos+a13*sin;
                    t2 = a22*cos+a23*sin;
                    t3 = a32*cos+a33*sin;
                    t4 = a42*cos+a43*sin;
                    a13 = a12*-sin+a13*cos;
                    a23 = a22*-sin+a23*cos;
                    a33 = a32*-sin+a33*cos;
                    a43 = a42*-sin+a43*cos;
                    a12 = t1;
                    a22 = t2;
                    a32 = t3;
                    a42 = t4;
                }
                if (sz !== 1) {
                    a13*=sz;
                    a23*=sz;
                    a33*=sz;
                    a43*=sz;
                }
                if (sy !== 1) {
                    a12*=sy;
                    a22*=sy;
                    a32*=sy;
                    a42*=sy;
                }
                if (sx !== 1) {
                    a11*=sx;
                    a21*=sx;
                    a31*=sx;
                    a41*=sx;
                }
                if (zOrigin) {
                    a34 -= zOrigin;
                    a14 = a13*a34;
                    a24 = a23*a34;
                    a34 = a33*a34+zOrigin;
                }
                //we round the x, y, and z slightly differently to allow even larger values.
                a14 = (t1 = (a14 += t.x) - (a14 |= 0)) ? ((t1 * rnd + (t1 < 0 ? -0.5 : 0.5)) | 0) / rnd + a14 : a14;
                a24 = (t1 = (a24 += t.y) - (a24 |= 0)) ? ((t1 * rnd + (t1 < 0 ? -0.5 : 0.5)) | 0) / rnd + a24 : a24;
                a34 = (t1 = (a34 += t.z) - (a34 |= 0)) ? ((t1 * rnd + (t1 < 0 ? -0.5 : 0.5)) | 0) / rnd + a34 : a34;
                style[_transformProp] = "matrix3d(" + [ (((a11 * rnd) | 0) / rnd), (((a21 * rnd) | 0) / rnd), (((a31 * rnd) | 0) / rnd), (((a41 * rnd) | 0) / rnd), (((a12 * rnd) | 0) / rnd), (((a22 * rnd) | 0) / rnd), (((a32 * rnd) | 0) / rnd), (((a42 * rnd) | 0) / rnd), (((a13 * rnd) | 0) / rnd), (((a23 * rnd) | 0) / rnd), (((a33 * rnd) | 0) / rnd), (((a43 * rnd) | 0) / rnd), a14, a24, a34, (perspective ? (1 + (-a34 / perspective)) : 1) ].join(",") + ")";
            },

            _set2DTransformRatio = _internals.set2DTransformRatio = function(v) {
                var t = this.data, //refers to the element's _gsTransform object
                    targ = this.t,
                    style = targ.style,
                    ang, skew, rnd, sx, sy;
                if (t.rotationX || t.rotationY || t.z || t.force3D === true || (t.force3D === "auto" && v !== 1 && v !== 0)) { //if a 3D tween begins while a 2D one is running, we need to kick the rendering over to the 3D method. For example, imagine a yoyo-ing, infinitely repeating scale tween running, and then the object gets rotated in 3D space with a different tween.
                    this.setRatio = _set3DTransformRatio;
                    _set3DTransformRatio.call(this, v);
                    return;
                }
                if (!t.rotation && !t.skewX) {
                    style[_transformProp] = "matrix(" + t.scaleX + ",0,0," + t.scaleY + "," + t.x + "," + t.y + ")";
                } else {
                    ang = t.rotation * _DEG2RAD;
                    skew = ang - t.skewX * _DEG2RAD;
                    rnd = 100000;
                    sx = t.scaleX * rnd;
                    sy = t.scaleY * rnd;
                    //some browsers have a hard time with very small values like 2.4492935982947064e-16 (notice the "e-" towards the end) and would render the object slightly off. So we round to 5 decimal places.
                    style[_transformProp] = "matrix(" + (((Math.cos(ang) * sx) | 0) / rnd) + "," + (((Math.sin(ang) * sx) | 0) / rnd) + "," + (((Math.sin(skew) * -sy) | 0) / rnd) + "," + (((Math.cos(skew) * sy) | 0) / rnd) + "," + t.x + "," + t.y + ")";
                }
            };

        _registerComplexSpecialProp("transform,scale,scaleX,scaleY,scaleZ,x,y,z,rotation,rotationX,rotationY,rotationZ,skewX,skewY,shortRotation,shortRotationX,shortRotationY,shortRotationZ,transformOrigin,transformPerspective,directionalRotation,parseTransform,force3D,skewType", {parser:function(t, e, p, cssp, pt, plugin, vars) {
            if (cssp._transform) { return pt; } //only need to parse the transform once, and only if the browser supports it.
            var m1 = cssp._transform = _getTransform(t, _cs, true, vars.parseTransform),
                style = t.style,
                min = 0.000001,
                i = _transformProps.length,
                v = vars,
                endRotations = {},
                m2, skewY, copy, orig, has3D, hasChange, dr;
            if (typeof(v.transform) === "string" && _transformProp) { //for values like transform:"rotate(60deg) scale(0.5, 0.8)"
                copy = _tempDiv.style; //don't use the original target because it might be SVG in which case some browsers don't report computed style correctly.
                copy[_transformProp] = v.transform;
                copy.display = "block"; //if display is "none", the browser often refuses to report the transform properties correctly.
                copy.position = "absolute";
                _doc.body.appendChild(_tempDiv);
                m2 = _getTransform(_tempDiv, null, false);
                _doc.body.removeChild(_tempDiv);
            } else if (typeof(v) === "object") { //for values like scaleX, scaleY, rotation, x, y, skewX, and skewY or transform:{...} (object)
                m2 = {scaleX:_parseVal((v.scaleX != null) ? v.scaleX : v.scale, m1.scaleX),
                    scaleY:_parseVal((v.scaleY != null) ? v.scaleY : v.scale, m1.scaleY),
                    scaleZ:_parseVal(v.scaleZ, m1.scaleZ),
                    x:_parseVal(v.x, m1.x),
                    y:_parseVal(v.y, m1.y),
                    z:_parseVal(v.z, m1.z),
                    perspective:_parseVal(v.transformPerspective, m1.perspective)};
                dr = v.directionalRotation;
                if (dr != null) {
                    if (typeof(dr) === "object") {
                        for (copy in dr) {
                            v[copy] = dr[copy];
                        }
                    } else {
                        v.rotation = dr;
                    }
                }
                m2.rotation = _parseAngle(("rotation" in v) ? v.rotation : ("shortRotation" in v) ? v.shortRotation + "_short" : ("rotationZ" in v) ? v.rotationZ : m1.rotation, m1.rotation, "rotation", endRotations);
                if (_supports3D) {
                    m2.rotationX = _parseAngle(("rotationX" in v) ? v.rotationX : ("shortRotationX" in v) ? v.shortRotationX + "_short" : m1.rotationX || 0, m1.rotationX, "rotationX", endRotations);
                    m2.rotationY = _parseAngle(("rotationY" in v) ? v.rotationY : ("shortRotationY" in v) ? v.shortRotationY + "_short" : m1.rotationY || 0, m1.rotationY, "rotationY", endRotations);
                }
                m2.skewX = (v.skewX == null) ? m1.skewX : _parseAngle(v.skewX, m1.skewX);

                //note: for performance reasons, we combine all skewing into the skewX and rotation values, ignoring skewY but we must still record it so that we can discern how much of the overall skew is attributed to skewX vs. skewY. Otherwise, if the skewY would always act relative (tween skewY to 10deg, for example, multiple times and if we always combine things into skewX, we can't remember that skewY was 10 from last time). Remember, a skewY of 10 degrees looks the same as a rotation of 10 degrees plus a skewX of -10 degrees.
                m2.skewY = (v.skewY == null) ? m1.skewY : _parseAngle(v.skewY, m1.skewY);
                if ((skewY = m2.skewY - m1.skewY)) {
                    m2.skewX += skewY;
                    m2.rotation += skewY;
                }
            }

            if (_supports3D && v.force3D != null) {
                m1.force3D = v.force3D;
                hasChange = true;
            }

            m1.skewType = v.skewType || m1.skewType || CSSPlugin.defaultSkewType;

            has3D = (m1.force3D || m1.z || m1.rotationX || m1.rotationY || m2.z || m2.rotationX || m2.rotationY || m2.perspective);
            if (!has3D && v.scale != null) {
                m2.scaleZ = 1; //no need to tween scaleZ.
            }

            while (--i > -1) {
                p = _transformProps[i];
                orig = m2[p] - m1[p];
                if (orig > min || orig < -min || _forcePT[p] != null) {
                    hasChange = true;
                    pt = new CSSPropTween(m1, p, m1[p], orig, pt);
                    if (p in endRotations) {
                        pt.e = endRotations[p]; //directional rotations typically have compensated values during the tween, but we need to make sure they end at exactly what the user requested
                    }
                    pt.xs0 = 0; //ensures the value stays numeric in setRatio()
                    pt.plugin = plugin;
                    cssp._overwriteProps.push(pt.n);
                }
            }

            orig = v.transformOrigin;
            if (orig || (_supports3D && has3D && m1.zOrigin)) { //if anything 3D is happening and there's a transformOrigin with a z component that's non-zero, we must ensure that the transformOrigin's z-component is set to 0 so that we can manually do those calculations to get around Safari bugs. Even if the user didn't specifically define a "transformOrigin" in this particular tween (maybe they did it via css directly).
                if (_transformProp) {
                    hasChange = true;
                    p = _transformOriginProp;
                    orig = (orig || _getStyle(t, p, _cs, false, "50% 50%")) + ""; //cast as string to avoid errors
                    pt = new CSSPropTween(style, p, 0, 0, pt, -1, "transformOrigin");
                    pt.b = style[p];
                    pt.plugin = plugin;
                    if (_supports3D) {
                        copy = m1.zOrigin;
                        orig = orig.split(" ");
                        m1.zOrigin = ((orig.length > 2 && !(copy !== 0 && orig[2] === "0px")) ? parseFloat(orig[2]) : copy) || 0; //Safari doesn't handle the z part of transformOrigin correctly, so we'll manually handle it in the _set3DTransformRatio() method.
                        pt.xs0 = pt.e = orig[0] + " " + (orig[1] || "50%") + " 0px"; //we must define a z value of 0px specifically otherwise iOS 5 Safari will stick with the old one (if one was defined)!
                        pt = new CSSPropTween(m1, "zOrigin", 0, 0, pt, -1, pt.n); //we must create a CSSPropTween for the _gsTransform.zOrigin so that it gets reset properly at the beginning if the tween runs backward (as opposed to just setting m1.zOrigin here)
                        pt.b = copy;
                        pt.xs0 = pt.e = m1.zOrigin;
                    } else {
                        pt.xs0 = pt.e = orig;
                    }

                //for older versions of IE (6-8), we need to manually calculate things inside the setRatio() function. We record origin x and y (ox and oy) and whether or not the values are percentages (oxp and oyp).
                } else {
                    _parsePosition(orig + "", m1);
                }
            }

            if (hasChange) {
                cssp._transformType = (has3D || this._transformType === 3) ? 3 : 2; //quicker than calling cssp._enableTransforms();
            }
            return pt;
        }, prefix:true});

        _registerComplexSpecialProp("boxShadow", {defaultValue:"0px 0px 0px 0px #999", prefix:true, color:true, multi:true, keyword:"inset"});

        _registerComplexSpecialProp("borderRadius", {defaultValue:"0px", parser:function(t, e, p, cssp, pt, plugin) {
            e = this.format(e);
            var props = ["borderTopLeftRadius","borderTopRightRadius","borderBottomRightRadius","borderBottomLeftRadius"],
                style = t.style,
                ea1, i, es2, bs2, bs, es, bn, en, w, h, esfx, bsfx, rel, hn, vn, em;
            w = parseFloat(t.offsetWidth);
            h = parseFloat(t.offsetHeight);
            ea1 = e.split(" ");
            for (i = 0; i < props.length; i++) { //if we're dealing with percentages, we must convert things separately for the horizontal and vertical axis!
                if (this.p.indexOf("border")) { //older browsers used a prefix
                    props[i] = _checkPropPrefix(props[i]);
                }
                bs = bs2 = _getStyle(t, props[i], _cs, false, "0px");
                if (bs.indexOf(" ") !== -1) {
                    bs2 = bs.split(" ");
                    bs = bs2[0];
                    bs2 = bs2[1];
                }
                es = es2 = ea1[i];
                bn = parseFloat(bs);
                bsfx = bs.substr((bn + "").length);
                rel = (es.charAt(1) === "=");
                if (rel) {
                    en = parseInt(es.charAt(0)+"1", 10);
                    es = es.substr(2);
                    en *= parseFloat(es);
                    esfx = es.substr((en + "").length - (en < 0 ? 1 : 0)) || "";
                } else {
                    en = parseFloat(es);
                    esfx = es.substr((en + "").length);
                }
                if (esfx === "") {
                    esfx = _suffixMap[p] || bsfx;
                }
                if (esfx !== bsfx) {
                    hn = _convertToPixels(t, "borderLeft", bn, bsfx); //horizontal number (we use a bogus "borderLeft" property just because the _convertToPixels() method searches for the keywords "Left", "Right", "Top", and "Bottom" to determine of it's a horizontal or vertical property, and we need "border" in the name so that it knows it should measure relative to the element itself, not its parent.
                    vn = _convertToPixels(t, "borderTop", bn, bsfx); //vertical number
                    if (esfx === "%") {
                        bs = (hn / w * 100) + "%";
                        bs2 = (vn / h * 100) + "%";
                    } else if (esfx === "em") {
                        em = _convertToPixels(t, "borderLeft", 1, "em");
                        bs = (hn / em) + "em";
                        bs2 = (vn / em) + "em";
                    } else {
                        bs = hn + "px";
                        bs2 = vn + "px";
                    }
                    if (rel) {
                        es = (parseFloat(bs) + en) + esfx;
                        es2 = (parseFloat(bs2) + en) + esfx;
                    }
                }
                pt = _parseComplex(style, props[i], bs + " " + bs2, es + " " + es2, false, "0px", pt);
            }
            return pt;
        }, prefix:true, formatter:_getFormatter("0px 0px 0px 0px", false, true)});
        _registerComplexSpecialProp("backgroundPosition", {defaultValue:"0 0", parser:function(t, e, p, cssp, pt, plugin) {
            var bp = "background-position",
                cs = (_cs || _getComputedStyle(t, null)),
                bs = this.format( ((cs) ? _ieVers ? cs.getPropertyValue(bp + "-x") + " " + cs.getPropertyValue(bp + "-y") : cs.getPropertyValue(bp) : t.currentStyle.backgroundPositionX + " " + t.currentStyle.backgroundPositionY) || "0 0"), //Internet Explorer doesn't report background-position correctly - we must query background-position-x and background-position-y and combine them (even in IE10). Before IE9, we must do the same with the currentStyle object and use camelCase
                es = this.format(e),
                ba, ea, i, pct, overlap, src;
            if ((bs.indexOf("%") !== -1) !== (es.indexOf("%") !== -1)) {
                src = _getStyle(t, "backgroundImage").replace(_urlExp, "");
                if (src && src !== "none") {
                    ba = bs.split(" ");
                    ea = es.split(" ");
                    _tempImg.setAttribute("src", src); //set the temp <img>'s src to the background-image so that we can measure its width/height
                    i = 2;
                    while (--i > -1) {
                        bs = ba[i];
                        pct = (bs.indexOf("%") !== -1);
                        if (pct !== (ea[i].indexOf("%") !== -1)) {
                            overlap = (i === 0) ? t.offsetWidth - _tempImg.width : t.offsetHeight - _tempImg.height;
                            ba[i] = pct ? (parseFloat(bs) / 100 * overlap) + "px" : (parseFloat(bs) / overlap * 100) + "%";
                        }
                    }
                    bs = ba.join(" ");
                }
            }
            return this.parseComplex(t.style, bs, es, pt, plugin);
        }, formatter:_parsePosition});
        _registerComplexSpecialProp("backgroundSize", {defaultValue:"0 0", formatter:_parsePosition});
        _registerComplexSpecialProp("perspective", {defaultValue:"0px", prefix:true});
        _registerComplexSpecialProp("perspectiveOrigin", {defaultValue:"50% 50%", prefix:true});
        _registerComplexSpecialProp("transformStyle", {prefix:true});
        _registerComplexSpecialProp("backfaceVisibility", {prefix:true});
        _registerComplexSpecialProp("userSelect", {prefix:true});
        _registerComplexSpecialProp("margin", {parser:_getEdgeParser("marginTop,marginRight,marginBottom,marginLeft")});
        _registerComplexSpecialProp("padding", {parser:_getEdgeParser("paddingTop,paddingRight,paddingBottom,paddingLeft")});
        _registerComplexSpecialProp("clip", {defaultValue:"rect(0px,0px,0px,0px)", parser:function(t, e, p, cssp, pt, plugin){
            var b, cs, delim;
            if (_ieVers < 9) { //IE8 and earlier don't report a "clip" value in the currentStyle - instead, the values are split apart into clipTop, clipRight, clipBottom, and clipLeft. Also, in IE7 and earlier, the values inside rect() are space-delimited, not comma-delimited.
                cs = t.currentStyle;
                delim = _ieVers < 8 ? " " : ",";
                b = "rect(" + cs.clipTop + delim + cs.clipRight + delim + cs.clipBottom + delim + cs.clipLeft + ")";
                e = this.format(e).split(",").join(delim);
            } else {
                b = this.format(_getStyle(t, this.p, _cs, false, this.dflt));
                e = this.format(e);
            }
            return this.parseComplex(t.style, b, e, pt, plugin);
        }});
        _registerComplexSpecialProp("textShadow", {defaultValue:"0px 0px 0px #999", color:true, multi:true});
        _registerComplexSpecialProp("autoRound,strictUnits", {parser:function(t, e, p, cssp, pt) {return pt;}}); //just so that we can ignore these properties (not tween them)
        _registerComplexSpecialProp("border", {defaultValue:"0px solid #000", parser:function(t, e, p, cssp, pt, plugin) {
                return this.parseComplex(t.style, this.format(_getStyle(t, "borderTopWidth", _cs, false, "0px") + " " + _getStyle(t, "borderTopStyle", _cs, false, "solid") + " " + _getStyle(t, "borderTopColor", _cs, false, "#000")), this.format(e), pt, plugin);
            }, color:true, formatter:function(v) {
                var a = v.split(" ");
                return a[0] + " " + (a[1] || "solid") + " " + (v.match(_colorExp) || ["#000"])[0];
            }});
        _registerComplexSpecialProp("borderWidth", {parser:_getEdgeParser("borderTopWidth,borderRightWidth,borderBottomWidth,borderLeftWidth")}); //Firefox doesn't pick up on borderWidth set in style sheets (only inline).
        _registerComplexSpecialProp("float,cssFloat,styleFloat", {parser:function(t, e, p, cssp, pt, plugin) {
            var s = t.style,
                prop = ("cssFloat" in s) ? "cssFloat" : "styleFloat";
            return new CSSPropTween(s, prop, 0, 0, pt, -1, p, false, 0, s[prop], e);
        }});

        //opacity-related
        var _setIEOpacityRatio = function(v) {
                var t = this.t, //refers to the element's style property
                    filters = t.filter || _getStyle(this.data, "filter"),
                    val = (this.s + this.c * v) | 0,
                    skip;
                if (val === 100) { //for older versions of IE that need to use a filter to apply opacity, we should remove the filter if opacity hits 1 in order to improve performance, but make sure there isn't a transform (matrix) or gradient in the filters.
                    if (filters.indexOf("atrix(") === -1 && filters.indexOf("radient(") === -1 && filters.indexOf("oader(") === -1) {
                        t.removeAttribute("filter");
                        skip = (!_getStyle(this.data, "filter")); //if a class is applied that has an alpha filter, it will take effect (we don't want that), so re-apply our alpha filter in that case. We must first remove it and then check.
                    } else {
                        t.filter = filters.replace(_alphaFilterExp, "");
                        skip = true;
                    }
                }
                if (!skip) {
                    if (this.xn1) {
                        t.filter = filters = filters || ("alpha(opacity=" + val + ")"); //works around bug in IE7/8 that prevents changes to "visibility" from being applied properly if the filter is changed to a different alpha on the same frame.
                    }
                    if (filters.indexOf("pacity") === -1) { //only used if browser doesn't support the standard opacity style property (IE 7 and 8). We omit the "O" to avoid case-sensitivity issues
                        if (val !== 0 || !this.xn1) { //bugs in IE7/8 won't render the filter properly if opacity is ADDED on the same frame/render as "visibility" changes (this.xn1 is 1 if this tween is an "autoAlpha" tween)
                            t.filter = filters + " alpha(opacity=" + val + ")"; //we round the value because otherwise, bugs in IE7/8 can prevent "visibility" changes from being applied properly.
                        }
                    } else {
                        t.filter = filters.replace(_opacityExp, "opacity=" + val);
                    }
                }
            };
        _registerComplexSpecialProp("opacity,alpha,autoAlpha", {defaultValue:"1", parser:function(t, e, p, cssp, pt, plugin) {
            var b = parseFloat(_getStyle(t, "opacity", _cs, false, "1")),
                style = t.style,
                isAutoAlpha = (p === "autoAlpha");
            if (typeof(e) === "string" && e.charAt(1) === "=") {
                e = ((e.charAt(0) === "-") ? -1 : 1) * parseFloat(e.substr(2)) + b;
            }
            if (isAutoAlpha && b === 1 && _getStyle(t, "visibility", _cs) === "hidden" && e !== 0) { //if visibility is initially set to "hidden", we should interpret that as intent to make opacity 0 (a convenience)
                b = 0;
            }
            if (_supportsOpacity) {
                pt = new CSSPropTween(style, "opacity", b, e - b, pt);
            } else {
                pt = new CSSPropTween(style, "opacity", b * 100, (e - b) * 100, pt);
                pt.xn1 = isAutoAlpha ? 1 : 0; //we need to record whether or not this is an autoAlpha so that in the setRatio(), we know to duplicate the setting of the alpha in order to work around a bug in IE7 and IE8 that prevents changes to "visibility" from taking effect if the filter is changed to a different alpha(opacity) at the same time. Setting it to the SAME value first, then the new value works around the IE7/8 bug.
                style.zoom = 1; //helps correct an IE issue.
                pt.type = 2;
                pt.b = "alpha(opacity=" + pt.s + ")";
                pt.e = "alpha(opacity=" + (pt.s + pt.c) + ")";
                pt.data = t;
                pt.plugin = plugin;
                pt.setRatio = _setIEOpacityRatio;
            }
            if (isAutoAlpha) { //we have to create the "visibility" PropTween after the opacity one in the linked list so that they run in the order that works properly in IE8 and earlier
                pt = new CSSPropTween(style, "visibility", 0, 0, pt, -1, null, false, 0, ((b !== 0) ? "inherit" : "hidden"), ((e === 0) ? "hidden" : "inherit"));
                pt.xs0 = "inherit";
                cssp._overwriteProps.push(pt.n);
                cssp._overwriteProps.push(p);
            }
            return pt;
        }});


        var _removeProp = function(s, p) {
                if (p) {
                    if (s.removeProperty) {
                        if (p.substr(0,2) === "ms") { //Microsoft browsers don't conform to the standard of capping the first prefix character, so we adjust so that when we prefix the caps with a dash, it's correct (otherwise it'd be "ms-transform" instead of "-ms-transform" for IE9, for example)
                            p = "M" + p.substr(1);
                        }
                        s.removeProperty(p.replace(_capsExp, "-$1").toLowerCase());
                    } else { //note: old versions of IE use "removeAttribute()" instead of "removeProperty()"
                        s.removeAttribute(p);
                    }
                }
            },
            _setClassNameRatio = function(v) {
                this.t._gsClassPT = this;
                if (v === 1 || v === 0) {
                    this.t.setAttribute("class", (v === 0) ? this.b : this.e);
                    var mpt = this.data, //first MiniPropTween
                        s = this.t.style;
                    while (mpt) {
                        if (!mpt.v) {
                            _removeProp(s, mpt.p);
                        } else {
                            s[mpt.p] = mpt.v;
                        }
                        mpt = mpt._next;
                    }
                    if (v === 1 && this.t._gsClassPT === this) {
                        this.t._gsClassPT = null;
                    }
                } else if (this.t.getAttribute("class") !== this.e) {
                    this.t.setAttribute("class", this.e);
                }
            };
        _registerComplexSpecialProp("className", {parser:function(t, e, p, cssp, pt, plugin, vars) {
            var b = t.getAttribute("class") || "", //don't use t.className because it doesn't work consistently on SVG elements; getAttribute("class") and setAttribute("class", value") is more reliable.
                cssText = t.style.cssText,
                difData, bs, cnpt, cnptLookup, mpt;
            pt = cssp._classNamePT = new CSSPropTween(t, p, 0, 0, pt, 2);
            pt.setRatio = _setClassNameRatio;
            pt.pr = -11;
            _hasPriority = true;
            pt.b = b;
            bs = _getAllStyles(t, _cs);
            //if there's a className tween already operating on the target, force it to its end so that the necessary inline styles are removed and the class name is applied before we determine the end state (we don't want inline styles interfering that were there just for class-specific values)
            cnpt = t._gsClassPT;
            if (cnpt) {
                cnptLookup = {};
                mpt = cnpt.data; //first MiniPropTween which stores the inline styles - we need to force these so that the inline styles don't contaminate things. Otherwise, there's a small chance that a tween could start and the inline values match the destination values and they never get cleaned.
                while (mpt) {
                    cnptLookup[mpt.p] = 1;
                    mpt = mpt._next;
                }
                cnpt.setRatio(1);
            }
            t._gsClassPT = pt;
            pt.e = (e.charAt(1) !== "=") ? e : b.replace(new RegExp("\\s*\\b" + e.substr(2) + "\\b"), "") + ((e.charAt(0) === "+") ? " " + e.substr(2) : "");
            if (cssp._tween._duration) { //if it's a zero-duration tween, there's no need to tween anything or parse the data. In fact, if we switch classes temporarily (which we must do for proper parsing) and the class has a transition applied, it could cause a quick flash to the end state and back again initially in some browsers.
                t.setAttribute("class", pt.e);
                difData = _cssDif(t, bs, _getAllStyles(t), vars, cnptLookup);
                t.setAttribute("class", b);
                pt.data = difData.firstMPT;
                t.style.cssText = cssText; //we recorded cssText before we swapped classes and ran _getAllStyles() because in cases when a className tween is overwritten, we remove all the related tweening properties from that class change (otherwise class-specific stuff can't override properties we've directly set on the target's style object due to specificity).
                pt = pt.xfirst = cssp.parse(t, difData.difs, pt, plugin); //we record the CSSPropTween as the xfirst so that we can handle overwriting propertly (if "className" gets overwritten, we must kill all the properties associated with the className part of the tween, so we can loop through from xfirst to the pt itself)
            }
            return pt;
        }});


        var _setClearPropsRatio = function(v) {
            if (v === 1 || v === 0) if (this.data._totalTime === this.data._totalDuration && this.data.data !== "isFromStart") { //this.data refers to the tween. Only clear at the END of the tween (remember, from() tweens make the ratio go from 1 to 0, so we can't just check that and if the tween is the zero-duration one that's created internally to render the starting values in a from() tween, ignore that because otherwise, for example, from(...{height:100, clearProps:"height", delay:1}) would wipe the height at the beginning of the tween and after 1 second, it'd kick back in).
                var s = this.t.style,
                    transformParse = _specialProps.transform.parse,
                    a, p, i, clearTransform;
                if (this.e === "all") {
                    s.cssText = "";
                    clearTransform = true;
                } else {
                    a = this.e.split(",");
                    i = a.length;
                    while (--i > -1) {
                        p = a[i];
                        if (_specialProps[p]) {
                            if (_specialProps[p].parse === transformParse) {
                                clearTransform = true;
                            } else {
                                p = (p === "transformOrigin") ? _transformOriginProp : _specialProps[p].p; //ensures that special properties use the proper browser-specific property name, like "scaleX" might be "-webkit-transform" or "boxShadow" might be "-moz-box-shadow"
                            }
                        }
                        _removeProp(s, p);
                    }
                }
                if (clearTransform) {
                    _removeProp(s, _transformProp);
                    if (this.t._gsTransform) {
                        delete this.t._gsTransform;
                    }
                }

            }
        };
        _registerComplexSpecialProp("clearProps", {parser:function(t, e, p, cssp, pt) {
            pt = new CSSPropTween(t, p, 0, 0, pt, 2);
            pt.setRatio = _setClearPropsRatio;
            pt.e = e;
            pt.pr = -10;
            pt.data = cssp._tween;
            _hasPriority = true;
            return pt;
        }});

        p = "bezier,throwProps,physicsProps,physics2D".split(",");
        i = p.length;
        while (i--) {
            _registerPluginProp(p[i]);
        }








        p = CSSPlugin.prototype;
        p._firstPT = null;

        //gets called when the tween renders for the first time. This kicks everything off, recording start/end values, etc.
        p._onInitTween = function(target, vars, tween) {
            if (!target.nodeType) { //css is only for dom elements
                return false;
            }
            this._target = target;
            this._tween = tween;
            this._vars = vars;
            _autoRound = vars.autoRound;
            _hasPriority = false;
            _suffixMap = vars.suffixMap || CSSPlugin.suffixMap;
            _cs = _getComputedStyle(target, "");
            _overwriteProps = this._overwriteProps;
            var style = target.style,
                v, pt, pt2, first, last, next, zIndex, tpt, threeD;
            if (_reqSafariFix) if (style.zIndex === "") {
                v = _getStyle(target, "zIndex", _cs);
                if (v === "auto" || v === "") {
                    //corrects a bug in [non-Android] Safari that prevents it from repainting elements in their new positions if they don't have a zIndex set. We also can't just apply this inside _parseTransform() because anything that's moved in any way (like using "left" or "top" instead of transforms like "x" and "y") can be affected, so it is best to ensure that anything that's tweening has a z-index. Setting "WebkitPerspective" to a non-zero value worked too except that on iOS Safari things would flicker randomly. Plus zIndex is less memory-intensive.
                    this._addLazySet(style, "zIndex", 0);
                }
            }

            if (typeof(vars) === "string") {
                first = style.cssText;
                v = _getAllStyles(target, _cs);
                style.cssText = first + ";" + vars;
                v = _cssDif(target, v, _getAllStyles(target)).difs;
                if (!_supportsOpacity && _opacityValExp.test(vars)) {
                    v.opacity = parseFloat( RegExp.$1 );
                }
                vars = v;
                style.cssText = first;
            }
            this._firstPT = pt = this.parse(target, vars, null);

            if (this._transformType) {
                threeD = (this._transformType === 3);
                if (!_transformProp) {
                    style.zoom = 1; //helps correct an IE issue.
                } else if (_isSafari) {
                    _reqSafariFix = true;
                    //if zIndex isn't set, iOS Safari doesn't repaint things correctly sometimes (seemingly at random).
                    if (style.zIndex === "") {
                        zIndex = _getStyle(target, "zIndex", _cs);
                        if (zIndex === "auto" || zIndex === "") {
                            this._addLazySet(style, "zIndex", 0);
                        }
                    }
                    //Setting WebkitBackfaceVisibility corrects 3 bugs:
                    // 1) [non-Android] Safari skips rendering changes to "top" and "left" that are made on the same frame/render as a transform update.
                    // 2) iOS Safari sometimes neglects to repaint elements in their new positions. Setting "WebkitPerspective" to a non-zero value worked too except that on iOS Safari things would flicker randomly.
                    // 3) Safari sometimes displayed odd artifacts when tweening the transform (or WebkitTransform) property, like ghosts of the edges of the element remained. Definitely a browser bug.
                    //Note: we allow the user to override the auto-setting by defining WebkitBackfaceVisibility in the vars of the tween.
                    if (_isSafariLT6) {
                        this._addLazySet(style, "WebkitBackfaceVisibility", this._vars.WebkitBackfaceVisibility || (threeD ? "visible" : "hidden"));
                    }
                }
                pt2 = pt;
                while (pt2 && pt2._next) {
                    pt2 = pt2._next;
                }
                tpt = new CSSPropTween(target, "transform", 0, 0, null, 2);
                this._linkCSSP(tpt, null, pt2);
                tpt.setRatio = (threeD && _supports3D) ? _set3DTransformRatio : _transformProp ? _set2DTransformRatio : _setIETransformRatio;
                tpt.data = this._transform || _getTransform(target, _cs, true);
                _overwriteProps.pop(); //we don't want to force the overwrite of all "transform" tweens of the target - we only care about individual transform properties like scaleX, rotation, etc. The CSSPropTween constructor automatically adds the property to _overwriteProps which is why we need to pop() here.
            }

            if (_hasPriority) {
                //reorders the linked list in order of pr (priority)
                while (pt) {
                    next = pt._next;
                    pt2 = first;
                    while (pt2 && pt2.pr > pt.pr) {
                        pt2 = pt2._next;
                    }
                    if ((pt._prev = pt2 ? pt2._prev : last)) {
                        pt._prev._next = pt;
                    } else {
                        first = pt;
                    }
                    if ((pt._next = pt2)) {
                        pt2._prev = pt;
                    } else {
                        last = pt;
                    }
                    pt = next;
                }
                this._firstPT = first;
            }
            return true;
        };


        p.parse = function(target, vars, pt, plugin) {
            var style = target.style,
                p, sp, bn, en, bs, es, bsfx, esfx, isStr, rel;
            for (p in vars) {
                es = vars[p]; //ending value string
                sp = _specialProps[p]; //SpecialProp lookup.
                if (sp) {
                    pt = sp.parse(target, es, p, this, pt, plugin, vars);

                } else {
                    bs = _getStyle(target, p, _cs) + "";
                    isStr = (typeof(es) === "string");
                    if (p === "color" || p === "fill" || p === "stroke" || p.indexOf("Color") !== -1 || (isStr && _rgbhslExp.test(es))) { //Opera uses background: to define color sometimes in addition to backgroundColor:
                        if (!isStr) {
                            es = _parseColor(es);
                            es = ((es.length > 3) ? "rgba(" : "rgb(") + es.join(",") + ")";
                        }
                        pt = _parseComplex(style, p, bs, es, true, "transparent", pt, 0, plugin);

                    } else if (isStr && (es.indexOf(" ") !== -1 || es.indexOf(",") !== -1)) {
                        pt = _parseComplex(style, p, bs, es, true, null, pt, 0, plugin);

                    } else {
                        bn = parseFloat(bs);
                        bsfx = (bn || bn === 0) ? bs.substr((bn + "").length) : ""; //remember, bs could be non-numeric like "normal" for fontWeight, so we should default to a blank suffix in that case.

                        if (bs === "" || bs === "auto") {
                            if (p === "width" || p === "height") {
                                bn = _getDimension(target, p, _cs);
                                bsfx = "px";
                            } else if (p === "left" || p === "top") {
                                bn = _calculateOffset(target, p, _cs);
                                bsfx = "px";
                            } else {
                                bn = (p !== "opacity") ? 0 : 1;
                                bsfx = "";
                            }
                        }

                        rel = (isStr && es.charAt(1) === "=");
                        if (rel) {
                            en = parseInt(es.charAt(0) + "1", 10);
                            es = es.substr(2);
                            en *= parseFloat(es);
                            esfx = es.replace(_suffixExp, "");
                        } else {
                            en = parseFloat(es);
                            esfx = isStr ? es.substr((en + "").length) || "" : "";
                        }

                        if (esfx === "") {
                            esfx = (p in _suffixMap) ? _suffixMap[p] : bsfx; //populate the end suffix, prioritizing the map, then if none is found, use the beginning suffix.
                        }

                        es = (en || en === 0) ? (rel ? en + bn : en) + esfx : vars[p]; //ensures that any += or -= prefixes are taken care of. Record the end value before normalizing the suffix because we always want to end the tween on exactly what they intended even if it doesn't match the beginning value's suffix.

                        //if the beginning/ending suffixes don't match, normalize them...
                        if (bsfx !== esfx) if (esfx !== "") if (en || en === 0) if (bn) { //note: if the beginning value (bn) is 0, we don't need to convert units!
                            bn = _convertToPixels(target, p, bn, bsfx);
                            if (esfx === "%") {
                                bn /= _convertToPixels(target, p, 100, "%") / 100;
                                if (vars.strictUnits !== true) { //some browsers report only "px" values instead of allowing "%" with getComputedStyle(), so we assume that if we're tweening to a %, we should start there too unless strictUnits:true is defined. This approach is particularly useful for responsive designs that use from() tweens.
                                    bs = bn + "%";
                                }

                            } else if (esfx === "em") {
                                bn /= _convertToPixels(target, p, 1, "em");

                            //otherwise convert to pixels.
                            } else if (esfx !== "px") {
                                en = _convertToPixels(target, p, en, esfx);
                                esfx = "px"; //we don't use bsfx after this, so we don't need to set it to px too.
                            }
                            if (rel) if (en || en === 0) {
                                es = (en + bn) + esfx; //the changes we made affect relative calculations, so adjust the end value here.
                            }
                        }

                        if (rel) {
                            en += bn;
                        }

                        if ((bn || bn === 0) && (en || en === 0)) { //faster than isNaN(). Also, previously we required en !== bn but that doesn't really gain much performance and it prevents _parseToProxy() from working properly if beginning and ending values match but need to get tweened by an external plugin anyway. For example, a bezier tween where the target starts at left:0 and has these points: [{left:50},{left:0}] wouldn't work properly because when parsing the last point, it'd match the first (current) one and a non-tweening CSSPropTween would be recorded when we actually need a normal tween (type:0) so that things get updated during the tween properly.
                            pt = new CSSPropTween(style, p, bn, en - bn, pt, 0, p, (_autoRound !== false && (esfx === "px" || p === "zIndex")), 0, bs, es);
                            pt.xs0 = esfx;
                            //DEBUG: _log("tween "+p+" from "+pt.b+" ("+bn+esfx+") to "+pt.e+" with suffix: "+pt.xs0);
                        } else if (style[p] === undefined || !es && (es + "" === "NaN" || es == null)) {
                            _log("invalid " + p + " tween value: " + vars[p]);
                        } else {
                            pt = new CSSPropTween(style, p, en || bn || 0, 0, pt, -1, p, false, 0, bs, es);
                            pt.xs0 = (es === "none" && (p === "display" || p.indexOf("Style") !== -1)) ? bs : es; //intermediate value should typically be set immediately (end value) except for "display" or things like borderTopStyle, borderBottomStyle, etc. which should use the beginning value during the tween.
                            //DEBUG: _log("non-tweening value "+p+": "+pt.xs0);
                        }
                    }
                }
                if (plugin) if (pt && !pt.plugin) {
                    pt.plugin = plugin;
                }
            }
            return pt;
        };


        //gets called every time the tween updates, passing the new ratio (typically a value between 0 and 1, but not always (for example, if an Elastic.easeOut is used, the value can jump above 1 mid-tween). It will always start and 0 and end at 1.
        p.setRatio = function(v) {
            var pt = this._firstPT,
                min = 0.000001,
                val, str, i;

            //at the end of the tween, we set the values to exactly what we received in order to make sure non-tweening values (like "position" or "float" or whatever) are set and so that if the beginning/ending suffixes (units) didn't match and we normalized to px, the value that the user passed in is used here. We check to see if the tween is at its beginning in case it's a from() tween in which case the ratio will actually go from 1 to 0 over the course of the tween (backwards).
            if (v === 1 && (this._tween._time === this._tween._duration || this._tween._time === 0)) {
                while (pt) {
                    if (pt.type !== 2) {
                        pt.t[pt.p] = pt.e;
                    } else {
                        pt.setRatio(v);
                    }
                    pt = pt._next;
                }

            } else if (v || !(this._tween._time === this._tween._duration || this._tween._time === 0) || this._tween._rawPrevTime === -0.000001) {
                while (pt) {
                    val = pt.c * v + pt.s;
                    if (pt.r) {
                        val = Math.round(val);
                    } else if (val < min) if (val > -min) {
                        val = 0;
                    }
                    if (!pt.type) {
                        pt.t[pt.p] = val + pt.xs0;
                    } else if (pt.type === 1) { //complex value (one that typically has multiple numbers inside a string, like "rect(5px,10px,20px,25px)"
                        i = pt.l;
                        if (i === 2) {
                            pt.t[pt.p] = pt.xs0 + val + pt.xs1 + pt.xn1 + pt.xs2;
                        } else if (i === 3) {
                            pt.t[pt.p] = pt.xs0 + val + pt.xs1 + pt.xn1 + pt.xs2 + pt.xn2 + pt.xs3;
                        } else if (i === 4) {
                            pt.t[pt.p] = pt.xs0 + val + pt.xs1 + pt.xn1 + pt.xs2 + pt.xn2 + pt.xs3 + pt.xn3 + pt.xs4;
                        } else if (i === 5) {
                            pt.t[pt.p] = pt.xs0 + val + pt.xs1 + pt.xn1 + pt.xs2 + pt.xn2 + pt.xs3 + pt.xn3 + pt.xs4 + pt.xn4 + pt.xs5;
                        } else {
                            str = pt.xs0 + val + pt.xs1;
                            for (i = 1; i < pt.l; i++) {
                                str += pt["xn"+i] + pt["xs"+(i+1)];
                            }
                            pt.t[pt.p] = str;
                        }

                    } else if (pt.type === -1) { //non-tweening value
                        pt.t[pt.p] = pt.xs0;

                    } else if (pt.setRatio) { //custom setRatio() for things like SpecialProps, external plugins, etc.
                        pt.setRatio(v);
                    }
                    pt = pt._next;
                }

            //if the tween is reversed all the way back to the beginning, we need to restore the original values which may have different units (like % instead of px or em or whatever).
            } else {
                while (pt) {
                    if (pt.type !== 2) {
                        pt.t[pt.p] = pt.b;
                    } else {
                        pt.setRatio(v);
                    }
                    pt = pt._next;
                }
            }
        };

        /**
         * @private
         * Forces rendering of the target's transforms (rotation, scale, etc.) whenever the CSSPlugin's setRatio() is called.
         * Basically, this tells the CSSPlugin to create a CSSPropTween (type 2) after instantiation that runs last in the linked
         * list and calls the appropriate (3D or 2D) rendering function. We separate this into its own method so that we can call
         * it from other plugins like BezierPlugin if, for example, it needs to apply an autoRotation and this CSSPlugin
         * doesn't have any transform-related properties of its own. You can call this method as many times as you
         * want and it won't create duplicate CSSPropTweens.
         *
         * @param {boolean} threeD if true, it should apply 3D tweens (otherwise, just 2D ones are fine and typically faster)
         */
        p._enableTransforms = function(threeD) {
            this._transformType = (threeD || this._transformType === 3) ? 3 : 2;
            this._transform = this._transform || _getTransform(this._target, _cs, true); //ensures that the element has a _gsTransform property with the appropriate values.
        };

        var lazySet = function(v) {
            this.t[this.p] = this.e;
            this.data._linkCSSP(this, this._next, null, true); //we purposefully keep this._next even though it'd make sense to null it, but this is a performance optimization, as this happens during the while (pt) {} loop in setRatio() at the bottom of which it sets pt = pt._next, so if we null it, the linked list will be broken in that loop.
        };
        /** @private Gives us a way to set a value on the first render (and only the first render). **/
        p._addLazySet = function(t, p, v) {
            var pt = this._firstPT = new CSSPropTween(t, p, 0, 0, this._firstPT, 2);
            pt.e = v;
            pt.setRatio = lazySet;
            pt.data = this;
        };

        /** @private **/
        p._linkCSSP = function(pt, next, prev, remove) {
            if (pt) {
                if (next) {
                    next._prev = pt;
                }
                if (pt._next) {
                    pt._next._prev = pt._prev;
                }
                if (pt._prev) {
                    pt._prev._next = pt._next;
                } else if (this._firstPT === pt) {
                    this._firstPT = pt._next;
                    remove = true; //just to prevent resetting this._firstPT 5 lines down in case pt._next is null. (optimized for speed)
                }
                if (prev) {
                    prev._next = pt;
                } else if (!remove && this._firstPT === null) {
                    this._firstPT = pt;
                }
                pt._next = next;
                pt._prev = prev;
            }
            return pt;
        };

        //we need to make sure that if alpha or autoAlpha is killed, opacity is too. And autoAlpha affects the "visibility" property.
        p._kill = function(lookup) {
            var copy = lookup,
                pt, p, xfirst;
            if (lookup.autoAlpha || lookup.alpha) {
                copy = {};
                for (p in lookup) { //copy the lookup so that we're not changing the original which may be passed elsewhere.
                    copy[p] = lookup[p];
                }
                copy.opacity = 1;
                if (copy.autoAlpha) {
                    copy.visibility = 1;
                }
            }
            if (lookup.className && (pt = this._classNamePT)) { //for className tweens, we need to kill any associated CSSPropTweens too; a linked list starts at the className's "xfirst".
                xfirst = pt.xfirst;
                if (xfirst && xfirst._prev) {
                    this._linkCSSP(xfirst._prev, pt._next, xfirst._prev._prev); //break off the prev
                } else if (xfirst === this._firstPT) {
                    this._firstPT = pt._next;
                }
                if (pt._next) {
                    this._linkCSSP(pt._next, pt._next._next, xfirst._prev);
                }
                this._classNamePT = null;
            }
            return TweenPlugin.prototype._kill.call(this, copy);
        };



        //used by cascadeTo() for gathering all the style properties of each child element into an array for comparison.
        var _getChildStyles = function(e, props, targets) {
                var children, i, child, type;
                if (e.slice) {
                    i = e.length;
                    while (--i > -1) {
                        _getChildStyles(e[i], props, targets);
                    }
                    return;
                }
                children = e.childNodes;
                i = children.length;
                while (--i > -1) {
                    child = children[i];
                    type = child.type;
                    if (child.style) {
                        props.push(_getAllStyles(child));
                        if (targets) {
                            targets.push(child);
                        }
                    }
                    if ((type === 1 || type === 9 || type === 11) && child.childNodes.length) {
                        _getChildStyles(child, props, targets);
                    }
                }
            };

        /**
         * Typically only useful for className tweens that may affect child elements, this method creates a TweenLite
         * and then compares the style properties of all the target's child elements at the tween's start and end, and
         * if any are different, it also creates tweens for those and returns an array containing ALL of the resulting
         * tweens (so that you can easily add() them to a TimelineLite, for example). The reason this functionality is
         * wrapped into a separate static method of CSSPlugin instead of being integrated into all regular className tweens
         * is because it creates entirely new tweens that may have completely different targets than the original tween,
         * so if they were all lumped into the original tween instance, it would be inconsistent with the rest of the API
         * and it would create other problems. For example:
         *  - If I create a tween of elementA, that tween instance may suddenly change its target to include 50 other elements (unintuitive if I specifically defined the target I wanted)
         *  - We can't just create new independent tweens because otherwise, what happens if the original/parent tween is reversed or pause or dropped into a TimelineLite for tight control? You'd expect that tween's behavior to affect all the others.
         *  - Analyzing every style property of every child before and after the tween is an expensive operation when there are many children, so this behavior shouldn't be imposed on all className tweens by default, especially since it's probably rare that this extra functionality is needed.
         *
         * @param {Object} target object to be tweened
         * @param {number} Duration in seconds (or frames for frames-based tweens)
         * @param {Object} Object containing the end values, like {className:"newClass", ease:Linear.easeNone}
         * @return {Array} An array of TweenLite instances
         */
        CSSPlugin.cascadeTo = function(target, duration, vars) {
            var tween = TweenLite.to(target, duration, vars),
                results = [tween],
                b = [],
                e = [],
                targets = [],
                _reservedProps = TweenLite._internals.reservedProps,
                i, difs, p;
            target = tween._targets || tween.target;
            _getChildStyles(target, b, targets);
            tween.render(duration, true);
            _getChildStyles(target, e);
            tween.render(0, true);
            tween._enabled(true);
            i = targets.length;
            while (--i > -1) {
                difs = _cssDif(targets[i], b[i], e[i]);
                if (difs.firstMPT) {
                    difs = difs.difs;
                    for (p in vars) {
                        if (_reservedProps[p]) {
                            difs[p] = vars[p];
                        }
                    }
                    results.push( TweenLite.to(targets[i], duration, difs) );
                }
            }
            return results;
        };

        TweenPlugin.activate([CSSPlugin]);
        return CSSPlugin;

    }, true);

    
    
    
    
    
    
    
    
    
    
/*
 * ----------------------------------------------------------------
 * RoundPropsPlugin
 * ----------------------------------------------------------------
 */
    (function() {

        var RoundPropsPlugin = window._gsDefine.plugin({
                propName: "roundProps",
                priority: -1,
                API: 2,

                //called when the tween renders for the first time. This is where initial values should be recorded and any setup routines should run.
                init: function(target, value, tween) {
                    this._tween = tween;
                    return true;
                }

            }),
            p = RoundPropsPlugin.prototype;

        p._onInitAllProps = function() {
            var tween = this._tween,
                rp = (tween.vars.roundProps instanceof Array) ? tween.vars.roundProps : tween.vars.roundProps.split(","),
                i = rp.length,
                lookup = {},
                rpt = tween._propLookup.roundProps,
                prop, pt, next;
            while (--i > -1) {
                lookup[rp[i]] = 1;
            }
            i = rp.length;
            while (--i > -1) {
                prop = rp[i];
                pt = tween._firstPT;
                while (pt) {
                    next = pt._next; //record here, because it may get removed
                    if (pt.pg) {
                        pt.t._roundProps(lookup, true);
                    } else if (pt.n === prop) {
                        this._add(pt.t, prop, pt.s, pt.c);
                        //remove from linked list
                        if (next) {
                            next._prev = pt._prev;
                        }
                        if (pt._prev) {
                            pt._prev._next = next;
                        } else if (tween._firstPT === pt) {
                            tween._firstPT = next;
                        }
                        pt._next = pt._prev = null;
                        tween._propLookup[prop] = rpt;
                    }
                    pt = next;
                }
            }
            return false;
        };

        p._add = function(target, p, s, c) {
            this._addTween(target, p, s, s + c, p, true);
            this._overwriteProps.push(p);
        };

    }());










/*
 * ----------------------------------------------------------------
 * AttrPlugin
 * ----------------------------------------------------------------
 */
    window._gsDefine.plugin({
        propName: "attr",
        API: 2,
        version: "0.3.2",

        //called when the tween renders for the first time. This is where initial values should be recorded and any setup routines should run.
        init: function(target, value, tween) {
            var p, start, end;
            if (typeof(target.setAttribute) !== "function") {
                return false;
            }
            this._target = target;
            this._proxy = {};
            this._start = {}; // we record start and end values exactly as they are in case they're strings (not numbers) - we need to be able to revert to them cleanly.
            this._end = {};
            for (p in value) {
                this._start[p] = this._proxy[p] = start = target.getAttribute(p);
                end = this._addTween(this._proxy, p, parseFloat(start), value[p], p);
                this._end[p] = end ? end.s + end.c : value[p];
                this._overwriteProps.push(p);
            }
            return true;
        },

        //called each time the values should be updated, and the ratio gets passed as the only parameter (typically it's a value between 0 and 1, but it can exceed those when using an ease like Elastic.easeOut or Back.easeOut, etc.)
        set: function(ratio) {
            this._super.setRatio.call(this, ratio);
            var props = this._overwriteProps,
                i = props.length,
                lookup = (ratio === 1) ? this._end : ratio ? this._proxy : this._start,
                p;
            while (--i > -1) {
                p = props[i];
                this._target.setAttribute(p, lookup[p] + "");
            }
        }

    });










/*
 * ----------------------------------------------------------------
 * DirectionalRotationPlugin
 * ----------------------------------------------------------------
 */
    window._gsDefine.plugin({
        propName: "directionalRotation",
        API: 2,
        version: "0.2.0",

        //called when the tween renders for the first time. This is where initial values should be recorded and any setup routines should run.
        init: function(target, value, tween) {
            if (typeof(value) !== "object") {
                value = {rotation:value};
            }
            this.finals = {};
            var cap = (value.useRadians === true) ? Math.PI * 2 : 360,
                min = 0.000001,
                p, v, start, end, dif, split;
            for (p in value) {
                if (p !== "useRadians") {
                    split = (value[p] + "").split("_");
                    v = split[0];
                    start = parseFloat( (typeof(target[p]) !== "function") ? target[p] : target[ ((p.indexOf("set") || typeof(target["get" + p.substr(3)]) !== "function") ? p : "get" + p.substr(3)) ]() );
                    end = this.finals[p] = (typeof(v) === "string" && v.charAt(1) === "=") ? start + parseInt(v.charAt(0) + "1", 10) * Number(v.substr(2)) : Number(v) || 0;
                    dif = end - start;
                    if (split.length) {
                        v = split.join("_");
                        if (v.indexOf("short") !== -1) {
                            dif = dif % cap;
                            if (dif !== dif % (cap / 2)) {
                                dif = (dif < 0) ? dif + cap : dif - cap;
                            }
                        }
                        if (v.indexOf("_cw") !== -1 && dif < 0) {
                            dif = ((dif + cap * 9999999999) % cap) - ((dif / cap) | 0) * cap;
                        } else if (v.indexOf("ccw") !== -1 && dif > 0) {
                            dif = ((dif - cap * 9999999999) % cap) - ((dif / cap) | 0) * cap;
                        }
                    }
                    if (dif > min || dif < -min) {
                        this._addTween(target, p, start, start + dif, p);
                        this._overwriteProps.push(p);
                    }
                }
            }
            return true;
        },

        //called each time the values should be updated, and the ratio gets passed as the only parameter (typically it's a value between 0 and 1, but it can exceed those when using an ease like Elastic.easeOut or Back.easeOut, etc.)
        set: function(ratio) {
            var pt;
            if (ratio !== 1) {
                this._super.setRatio.call(this, ratio);
            } else {
                pt = this._firstPT;
                while (pt) {
                    if (pt.f) {
                        pt.t[pt.p](this.finals[pt.p]);
                    } else {
                        pt.t[pt.p] = this.finals[pt.p];
                    }
                    pt = pt._next;
                }
            }
        }

    })._autoCSS = true;







    
    
    
    
/*
 * ----------------------------------------------------------------
 * EasePack
 * ----------------------------------------------------------------
 */
    window._gsDefine("easing.Back", ["easing.Ease"], function(Ease) {
        
        var w = (window.GreenSockGlobals || window),
            gs = w.com.greensock,
            _2PI = Math.PI * 2,
            _HALF_PI = Math.PI / 2,
            _class = gs._class,
            _create = function(n, f) {
                var C = _class("easing." + n, function(){}, true),
                    p = C.prototype = new Ease();
                p.constructor = C;
                p.getRatio = f;
                return C;
            },
            _easeReg = Ease.register || function(){}, //put an empty function in place just as a safety measure in case someone loads an OLD version of TweenLite.js where Ease.register doesn't exist.
            _wrap = function(name, EaseOut, EaseIn, EaseInOut, aliases) {
                var C = _class("easing."+name, {
                    easeOut:new EaseOut(),
                    easeIn:new EaseIn(),
                    easeInOut:new EaseInOut()
                }, true);
                _easeReg(C, name);
                return C;
            },
            EasePoint = function(time, value, next) {
                this.t = time;
                this.v = value;
                if (next) {
                    this.next = next;
                    next.prev = this;
                    this.c = next.v - value;
                    this.gap = next.t - time;
                }
            },

            //Back
            _createBack = function(n, f) {
                var C = _class("easing." + n, function(overshoot) {
                        this._p1 = (overshoot || overshoot === 0) ? overshoot : 1.70158;
                        this._p2 = this._p1 * 1.525;
                    }, true),
                    p = C.prototype = new Ease();
                p.constructor = C;
                p.getRatio = f;
                p.config = function(overshoot) {
                    return new C(overshoot);
                };
                return C;
            },

            Back = _wrap("Back",
                _createBack("BackOut", function(p) {
                    return ((p = p - 1) * p * ((this._p1 + 1) * p + this._p1) + 1);
                }),
                _createBack("BackIn", function(p) {
                    return p * p * ((this._p1 + 1) * p - this._p1);
                }),
                _createBack("BackInOut", function(p) {
                    return ((p *= 2) < 1) ? 0.5 * p * p * ((this._p2 + 1) * p - this._p2) : 0.5 * ((p -= 2) * p * ((this._p2 + 1) * p + this._p2) + 2);
                })
            ),


            //SlowMo
            SlowMo = _class("easing.SlowMo", function(linearRatio, power, yoyoMode) {
                power = (power || power === 0) ? power : 0.7;
                if (linearRatio == null) {
                    linearRatio = 0.7;
                } else if (linearRatio > 1) {
                    linearRatio = 1;
                }
                this._p = (linearRatio !== 1) ? power : 0;
                this._p1 = (1 - linearRatio) / 2;
                this._p2 = linearRatio;
                this._p3 = this._p1 + this._p2;
                this._calcEnd = (yoyoMode === true);
            }, true),
            p = SlowMo.prototype = new Ease(),
            SteppedEase, RoughEase, _createElastic;

        p.constructor = SlowMo;
        p.getRatio = function(p) {
            var r = p + (0.5 - p) * this._p;
            if (p < this._p1) {
                return this._calcEnd ? 1 - ((p = 1 - (p / this._p1)) * p) : r - ((p = 1 - (p / this._p1)) * p * p * p * r);
            } else if (p > this._p3) {
                return this._calcEnd ? 1 - (p = (p - this._p3) / this._p1) * p : r + ((p - r) * (p = (p - this._p3) / this._p1) * p * p * p);
            }
            return this._calcEnd ? 1 : r;
        };
        SlowMo.ease = new SlowMo(0.7, 0.7);

        p.config = SlowMo.config = function(linearRatio, power, yoyoMode) {
            return new SlowMo(linearRatio, power, yoyoMode);
        };


        //SteppedEase
        SteppedEase = _class("easing.SteppedEase", function(steps) {
                steps = steps || 1;
                this._p1 = 1 / steps;
                this._p2 = steps + 1;
            }, true);
        p = SteppedEase.prototype = new Ease();
        p.constructor = SteppedEase;
        p.getRatio = function(p) {
            if (p < 0) {
                p = 0;
            } else if (p >= 1) {
                p = 0.999999999;
            }
            return ((this._p2 * p) >> 0) * this._p1;
        };
        p.config = SteppedEase.config = function(steps) {
            return new SteppedEase(steps);
        };


        //RoughEase
        RoughEase = _class("easing.RoughEase", function(vars) {
            vars = vars || {};
            var taper = vars.taper || "none",
                a = [],
                cnt = 0,
                points = (vars.points || 20) | 0,
                i = points,
                randomize = (vars.randomize !== false),
                clamp = (vars.clamp === true),
                template = (vars.template instanceof Ease) ? vars.template : null,
                strength = (typeof(vars.strength) === "number") ? vars.strength * 0.4 : 0.4,
                x, y, bump, invX, obj, pnt;
            while (--i > -1) {
                x = randomize ? Math.random() : (1 / points) * i;
                y = template ? template.getRatio(x) : x;
                if (taper === "none") {
                    bump = strength;
                } else if (taper === "out") {
                    invX = 1 - x;
                    bump = invX * invX * strength;
                } else if (taper === "in") {
                    bump = x * x * strength;
                } else if (x < 0.5) {  //"both" (start)
                    invX = x * 2;
                    bump = invX * invX * 0.5 * strength;
                } else {                //"both" (end)
                    invX = (1 - x) * 2;
                    bump = invX * invX * 0.5 * strength;
                }
                if (randomize) {
                    y += (Math.random() * bump) - (bump * 0.5);
                } else if (i % 2) {
                    y += bump * 0.5;
                } else {
                    y -= bump * 0.5;
                }
                if (clamp) {
                    if (y > 1) {
                        y = 1;
                    } else if (y < 0) {
                        y = 0;
                    }
                }
                a[cnt++] = {x:x, y:y};
            }
            a.sort(function(a, b) {
                return a.x - b.x;
            });

            pnt = new EasePoint(1, 1, null);
            i = points;
            while (--i > -1) {
                obj = a[i];
                pnt = new EasePoint(obj.x, obj.y, pnt);
            }

            this._prev = new EasePoint(0, 0, (pnt.t !== 0) ? pnt : pnt.next);
        }, true);
        p = RoughEase.prototype = new Ease();
        p.constructor = RoughEase;
        p.getRatio = function(p) {
            var pnt = this._prev;
            if (p > pnt.t) {
                while (pnt.next && p >= pnt.t) {
                    pnt = pnt.next;
                }
                pnt = pnt.prev;
            } else {
                while (pnt.prev && p <= pnt.t) {
                    pnt = pnt.prev;
                }
            }
            this._prev = pnt;
            return (pnt.v + ((p - pnt.t) / pnt.gap) * pnt.c);
        };
        p.config = function(vars) {
            return new RoughEase(vars);
        };
        RoughEase.ease = new RoughEase();


        //Bounce
        _wrap("Bounce",
            _create("BounceOut", function(p) {
                if (p < 1 / 2.75) {
                    return 7.5625 * p * p;
                } else if (p < 2 / 2.75) {
                    return 7.5625 * (p -= 1.5 / 2.75) * p + 0.75;
                } else if (p < 2.5 / 2.75) {
                    return 7.5625 * (p -= 2.25 / 2.75) * p + 0.9375;
                }
                return 7.5625 * (p -= 2.625 / 2.75) * p + 0.984375;
            }),
            _create("BounceIn", function(p) {
                if ((p = 1 - p) < 1 / 2.75) {
                    return 1 - (7.5625 * p * p);
                } else if (p < 2 / 2.75) {
                    return 1 - (7.5625 * (p -= 1.5 / 2.75) * p + 0.75);
                } else if (p < 2.5 / 2.75) {
                    return 1 - (7.5625 * (p -= 2.25 / 2.75) * p + 0.9375);
                }
                return 1 - (7.5625 * (p -= 2.625 / 2.75) * p + 0.984375);
            }),
            _create("BounceInOut", function(p) {
                var invert = (p < 0.5);
                if (invert) {
                    p = 1 - (p * 2);
                } else {
                    p = (p * 2) - 1;
                }
                if (p < 1 / 2.75) {
                    p = 7.5625 * p * p;
                } else if (p < 2 / 2.75) {
                    p = 7.5625 * (p -= 1.5 / 2.75) * p + 0.75;
                } else if (p < 2.5 / 2.75) {
                    p = 7.5625 * (p -= 2.25 / 2.75) * p + 0.9375;
                } else {
                    p = 7.5625 * (p -= 2.625 / 2.75) * p + 0.984375;
                }
                return invert ? (1 - p) * 0.5 : p * 0.5 + 0.5;
            })
        );


        //CIRC
        _wrap("Circ",
            _create("CircOut", function(p) {
                return Math.sqrt(1 - (p = p - 1) * p);
            }),
            _create("CircIn", function(p) {
                return -(Math.sqrt(1 - (p * p)) - 1);
            }),
            _create("CircInOut", function(p) {
                return ((p*=2) < 1) ? -0.5 * (Math.sqrt(1 - p * p) - 1) : 0.5 * (Math.sqrt(1 - (p -= 2) * p) + 1);
            })
        );


        //Elastic
        _createElastic = function(n, f, def) {
            var C = _class("easing." + n, function(amplitude, period) {
                    this._p1 = amplitude || 1;
                    this._p2 = period || def;
                    this._p3 = this._p2 / _2PI * (Math.asin(1 / this._p1) || 0);
                }, true),
                p = C.prototype = new Ease();
            p.constructor = C;
            p.getRatio = f;
            p.config = function(amplitude, period) {
                return new C(amplitude, period);
            };
            return C;
        };
        _wrap("Elastic",
            _createElastic("ElasticOut", function(p) {
                return this._p1 * Math.pow(2, -10 * p) * Math.sin( (p - this._p3) * _2PI / this._p2 ) + 1;
            }, 0.3),
            _createElastic("ElasticIn", function(p) {
                return -(this._p1 * Math.pow(2, 10 * (p -= 1)) * Math.sin( (p - this._p3) * _2PI / this._p2 ));
            }, 0.3),
            _createElastic("ElasticInOut", function(p) {
                return ((p *= 2) < 1) ? -0.5 * (this._p1 * Math.pow(2, 10 * (p -= 1)) * Math.sin( (p - this._p3) * _2PI / this._p2)) : this._p1 * Math.pow(2, -10 *(p -= 1)) * Math.sin( (p - this._p3) * _2PI / this._p2 ) *0.5 + 1;
            }, 0.45)
        );


        //Expo
        _wrap("Expo",
            _create("ExpoOut", function(p) {
                return 1 - Math.pow(2, -10 * p);
            }),
            _create("ExpoIn", function(p) {
                return Math.pow(2, 10 * (p - 1)) - 0.001;
            }),
            _create("ExpoInOut", function(p) {
                return ((p *= 2) < 1) ? 0.5 * Math.pow(2, 10 * (p - 1)) : 0.5 * (2 - Math.pow(2, -10 * (p - 1)));
            })
        );


        //Sine
        _wrap("Sine",
            _create("SineOut", function(p) {
                return Math.sin(p * _HALF_PI);
            }),
            _create("SineIn", function(p) {
                return -Math.cos(p * _HALF_PI) + 1;
            }),
            _create("SineInOut", function(p) {
                return -0.5 * (Math.cos(Math.PI * p) - 1);
            })
        );

        _class("easing.EaseLookup", {
                find:function(s) {
                    return Ease.map[s];
                }
            }, true);

        //register the non-standard eases
        _easeReg(w.SlowMo, "SlowMo", "ease,");
        _easeReg(RoughEase, "RoughEase", "ease,");
        _easeReg(SteppedEase, "SteppedEase", "ease,");

        return Back;
        
    }, true);


}); 











/*
 * ----------------------------------------------------------------
 * Base classes like TweenLite, SimpleTimeline, Ease, Ticker, etc.
 * ----------------------------------------------------------------
 */
(function(window) {

        "use strict";
        var _globals = window.GreenSockGlobals || window;
        if (_globals.TweenLite) {
            return; //in case the core set of classes is already loaded, don't instantiate twice.
        }
        var _namespace = function(ns) {
                var a = ns.split("."),
                    p = _globals, i;
                for (i = 0; i < a.length; i++) {
                    p[a[i]] = p = p[a[i]] || {};
                }
                return p;
            },
            gs = _namespace("com.greensock"),
            _tinyNum = 0.0000000001,
            _slice = [].slice,
            _emptyFunc = function() {},
            _isArray = (function() { //works around issues in iframe environments where the Array global isn't shared, thus if the object originates in a different window/iframe, "(obj instanceof Array)" will evaluate false. We added some speed optimizations to avoid Object.prototype.toString.call() unless it's absolutely necessary because it's VERY slow (like 20x slower)
                var toString = Object.prototype.toString,
                    array = toString.call([]);
                return function(obj) {
                    return obj != null && (obj instanceof Array || (typeof(obj) === "object" && !!obj.push && toString.call(obj) === array));
                };
            }()),
            a, i, p, _ticker, _tickerActive,
            _defLookup = {},

            /**
             * @constructor
             * Defines a GreenSock class, optionally with an array of dependencies that must be instantiated first and passed into the definition.
             * This allows users to load GreenSock JS files in any order even if they have interdependencies (like CSSPlugin extends TweenPlugin which is
             * inside TweenLite.js, but if CSSPlugin is loaded first, it should wait to run its code until TweenLite.js loads and instantiates TweenPlugin
             * and then pass TweenPlugin to CSSPlugin's definition). This is all done automatically and internally.
             *
             * Every definition will be added to a "com.greensock" global object (typically window, but if a window.GreenSockGlobals object is found,
             * it will go there as of v1.7). For example, TweenLite will be found at window.com.greensock.TweenLite and since it's a global class that should be available anywhere,
             * it is ALSO referenced at window.TweenLite. However some classes aren't considered global, like the base com.greensock.core.Animation class, so
             * those will only be at the package like window.com.greensock.core.Animation. Again, if you define a GreenSockGlobals object on the window, everything
             * gets tucked neatly inside there instead of on the window directly. This allows you to do advanced things like load multiple versions of GreenSock
             * files and put them into distinct objects (imagine a banner ad uses a newer version but the main site uses an older one). In that case, you could
             * sandbox the banner one like:
             *
             * <script>
             *     var gs = window.GreenSockGlobals = {}; //the newer version we're about to load could now be referenced in a "gs" object, like gs.TweenLite.to(...). Use whatever alias you want as long as it's unique, "gs" or "banner" or whatever.
             * </script>
             * <script src="js/greensock/v1.7/TweenMax.js"></script>
             * <script>
             *     window.GreenSockGlobals = null; //reset it back to null so that the next load of TweenMax affects the window and we can reference things directly like TweenLite.to(...)
             * </script>
             * <script src="js/greensock/v1.6/TweenMax.js"></script>
             * <script>
             *     gs.TweenLite.to(...); //would use v1.7
             *     TweenLite.to(...); //would use v1.6
             * </script>
             *
             * @param {!string} ns The namespace of the class definition, leaving off "com.greensock." as that's assumed. For example, "TweenLite" or "plugins.CSSPlugin" or "easing.Back".
             * @param {!Array.<string>} dependencies An array of dependencies (described as their namespaces minus "com.greensock." prefix). For example ["TweenLite","plugins.TweenPlugin","core.Animation"]
             * @param {!function():Object} func The function that should be called and passed the resolved dependencies which will return the actual class for this definition.
             * @param {boolean=} global If true, the class will be added to the global scope (typically window unless you define a window.GreenSockGlobals object)
             */
            Definition = function(ns, dependencies, func, global) {
                this.sc = (_defLookup[ns]) ? _defLookup[ns].sc : []; //subclasses
                _defLookup[ns] = this;
                this.gsClass = null;
                this.func = func;
                var _classes = [];
                this.check = function(init) {
                    var i = dependencies.length,
                        missing = i,
                        cur, a, n, cl;
                    while (--i > -1) {
                        if ((cur = _defLookup[dependencies[i]] || new Definition(dependencies[i], [])).gsClass) {
                            _classes[i] = cur.gsClass;
                            missing--;
                        } else if (init) {
                            cur.sc.push(this);
                        }
                    }
                    if (missing === 0 && func) {
                        a = ("com.greensock." + ns).split(".");
                        n = a.pop();
                        cl = _namespace(a.join("."))[n] = this.gsClass = func.apply(func, _classes);

                        //exports to multiple environments
                        if (global) {
                            _globals[n] = cl; //provides a way to avoid global namespace pollution. By default, the main classes like TweenLite, Power1, Strong, etc. are added to window unless a GreenSockGlobals is defined. So if you want to have things added to a custom object instead, just do something like window.GreenSockGlobals = {} before loading any GreenSock files. You can even set up an alias like window.GreenSockGlobals = windows.gs = {} so that you can access everything like gs.TweenLite. Also remember that ALL classes are added to the window.com.greensock object (in their respective packages, like com.greensock.easing.Power1, com.greensock.TweenLite, etc.)
                            if (typeof(define) === "function" && define.amd){ //AMD
                                define((window.GreenSockAMDPath ? window.GreenSockAMDPath + "/" : "") + ns.split(".").join("/"), [], function() { return cl; });
                            } else if (typeof(module) !== "undefined" && module.exports){ //node
                                module.exports = cl;
                            }
                        }
                        for (i = 0; i < this.sc.length; i++) {
                            this.sc[i].check();
                        }
                    }
                };
                this.check(true);
            },

            //used to create Definition instances (which basically registers a class that has dependencies).
            _gsDefine = window._gsDefine = function(ns, dependencies, func, global) {
                return new Definition(ns, dependencies, func, global);
            },

            //a quick way to create a class that doesn't have any dependencies. Returns the class, but first registers it in the GreenSock namespace so that other classes can grab it (other classes might be dependent on the class).
            _class = gs._class = function(ns, func, global) {
                func = func || function() {};
                _gsDefine(ns, [], function(){ return func; }, global);
                return func;
            };

        _gsDefine.globals = _globals;



/*
 * ----------------------------------------------------------------
 * Ease
 * ----------------------------------------------------------------
 */
        var _baseParams = [0, 0, 1, 1],
            _blankArray = [],
            Ease = _class("easing.Ease", function(func, extraParams, type, power) {
                this._func = func;
                this._type = type || 0;
                this._power = power || 0;
                this._params = extraParams ? _baseParams.concat(extraParams) : _baseParams;
            }, true),
            _easeMap = Ease.map = {},
            _easeReg = Ease.register = function(ease, names, types, create) {
                var na = names.split(","),
                    i = na.length,
                    ta = (types || "easeIn,easeOut,easeInOut").split(","),
                    e, name, j, type;
                while (--i > -1) {
                    name = na[i];
                    e = create ? _class("easing."+name, null, true) : gs.easing[name] || {};
                    j = ta.length;
                    while (--j > -1) {
                        type = ta[j];
                        _easeMap[name + "." + type] = _easeMap[type + name] = e[type] = ease.getRatio ? ease : ease[type] || new ease();
                    }
                }
            };

        p = Ease.prototype;
        p._calcEnd = false;
        p.getRatio = function(p) {
            if (this._func) {
                this._params[0] = p;
                return this._func.apply(null, this._params);
            }
            var t = this._type,
                pw = this._power,
                r = (t === 1) ? 1 - p : (t === 2) ? p : (p < 0.5) ? p * 2 : (1 - p) * 2;
            if (pw === 1) {
                r *= r;
            } else if (pw === 2) {
                r *= r * r;
            } else if (pw === 3) {
                r *= r * r * r;
            } else if (pw === 4) {
                r *= r * r * r * r;
            }
            return (t === 1) ? 1 - r : (t === 2) ? r : (p < 0.5) ? r / 2 : 1 - (r / 2);
        };

        //create all the standard eases like Linear, Quad, Cubic, Quart, Quint, Strong, Power0, Power1, Power2, Power3, and Power4 (each with easeIn, easeOut, and easeInOut)
        a = ["Linear","Quad","Cubic","Quart","Quint,Strong"];
        i = a.length;
        while (--i > -1) {
            p = a[i]+",Power"+i;
            _easeReg(new Ease(null,null,1,i), p, "easeOut", true);
            _easeReg(new Ease(null,null,2,i), p, "easeIn" + ((i === 0) ? ",easeNone" : ""));
            _easeReg(new Ease(null,null,3,i), p, "easeInOut");
        }
        _easeMap.linear = gs.easing.Linear.easeIn;
        _easeMap.swing = gs.easing.Quad.easeInOut; //for jQuery folks


/*
 * ----------------------------------------------------------------
 * EventDispatcher
 * ----------------------------------------------------------------
 */
        var EventDispatcher = _class("events.EventDispatcher", function(target) {
            this._listeners = {};
            this._eventTarget = target || this;
        });
        p = EventDispatcher.prototype;

        p.addEventListener = function(type, callback, scope, useParam, priority) {
            priority = priority || 0;
            var list = this._listeners[type],
                index = 0,
                listener, i;
            if (list == null) {
                this._listeners[type] = list = [];
            }
            i = list.length;
            while (--i > -1) {
                listener = list[i];
                if (listener.c === callback && listener.s === scope) {
                    list.splice(i, 1);
                } else if (index === 0 && listener.pr < priority) {
                    index = i + 1;
                }
            }
            list.splice(index, 0, {c:callback, s:scope, up:useParam, pr:priority});
            if (this === _ticker && !_tickerActive) {
                _ticker.wake();
            }
        };

        p.removeEventListener = function(type, callback) {
            var list = this._listeners[type], i;
            if (list) {
                i = list.length;
                while (--i > -1) {
                    if (list[i].c === callback) {
                        list.splice(i, 1);
                        return;
                    }
                }
            }
        };

        p.dispatchEvent = function(type) {
            var list = this._listeners[type],
                i, t, listener;
            if (list) {
                i = list.length;
                t = this._eventTarget;
                while (--i > -1) {
                    listener = list[i];
                    if (listener.up) {
                        listener.c.call(listener.s || t, {type:type, target:t});
                    } else {
                        listener.c.call(listener.s || t);
                    }
                }
            }
        };


/*
 * ----------------------------------------------------------------
 * Ticker
 * ----------------------------------------------------------------
 */
        var _reqAnimFrame = window.requestAnimationFrame,
            _cancelAnimFrame = window.cancelAnimationFrame,
            _getTime = Date.now || function() {return new Date().getTime();},
            _lastUpdate = _getTime();

        //now try to determine the requestAnimationFrame and cancelAnimationFrame functions and if none are found, we'll use a setTimeout()/clearTimeout() polyfill.
        a = ["ms","moz","webkit","o"];
        i = a.length;
        while (--i > -1 && !_reqAnimFrame) {
            _reqAnimFrame = window[a[i] + "RequestAnimationFrame"];
            _cancelAnimFrame = window[a[i] + "CancelAnimationFrame"] || window[a[i] + "CancelRequestAnimationFrame"];
        }

        _class("Ticker", function(fps, useRAF) {
            var _self = this,
                _startTime = _getTime(),
                _useRAF = (useRAF !== false && _reqAnimFrame),
                _lagThreshold = 500,
                _adjustedLag = 33,
                _fps, _req, _id, _gap, _nextTime,
                _tick = function(manual) {
                    var elapsed = _getTime() - _lastUpdate,
                        overlap, dispatch;
                    if (elapsed > _lagThreshold) {
                        _startTime += elapsed - _adjustedLag;
                    }
                    _lastUpdate += elapsed;
                    _self.time = (_lastUpdate - _startTime) / 1000;
                    overlap = _self.time - _nextTime;
                    if (!_fps || overlap > 0 || manual === true) {
                        _self.frame++;
                        _nextTime += overlap + (overlap >= _gap ? 0.004 : _gap - overlap);
                        dispatch = true;
                    }
                    if (manual !== true) { //make sure the request is made before we dispatch the "tick" event so that timing is maintained. Otherwise, if processing the "tick" requires a bunch of time (like 15ms) and we're using a setTimeout() that's based on 16.7ms, it'd technically take 31.7ms between frames otherwise.
                        _id = _req(_tick);
                    }
                    if (dispatch) {
                        _self.dispatchEvent("tick");
                    }
                };

            EventDispatcher.call(_self);
            _self.time = _self.frame = 0;
            _self.tick = function() {
                _tick(true);
            };

            _self.lagSmoothing = function(threshold, adjustedLag) {
                _lagThreshold = threshold || (1 / _tinyNum); //zero should be interpreted as basically unlimited
                _adjustedLag = Math.min(adjustedLag, _lagThreshold, 0);
            };

            _self.sleep = function() {
                if (_id == null) {
                    return;
                }
                if (!_useRAF || !_cancelAnimFrame) {
                    clearTimeout(_id);
                } else {
                    _cancelAnimFrame(_id);
                }
                _req = _emptyFunc;
                _id = null;
                if (_self === _ticker) {
                    _tickerActive = false;
                }
            };

            _self.wake = function() {
                if (_id !== null) {
                    _self.sleep();
                } else if (_self.frame > 10) { //don't trigger lagSmoothing if we're just waking up, and make sure that at least 10 frames have elapsed because of the iOS bug that we work around below with the 1.5-second setTimout().
                    _lastUpdate = _getTime() - _lagThreshold + 5;
                }
                _req = (_fps === 0) ? _emptyFunc : (!_useRAF || !_reqAnimFrame) ? function(f) { return setTimeout(f, ((_nextTime - _self.time) * 1000 + 1) | 0); } : _reqAnimFrame;
                if (_self === _ticker) {
                    _tickerActive = true;
                }
                _tick(2);
            };

            _self.fps = function(value) {
                if (!arguments.length) {
                    return _fps;
                }
                _fps = value;
                _gap = 1 / (_fps || 60);
                _nextTime = this.time + _gap;
                _self.wake();
            };

            _self.useRAF = function(value) {
                if (!arguments.length) {
                    return _useRAF;
                }
                _self.sleep();
                _useRAF = value;
                _self.fps(_fps);
            };
            _self.fps(fps);

            //a bug in iOS 6 Safari occasionally prevents the requestAnimationFrame from working initially, so we use a 1.5-second timeout that automatically falls back to setTimeout() if it senses this condition.
            setTimeout(function() {
                if (_useRAF && (!_id || _self.frame < 5)) {
                    _self.useRAF(false);
                }
            }, 1500);
        });

        p = gs.Ticker.prototype = new gs.events.EventDispatcher();
        p.constructor = gs.Ticker;


/*
 * ----------------------------------------------------------------
 * Animation
 * ----------------------------------------------------------------
 */
        var Animation = _class("core.Animation", function(duration, vars) {
                this.vars = vars = vars || {};
                this._duration = this._totalDuration = duration || 0;
                this._delay = Number(vars.delay) || 0;
                this._timeScale = 1;
                this._active = (vars.immediateRender === true);
                this.data = vars.data;
                this._reversed = (vars.reversed === true);

                if (!_rootTimeline) {
                    return;
                }
                if (!_tickerActive) { //some browsers (like iOS 6 Safari) shut down JavaScript execution when the tab is disabled and they [occasionally] neglect to start up requestAnimationFrame again when returning - this code ensures that the engine starts up again properly.
                    _ticker.wake();
                }

                var tl = this.vars.useFrames ? _rootFramesTimeline : _rootTimeline;
                tl.add(this, tl._time);

                if (this.vars.paused) {
                    this.paused(true);
                }
            });

        _ticker = Animation.ticker = new gs.Ticker();
        p = Animation.prototype;
        p._dirty = p._gc = p._initted = p._paused = false;
        p._totalTime = p._time = 0;
        p._rawPrevTime = -1;
        p._next = p._last = p._onUpdate = p._timeline = p.timeline = null;
        p._paused = false;


        //some browsers (like iOS) occasionally drop the requestAnimationFrame event when the user switches to a different tab and then comes back again, so we use a 2-second setTimeout() to sense if/when that condition occurs and then wake() the ticker.
        var _checkTimeout = function() {
                if (_tickerActive && _getTime() - _lastUpdate > 2000) {
                    _ticker.wake();
                }
                setTimeout(_checkTimeout, 2000);
            };
        _checkTimeout();


        p.play = function(from, suppressEvents) {
            if (from != null) {
                this.seek(from, suppressEvents);
            }
            return this.reversed(false).paused(false);
        };

        p.pause = function(atTime, suppressEvents) {
            if (atTime != null) {
                this.seek(atTime, suppressEvents);
            }
            return this.paused(true);
        };

        p.resume = function(from, suppressEvents) {
            if (from != null) {
                this.seek(from, suppressEvents);
            }
            return this.paused(false);
        };

        p.seek = function(time, suppressEvents) {
            return this.totalTime(Number(time), suppressEvents !== false);
        };

        p.restart = function(includeDelay, suppressEvents) {
            return this.reversed(false).paused(false).totalTime(includeDelay ? -this._delay : 0, (suppressEvents !== false), true);
        };

        p.reverse = function(from, suppressEvents) {
            if (from != null) {
                this.seek((from || this.totalDuration()), suppressEvents);
            }
            return this.reversed(true).paused(false);
        };

        p.render = function(time, suppressEvents, force) {
            //stub - we override this method in subclasses.
        };

        p.invalidate = function() {
            return this;
        };

        p.isActive = function() {
            var tl = this._timeline, //the 2 root timelines won't have a _timeline; they're always active.
                startTime = this._startTime,
                rawTime;
            return (!tl || (!this._gc && !this._paused && tl.isActive() && (rawTime = tl.rawTime()) >= startTime && rawTime < startTime + this.totalDuration() / this._timeScale));
        };

        p._enabled = function (enabled, ignoreTimeline) {
            if (!_tickerActive) {
                _ticker.wake();
            }
            this._gc = !enabled;
            this._active = this.isActive();
            if (ignoreTimeline !== true) {
                if (enabled && !this.timeline) {
                    this._timeline.add(this, this._startTime - this._delay);
                } else if (!enabled && this.timeline) {
                    this._timeline._remove(this, true);
                }
            }
            return false;
        };


        p._kill = function(vars, target) {
            return this._enabled(false, false);
        };

        p.kill = function(vars, target) {
            this._kill(vars, target);
            return this;
        };

        p._uncache = function(includeSelf) {
            var tween = includeSelf ? this : this.timeline;
            while (tween) {
                tween._dirty = true;
                tween = tween.timeline;
            }
            return this;
        };

        p._swapSelfInParams = function(params) {
            var i = params.length,
                copy = params.concat();
            while (--i > -1) {
                if (params[i] === "{self}") {
                    copy[i] = this;
                }
            }
            return copy;
        };

//----Animation getters/setters --------------------------------------------------------

        p.eventCallback = function(type, callback, params, scope) {
            if ((type || "").substr(0,2) === "on") {
                var v = this.vars;
                if (arguments.length === 1) {
                    return v[type];
                }
                if (callback == null) {
                    delete v[type];
                } else {
                    v[type] = callback;
                    v[type + "Params"] = (_isArray(params) && params.join("").indexOf("{self}") !== -1) ? this._swapSelfInParams(params) : params;
                    v[type + "Scope"] = scope;
                }
                if (type === "onUpdate") {
                    this._onUpdate = callback;
                }
            }
            return this;
        };

        p.delay = function(value) {
            if (!arguments.length) {
                return this._delay;
            }
            if (this._timeline.smoothChildTiming) {
                this.startTime( this._startTime + value - this._delay );
            }
            this._delay = value;
            return this;
        };

        p.duration = function(value) {
            if (!arguments.length) {
                this._dirty = false;
                return this._duration;
            }
            this._duration = this._totalDuration = value;
            this._uncache(true); //true in case it's a TweenMax or TimelineMax that has a repeat - we'll need to refresh the totalDuration.
            if (this._timeline.smoothChildTiming) if (this._time > 0) if (this._time < this._duration) if (value !== 0) {
                this.totalTime(this._totalTime * (value / this._duration), true);
            }
            return this;
        };

        p.totalDuration = function(value) {
            this._dirty = false;
            return (!arguments.length) ? this._totalDuration : this.duration(value);
        };

        p.time = function(value, suppressEvents) {
            if (!arguments.length) {
                return this._time;
            }
            if (this._dirty) {
                this.totalDuration();
            }
            return this.totalTime((value > this._duration) ? this._duration : value, suppressEvents);
        };

        p.totalTime = function(time, suppressEvents, uncapped) {
            if (!_tickerActive) {
                _ticker.wake();
            }
            if (!arguments.length) {
                return this._totalTime;
            }
            if (this._timeline) {
                if (time < 0 && !uncapped) {
                    time += this.totalDuration();
                }
                if (this._timeline.smoothChildTiming) {
                    if (this._dirty) {
                        this.totalDuration();
                    }
                    var totalDuration = this._totalDuration,
                        tl = this._timeline;
                    if (time > totalDuration && !uncapped) {
                        time = totalDuration;
                    }
                    this._startTime = (this._paused ? this._pauseTime : tl._time) - ((!this._reversed ? time : totalDuration - time) / this._timeScale);
                    if (!tl._dirty) { //for performance improvement. If the parent's cache is already dirty, it already took care of marking the ancestors as dirty too, so skip the function call here.
                        this._uncache(false);
                    }
                    //in case any of the ancestor timelines had completed but should now be enabled, we should reset their totalTime() which will also ensure that they're lined up properly and enabled. Skip for animations that are on the root (wasteful). Example: a TimelineLite.exportRoot() is performed when there's a paused tween on the root, the export will not complete until that tween is unpaused, but imagine a child gets restarted later, after all [unpaused] tweens have completed. The startTime of that child would get pushed out, but one of the ancestors may have completed.
                    if (tl._timeline) {
                        while (tl._timeline) {
                            if (tl._timeline._time !== (tl._startTime + tl._totalTime) / tl._timeScale) {
                                tl.totalTime(tl._totalTime, true);
                            }
                            tl = tl._timeline;
                        }
                    }
                }
                if (this._gc) {
                    this._enabled(true, false);
                }
                if (this._totalTime !== time || this._duration === 0) {
                    this.render(time, suppressEvents, false);
                    if (_lazyTweens.length) { //in case rendering caused any tweens to lazy-init, we should render them because typically when someone calls seek() or time() or progress(), they expect an immediate render.
                        _lazyRender();
                    }
                }
            }
            return this;
        };

        p.progress = p.totalProgress = function(value, suppressEvents) {
            return (!arguments.length) ? this._time / this.duration() : this.totalTime(this.duration() * value, suppressEvents);
        };

        p.startTime = function(value) {
            if (!arguments.length) {
                return this._startTime;
            }
            if (value !== this._startTime) {
                this._startTime = value;
                if (this.timeline) if (this.timeline._sortChildren) {
                    this.timeline.add(this, value - this._delay); //ensures that any necessary re-sequencing of Animations in the timeline occurs to make sure the rendering order is correct.
                }
            }
            return this;
        };

        p.timeScale = function(value) {
            if (!arguments.length) {
                return this._timeScale;
            }
            value = value || _tinyNum; //can't allow zero because it'll throw the math off
            if (this._timeline && this._timeline.smoothChildTiming) {
                var pauseTime = this._pauseTime,
                    t = (pauseTime || pauseTime === 0) ? pauseTime : this._timeline.totalTime();
                this._startTime = t - ((t - this._startTime) * this._timeScale / value);
            }
            this._timeScale = value;
            return this._uncache(false);
        };

        p.reversed = function(value) {
            if (!arguments.length) {
                return this._reversed;
            }
            if (value != this._reversed) {
                this._reversed = value;
                this.totalTime(((this._timeline && !this._timeline.smoothChildTiming) ? this.totalDuration() - this._totalTime : this._totalTime), true);
            }
            return this;
        };

        p.paused = function(value) {
            if (!arguments.length) {
                return this._paused;
            }
            if (value != this._paused) if (this._timeline) {
                if (!_tickerActive && !value) {
                    _ticker.wake();
                }
                var tl = this._timeline,
                    raw = tl.rawTime(),
                    elapsed = raw - this._pauseTime;
                if (!value && tl.smoothChildTiming) {
                    this._startTime += elapsed;
                    this._uncache(false);
                }
                this._pauseTime = value ? raw : null;
                this._paused = value;
                this._active = this.isActive();
                if (!value && elapsed !== 0 && this._initted && this.duration()) {
                    this.render((tl.smoothChildTiming ? this._totalTime : (raw - this._startTime) / this._timeScale), true, true); //in case the target's properties changed via some other tween or manual update by the user, we should force a render.
                }
            }
            if (this._gc && !value) {
                this._enabled(true, false);
            }
            return this;
        };


/*
 * ----------------------------------------------------------------
 * SimpleTimeline
 * ----------------------------------------------------------------
 */
        var SimpleTimeline = _class("core.SimpleTimeline", function(vars) {
            Animation.call(this, 0, vars);
            this.autoRemoveChildren = this.smoothChildTiming = true;
        });

        p = SimpleTimeline.prototype = new Animation();
        p.constructor = SimpleTimeline;
        p.kill()._gc = false;
        p._first = p._last = null;
        p._sortChildren = false;

        p.add = p.insert = function(child, position, align, stagger) {
            var prevTween, st;
            child._startTime = Number(position || 0) + child._delay;
            if (child._paused) if (this !== child._timeline) { //we only adjust the _pauseTime if it wasn't in this timeline already. Remember, sometimes a tween will be inserted again into the same timeline when its startTime is changed so that the tweens in the TimelineLite/Max are re-ordered properly in the linked list (so everything renders in the proper order).
                child._pauseTime = child._startTime + ((this.rawTime() - child._startTime) / child._timeScale);
            }
            if (child.timeline) {
                child.timeline._remove(child, true); //removes from existing timeline so that it can be properly added to this one.
            }
            child.timeline = child._timeline = this;
            if (child._gc) {
                child._enabled(true, true);
            }
            prevTween = this._last;
            if (this._sortChildren) {
                st = child._startTime;
                while (prevTween && prevTween._startTime > st) {
                    prevTween = prevTween._prev;
                }
            }
            if (prevTween) {
                child._next = prevTween._next;
                prevTween._next = child;
            } else {
                child._next = this._first;
                this._first = child;
            }
            if (child._next) {
                child._next._prev = child;
            } else {
                this._last = child;
            }
            child._prev = prevTween;
            if (this._timeline) {
                this._uncache(true);
            }
            return this;
        };

        p._remove = function(tween, skipDisable) {
            if (tween.timeline === this) {
                if (!skipDisable) {
                    tween._enabled(false, true);
                }
                tween.timeline = null;

                if (tween._prev) {
                    tween._prev._next = tween._next;
                } else if (this._first === tween) {
                    this._first = tween._next;
                }
                if (tween._next) {
                    tween._next._prev = tween._prev;
                } else if (this._last === tween) {
                    this._last = tween._prev;
                }

                if (this._timeline) {
                    this._uncache(true);
                }
            }
            return this;
        };

        p.render = function(time, suppressEvents, force) {
            var tween = this._first,
                next;
            this._totalTime = this._time = this._rawPrevTime = time;
            while (tween) {
                next = tween._next; //record it here because the value could change after rendering...
                if (tween._active || (time >= tween._startTime && !tween._paused)) {
                    if (!tween._reversed) {
                        tween.render((time - tween._startTime) * tween._timeScale, suppressEvents, force);
                    } else {
                        tween.render(((!tween._dirty) ? tween._totalDuration : tween.totalDuration()) - ((time - tween._startTime) * tween._timeScale), suppressEvents, force);
                    }
                }
                tween = next;
            }
        };

        p.rawTime = function() {
            if (!_tickerActive) {
                _ticker.wake();
            }
            return this._totalTime;
        };

/*
 * ----------------------------------------------------------------
 * TweenLite
 * ----------------------------------------------------------------
 */
        var TweenLite = _class("TweenLite", function(target, duration, vars) {
                Animation.call(this, duration, vars);
                this.render = TweenLite.prototype.render; //speed optimization (avoid prototype lookup on this "hot" method)

                if (target == null) {
                    throw "Cannot tween a null target.";
                }

                this.target = target = (typeof(target) !== "string") ? target : TweenLite.selector(target) || target;

                var isSelector = (target.jquery || (target.length && target !== window && target[0] && (target[0] === window || (target[0].nodeType && target[0].style && !target.nodeType)))),
                    overwrite = this.vars.overwrite,
                    i, targ, targets;

                this._overwrite = overwrite = (overwrite == null) ? _overwriteLookup[TweenLite.defaultOverwrite] : (typeof(overwrite) === "number") ? overwrite >> 0 : _overwriteLookup[overwrite];

                if ((isSelector || target instanceof Array || (target.push && _isArray(target))) && typeof(target[0]) !== "number") {
                    this._targets = targets = _slice.call(target, 0);
                    this._propLookup = [];
                    this._siblings = [];
                    for (i = 0; i < targets.length; i++) {
                        targ = targets[i];
                        if (!targ) {
                            targets.splice(i--, 1);
                            continue;
                        } else if (typeof(targ) === "string") {
                            targ = targets[i--] = TweenLite.selector(targ); //in case it's an array of strings
                            if (typeof(targ) === "string") {
                                targets.splice(i+1, 1); //to avoid an endless loop (can't imagine why the selector would return a string, but just in case)
                            }
                            continue;
                        } else if (targ.length && targ !== window && targ[0] && (targ[0] === window || (targ[0].nodeType && targ[0].style && !targ.nodeType))) { //in case the user is passing in an array of selector objects (like jQuery objects), we need to check one more level and pull things out if necessary. Also note that <select> elements pass all the criteria regarding length and the first child having style, so we must also check to ensure the target isn't an HTML node itself.
                            targets.splice(i--, 1);
                            this._targets = targets = targets.concat(_slice.call(targ, 0));
                            continue;
                        }
                        this._siblings[i] = _register(targ, this, false);
                        if (overwrite === 1) if (this._siblings[i].length > 1) {
                            _applyOverwrite(targ, this, null, 1, this._siblings[i]);
                        }
                    }

                } else {
                    this._propLookup = {};
                    this._siblings = _register(target, this, false);
                    if (overwrite === 1) if (this._siblings.length > 1) {
                        _applyOverwrite(target, this, null, 1, this._siblings);
                    }
                }
                if (this.vars.immediateRender || (duration === 0 && this._delay === 0 && this.vars.immediateRender !== false)) {
                    this._time = -_tinyNum; //forces a render without having to set the render() "force" parameter to true because we want to allow lazying by default (using the "force" parameter always forces an immediate full render)
                    this.render(-this._delay);
                }
            }, true),
            _isSelector = function(v) {
                return (v.length && v !== window && v[0] && (v[0] === window || (v[0].nodeType && v[0].style && !v.nodeType))); //we cannot check "nodeType" if the target is window from within an iframe, otherwise it will trigger a security error in some browsers like Firefox.
            },
            _autoCSS = function(vars, target) {
                var css = {},
                    p;
                for (p in vars) {
                    if (!_reservedProps[p] && (!(p in target) || p === "transform" || p === "x" || p === "y" || p === "width" || p === "height" || p === "className" || p === "border") && (!_plugins[p] || (_plugins[p] && _plugins[p]._autoCSS))) { //note: <img> elements contain read-only "x" and "y" properties. We should also prioritize editing css width/height rather than the element's properties.
                        css[p] = vars[p];
                        delete vars[p];
                    }
                }
                vars.css = css;
            };

        p = TweenLite.prototype = new Animation();
        p.constructor = TweenLite;
        p.kill()._gc = false;

//----TweenLite defaults, overwrite management, and root updates ----------------------------------------------------

        p.ratio = 0;
        p._firstPT = p._targets = p._overwrittenProps = p._startAt = null;
        p._notifyPluginsOfEnabled = p._lazy = false;

        TweenLite.version = "1.12.1";
        TweenLite.defaultEase = p._ease = new Ease(null, null, 1, 1);
        TweenLite.defaultOverwrite = "auto";
        TweenLite.ticker = _ticker;
        TweenLite.autoSleep = true;
        TweenLite.lagSmoothing = function(threshold, adjustedLag) {
            _ticker.lagSmoothing(threshold, adjustedLag);
        };
        TweenLite.selector = window.$ || window.jQuery || function(e) { if (window.$) { TweenLite.selector = window.$; return window.$(e); } return window.document ? window.document.getElementById((e.charAt(0) === "#") ? e.substr(1) : e) : e; };

        var _lazyTweens = [],
            _lazyLookup = {},
            _internals = TweenLite._internals = {isArray:_isArray, isSelector:_isSelector, lazyTweens:_lazyTweens}, //gives us a way to expose certain private values to other GreenSock classes without contaminating tha main TweenLite object.
            _plugins = TweenLite._plugins = {},
            _tweenLookup = _internals.tweenLookup = {},
            _tweenLookupNum = 0,
            _reservedProps = _internals.reservedProps = {ease:1, delay:1, overwrite:1, onComplete:1, onCompleteParams:1, onCompleteScope:1, useFrames:1, runBackwards:1, startAt:1, onUpdate:1, onUpdateParams:1, onUpdateScope:1, onStart:1, onStartParams:1, onStartScope:1, onReverseComplete:1, onReverseCompleteParams:1, onReverseCompleteScope:1, onRepeat:1, onRepeatParams:1, onRepeatScope:1, easeParams:1, yoyo:1, immediateRender:1, repeat:1, repeatDelay:1, data:1, paused:1, reversed:1, autoCSS:1, lazy:1},
            _overwriteLookup = {none:0, all:1, auto:2, concurrent:3, allOnStart:4, preexisting:5, "true":1, "false":0},
            _rootFramesTimeline = Animation._rootFramesTimeline = new SimpleTimeline(),
            _rootTimeline = Animation._rootTimeline = new SimpleTimeline(),
            _lazyRender = function() {
                var i = _lazyTweens.length;
                _lazyLookup = {};
                while (--i > -1) {
                    a = _lazyTweens[i];
                    if (a && a._lazy !== false) {
                        a.render(a._lazy, false, true);
                        a._lazy = false;
                    }
                }
                _lazyTweens.length = 0;
            };

        _rootTimeline._startTime = _ticker.time;
        _rootFramesTimeline._startTime = _ticker.frame;
        _rootTimeline._active = _rootFramesTimeline._active = true;
        setTimeout(_lazyRender, 1); //on some mobile devices, there isn't a "tick" before code runs which means any lazy renders wouldn't run before the next official "tick".

        Animation._updateRoot = TweenLite.render = function() {
                var i, a, p;
                if (_lazyTweens.length) { //if code is run outside of the requestAnimationFrame loop, there may be tweens queued AFTER the engine refreshed, so we need to ensure any pending renders occur before we refresh again.
                    _lazyRender();
                }
                _rootTimeline.render((_ticker.time - _rootTimeline._startTime) * _rootTimeline._timeScale, false, false);
                _rootFramesTimeline.render((_ticker.frame - _rootFramesTimeline._startTime) * _rootFramesTimeline._timeScale, false, false);
                if (_lazyTweens.length) {
                    _lazyRender();
                }
                if (!(_ticker.frame % 120)) { //dump garbage every 120 frames...
                    for (p in _tweenLookup) {
                        a = _tweenLookup[p].tweens;
                        i = a.length;
                        while (--i > -1) {
                            if (a[i]._gc) {
                                a.splice(i, 1);
                            }
                        }
                        if (a.length === 0) {
                            delete _tweenLookup[p];
                        }
                    }
                    //if there are no more tweens in the root timelines, or if they're all paused, make the _timer sleep to reduce load on the CPU slightly
                    p = _rootTimeline._first;
                    if (!p || p._paused) if (TweenLite.autoSleep && !_rootFramesTimeline._first && _ticker._listeners.tick.length === 1) {
                        while (p && p._paused) {
                            p = p._next;
                        }
                        if (!p) {
                            _ticker.sleep();
                        }
                    }
                }
            };

        _ticker.addEventListener("tick", Animation._updateRoot);

        var _register = function(target, tween, scrub) {
                var id = target._gsTweenID, a, i;
                if (!_tweenLookup[id || (target._gsTweenID = id = "t" + (_tweenLookupNum++))]) {
                    _tweenLookup[id] = {target:target, tweens:[]};
                }
                if (tween) {
                    a = _tweenLookup[id].tweens;
                    a[(i = a.length)] = tween;
                    if (scrub) {
                        while (--i > -1) {
                            if (a[i] === tween) {
                                a.splice(i, 1);
                            }
                        }
                    }
                }
                return _tweenLookup[id].tweens;
            },

            _applyOverwrite = function(target, tween, props, mode, siblings) {
                var i, changed, curTween, l;
                if (mode === 1 || mode >= 4) {
                    l = siblings.length;
                    for (i = 0; i < l; i++) {
                        if ((curTween = siblings[i]) !== tween) {
                            if (!curTween._gc) if (curTween._enabled(false, false)) {
                                changed = true;
                            }
                        } else if (mode === 5) {
                            break;
                        }
                    }
                    return changed;
                }
                //NOTE: Add 0.0000000001 to overcome floating point errors that can cause the startTime to be VERY slightly off (when a tween's time() is set for example)
                var startTime = tween._startTime + _tinyNum,
                    overlaps = [],
                    oCount = 0,
                    zeroDur = (tween._duration === 0),
                    globalStart;
                i = siblings.length;
                while (--i > -1) {
                    if ((curTween = siblings[i]) === tween || curTween._gc || curTween._paused) {
                        //ignore
                    } else if (curTween._timeline !== tween._timeline) {
                        globalStart = globalStart || _checkOverlap(tween, 0, zeroDur);
                        if (_checkOverlap(curTween, globalStart, zeroDur) === 0) {
                            overlaps[oCount++] = curTween;
                        }
                    } else if (curTween._startTime <= startTime) if (curTween._startTime + curTween.totalDuration() / curTween._timeScale > startTime) if (!((zeroDur || !curTween._initted) && startTime - curTween._startTime <= 0.0000000002)) {
                        overlaps[oCount++] = curTween;
                    }
                }

                i = oCount;
                while (--i > -1) {
                    curTween = overlaps[i];
                    if (mode === 2) if (curTween._kill(props, target)) {
                        changed = true;
                    }
                    if (mode !== 2 || (!curTween._firstPT && curTween._initted)) {
                        if (curTween._enabled(false, false)) { //if all property tweens have been overwritten, kill the tween.
                            changed = true;
                        }
                    }
                }
                return changed;
            },

            _checkOverlap = function(tween, reference, zeroDur) {
                var tl = tween._timeline,
                    ts = tl._timeScale,
                    t = tween._startTime;
                while (tl._timeline) {
                    t += tl._startTime;
                    ts *= tl._timeScale;
                    if (tl._paused) {
                        return -100;
                    }
                    tl = tl._timeline;
                }
                t /= ts;
                return (t > reference) ? t - reference : ((zeroDur && t === reference) || (!tween._initted && t - reference < 2 * _tinyNum)) ? _tinyNum : ((t += tween.totalDuration() / tween._timeScale / ts) > reference + _tinyNum) ? 0 : t - reference - _tinyNum;
            };


//---- TweenLite instance methods -----------------------------------------------------------------------------

        p._init = function() {
            var v = this.vars,
                op = this._overwrittenProps,
                dur = this._duration,
                immediate = !!v.immediateRender,
                ease = v.ease,
                i, initPlugins, pt, p, startVars;
            if (v.startAt) {
                if (this._startAt) {
                    this._startAt.render(-1, true); //if we've run a startAt previously (when the tween instantiated), we should revert it so that the values re-instantiate correctly particularly for relative tweens. Without this, a TweenLite.fromTo(obj, 1, {x:"+=100"}, {x:"-=100"}), for example, would actually jump to +=200 because the startAt would run twice, doubling the relative change.
                    this._startAt.kill();
                }
                startVars = {};
                for (p in v.startAt) { //copy the properties/values into a new object to avoid collisions, like var to = {x:0}, from = {x:500}; timeline.fromTo(e, 1, from, to).fromTo(e, 1, to, from);
                    startVars[p] = v.startAt[p];
                }
                startVars.overwrite = false;
                startVars.immediateRender = true;
                startVars.lazy = (immediate && v.lazy !== false);
                startVars.startAt = startVars.delay = null; //no nesting of startAt objects allowed (otherwise it could cause an infinite loop).
                this._startAt = TweenLite.to(this.target, 0, startVars);
                if (immediate) {
                    if (this._time > 0) {
                        this._startAt = null; //tweens that render immediately (like most from() and fromTo() tweens) shouldn't revert when their parent timeline's playhead goes backward past the startTime because the initial render could have happened anytime and it shouldn't be directly correlated to this tween's startTime. Imagine setting up a complex animation where the beginning states of various objects are rendered immediately but the tween doesn't happen for quite some time - if we revert to the starting values as soon as the playhead goes backward past the tween's startTime, it will throw things off visually. Reversion should only happen in TimelineLite/Max instances where immediateRender was false (which is the default in the convenience methods like from()).
                    } else if (dur !== 0) {
                        return; //we skip initialization here so that overwriting doesn't occur until the tween actually begins. Otherwise, if you create several immediateRender:true tweens of the same target/properties to drop into a TimelineLite or TimelineMax, the last one created would overwrite the first ones because they didn't get placed into the timeline yet before the first render occurs and kicks in overwriting.
                    }
                }
            } else if (v.runBackwards && dur !== 0) {
                //from() tweens must be handled uniquely: their beginning values must be rendered but we don't want overwriting to occur yet (when time is still 0). Wait until the tween actually begins before doing all the routines like overwriting. At that time, we should render at the END of the tween to ensure that things initialize correctly (remember, from() tweens go backwards)
                if (this._startAt) {
                    this._startAt.render(-1, true);
                    this._startAt.kill();
                    this._startAt = null;
                } else {
                    pt = {};
                    for (p in v) { //copy props into a new object and skip any reserved props, otherwise onComplete or onUpdate or onStart could fire. We should, however, permit autoCSS to go through.
                        if (!_reservedProps[p] || p === "autoCSS") {
                            pt[p] = v[p];
                        }
                    }
                    pt.overwrite = 0;
                    pt.data = "isFromStart"; //we tag the tween with as "isFromStart" so that if [inside a plugin] we need to only do something at the very END of a tween, we have a way of identifying this tween as merely the one that's setting the beginning values for a "from()" tween. For example, clearProps in CSSPlugin should only get applied at the very END of a tween and without this tag, from(...{height:100, clearProps:"height", delay:1}) would wipe the height at the beginning of the tween and after 1 second, it'd kick back in.
                    pt.lazy = (immediate && v.lazy !== false);
                    pt.immediateRender = immediate; //zero-duration tweens render immediately by default, but if we're not specifically instructed to render this tween immediately, we should skip this and merely _init() to record the starting values (rendering them immediately would push them to completion which is wasteful in that case - we'd have to render(-1) immediately after)
                    this._startAt = TweenLite.to(this.target, 0, pt);
                    if (!immediate) {
                        this._startAt._init(); //ensures that the initial values are recorded
                        this._startAt._enabled(false); //no need to have the tween render on the next cycle. Disable it because we'll always manually control the renders of the _startAt tween.
                    } else if (this._time === 0) {
                        return;
                    }
                }
            }
            if (!ease) {
                this._ease = TweenLite.defaultEase;
            } else if (ease instanceof Ease) {
                this._ease = (v.easeParams instanceof Array) ? ease.config.apply(ease, v.easeParams) : ease;
            } else {
                this._ease = (typeof(ease) === "function") ? new Ease(ease, v.easeParams) : _easeMap[ease] || TweenLite.defaultEase;
            }
            this._easeType = this._ease._type;
            this._easePower = this._ease._power;
            this._firstPT = null;

            if (this._targets) {
                i = this._targets.length;
                while (--i > -1) {
                    if ( this._initProps( this._targets[i], (this._propLookup[i] = {}), this._siblings[i], (op ? op[i] : null)) ) {
                        initPlugins = true;
                    }
                }
            } else {
                initPlugins = this._initProps(this.target, this._propLookup, this._siblings, op);
            }

            if (initPlugins) {
                TweenLite._onPluginEvent("_onInitAllProps", this); //reorders the array in order of priority. Uses a static TweenPlugin method in order to minimize file size in TweenLite
            }
            if (op) if (!this._firstPT) if (typeof(this.target) !== "function") { //if all tweening properties have been overwritten, kill the tween. If the target is a function, it's probably a delayedCall so let it live.
                this._enabled(false, false);
            }
            if (v.runBackwards) {
                pt = this._firstPT;
                while (pt) {
                    pt.s += pt.c;
                    pt.c = -pt.c;
                    pt = pt._next;
                }
            }
            this._onUpdate = v.onUpdate;
            this._initted = true;
        };

        p._initProps = function(target, propLookup, siblings, overwrittenProps) {
            var p, i, initPlugins, plugin, pt, v;
            if (target == null) {
                return false;
            }

            if (_lazyLookup[target._gsTweenID]) {
                _lazyRender(); //if other tweens of the same target have recently initted but haven't rendered yet, we've got to force the render so that the starting values are correct (imagine populating a timeline with a bunch of sequential tweens and then jumping to the end)
            }

            if (!this.vars.css) if (target.style) if (target !== window && target.nodeType) if (_plugins.css) if (this.vars.autoCSS !== false) { //it's so common to use TweenLite/Max to animate the css of DOM elements, we assume that if the target is a DOM element, that's what is intended (a convenience so that users don't have to wrap things in css:{}, although we still recommend it for a slight performance boost and better specificity). Note: we cannot check "nodeType" on the window inside an iframe.
                _autoCSS(this.vars, target);
            }
            for (p in this.vars) {
                v = this.vars[p];
                if (_reservedProps[p]) {
                    if (v) if ((v instanceof Array) || (v.push && _isArray(v))) if (v.join("").indexOf("{self}") !== -1) {
                        this.vars[p] = v = this._swapSelfInParams(v, this);
                    }

                } else if (_plugins[p] && (plugin = new _plugins[p]())._onInitTween(target, this.vars[p], this)) {

                    //t - target        [object]
                    //p - property      [string]
                    //s - start         [number]
                    //c - change        [number]
                    //f - isFunction    [boolean]
                    //n - name          [string]
                    //pg - isPlugin     [boolean]
                    //pr - priority     [number]
                    this._firstPT = pt = {_next:this._firstPT, t:plugin, p:"setRatio", s:0, c:1, f:true, n:p, pg:true, pr:plugin._priority};
                    i = plugin._overwriteProps.length;
                    while (--i > -1) {
                        propLookup[plugin._overwriteProps[i]] = this._firstPT;
                    }
                    if (plugin._priority || plugin._onInitAllProps) {
                        initPlugins = true;
                    }
                    if (plugin._onDisable || plugin._onEnable) {
                        this._notifyPluginsOfEnabled = true;
                    }

                } else {
                    this._firstPT = propLookup[p] = pt = {_next:this._firstPT, t:target, p:p, f:(typeof(target[p]) === "function"), n:p, pg:false, pr:0};
                    pt.s = (!pt.f) ? parseFloat(target[p]) : target[ ((p.indexOf("set") || typeof(target["get" + p.substr(3)]) !== "function") ? p : "get" + p.substr(3)) ]();
                    pt.c = (typeof(v) === "string" && v.charAt(1) === "=") ? parseInt(v.charAt(0) + "1", 10) * Number(v.substr(2)) : (Number(v) - pt.s) || 0;
                }
                if (pt) if (pt._next) {
                    pt._next._prev = pt;
                }
            }

            if (overwrittenProps) if (this._kill(overwrittenProps, target)) { //another tween may have tried to overwrite properties of this tween before init() was called (like if two tweens start at the same time, the one created second will run first)
                return this._initProps(target, propLookup, siblings, overwrittenProps);
            }
            if (this._overwrite > 1) if (this._firstPT) if (siblings.length > 1) if (_applyOverwrite(target, this, propLookup, this._overwrite, siblings)) {
                this._kill(propLookup, target);
                return this._initProps(target, propLookup, siblings, overwrittenProps);
            }
            if (this._firstPT) if ((this.vars.lazy !== false && this._duration) || (this.vars.lazy && !this._duration)) { //zero duration tweens don't lazy render by default; everything else does.
                _lazyLookup[target._gsTweenID] = true;
            }
            return initPlugins;
        };

        p.render = function(time, suppressEvents, force) {
            var prevTime = this._time,
                duration = this._duration,
                prevRawPrevTime = this._rawPrevTime,
                isComplete, callback, pt, rawPrevTime;
            if (time >= duration) {
                this._totalTime = this._time = duration;
                this.ratio = this._ease._calcEnd ? this._ease.getRatio(1) : 1;
                if (!this._reversed ) {
                    isComplete = true;
                    callback = "onComplete";
                }
                if (duration === 0) if (this._initted || !this.vars.lazy || force) { //zero-duration tweens are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                    if (this._startTime === this._timeline._duration) { //if a zero-duration tween is at the VERY end of a timeline and that timeline renders at its end, it will typically add a tiny bit of cushion to the render time to prevent rounding errors from getting in the way of tweens rendering their VERY end. If we then reverse() that timeline, the zero-duration tween will trigger its onReverseComplete even though technically the playhead didn't pass over it again. It's a very specific edge case we must accommodate.
                        time = 0;
                    }
                    if (time === 0 || prevRawPrevTime < 0 || prevRawPrevTime === _tinyNum) if (prevRawPrevTime !== time) {
                        force = true;
                        if (prevRawPrevTime > _tinyNum) {
                            callback = "onReverseComplete";
                        }
                    }
                    this._rawPrevTime = rawPrevTime = (!suppressEvents || time || prevRawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                }

            } else if (time < 0.0000001) { //to work around occasional floating point math artifacts, round super small values to 0.
                this._totalTime = this._time = 0;
                this.ratio = this._ease._calcEnd ? this._ease.getRatio(0) : 0;
                if (prevTime !== 0 || (duration === 0 && prevRawPrevTime > 0 && prevRawPrevTime !== _tinyNum)) {
                    callback = "onReverseComplete";
                    isComplete = this._reversed;
                }
                if (time < 0) {
                    this._active = false;
                    if (duration === 0) if (this._initted || !this.vars.lazy || force) { //zero-duration tweens are tricky because we must discern the momentum/direction of time in order to determine whether the starting values should be rendered or the ending values. If the "playhead" of its timeline goes past the zero-duration tween in the forward direction or lands directly on it, the end values should be rendered, but if the timeline's "playhead" moves past it in the backward direction (from a postitive time to a negative time), the starting values must be rendered.
                        if (prevRawPrevTime >= 0) {
                            force = true;
                        }
                        this._rawPrevTime = rawPrevTime = (!suppressEvents || time || prevRawPrevTime === time) ? time : _tinyNum; //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect. We set the _rawPrevTime to be a precise tiny number to indicate this scenario rather than using another property/variable which would increase memory usage. This technique is less readable, but more efficient.
                    }
                } else if (!this._initted) { //if we render the very beginning (time == 0) of a fromTo(), we must force the render (normal tweens wouldn't need to render at a time of 0 when the prevTime was also 0). This is also mandatory to make sure overwriting kicks in immediately.
                    force = true;
                }
            } else {
                this._totalTime = this._time = time;

                if (this._easeType) {
                    var r = time / duration, type = this._easeType, pow = this._easePower;
                    if (type === 1 || (type === 3 && r >= 0.5)) {
                        r = 1 - r;
                    }
                    if (type === 3) {
                        r *= 2;
                    }
                    if (pow === 1) {
                        r *= r;
                    } else if (pow === 2) {
                        r *= r * r;
                    } else if (pow === 3) {
                        r *= r * r * r;
                    } else if (pow === 4) {
                        r *= r * r * r * r;
                    }

                    if (type === 1) {
                        this.ratio = 1 - r;
                    } else if (type === 2) {
                        this.ratio = r;
                    } else if (time / duration < 0.5) {
                        this.ratio = r / 2;
                    } else {
                        this.ratio = 1 - (r / 2);
                    }

                } else {
                    this.ratio = this._ease.getRatio(time / duration);
                }
            }

            if (this._time === prevTime && !force) {
                return;
            } else if (!this._initted) {
                this._init();
                if (!this._initted || this._gc) { //immediateRender tweens typically won't initialize until the playhead advances (_time is greater than 0) in order to ensure that overwriting occurs properly. Also, if all of the tweening properties have been overwritten (which would cause _gc to be true, as set in _init()), we shouldn't continue otherwise an onStart callback could be called for example.
                    return;
                } else if (!force && this._firstPT && ((this.vars.lazy !== false && this._duration) || (this.vars.lazy && !this._duration))) {
                    this._time = this._totalTime = prevTime;
                    this._rawPrevTime = prevRawPrevTime;
                    _lazyTweens.push(this);
                    this._lazy = time;
                    return;
                }
                //_ease is initially set to defaultEase, so now that init() has run, _ease is set properly and we need to recalculate the ratio. Overall this is faster than using conditional logic earlier in the method to avoid having to set ratio twice because we only init() once but renderTime() gets called VERY frequently.
                if (this._time && !isComplete) {
                    this.ratio = this._ease.getRatio(this._time / duration);
                } else if (isComplete && this._ease._calcEnd) {
                    this.ratio = this._ease.getRatio((this._time === 0) ? 0 : 1);
                }
            }
            if (this._lazy !== false) { //in case a lazy render is pending, we should flush it because the new render is occuring now (imagine a lazy tween instantiating and then immediately the user calls tween.seek(tween.duration()), skipping to the end - the end render would be forced, and then if we didn't flush the lazy render, it'd fire AFTER the seek(), rendering it at the wrong time.
                this._lazy = false;
            }
            if (!this._active) if (!this._paused && this._time !== prevTime && time >= 0) {
                this._active = true;  //so that if the user renders a tween (as opposed to the timeline rendering it), the timeline is forced to re-render and align it with the proper time/frame on the next rendering cycle. Maybe the tween already finished but the user manually re-renders it as halfway done.
            }
            if (prevTime === 0) {
                if (this._startAt) {
                    if (time >= 0) {
                        this._startAt.render(time, suppressEvents, force);
                    } else if (!callback) {
                        callback = "_dummyGS"; //if no callback is defined, use a dummy value just so that the condition at the end evaluates as true because _startAt should render AFTER the normal render loop when the time is negative. We could handle this in a more intuitive way, of course, but the render loop is the MOST important thing to optimize, so this technique allows us to avoid adding extra conditional logic in a high-frequency area.
                    }
                }
                if (this.vars.onStart) if (this._time !== 0 || duration === 0) if (!suppressEvents) {
                    this.vars.onStart.apply(this.vars.onStartScope || this, this.vars.onStartParams || _blankArray);
                }
            }

            pt = this._firstPT;
            while (pt) {
                if (pt.f) {
                    pt.t[pt.p](pt.c * this.ratio + pt.s);
                } else {
                    pt.t[pt.p] = pt.c * this.ratio + pt.s;
                }
                pt = pt._next;
            }

            if (this._onUpdate) {
                if (time < 0) if (this._startAt && this._startTime) { //if the tween is positioned at the VERY beginning (_startTime 0) of its parent timeline, it's illegal for the playhead to go back further, so we should not render the recorded startAt values.
                    this._startAt.render(time, suppressEvents, force); //note: for performance reasons, we tuck this conditional logic inside less traveled areas (most tweens don't have an onUpdate). We'd just have it at the end before the onComplete, but the values should be updated before any onUpdate is called, so we ALSO put it here and then if it's not called, we do so later near the onComplete.
                }
                if (!suppressEvents) if (this._time !== prevTime || isComplete) {
                    this._onUpdate.apply(this.vars.onUpdateScope || this, this.vars.onUpdateParams || _blankArray);
                }
            }

            if (callback) if (!this._gc) { //check _gc because there's a chance that kill() could be called in an onUpdate
                if (time < 0 && this._startAt && !this._onUpdate && this._startTime) { //if the tween is positioned at the VERY beginning (_startTime 0) of its parent timeline, it's illegal for the playhead to go back further, so we should not render the recorded startAt values.
                    this._startAt.render(time, suppressEvents, force);
                }
                if (isComplete) {
                    if (this._timeline.autoRemoveChildren) {
                        this._enabled(false, false);
                    }
                    this._active = false;
                }
                if (!suppressEvents && this.vars[callback]) {
                    this.vars[callback].apply(this.vars[callback + "Scope"] || this, this.vars[callback + "Params"] || _blankArray);
                }
                if (duration === 0 && this._rawPrevTime === _tinyNum && rawPrevTime !== _tinyNum) { //the onComplete or onReverseComplete could trigger movement of the playhead and for zero-duration tweens (which must discern direction) that land directly back on their start time, we don't want to fire again on the next render. Think of several addPause()'s in a timeline that forces the playhead to a certain spot, but what if it's already paused and another tween is tweening the "time" of the timeline? Each time it moves [forward] past that spot, it would move back, and since suppressEvents is true, it'd reset _rawPrevTime to _tinyNum so that when it begins again, the callback would fire (so ultimately it could bounce back and forth during that tween). Again, this is a very uncommon scenario, but possible nonetheless.
                    this._rawPrevTime = 0;
                }
            }

        };

        p._kill = function(vars, target) {
            if (vars === "all") {
                vars = null;
            }
            if (vars == null) if (target == null || target === this.target) {
                this._lazy = false;
                return this._enabled(false, false);
            }
            target = (typeof(target) !== "string") ? (target || this._targets || this.target) : TweenLite.selector(target) || target;
            var i, overwrittenProps, p, pt, propLookup, changed, killProps, record;
            if ((_isArray(target) || _isSelector(target)) && typeof(target[0]) !== "number") {
                i = target.length;
                while (--i > -1) {
                    if (this._kill(vars, target[i])) {
                        changed = true;
                    }
                }
            } else {
                if (this._targets) {
                    i = this._targets.length;
                    while (--i > -1) {
                        if (target === this._targets[i]) {
                            propLookup = this._propLookup[i] || {};
                            this._overwrittenProps = this._overwrittenProps || [];
                            overwrittenProps = this._overwrittenProps[i] = vars ? this._overwrittenProps[i] || {} : "all";
                            break;
                        }
                    }
                } else if (target !== this.target) {
                    return false;
                } else {
                    propLookup = this._propLookup;
                    overwrittenProps = this._overwrittenProps = vars ? this._overwrittenProps || {} : "all";
                }

                if (propLookup) {
                    killProps = vars || propLookup;
                    record = (vars !== overwrittenProps && overwrittenProps !== "all" && vars !== propLookup && (typeof(vars) !== "object" || !vars._tempKill)); //_tempKill is a super-secret way to delete a particular tweening property but NOT have it remembered as an official overwritten property (like in BezierPlugin)
                    for (p in killProps) {
                        if ((pt = propLookup[p])) {
                            if (pt.pg && pt.t._kill(killProps)) {
                                changed = true; //some plugins need to be notified so they can perform cleanup tasks first
                            }
                            if (!pt.pg || pt.t._overwriteProps.length === 0) {
                                if (pt._prev) {
                                    pt._prev._next = pt._next;
                                } else if (pt === this._firstPT) {
                                    this._firstPT = pt._next;
                                }
                                if (pt._next) {
                                    pt._next._prev = pt._prev;
                                }
                                pt._next = pt._prev = null;
                            }
                            delete propLookup[p];
                        }
                        if (record) {
                            overwrittenProps[p] = 1;
                        }
                    }
                    if (!this._firstPT && this._initted) { //if all tweening properties are killed, kill the tween. Without this line, if there's a tween with multiple targets and then you killTweensOf() each target individually, the tween would technically still remain active and fire its onComplete even though there aren't any more properties tweening.
                        this._enabled(false, false);
                    }
                }
            }
            return changed;
        };

        p.invalidate = function() {
            if (this._notifyPluginsOfEnabled) {
                TweenLite._onPluginEvent("_onDisable", this);
            }
            this._firstPT = null;
            this._overwrittenProps = null;
            this._onUpdate = null;
            this._startAt = null;
            this._initted = this._active = this._notifyPluginsOfEnabled = this._lazy = false;
            this._propLookup = (this._targets) ? {} : [];
            return this;
        };

        p._enabled = function(enabled, ignoreTimeline) {
            if (!_tickerActive) {
                _ticker.wake();
            }
            if (enabled && this._gc) {
                var targets = this._targets,
                    i;
                if (targets) {
                    i = targets.length;
                    while (--i > -1) {
                        this._siblings[i] = _register(targets[i], this, true);
                    }
                } else {
                    this._siblings = _register(this.target, this, true);
                }
            }
            Animation.prototype._enabled.call(this, enabled, ignoreTimeline);
            if (this._notifyPluginsOfEnabled) if (this._firstPT) {
                return TweenLite._onPluginEvent((enabled ? "_onEnable" : "_onDisable"), this);
            }
            return false;
        };


//----TweenLite static methods -----------------------------------------------------

        TweenLite.to = function(target, duration, vars) {
            return new TweenLite(target, duration, vars);
        };

        TweenLite.from = function(target, duration, vars) {
            vars.runBackwards = true;
            vars.immediateRender = (vars.immediateRender != false);
            return new TweenLite(target, duration, vars);
        };

        TweenLite.fromTo = function(target, duration, fromVars, toVars) {
            toVars.startAt = fromVars;
            toVars.immediateRender = (toVars.immediateRender != false && fromVars.immediateRender != false);
            return new TweenLite(target, duration, toVars);
        };

        TweenLite.delayedCall = function(delay, callback, params, scope, useFrames) {
            return new TweenLite(callback, 0, {delay:delay, onComplete:callback, onCompleteParams:params, onCompleteScope:scope, onReverseComplete:callback, onReverseCompleteParams:params, onReverseCompleteScope:scope, immediateRender:false, useFrames:useFrames, overwrite:0});
        };

        TweenLite.set = function(target, vars) {
            return new TweenLite(target, 0, vars);
        };

        TweenLite.getTweensOf = function(target, onlyActive) {
            if (target == null) { return []; }
            target = (typeof(target) !== "string") ? target : TweenLite.selector(target) || target;
            var i, a, j, t;
            if ((_isArray(target) || _isSelector(target)) && typeof(target[0]) !== "number") {
                i = target.length;
                a = [];
                while (--i > -1) {
                    a = a.concat(TweenLite.getTweensOf(target[i], onlyActive));
                }
                i = a.length;
                //now get rid of any duplicates (tweens of arrays of objects could cause duplicates)
                while (--i > -1) {
                    t = a[i];
                    j = i;
                    while (--j > -1) {
                        if (t === a[j]) {
                            a.splice(i, 1);
                        }
                    }
                }
            } else {
                a = _register(target).concat();
                i = a.length;
                while (--i > -1) {
                    if (a[i]._gc || (onlyActive && !a[i].isActive())) {
                        a.splice(i, 1);
                    }
                }
            }
            return a;
        };

        TweenLite.killTweensOf = TweenLite.killDelayedCallsTo = function(target, onlyActive, vars) {
            if (typeof(onlyActive) === "object") {
                vars = onlyActive; //for backwards compatibility (before "onlyActive" parameter was inserted)
                onlyActive = false;
            }
            var a = TweenLite.getTweensOf(target, onlyActive),
                i = a.length;
            while (--i > -1) {
                a[i]._kill(vars, target);
            }
        };



/*
 * ----------------------------------------------------------------
 * TweenPlugin   (could easily be split out as a separate file/class, but included for ease of use (so that people don't need to include another <script> call before loading plugins which is easy to forget)
 * ----------------------------------------------------------------
 */
        var TweenPlugin = _class("plugins.TweenPlugin", function(props, priority) {
                    this._overwriteProps = (props || "").split(",");
                    this._propName = this._overwriteProps[0];
                    this._priority = priority || 0;
                    this._super = TweenPlugin.prototype;
                }, true);

        p = TweenPlugin.prototype;
        TweenPlugin.version = "1.10.1";
        TweenPlugin.API = 2;
        p._firstPT = null;

        p._addTween = function(target, prop, start, end, overwriteProp, round) {
            var c, pt;
            if (end != null && (c = (typeof(end) === "number" || end.charAt(1) !== "=") ? Number(end) - start : parseInt(end.charAt(0) + "1", 10) * Number(end.substr(2)))) {
                this._firstPT = pt = {_next:this._firstPT, t:target, p:prop, s:start, c:c, f:(typeof(target[prop]) === "function"), n:overwriteProp || prop, r:round};
                if (pt._next) {
                    pt._next._prev = pt;
                }
                return pt;
            }
        };

        p.setRatio = function(v) {
            var pt = this._firstPT,
                min = 0.000001,
                val;
            while (pt) {
                val = pt.c * v + pt.s;
                if (pt.r) {
                    val = Math.round(val);
                } else if (val < min) if (val > -min) { //prevents issues with converting very small numbers to strings in the browser
                    val = 0;
                }
                if (pt.f) {
                    pt.t[pt.p](val);
                } else {
                    pt.t[pt.p] = val;
                }
                pt = pt._next;
            }
        };

        p._kill = function(lookup) {
            var a = this._overwriteProps,
                pt = this._firstPT,
                i;
            if (lookup[this._propName] != null) {
                this._overwriteProps = [];
            } else {
                i = a.length;
                while (--i > -1) {
                    if (lookup[a[i]] != null) {
                        a.splice(i, 1);
                    }
                }
            }
            while (pt) {
                if (lookup[pt.n] != null) {
                    if (pt._next) {
                        pt._next._prev = pt._prev;
                    }
                    if (pt._prev) {
                        pt._prev._next = pt._next;
                        pt._prev = null;
                    } else if (this._firstPT === pt) {
                        this._firstPT = pt._next;
                    }
                }
                pt = pt._next;
            }
            return false;
        };

        p._roundProps = function(lookup, value) {
            var pt = this._firstPT;
            while (pt) {
                if (lookup[this._propName] || (pt.n != null && lookup[ pt.n.split(this._propName + "_").join("") ])) { //some properties that are very plugin-specific add a prefix named after the _propName plus an underscore, so we need to ignore that extra stuff here.
                    pt.r = value;
                }
                pt = pt._next;
            }
        };

        TweenLite._onPluginEvent = function(type, tween) {
            var pt = tween._firstPT,
                changed, pt2, first, last, next;
            if (type === "_onInitAllProps") {
                //sorts the PropTween linked list in order of priority because some plugins need to render earlier/later than others, like MotionBlurPlugin applies its effects after all x/y/alpha tweens have rendered on each frame.
                while (pt) {
                    next = pt._next;
                    pt2 = first;
                    while (pt2 && pt2.pr > pt.pr) {
                        pt2 = pt2._next;
                    }
                    if ((pt._prev = pt2 ? pt2._prev : last)) {
                        pt._prev._next = pt;
                    } else {
                        first = pt;
                    }
                    if ((pt._next = pt2)) {
                        pt2._prev = pt;
                    } else {
                        last = pt;
                    }
                    pt = next;
                }
                pt = tween._firstPT = first;
            }
            while (pt) {
                if (pt.pg) if (typeof(pt.t[type]) === "function") if (pt.t[type]()) {
                    changed = true;
                }
                pt = pt._next;
            }
            return changed;
        };

        TweenPlugin.activate = function(plugins) {
            var i = plugins.length;
            while (--i > -1) {
                if (plugins[i].API === TweenPlugin.API) {
                    _plugins[(new plugins[i]())._propName] = plugins[i];
                }
            }
            return true;
        };

        //provides a more concise way to define plugins that have no dependencies besides TweenPlugin and TweenLite, wrapping common boilerplate stuff into one function (added in 1.9.0). You don't NEED to use this to define a plugin - the old way still works and can be useful in certain (rare) situations.
        _gsDefine.plugin = function(config) {
            if (!config || !config.propName || !config.init || !config.API) { throw "illegal plugin definition."; }
            var propName = config.propName,
                priority = config.priority || 0,
                overwriteProps = config.overwriteProps,
                map = {init:"_onInitTween", set:"setRatio", kill:"_kill", round:"_roundProps", initAll:"_onInitAllProps"},
                Plugin = _class("plugins." + propName.charAt(0).toUpperCase() + propName.substr(1) + "Plugin",
                    function() {
                        TweenPlugin.call(this, propName, priority);
                        this._overwriteProps = overwriteProps || [];
                    }, (config.global === true)),
                p = Plugin.prototype = new TweenPlugin(propName),
                prop;
            p.constructor = Plugin;
            Plugin.API = config.API;
            for (prop in map) {
                if (typeof(config[prop]) === "function") {
                    p[map[prop]] = config[prop];
                }
            }
            Plugin.version = config.version;
            TweenPlugin.activate([Plugin]);
            return Plugin;
        };


        //now run through all the dependencies discovered and if any are missing, log that to the console as a warning. This is why it's best to have TweenLite load last - it can check all the dependencies for you.
        a = window._gsQueue;
        if (a) {
            for (i = 0; i < a.length; i++) {
                a[i]();
            }
            for (p in _defLookup) {
                if (!_defLookup[p].func) {
                    //window.console.log("GSAP encountered missing dependency: com.greensock." + p);
                }
            }
        }

        _tickerActive = false; //ensures that the first official animation forces a ticker.tick() to update the time when it is instantiated

})(window);

angular.module('ddh.att.transition', [])

.factory('$transition', ['$q', '$timeout', '$rootScope', function($q, $timeout, $rootScope) {

  var $transition = function(element, trigger, options) {
    options = options || {};
    var deferred = $q.defer();
    var endEventName = $transition[options.animation ? "animationEndEventName" : "transitionEndEventName"];

    var transitionEndHandler = function() {
      $rootScope.$apply(function() {
        element.unbind(endEventName, transitionEndHandler);
        deferred.resolve(element);
      });
    };

    if (endEventName) {
      element.bind(endEventName, transitionEndHandler);
    }

    // Wrap in a timeout to allow the browser time to update the DOM before the transition is to occur
    $timeout(function() {
      if ( angular.isString(trigger) ) {
        element.addClass(trigger);
      } else if ( angular.isFunction(trigger) ) {
        trigger(element);
      } else if ( angular.isObject(trigger) ) {
        element.css(trigger);
      }
      //If browser does not support transitions, instantly resolve
      if ( !endEventName ) {
        deferred.resolve(element);
      }
    }, 100);

    // Add our custom cancel function to the promise that is returned
    // We can call this if we are about to run a new transition, which we know will prevent this transition from ending,
    // i.e. it will therefore never raise a transitionEnd event for that transition
    deferred.promise.cancel = function() {
      if ( endEventName ) {
        element.unbind(endEventName, transitionEndHandler);
      }
      deferred.reject('Transition cancelled');
    };

    return deferred.promise;
  };

  // Work out the name of the transitionEnd event
  var transElement = document.createElement('trans');
  var transitionEndEventNames = {
    'WebkitTransition': 'webkitTransitionEnd',
    'MozTransition': 'transitionend',
    'OTransition': 'oTransitionEnd',
    'transition': 'transitionend'
  };
  var animationEndEventNames = {
    'WebkitTransition': 'webkitAnimationEnd',
    'MozTransition': 'animationend',
    'OTransition': 'oAnimationEnd',
    'transition': 'animationend'
  };
  function findEndEventName(endEventNames) {
    for (var name in endEventNames){
      if (transElement.style[name] !== undefined) {
        return endEventNames[name];
      }
    }
  }
  $transition.transitionEndEventName = findEndEventName(transitionEndEventNames);
  $transition.animationEndEventName = findEndEventName(animationEndEventNames);
  return $transition;
}])

.factory('$scrollTo', ['$window', function($window) {
    var $scrollTo = function(offsetLeft, offsetTop, duration) {
        TweenMax.to($window, duration || 1, {scrollTo: {y: offsetTop, x: offsetLeft}, ease: Power4.easeOut});
    };
    return $scrollTo;
}])
.factory('animation', function(){
    return TweenMax;
})
.factory('$progressBar', function(){

   //Provides a function to pass in code for closure purposes
   var loadingAnimationCreator = function(onUpdateCallback){

      //Use closure to setup some resuable code
      var loadingAnimation = function(callback, duration){
          TweenMax.to({}, duration, {
              onUpdateParams: ["{self}"],
              onUpdate: onUpdateCallback,
              onComplete: callback
          });
      };
      //Returns a function that takes a callback function and a duration for the animation
      return (function(){
        return loadingAnimation;
      })();
    };

  return loadingAnimationCreator;
})
.factory('$height', function(){
  var heightAnimation = function(element,duration,height,alpha){
    TweenMax.to(element,
      duration,
      {height:height, autoAlpha:alpha},
      0);
  };
  return heightAnimation;
});
angular.module('ddh.att.collapse', ['ddh.att.transition'])

// The collapsible directive indicates a block of html that will expand and collapse
.directive('ddhCollapse', ['$transition', function($transition) {
    // CSS transitions don't work with height: auto, so we have to manually change the height to a
    // specific value and then once the animation completes, we can reset the height to auto.
    // Unfortunately if you do this while the CSS transitions are specified (i.e. in the CSS class
    // "collapse") then you trigger a change to height 0 in between.
    // The fix is to remove the "collapse" CSS class while changing the height back to auto - phew!

    var props = {
        open: {
            marginTop: null,
            marginBottom: null,
            paddingTop: null,
            paddingBottom: null
        },
        closed: {
            marginTop: 0,
            marginBottom: 0,
            paddingTop: 0,
            paddingBottom: 0
        }
    };

    var fixUpHeight = function(scope, element, height) {
        // We remove the collapse CSS class to prevent a transition when we change to height: auto
        element.removeClass('ddhCollapse');
        element.css({height: height});
        //adjusting for any margin or padding
        if (height === 0) {
            element.css(props.closed);
        } else {
            element.css(props.open);
        }
        // It appears that  reading offsetWidth makes the browser realise that we have changed the
        // height already :-/
        var x = element[0].offsetWidth;
        element.addClass('ddhCollapse');
    };

    return {
        link: function(scope, element, attrs) {
            // alert(element.css("marginTop"));

            var isCollapsed;
            var initialAnimSkip = true;
            scope.$watch(function() {
                return element[0].scrollHeight;
            }, function(value) {
                //The listener is called when scrollHeight changes
                //It actually does on 2 scenarios: 
                // 1. Parent is set to display none
                // 2. angular bindings inside are resolved
                //When we have a change of scrollHeight we are setting again the correct height if the group is opened
                if (element[0].scrollHeight !== 0) {
                    if (!isCollapsed) {
                        if (initialAnimSkip) {
                            fixUpHeight(scope, element, element[0].scrollHeight + 'px');
                        } else {
                            fixUpHeight(scope, element, 'auto');
                        }
                    }
                }
            });

            scope.$watch(attrs.ddhCollapse, function(value) {
                if (value) {
                    collapse();
                } else {
                    expand();
                }
            });


            var currentTransition;
            var doTransition = function(change) {
                if (currentTransition) {
                    currentTransition.cancel();
                }
                currentTransition = $transition(element, change);
                currentTransition.then(
                        function() {
                            currentTransition = undefined;
                        },
                        function() {
                            currentTransition = undefined;
                        }
                );
                return currentTransition;
            };

            var expand = function() {
                //element.show();
                scope.postTransition = true;
                if (initialAnimSkip) {
                    initialAnimSkip = false;
                    if (!isCollapsed) {
                        fixUpHeight(scope, element, 'auto');
                    }
                } else {
                    //doTransition({ height : element[0].scrollHeight + 'px' })
                    doTransition(angular.extend({height: element[0].scrollHeight + 'px'}, props.open))
                            .then(function() {
                                // This check ensures that we don't accidentally update the height if the user has closed
                                // the group while the animation was still running
                                if (!isCollapsed) {
                                    fixUpHeight(scope, element, 'auto');
                                }
                            });
                }
                isCollapsed = false;
            };

            var collapse = function() {
                isCollapsed = true;
                if (initialAnimSkip) {
                    initialAnimSkip = false;
                    fixUpHeight(scope, element, 0);
                } else {
                    fixUpHeight(scope, element, element[0].scrollHeight + 'px');
                    //doTransition({'height':'0'}).then(function() {
                    doTransition(angular.extend({height: 0}, props.closed)).then(function() {
                        //element.hide();
                        scope.postTransition = false;
                    });
                }
            };
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Tabs, tables & accordions.att:expandCollapse
 *
 * @description
 *  <file src="src/expandCollapse/docs/readme.md" />
 *
 * @usage
 *   <ddh-accordion close-others="true" scroll-top="false">
 *       <ddh-accordion-group heading="Heading goes here">
 *           Content will be placed here
 *       </ddh-accordion-group>     
 *   </ddh-accordion> 
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/expandCollapse/docs/demo.html" />
            <file src="src/expandCollapse/docs/demo.js" />
        </example>
    </section>
 *
 */

angular.module('ddh.att.expandCollapse', ['ddh.att.collapse','ddh.att.transition','ddh.att.position','ddh.att.utilities'])     
.constant('ddhAccordionConfig', {
    closeOthers: false,
    scrollTop: true
})
.controller('ddhAccordionController', ['$scope', '$attrs', 'ddhAccordionConfig', '$log',
    function ($scope, $attrs, accordionConfig, $log) {
    // This array keeps track of the accordion groups
    this.groups = [];
    this.index = -1;
    // Keep reference to user's scope to properly assign `is-open`
    this.scope = $scope;
    // Ensure that all the groups in this accordion are closed, unless close-others explicitly says not to
    $scope.forceExpand = false;
    this.closeOthers = function (openGroup) {
        var closeOthers = angular.isDefined($attrs.closeOthers) ? $scope.$eval($attrs.closeOthers) : accordionConfig.closeOthers;
        if (closeOthers) {
            angular.forEach(this.groups, function (group) {
                if (group !== openGroup) {
                    group.isOpen = false;
                }
            });
        }
    };    
    this.expandAll = function () {
        $scope.forceExpand = true;
        angular.forEach(this.groups, function (group) {
            group.isOpen = true;
        });
    };
    this.collapseAll = function () {
        angular.forEach(this.groups, function (group) {
            group.isOpen = false;
        });
    };    
    
    this.isCloseOthers = function () {
        var closeOthers = angular.isDefined($attrs.closeOthers) ? $scope.$eval($attrs.closeOthers) : accordionConfig.closeOthers;
        return closeOthers;
    };
    this.scrollTop = function () {
        var scrollTop = angular.isDefined($attrs.scrollTop) ? $scope.$eval($attrs.scrollTop) : accordionConfig.scrollTop;
        return scrollTop;
    };
    /**
     *
     * function focus
     * @param focusGroup
     */
    this.focus = function (focusGroup) {
        var self = this;
        angular.forEach(this.groups, function (group, index) {
            if (group !== focusGroup) {
                group.focused = false;
            } else {
                self.index = index;
                group.focused = true;
            }
        });
    };
    /**
     *
     * @param blurGroup
     */
    this.blur = function (blurGroup) {
        blurGroup.focused = false;
        this.index = -1;
    };
    /**
     *
     * @param group - the group in current focus
     * @param down - cycling down
     */
    this.cycle = function (group, down, noRecycle) {
        if (!down) {
            if (this.index <= 0 && !noRecycle) {
                this.index = this.groups.length - 1;
            } else {
                this.index--;
            }
        } else {
            if (this.index >= (this.groups.length - 1) && !noRecycle) {
                this.index = 0;
            } else {
                this.index++;
            }
        }
        group.focused = false;
        this.groups[this.index].focused = true;
        $scope.$apply();
    };
    // This is called from the accordion-group directive to add itself to the accordion
    this.addGroup = function (groupScope) {
        var that = this;
        groupScope.index = this.groups.length;
        groupScope.focused = false;
        this.groups.push(groupScope);

        groupScope.$on('$destroy', function () {
            that.removeGroup(groupScope);
        });
    };
    // This is called from the accordion-group directive when to remove itself
    this.removeGroup = function (group) {
        var index = this.groups.indexOf(group);
        if (index !== -1) {
            this.groups.splice(this.groups.indexOf(group), 1);
        }
    };
}])

// The accordion directive simply sets up the directive controller
// and adds an accordion CSS class to itself element.
.directive('ddhAccordion', function () {
    return {
        restrict: 'EA',
        controller: 'ddhAccordionController',
        transclude: true,
        replace: true,
        scope: {
            cClass: '@css',
            expandAll: "=?",
            collapseAll: "=?"
        },
        templateUrl: 'template/expandCollapse/expandCollapse.html',
        link: function (scope, elem, attribute, ctrl) {
            scope.$watch("expandAll", function (value) {
                if (value) {
                    ctrl.expandAll();
                    scope.expandAll = false;
                }
            });
            scope.$watch("collapseAll", function (value) {
                if (value) {
                    ctrl.collapseAll();
                    scope.collapseAll = false;
                }
            });
        }
    };
})

// The accordion-group directive indicates a block of html that will expand and collapse in an accordion
.directive('ddhAccordionGroup', ['$parse', '$transition', '$scrollTo', '$timeout', '$log', '$position', 'keymap',
    function ($parse, $transition, $scrollTo, $timeout, $log, $position, keymap) {
    return {
        // We need this directive to be inside an accordion
        require: '^ddhAccordion',
        restrict: 'EA', transclude: true, replace: true,
        templateUrl: 'template/expandCollapse/expandCollapse-group.html',
        scope: {heading: '@', isOpen: '=?', isDisabled: '=?', onExpand:'&', onCollapse:'&'},
        controller: [function () {
                this.setHeading = function (element) {
                    this.heading = element;
                };
            }],
        link: function (scope, element, attrs, accordionCtrl) {
            scope.isMobile   = /Android|webOS|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
            var tab = element.children().eq(0);
            var panel = element.children().eq(1);
            var handleKeydown = function (ev) {
                var boolFlag = true;
                switch (ev.keyCode) {
                    case keymap.KEY.ENTER:
                        ev.preventDefault();
                        scope.toggle();
                        scope.$apply();
                        break;
                    case keymap.KEY.UP:
                    case keymap.KEY.LEFT:
                        accordionCtrl.cycle(scope, false);
                        break;
                    case keymap.KEY.DOWN:
                    case keymap.KEY.RIGHT:
                        accordionCtrl.cycle(scope, true);
                        break;
                    default:
                        boolFlag = false;
                        break;
                }
                ev.stopPropagation();
                return boolFlag;
            };
            var swallowKeypress = function (e) {
                if (e.altKey)
                {
                    return true;
                }
                var boolFlag = true;
                switch (e.keyCode)
                {
                    case keymap.KEY.PAGE_UP:
                    case keymap.KEY.PAGE_DOWN:
                        if (!e.ctrlKey) {
                            boolFlag = true;
                            break;
                        }
                        e.stopPropagation();
                        boolFlag = false;
                        break;
                    default:
                        e.stopPropagation();
                        boolFlag = false;
                        break;
                }
                return boolFlag;
            };
            if (angular.isUndefined(scope.isOpen)) {
                scope.isOpen = false;
            }
            panel.bind("keypress", swallowKeypress);
            tab.bind("keydown", handleKeydown);
            accordionCtrl.addGroup(scope);
            scope.toggle = function () {
                if (!scope.isDisabled) {
                    tab.attr('aria-live', 'polite');
                    tab.attr('aria-atomic', 'false');                    
                    scope.isOpen = !scope.isOpen;
                    accordionCtrl.focus(scope);
                    if (scope.isOpen && accordionCtrl.scrollTop()) {
                        if (accordionCtrl.isCloseOthers()) {
                            $timeout(function () {
                                $scrollTo(0, $position.offset(element).top);
                            }, 1000);
                        } else {
                            $scrollTo(0, $position.offset(element).top);
                        }
                    }
                }
            };
            scope.$watch('isOpen', function (value) {
                if (value) {
                    accordionCtrl.closeOthers(scope);
                        //panel.attr("tabindex", "0"); 
                    if(scope.onExpand)scope.onExpand();
                } else {
                        //panel.attr("tabindex", "-1");                                       
                    if(scope.onCollapse)scope.onCollapse();
                }
            });
            scope.$watch("focused", function (value) {
                if (!!value) {
                    $timeout(function () {
                        tab[0].focus();
                        }, 0);
                }
            });
        }
    };
}])

.directive('ddhAccordionTable', ['$parse', '$transition', '$scrollTo', '$timeout', '$log', '$position', 'keymap',
    function ($parse, $transition, $scrollTo, $timeout, $log, $position, keymap) {
    return {
        // We need this directive to be inside an accordion
        require: '^ddhAccordion',
        restrict: 'EA', transclude: true, replace: true,
        templateUrl: 'template/expandCollapse/expandCollapse-table.html',
        scope: {column1: '@', column2: '@', isOpen: '=?', isDisabled: '=?'},
        controller: [function () {
                this.setHeading = function (element) {
                    this.heading = element;
                };
            }],
        link: function (scope, element, attrs, accordionCtrl) {
            var tab = element.children().eq(0);
            var panel = element.children().eq(1);
            var handleKeydown = function (ev) {
                var boolFlag = true;
                switch (ev.keyCode) {
                    case keymap.KEY.ENTER:
                        ev.preventDefault();
                        scope.toggle();
                        scope.$apply();
                        break;
                    case keymap.KEY.UP:
                    case keymap.KEY.LEFT:
                        accordionCtrl.cycle(scope, false);
                        break;
                    case keymap.KEY.DOWN:
                    case keymap.KEY.RIGHT:
                        accordionCtrl.cycle(scope, true);
                        break;
                    default:
                        boolFlag = false;
                        break;
                }
                ev.stopPropagation();
                return boolFlag;
            };
            var swallowKeypress = function (e) {
                if (e.altKey)
                {
                    return true;
                }
                var boolFlag = true;
                switch (e.keyCode)
                {
                    case keymap.KEY.PAGE_UP:
                    case keymap.KEY.PAGE_DOWN:
                        if (!e.ctrlKey) {
                            boolFlag = true;
                            break;
                        }
                        e.stopPropagation();
                        boolFlag = false;
                        break;
                    default:
                        e.stopPropagation();
                        boolFlag = false;
                        break;
                }
                return boolFlag;
            };
            if (angular.isUndefined(scope.isOpen)) {
                scope.isOpen = false;
            }
            panel.bind("keypress", swallowKeypress);
            tab.bind("keydown", handleKeydown);
            accordionCtrl.addGroup(scope);
            scope.toggle = function () {
                if (!scope.isDisabled) {
                    tab.attr('aria-live', 'polite');
                    tab.attr('aria-atomic', 'false');
                    scope.isOpen = !scope.isOpen;
                    accordionCtrl.focus(scope);
                    if (scope.isOpen && accordionCtrl.scrollTop()) {
                        if (accordionCtrl.isCloseOthers()) {
                            $timeout(function () {
                                $scrollTo(0, $position.offset(element).top);
                            }, 1000);
                        } else {
                            $scrollTo(0, $position.offset(element).top);
                        }
                    }
                }
            };
            scope.$watch('isOpen', function (value) {
                if (value) {
                    accordionCtrl.closeOthers(scope);
                    panel.attr("tabindex", "0");                    
                } else {
                    panel.attr("tabindex", "-1");                    
                }
            });
            scope.$watch("focused", function (value) {
                if (!!value) {
                    $timeout(function () {
                        tab[0].focus();
                    }, 0)
                            ;
                }
            });
        }
    };
}])

.directive('ddhAccordionHeading', function () {
    return {
        restrict: 'EA', transclude: true, template: '', replace: true,
        require: '^ddhAccordionGroup',
        compile: function (element, attr, transclude) {
            var link = function (scope, element, attr, accordionGroupCtrl) {
                // Pass the heading to the accordion-group controller
                // so that it can be transcluded into the right place in the template
                // [The second parameter to transclude causes the elements to be cloned so that they work in ng-repeat]
                accordionGroupCtrl.setHeading(transclude(scope, function () {
                }));
            };
            return link;
        }
    };
})

.directive('ddhAccordionTransclude', function () {
    return {
        require: '^ddhAccordionGroup',
        link: function (scope, element, attr, controller) {
            scope.$watch(function () {
                return controller[attr.ddhAccordionTransclude];
            }, function (heading) {
                if (heading) {
                    element.find("span").html('');
                    element.find("span").append(heading);
                }
            });
        }
    };
});


/**
 * @ngdoc directive
 * @name Banners, marquees & tiles.ddh:filmstrip
 *
 * @description
 *  <file src="src/filmstrip/docs/readme.md" />
 * @usage
 *  <select name="dropdown1" ddh-dropdown ng-model="dropdownValue1.value">
        <option ddh-dropdown-list option-repeat="d in dropdownOptions1" value="{{d.value}}">{{d.text}}</option>
    </select>
 *
 * @example
   <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
     <file src="src/filmstrip/docs/demo.html" />
     <file src="src/filmstrip/docs/demo.js" />
    </example>
   </section>
 */
angular.module('ddh.att.filmstrip', ['ddh.att.position'])

.directive('ddhFilmstrip', [function() {
    return {
        restrict: 'EA',
        transclude: true,
        replace: true,
        scope:{
            filmstripType: '@',
            filmstripHeading: '@'
        },
        templateUrl: 'template/filmstrip/ddhFilmstrip.html',
        controller: ['$scope', '$timeout', '$position', '$element', '$window', function($scope, $timeout, $position, $element, $window) {
            this.registerElement = function (elem) {
                var window = angular.element($window);
                var showFilmstripContent = function () {
                    if ($position.isElementInViewport($element)) {
                        if (elem) {
                            $timeout(function () {
                                elem.addClass('items-in');
                            });
                        }
                        $timeout(function () {
                            window.unbind('scroll', showFilmstripContent);
                            window.unbind('orientationchange', showFilmstripContent);
                            window.unbind('resize', showFilmstripContent);
                        });
                    }
                    $scope.$apply();
                };
                window.bind('scroll', showFilmstripContent);
                window.bind('orientationchange', showFilmstripContent);
                window.bind('resize', showFilmstripContent);
                $timeout(showFilmstripContent, 500);
            };
        }],
        link: function(scope, elem, attr, ctrl) {}
    };
}])

.directive('ddhFilmstripContent', [function() {
    return {
        restrict: 'EA',
        transclude: true,
        replace: true,
        scope:{},
        require: '^ddhFilmstrip',
        templateUrl: 'template/filmstrip/ddhFilmstripContent.html',
        link: function (scope, elem, attr, ctrl) {
            ctrl.registerElement(elem);
        }
    };
}]);

/**
 * @ngdoc directive
 * @name Headings & copy.att:headings
 *
 * @description
 *  <file src="src/headings/docs/readme.md" />
 *
 * @usage
  <p>Headings are titles, subtitles, or topics that are placed above body copy to help users understand the overall structure and navigation of content to specific information. </p><br>
    <p class="heading-page">38px page heading</p>
    <p class="heading-major-section">30px major section heading</p>
    <p class="heading-sub-section">24px sub-section heading</p>
    <p class="heading-medium">20px medium heading</p>
    <p class="heading-medium-emphasis">20px medium emphasis heading</p>
    <p class="heading-small">18px small heading</p>
    <p class="heading-small-emphasis">18px small emphasis heading</p>
    <p class="heading-micro">13px micro heading</p>
    <h2 class="heading-group">Lead</h2>
    <p class="heading-page">This is 38px heading</p>
    <p class="lead">This is lead text...The next big thing since the last big thing we announced.</p>
    <h2 class="heading-group">Eyebrow</h2>
    <p class="eyebrow">EYEBROW TEXT</p>
    <p class="heading-major-section">This is a 30px heading</p>
    <p class="eyebrow">EYEBROW TEXT</p>
    <p class="heading-sub-section">24px sub-section heading</p>
    <h2 class="heading-group">Subheading</h2>
    <p class="heading-major-section">This is a 30px heading</p>
    <p class="subheading">A subheading here to support what was said above</p>
 * @example
 <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
    <file src="src/headings/docs/demo.html" />
</example>
</section>
 */

var ddhLegalCopy = angular.module('ddh.att.headings', []);
/**
 * @ngdoc directive
 * @name Navigation.att:leftNavigation
 *
 * @description
 *  <file src="src/leftNavigation/docs/readme.md" />
 *
 * @usage
 *   <ddh-left-navigation data-menu="menuData"></ddh-left-navigation> 
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/leftNavigation/docs/demo.html" />
            <file src="src/leftNavigation/docs/demo.js" />
       </example>
    </section>
 *
 */
angular.module('ddh.att.leftNavigation', [])
    .directive('ddhLeftNavigation', ['$location', function ($location) {
        return {
            restrict: 'EA',
            templateUrl: 'template/leftNavigation/leftNavigation.html',
            scope: {
                menuData: '='
            },
            link: function (scope, element, attrs, ctrl) {
                scope.idx = -1;
                scope.itemIdx = -1;
                scope.navIdx = -1;
                scope.toggleNav = function (val) {
                    if (val === scope.idx) {
                        scope.idx = -1;
                        return;
                    }
                    scope.idx = val;
                };
                scope.liveLink = function (evt, val1, val2) {
                    scope.itemIdx = val1;
                    scope.navIdx = val2;
                    evt.stopPropagation();
                };
                scope.navigate = function(url) {
                	if(url != null && url.length > 0){
                		window.location.href = url;
                	}else {
                		return;
                	}
                }
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Headings & copy.att:Legal Copy
 *
 * @description
 *  <file src="src/legalCopy/docs/readme.md" />
 *
 * @usage
 * <h2 class="ds2 heading-group">.text-legal</h2>
 * <p class="text-legal">
 * .....
 * </p> 
 *
 * @example
 <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
    <file src="src/legalCopy/docs/demo.html" />
</example>
</section>
 */

var ddhLegalCopy = angular.module('ddh.att.legalCopy', []);
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:links
 *
 * @description
 *  <file src="src/links/docs/readme.md" />
 * @usage
 *      <!-- See below examples for link implementation -->
 *      
 * @example
       <section id="code">              
           <b>HTML + AngularJS</b>
           <example module="ddh.att">
           <file src="src/links/docs/demo.html" />
            <file src="src/links/docs/demo.js" />            
          </example>          
        </section>
 */
angular.module('ddh.att.links', []);
/**
 * @ngdoc directive
 * @name Videos & animation.att:loaderAnimation
 *
 * @description
 *  <file src="src/loaderAnimation/docs/readme.md" />
 *
 * @usage
 *   <!-- Below demo js shows--> 
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/loaderAnimation/docs/demo.html" />
            <file src="src/loaderAnimation/docs/demo.js" />
       </example>
    </section>
 *
 */
angular.module('ddh.att.loaderAnimation', [])
    .constant('ddhSpinnerConfig', {
        loadingText: 'Loading...',
        startEvent: 'startButtonSpinner',
        stopEvent: 'stopButtonSpinner'
    })
    .constant("progressTrackerConfig", {
        loadingText: 'Loading...',
        minDuration: "",
        activationDelay: "",
        minDurationPromise: "",
        activationDelayPromise: ""
    })

.provider('progressTracker', function () {
    this.$get = ['$q', '$timeout', function ($q, $timeout) {
        function cancelTimeout(promise) {
            if (promise) {
                $timeout.cancel(promise);
            }
        }
        return function ProgressTracker(options) {
            //do new if user doesn't
            if (!(this instanceof ProgressTracker)) {
                return new ProgressTracker(options);
            }

            options = options || {};
            //Array of promises being tracked
            var tracked = [];
            var self = this;
            //Allow an optional "minimum duration" that the tracker has to stay active for.
            var minDuration = options.minDuration;
            //Allow a delay that will stop the tracker from activating until that time is reached
            var activationDelay = options.activationDelay;
            var minDurationPromise;
            var activationDelayPromise;
            self.active = function () {
                //Even if we have a promise in our tracker, we aren't active until delay is elapsed
                if (activationDelayPromise) {
                    return false;
                }
                return tracked.length > 0;
            };
            self.tracking = function () {
                //Even if we aren't active, we could still have a promise in our tracker
                return tracked.length > 0;
            };
            self.destroy = self.cancel = function () {
                minDurationPromise = cancelTimeout(minDurationPromise);
                activationDelayPromise = cancelTimeout(activationDelayPromise);
                for (var i = tracked.length - 1; i >= 0; i--) {
                    tracked[i].resolve();
                }
                tracked.length = 0;
            };
            //Create a promise that will make our tracker active until it is resolved.
            // @return deferred - our deferred object that is being tracked
            self.createPromise = function () {
                var deferred = $q.defer();
                tracked.push(deferred);
                //If the tracker was just inactive and this the first in the list of promises, we reset our delay and minDuration again.
                if (tracked.length === 1) {
                    if (activationDelay) {
                        activationDelayPromise = $timeout(function () {
                            activationDelayPromise = cancelTimeout(activationDelayPromise);
                            startMinDuration();
                        }, activationDelay);
                    } else {
                        startMinDuration();
                    }
                }
                deferred.promise.then(onDone(false), onDone(true));
                return deferred;

                function startMinDuration() {
                    if (minDuration) {
                        minDurationPromise = $timeout(angular.noop, minDuration);
                    }
                }
                //Create a callback for when this promise is done. It will remove our tracked promise from the array if once minDuration is complete
                function onDone() {
                    return function () {
                        (minDurationPromise || $q.when()).then(function () {
                            var index = tracked.indexOf(deferred);
                            tracked.splice(index, 1);
                            //If this is the last promise, cleanup the timeouts for activationDelay
                            if (tracked.length === 0) {
                                activationDelayPromise = cancelTimeout(activationDelayPromise);
                            }
                        });
                    };
                }
            };
            self.addPromise = function (promise) {
                
//                we cannot assign then function in other var and then add the resolve and reject 
                var thenFxn = promise && (promise.then || promise.$then || (promise.$promise && promise.$promise.then));                
                if (!thenFxn) {
                    throw new Error("progressTracker expects a promise object :: Not found");
                }
                var deferred = self.createPromise();
                //When given promise is done, resolve our created promise
                //Allow $then for angular-resource objects

                promise.then(function (value) {
                        deferred.resolve(value);
                        return value;
                    }, function (value) {
                        deferred.reject(value);
                        return $q.reject(value);
                    }
                );
                return deferred;
            };
        };
    }];
})

.config(['$httpProvider', function ($httpProvider) {
    $httpProvider.interceptors.push(['$q', 'progressTracker', function ($q) {
        return {
            request: function (config) {
                if (config.tracker) {
                    if (!angular.isArray(config.tracker)) {
                        config.tracker = [config.tracker];
                    }
                    config.$promiseTrackerDeferred = config.$promiseTrackerDeferred || [];

                    angular.forEach(config.tracker, function (tracker) {
                        var deferred = tracker.createPromise();
                        config.$promiseTrackerDeferred.push(deferred);
                    });
                }
                return $q.when(config);
            },
            response: function (response) {
                if (response.config && response.config.$promiseTrackerDeferred) {
                    angular.forEach(response.config.$promiseTrackerDeferred, function (deferred) {
                        deferred.resolve(response);
                    });
                }
                return $q.when(response);
            },
            responseError: function (response) {
                if (response.config && response.config.$promiseTrackerDeferred) {
                    angular.forEach(response.config.$promiseTrackerDeferred, function (deferred) {
                        deferred.reject(response);
                    });
                }
                return $q.reject(response);
            }
        };
    }]);
}])

.directive('ddhClickSpin', ['$timeout', '$parse', '$rootScope', 'progressTracker', function ($timeout, $parse, $rootScope, progressTracker) {
    return {
        restrict: 'A',
        link: function (scope, elm, attrs) {
            var fn = $parse(attrs.ddhClickSpin);
            elm.on('click', function (event) {
                var promise = $timeout(function () {console.log("inside Promise")}, 5000);
                scope.$apply(function () {
                    fn(scope, {
                        $event: event
                    });
                });
                //comment this line if not running unit test
                $rootScope.loadingTracker = progressTracker({
                    minDuration: 750
                });
                $rootScope.loadingTracker.addPromise(promise);
                angular.forEach("$routeChangeSuccess $viewContentLoaded $locationChangeSuccess".split(" "), function (event) {
                    $rootScope.$on(event, function () {

                        $timeout.cancel(promise);
                    });
                });
            });
        }
    };
}])

.directive('ddhProgressTracker', ['progressTrackerConfig', function (ptc) {
    return {
        restrict: 'EA',
        replace: true,
        template: '<div><div ng-show="loadingTracker.active()" style="width:100%; text-align:center"><i class=\"icon-primary-spinner\"></i></div><div ng-show="loadingTracker.active()" style="width:100%;margin-top:10px; text-align:center">'+ ptc.loadingText+'</div></div>'
    };
}])

.directive('ddhLoadButton', ['ddhSpinnerConfig', '$timeout', function (spinnerConfig, $timeout) {
    var spinButton = function (state, element, data) {
        
        var attr = element.html() ? 'html' : 'val';
        state = state + 'Text';
        if (state === 'loadingText') {
            element[attr](data[state]);
            element.attr("disabled",'disabled');
        } else if (state === 'resetText') {
            element[attr](data[state]);
            element.removeAttr("disabled");
        }
    };

    return {
        restrict: 'A',
        replace: false,
        scope: {
            promise: '=promise',
            startEvent: '@startEvent',
            stopEvent: '@stopEvent'
        },
        link: function (scope, element, attr) {
            var validAttr = element.html() ? 'html' : 'val';
            var data = {
                loadingText: '',
                resetText: ''
            };

            var updateLoadingText = function (val) {
                var loadingText = val;
                if (!angular.isDefined(loadingText) || loadingText === "") {
                    loadingText = spinnerConfig.loadingText;
                }
                data.loadingText = validAttr === 'html' ? "<i class=\"icon-primary-spinner\"></i>" + loadingText : loadingText;
            };
            var updateResetText = function (val) {
                data.resetText = val;
            };

            attr.$observe('ddhLoadButton', function (val) {
                updateLoadingText(val);
            });
            $timeout(function () {
                updateResetText(element[validAttr]());
            }, 500);

            if (!angular.isDefined(scope.startEvent) || scope.startEvent === "") {
                scope.startEvent = spinnerConfig.startEvent;
            }

            if (!angular.isDefined(scope.stopEvent) || scope.stopEvent === "") {
                scope.stopEvent = spinnerConfig.stopEvent;
            }

            scope.$watch('promise', function () {
                if (angular.isDefined(scope.promise) && angular.isFunction(scope.promise.then)) {
                    spinButton('loading', element, data);
                    scope.promise.then(function () {
                        spinButton('reset', element, data);
                    }, function () {
                        spinButton('reset', element, data);
                    });
                }
            });

            scope.$on(scope.startEvent, function () {
                spinButton('loading', element, data);
                scope.$on(scope.stopEvent, function () {
                    spinButton('reset', element, data);
                });
            });
        }
    };
}])


;
/**
 * @ngdoc directive
 * @name Messages, modals & alerts.att:modalsAndAlerts
 *
 * @description
 *  <file src="src/modalsAndAlerts/docs/readme.md" />
 *
 * @usage
 *  <button class="btn" ddh-modal="template/modalsAndAlerts/demo_modal.html" modal-ok="ok()" modal-cancel="cancel()">Launch demo modal</button>
 *
 * @example
 *  <section id="code">
     <example module="ddh.att">
      <file src="src/modalsAndAlerts/docs/demo.html" />
      <file src="src/modalsAndAlerts/docs/demo.js" />
     </example>
    </section>
 *
 */
angular.module('ddh.att.modalsAndAlerts', ['ddh.att.position', 'ddh.att.transition', 'ddh.att.utilities'])

/**
 * A helper, internal data structure that acts as a map but also allows getting / removing
 * elements in the LIFO order
 */
.factory('$$stackedMap', function () {
    return {
        createNew: function () {
            var stack = [];

            return {
                add: function (key, value) {
                    stack.push({
                        key: key,
                        value: value
                    });
                },
                get: function (key) {
                    for (var i = 0; i < stack.length; i++) {
                        if (key == stack[i].key) {
                            return stack[i];
                        }
                    }
                },
                keys: function () {
                    var keys = [];
                    for (var i = 0; i < stack.length; i++) {
                        keys.push(stack[i].key);
                    }
                    return keys;
                },
                top: function () {
                    return stack[stack.length - 1];
                },
                remove: function (key) {
                    var idx = -1;
                    for (var i = 0; i < stack.length; i++) {
                        if (key == stack[i].key) {
                            idx = i;
                            break;
                        }
                    }
                    return stack.splice(idx, 1)[0];
                },
                removeTop: function () {
                    return stack.splice(stack.length - 1, 1)[0];
                },
                length: function () {
                    return stack.length;
                }
            };
        }
    };
})

/**
 * A helper directive for the $modal service. It creates a backdrop element.
 */
.directive('ddhModalBackdrop', ['$modalStack', '$timeout', function ($modalStack, $timeout) {
    return {
        restrict: 'EA',
        replace: true,
        templateUrl: 'template/modalsAndAlerts/backdrop.html',
        link: function (scope, element, attrs) {
            scope.close = function (evt) {
                var modal = $modalStack.getTop();
                if (modal && modal.value.backdrop && modal.value.backdrop != 'static') {
                    evt.preventDefault();
                    evt.stopPropagation();
                    $modalStack.dismiss(modal.key, 'backdrop click');
                }
            };
        }
    };
}])

.directive('ddhModalWindow', ['$timeout', 'windowOrientation', '$window', function ($timeout, windowOrientation, $window) {
    return {
        restrict: 'EA',
        scope: {
            index: '@'
        },
        replace: true,
        transclude: true,
        templateUrl: 'template/modalsAndAlerts/window.html',
        controller: ['$scope', '$element', '$attrs', function (scope, element, attrs) {
            scope.windowClass = attrs.windowClass || '';
            scope.sizeClass = attrs.sizeClass || '';

            this.setTitle = function (title) {
                scope.title = title;
            };

            this.setContent = function (content) {
                scope.content = content;
            };

            this.isDockedModal = scope.windowClass.indexOf('modal-docked') > -1;
        }],
        link: function (scope, element, attrs, ctrl) {
            if (ctrl.isDockedModal) {
                scope.isModalLandscape = false;

                var window = angular.element($window);
                scope.updateCss = function () {
                    if (windowOrientation.isPotrait()) { // Potrait Mode
                        scope.isModalLandscape = false;
                    } else if (windowOrientation.isLandscape()) { // Landscape Mode
                        scope.isModalLandscape = true;
                    }
                };

                $timeout(function () {
                    scope.updateCss();
                    scope.$apply();
                }, 100);
                window.bind('orientationchange', function () {
                    scope.updateCss();
                    scope.$apply();
                });
                window.bind('resize', function () {
                    scope.updateCss();
                    scope.$apply();
                });
            }
        }
    };
}])

.directive('modalTitle', [function () {
    return {
        restrict: 'A',
        require: '^ddhModalWindow',
        link: function (scope, elem, attr, ctrl) {
            ctrl.setTitle(attr.id);
        }
    };
}])

.directive('modalContent', [function () {
    return {
        restrict: 'A',
        require: '^ddhModalWindow',
        link: function (scope, elem, attr, ctrl) {
            ctrl.setContent(attr.id);
        }
    };
}])


.directive('modalBody', ['$timeout', '$position', '$document', '$window', 'windowOrientation', 'ddhAwdBreakpoints', function ($timeout, $position, $document, $window, windowOrientation, ddhAwdBreakpoints) {
    return {
        restrict: 'AC',
        scope: {
            index: '@'
        },
        require: '^ddhModalWindow',
        link: function (scope, element, attrs, ctrl) {
            var window = angular.element($window);
            var body = $document.find('body').eq(0);
            scope.setModalHeight = function () {
                var modalHeaderHeight, modalFooterHeight, modalBodyHeight, windowHeight, windowWidth, modalHeight;
                modalHeaderHeight = 0;
                modalFooterHeight = 0;
                windowHeight = $window.innerHeight;
                windowWidth = $window.innerWidth;
                body.css({
                    'height': windowHeight + 'px'
                });

                if (ctrl.isDockedModal) {
                    var modalElements = element.parent().children();
                    for (var i = 0; i < modalElements.length; i++) {
                        if (modalElements.eq(i).hasClass('modal-header')) {
                            modalHeaderHeight = $position.position(modalElements.eq(i)).height;
                        } else if (modalElements.eq(i).hasClass('modal-footer')) {
                            modalFooterHeight = $position.position(modalElements.eq(i)).height;
                        }
                    }

                    modalHeight = $position.position(element.parent()).height;

                    modalBodyHeight = modalHeight - (modalHeaderHeight + modalFooterHeight) + 'px';

                    if (windowOrientation.isPotrait()) { // Potrait Mode
                        element.removeAttr('style').css({
                            height: modalBodyHeight
                        });
                    } else if (windowOrientation.isLandscape() && windowWidth < ddhAwdBreakpoints.breakpoints.mobile.max) { // Landscape Mode Mobile
                        element.removeAttr('style');
                    } else if (windowOrientation.isLandscape() && windowWidth >= ddhAwdBreakpoints.breakpoints.mobile.max) { // Landscape Mode Non-Mobile
                        element.removeAttr('style').css({
                            height: modalBodyHeight
                        });
                    }
                }
            };

            $timeout(function () {
                scope.setModalHeight();
                scope.$apply();
            }, 100);
            window.bind('orientationchange', function () {
                scope.setModalHeight();
                scope.$apply();
            });
            window.bind('resize', function () {
                scope.setModalHeight();
                scope.$apply();
            });
        }
    };
}])

.directive('modalFooter', ['windowOrientation', '$window', function (windowOrientation, $window) {
    return {
        restrict: 'AC',
        scope: {
            index: '@'
        },
        link: function (scope, element, attrs) {

        }
    };
}])

.factory('$modalStack', ['$document', '$compile', '$rootScope', '$$stackedMap', '$log', '$timeout', 'trapFocusInElement', function ($document, $compile, $rootScope, $$stackedMap, $log, $timeout, trapFocusInElement) {
    var backdropjqLiteEl, backdropDomEl;
    var backdropScope = $rootScope.$new(true);
    var body = $document.find('body').eq(0);
    var html = $document.find('html').eq(0);
    var openedWindows = $$stackedMap.createNew();
    var $modalStack = {};

    function backdropIndex() {
        var topBackdropIndex = -1;
        var opened = openedWindows.keys();
        for (var i = 0; i < opened.length; i++) {
            if (openedWindows.get(opened[i]).value.backdrop) {
                topBackdropIndex = i;
            }
        }
        return topBackdropIndex;
    }

    $rootScope.$watch(backdropIndex, function (newBackdropIndex) {
        backdropScope.index = newBackdropIndex;
    });

    function removeModalWindow(modalInstance) {
        //background scroll fix
        html.removeAttr('style');
        body.removeAttr('style');

        var ddhModalWindow = openedWindows.get(modalInstance).value;
        trapFocusInElement(false, ddhModalWindow.modalDomEl);

        //clean up the stack
        openedWindows.remove(modalInstance);

        //remove window DOM element
        ddhModalWindow.modalDomEl.remove();

        //remove backdrop if no longer needed
        if (backdropDomEl && backdropIndex() == -1) {
            backdropDomEl.remove();
            backdropDomEl = undefined;
        }

        //destroy scope
        ddhModalWindow.modalScope.$destroy();
    }

    $document.bind('keydown', function (evt) {
        var modal;

        if (evt.which === 27) {
            modal = openedWindows.top();
            if (modal && modal.value.keyboard) {
                $rootScope.$apply(function () {
                    $modalStack.dismiss(modal.key);
                });
            }
        }
    });

    $modalStack.open = function (modalInstance, modal) {

        openedWindows.add(modalInstance, {
            deferred: modal.deferred,
            modalScope: modal.scope,
            backdrop: modal.backdrop,
            keyboard: modal.keyboard
        });

        var angularDomEl = angular.element('<div ddh-modal-window></div>');
        angularDomEl.attr('window-class', modal.windowClass);
        angularDomEl.attr('size-class', modal.sizeClass);
        angularDomEl.attr('index', openedWindows.length() - 1);
        angularDomEl.html(modal.content);

        var modalDomEl = $compile(angularDomEl)(modal.scope);
        openedWindows.top().value.modalDomEl = modalDomEl;
        //background page scroll fix
        html.css({
            'overflow-y': 'hidden'
        });
        body.css({
            'overflow-y': 'hidden',
            'width': '100%',
            'height': window.innerHeight + 'px'
        });
        body.addClass('styled-by-modal');
        body.append(modalDomEl);

        if (backdropIndex() >= 0 && !backdropDomEl) {
            backdropjqLiteEl = angular.element('<div ddh-modal-backdrop></div>');
            backdropDomEl = $compile(backdropjqLiteEl)(backdropScope);
            body.append(backdropDomEl);
        }

        $timeout(function () {
            angular.element(modalDomEl)[0].focus();
            trapFocusInElement(true, angular.element(modalDomEl).eq(0));
        }, 200);
    };

    $modalStack.close = function (modalInstance, result) {
        var modal = openedWindows.get(modalInstance);
        if (modal) {
            modal.value.deferred.resolve(result);
            removeModalWindow(modalInstance);
        }
    };

    $modalStack.dismiss = function (modalInstance, reason) {
        var ddhModalWindow = openedWindows.get(modalInstance).value;
        if (ddhModalWindow) {
            ddhModalWindow.deferred.reject(reason);
            removeModalWindow(modalInstance);
        }
    };

    $modalStack.getTop = function () {
        return openedWindows.top();
    };

    return $modalStack;
}])

.provider('$modal', function () {
    var $modalProvider = {
        options: {
            backdrop: true, //can be also false or 'static'
            keyboard: true
        },
        $get: ['$injector', '$rootScope', '$q', '$http', '$templateCache', '$controller', '$modalStack', function ($injector, $rootScope, $q, $http, $templateCache, $controller, $modalStack) {
            var $modal = {};

            function getTemplatePromise(options) {
                return options.template ? $q.when(options.template) :
                    $http.get(options.templateUrl, {
                        cache: $templateCache
                    }).then(function (result) {
                        return result.data;
                    });
            }

            function getResolvePromises(resolves) {
                var promisesArr = [];
                angular.forEach(resolves, function (value, key) {
                    if (angular.isFunction(value) || angular.isArray(value)) {
                        promisesArr.push($q.when($injector.invoke(value)));
                    }
                });
                return promisesArr;
            }

            $modal.open = function (modalOptions) {

                var modalResultDeferred = $q.defer();
                var modalOpenedDeferred = $q.defer();
                //prepare an instance of a modal to be injected into controllers and returned to a caller
                var modalInstance = {
                    result: modalResultDeferred.promise,
                    opened: modalOpenedDeferred.promise,
                    close: function (result) {
                        $modalStack.close(modalInstance, result);
                    },
                    dismiss: function (reason) {
                        $modalStack.dismiss(modalInstance, reason);
                    }
                };

                //merge and clean up options
                modalOptions = angular.extend({}, $modalProvider.options, modalOptions);
                modalOptions.resolve = modalOptions.resolve || {};

                //verify options
                if (!modalOptions.template && !modalOptions.templateUrl) {
                    throw new Error('One of template or templateUrl options is required.');
                }

                var templateAndResolvePromise =
                    $q.all([getTemplatePromise(modalOptions)].concat(getResolvePromises(modalOptions.resolve)));


                templateAndResolvePromise.then(function resolveSuccess(tplAndVars) {

                    var modalScope = (modalOptions.scope || $rootScope).$new();
                    modalScope.$close = modalInstance.close;
                    modalScope.$dismiss = modalInstance.dismiss;

                    var ctrlInstance, ctrlLocals = {};
                    var resolveIter = 1;

                    //controllers
                    if (modalOptions.controller) {
                        ctrlLocals.$scope = modalScope;
                        ctrlLocals.$modalInstance = modalInstance;
                        angular.forEach(modalOptions.resolve, function (value, key) {
                            ctrlLocals[key] = tplAndVars[resolveIter++];
                        });

                        ctrlInstance = $controller(modalOptions.controller, ctrlLocals);
                    }

                    $modalStack.open(modalInstance, {
                        scope: modalScope,
                        deferred: modalResultDeferred,
                        content: tplAndVars[0],
                        backdrop: modalOptions.backdrop,
                        keyboard: modalOptions.keyboard,
                        windowClass: modalOptions.windowClass,
                        sizeClass: modalOptions.sizeClass
                    });

                }, function resolveError(reason) {
                    modalResultDeferred.reject(reason);
                });

                templateAndResolvePromise.then(function () {
                    modalOpenedDeferred.resolve(true);
                }, function () {
                    modalOpenedDeferred.reject(false);
                });

                return modalInstance;
            };

            return $modal;
        }]
    };

    return $modalProvider;
})

.directive("ddhModal", ["$modal", "$log", '$scrollTo', function ($modal, $log, $scrollTo) {
    return {
        restrict: 'A',
        scope: {
            ddhModal: '@',
            modalController: '@',
            modalOk: '&',
            modalCancel: '&',
            windowClass: '@',
            sizeClass: '@'
        },
        link: function (scope, elm, attr) {
            elm.bind('click', function (ev) {
                var currentPosition = ev.pageY - ev.clientY;
                ev.preventDefault();
                if (angular.isDefined(elm.attr("href")) && elm.attr("href") !== "") {
                    scope.ddhModal = elm.attr("href");
                }
                $modal.open({
                    templateUrl: scope.ddhModal,
                    controller: scope.modalController,
                    windowClass: scope.windowClass,
                    sizeClass: scope.sizeClass
                }).result.then(function (value) {
                    scope.modalOk({
                        value: value
                    });
                    elm[0].focus();
                }, function (value) {
                    scope.modalCancel({
                        value: value
                    });
                    elm[0].focus();
                });
            });
        }
    };
}])

.directive("utilityFilter", ["$modal", "$log", '$scrollTo', function ($modal, $log, $scrollTo) {
    return {
        restrict: 'EA',
        scope: {
            utilityFilter: '@'
        },
        require: 'ngModel',
        templateUrl: 'template/modal/u-filter.html',
        link: function (scope, element, attribute, ctrl) {
            //controller to be passed to $modal service
            scope.options = angular.copy(scope.$parent.$eval(attribute.ngModel));
            scope.$parent.$watch(attribute.ngModel, function (newVal, oldVal) {
                if (newVal !== oldVal) {
                    scope.options = newVal;
                }
            });
            var modalCtrl = function ($scope, options) {
                $scope.options = angular.copy(options);
            };

            if (angular.isDefined(scope.utilityFilter)) {
                scope.templateUrl = scope.utilityFilter;
            } else {
                scope.templateUrl = 'template/modal/u-filter-window.html';
            }
            element.bind('click', function (ev) {
                var currentPosition = ev.pageY - ev.clientY;
                $modal.open({
                    templateUrl: scope.templateUrl,
                    controller: modalCtrl,
                    resolve: {
                        options: function () {
                            return scope.options;
                        }
                    }
                }).result.then(function (value) {
                    ctrl.$setViewValue(value);
                    element[0].focus();
                    $scrollTo(0, currentPosition, 0);
                }, function () {
                    element[0].focus();
                    $scrollTo(0, currentPosition, 0);
                });
            });
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Messages, modals & alerts.att:notesMessagesAndErrors
 *
 * @description
 *  <file src="src/notesMessagesAndErrors/docs/readme.md" />
 *
 * @usage
 *  <div class="alert valign-children-middle">
        <div class="alert-error">
            <i class="icon-primary-badgealert" role="img" aria-label="Error"></i>
        </div>
        <div>
            <h3>Your payment is past due.</h3>
            <p>
                Our records show your wireless bill is 5 days overdue. <a href="javascript:void(0)">Pay now</a> | <a href="javascript:void(0)">Go to myAT&T</a>
            </p>
        </div>
    </div>
    
 *  <div class="alert valign-children-middle" ng-if="showInfo">
        <div class="alert-info">
            <i class="icon-primary-flat-info" role="img" aria-label="Info"></i>
        </div>
        <div>
            <h3>Restocking fee</h3>
            <p>
                Your LG Watch Urbane return has a 20% restocking fee. <a href="javascript:void(0)">Learn more</a>
            </p>
        </div>
        <button type="button" class="close" aria-label="dismiss alert header" ng-click="dismissMe('showInfo')"></button>
    </div>
    
 *  <div class="alert valign-children-middle" ng-if="showWarn">
        <div class="alert-info">
            <i class="icon-primary-alert" role="img" aria-label="Warning"></i>
        </div>
        <div>
            <h3>You have used 90% of your talk minutes.</h3>
            <p>
                When you have used all your talk minutes, you'll start using your Rollover Minutes.
            </p>
        </div>
        <button type="button" class="close" aria-label="dismiss alert header" ng-click="dismissMe('showWarn')"></button>
    </div>
    
 *  <div class="alert valign-children-middle" ng-if="showSuccess">
        <div class="alert-success">
            <i class="icon-primary-approval" role="img" aria-label="Checkmark"></i>
        </div>
        <div>
            <h3>You added a line to your Mobile Share Valueplan.</h3>
            <p>
                <a href="javascript:void(0)">Learn more</a>
            </p>
        </div>
        <button type="button" class="close" aria-label="dismiss alert header" ng-click="dismissMe('showSuccess')"></button>
    </div>
    
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/notesMessagesAndErrors/docs/demo.html" />
            <file src="src/notesMessagesAndErrors/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.notesMessagesAndErrors', []);
/**
 * @ngdoc directive
 * @name Navigation.att:pagination
 *
 * @description
 *  <file src="src/pagination/docs/readme.md" />
 *
 * @usage
 *   <div ddh-pagination="" total-pages="totalPages1" current-page="currentPage1" click-handler="customHandler" show-input="showInput"></div> 
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/pagination/docs/demo.html" />
            <file src="src/pagination/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.pagination', ['ddh.att.utilities', 'ngTouch'])
    .directive('ddhPagination', ['userAgent', '$window', function (userAgent, $window) {
        return {
            restrict: 'A',
            scope: {
                totalPages: '=',
                currentPage: '=',
                showInput: '=',
                clickHandler: '=?'
            },
            replace: true,
            templateUrl: 'template/pagination/pagination.html',
            link: function (scope) {
                scope.isMobile = userAgent.isMobile();
                scope.notMobile = userAgent.notMobile();
                scope.focusedPage;
                scope.meanVal = 3;
                scope.$watch('totalPages', function (value) {
                    if (angular.isDefined(value) && value !== null) {
                        scope.pages = [];
                        if (value < 1) {
                            scope.totalPages = 1;
                            return;
                        }
                        if (value <= 10) {
                            for (var i = 1; i <= value; i++) {
                                scope.pages.push(i);
                            }
                        } else if (value > 10) {
                            var midVal = Math.ceil(value / 2);
                            scope.pages = [midVal - 2, midVal - 1, midVal, midVal + 1, midVal + 2];
                        }
                        currentPageChanged(1);
                    }
                });
                scope.$watch('currentPage', function (value) {
                    currentPageChanged(value);
                    callbackHandler(value);
                });
                var callbackHandler = function (num) {
                    if (angular.isFunction(scope.clickHandler)) {
                        scope.clickHandler(num);
                    }
                };

                function currentPageChanged(value) {
                    if (angular.isDefined(value) && value !== null) {
                        if (!value || value < 1) {
                            value = 1;
                        }
                        if (value > scope.totalPages) {
                            value = scope.totalPages;
                        }
                        if (scope.currentPage !== value) {
                            scope.currentPage = value;
                            callbackHandler(scope.currentPage);
                        }
                        if (scope.totalPages > 10) {
                            var val = parseInt(value);
                            if (val <= 6) {
                                scope.pages = [1, 2, 3, 4, 5, 6, 7, 8];
                            } else if (val > 6 && val <= scope.totalPages - 5) {
                                scope.pages = [val - 2, val - 1, val, val + 1, val + 2];
                            } else if (val >= scope.totalPages - 5) {
                                scope.pages = [scope.totalPages - 7, scope.totalPages - 6, scope.totalPages - 5, scope.totalPages - 4, scope.totalPages - 3, scope.totalPages - 2, scope.totalPages - 1, scope.totalPages];
                            }
                        }
                        if (scope.isMobile) {
                            var inWidth = $window.innerWidth;
                            var viewLimit = 7;
                            if (inWidth <= 400) {
                                viewLimit = 7;
                            } else if (inWidth > 400 && inWidth < 500) {
                                viewLimit = 9;
                            } else if (inWidth >= 500 && inWidth < 600) {
                                viewLimit = 11;
                            } else if (inWidth >= 600 && inWidth < 700) {
                                viewLimit = 13;
                            } else if (inWidth >= 700 && inWidth < 800) {
                                viewLimit = 15;
                            }

                            var val = parseInt(value);

                            scope.meanVal = Math.floor(viewLimit / 2);
                            var lowerLimit = (val - scope.meanVal) < 1 ? 1 : val - scope.meanVal;
                            var upperLimit = (lowerLimit + viewLimit - 1) > scope.totalPages ? scope.totalPages : lowerLimit + viewLimit - 1;
                            scope.pages = [];
                            for (var i = lowerLimit; i <= upperLimit; i++) {
                                scope.pages.push(i);
                            }
                        }
                    }
                }
                scope.next = function (event) {
                    if (event != undefined) {
                        event.preventDefault();
                    }
                    if (scope.currentPage < scope.totalPages) {
                        scope.currentPage += 1;
                        callbackHandler(scope.currentPage);
                    }
                };
                scope.prev = function (event) {
                    if (event != undefined) {
                        event.preventDefault();
                    }
                    if (scope.currentPage > 1) {
                        scope.currentPage -= 1;
                        callbackHandler(scope.currentPage);
                    }
                };
                scope.selectPage = function (value, event) {
                    event.preventDefault();
                    scope.currentPage = value;
                    scope.focusedPage = value;
                    callbackHandler(scope.currentPage);
                };
                scope.checkSelectedPage = function (value) {
                    if (scope.currentPage === value) {
                        return true;
                    }
                    return false;
                };
                scope.isFocused = function (page) {
                    return scope.focusedPage === page;
                };
            }
        };
    }])
    .directive('onlyDigits', function () {
        return {
            restrict: 'A',
            require: 'ngModel',
            link: function (scope, elm, attrs, ctrl) {
                elm.on('keydown', function (event) {
                    if ((elm.val().length) > 1) {
                        if ((event.which == 37) 
                             || (event.which == 38)
                             || (event.which == 39) 
                             || (event.which == 40) 
                             || (event.which == 46) 
                             || (event.which == 8) 
                             || (event.which == 9)) {
                            return true;
                        } else {
                            event.preventDefault();
                            return false;
                        }
                    }
                    if (event.shiftKey) {
                        event.preventDefault();
                        return false;
                    }
                    if ((event.which >= 48 && event.which <= 57) 
                             || (e.keyCode >= 96 && e.keyCode <= 105)   
                             || (event.which == 37) 
                             || (event.which == 38)
                             || (event.which == 39) 
                             || (event.which == 40) 
                             || (event.which == 46) 
                             || (event.which == 8) 
                             || (event.which == 9)) {
                        return true;
                    } else {
                        event.preventDefault();
                        return false;
                    }
                });
                var validateString = function (value) {
                    if (angular.isUndefined(value) || value === null || value === '') {
                        return ctrl.$modelValue;
                    }
                    return value;
                };
                ctrl.$parsers.unshift(validateString);
            }
        }
    });
/**
 * @ngdoc directive
 * @name Navigation.att:Pivot Links
 *
 * @description
 *  <file src="src/pivotLinks/docs/readme.md" />
 *
 * @usage
 *   <!-- HTML Imlementation provided below --> 
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/pivotLinks/docs/demo.html" />
            <file src="src/pivotLinks/docs/demo.js" />
       </example>
    </section>
 *
 */
angular.module('ddh.att.pivotLinks', []);
/**
 * @ngdoc directive
 * @name Headings & copy.att:Pricing
 *
 * @description
 *  <file src="src/pricing/docs/readme.md" />
 *
 * @usage
 * <p class="usd" style="font-size:19px; color:#ef6f00">....</p>
 *
 * @example
   <section id="code">
        <example module="ddh.att">
            <file src="src/pricing/docs/demo.html" />
            <file src="src/pricing/docs/demo.js" />
       </example>
    </section>
 */
var ddhPricing = angular.module('ddh.att.pricing', []);
/**
 * @ngdoc directive
 * @name Banners, marquees & tiles.att:promoCards
 *
 * @description
 *  <file src="src/promoCards/docs/readme.md" />
 *
 * @usage
 *  <div ddh-promocard class="promo-card" style="background-image:url(../promo-image-path.png)">
        <div class="promo-overlay blur-overlay">
            Content in promo card.
        </div>
    </div>
 *
 * @example
 *  <section id="code">
       <b>HTML + AngularJS</b>
        <example module="ddh.att">
            <file src="src/promoCards/docs/demo.html" />
            <file src="src/promoCards/docs/demo.js" />
       </example>
    </section>
 * 
 */
angular.module('ddh.att.promoCards', [])
    .directive('ddhPromocard', ['$window', '$timeout', function ($window, $timeout) {
        return {
            restrict: 'EA',
            scope: {},
            link: function (scope, elem) {

                var isIE = /msie|trident/i.test(navigator.userAgent);

                var blurOverlay = elem[0].querySelector('.blur-overlay');

                var getProperties = function (e, p, g) {
                    g = $window.getComputedStyle;
                    return (g ? g(e) : e.currentStyle)[p.replace(/-(\w)/gi, function (w, l) {
                        return l.toUpperCase();
                    })];
                };

                var blurIMG;
                if (isIE) {

                    scope.blurFunction = (function (imgWidth, imgHeight) {
                        var bgValues = getProperties(elem[0], 'background-position').split(" ");
                        var marqueeWidth = parseFloat(getProperties(elem[0], 'width'));
                        var xPosition = parseFloat(bgValues[0]),
                            posPercent = xPosition / 100,
                            posDifference = posPercent - 0.5;

                        var blurPosition = (function () {
                            var blurPosX = (marqueeWidth * posDifference) + ((marqueeWidth - imgWidth) * posDifference);
                            return blurPosX;
                        });

                        if (blurIMG) {
                            if (xPosition == 50) {
                                blurIMG[0].querySelector('image').setAttribute('x', 0);
                            } else {
                                blurIMG[0].querySelector('image').setAttribute('x', blurPosition());
                            }
                        }
                    });

                    var blur_bgImg = (function () {
                        var bg = getProperties(elem[0], 'background-image');
                        bg = bg.replace('url("', '').replace('")', '');
                        return bg;
                    });

                    //blur_bgImg();
                    var bgImg = new Image();
                    bgImg.src = blur_bgImg();
                    
                    blurIMG = angular.element('<svg class="blurBg" preserveAspectRatio="xMidYMin slice"><image width=100% height=100% style="filter:url(#blur-effect-1);" x="0" y="0" xlink:href="' + blur_bgImg() + '" /><filter id="blur-effect-1"><feGaussianBlur stdDeviation="10" /></filter></svg>');

                    var imgSize = function () {
                        bgImg.onload = function () {
                            scope.imgWidth = this.width;
                            scope.imgHeight = this.height;

                            blurIMG[0].setAttribute('viewBox', '0 0 '+ scope.imgWidth + ' ' + scope.imgHeight);

                            scope.blurFunction(scope.imgWidth, scope.imgHeight);
                        };
                    };

                    imgSize();

                } else {
                    blurIMG = angular.element('<div class="blurBg"></div>');
                }

                // add blurBg overlay
                elem.prepend(blurIMG);



                var blurIMGCssCalculation = function () {
                    var blur_width = elem[0].offsetWidth,
                        blur_height = elem[0].offsetHeight,
                        bgImg = getProperties(elem[0], 'background-image'),
                        bgPos = getProperties(elem[0], 'background-position'),
                        bgSize = getProperties(elem[0], 'background-size'),
                        overlayY = blurOverlay.offsetTop,
                        overlayX = blurOverlay.offsetLeft,
                        overlayW = blurOverlay.offsetWidth,
                        overlayH = blurOverlay.offsetHeight,
                        newClip = 'rect(' + overlayY + 'px, ' + (overlayW + overlayX) + 'px, ' + (overlayH + overlayY) + 'px, ' + overlayX + 'px)',
                        clipPath = 'inset(' + overlayY + 'px ' + (overlayW + overlayX) + 'px ' + (overlayH + overlayY) + 'px ' + overlayX + 'px)';

                    blurIMG.css({
                        'background-image': bgImg,
                        'background-position': bgPos,
                        'background-size': bgSize,
                        'clip': newClip,
                        'clip-path': clipPath,
                        'height': blur_height,
                        'width': blur_width
                    });
                };

                blurIMGCssCalculation();

                angular.element($window).bind('resize', function () {
                    $timeout(function () {
                        blurIMG.removeAttr('style');
                        blurIMGCssCalculation();

                        if (isIE) {
                            scope.blurFunction(scope.imgWidth, scope.imgHeight);
                        }
                    }, 400);
                });
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Product selection.att:radioProductSelect
 *
 * @description
 *  <file src="src/radioProductSelect/docs/readme.md" />
 *
 * @usage
 *  <form>
     <fieldset>
     <ul class="list list-radio" radio-button-color-selector="orange">
                <li ng-class="{active:color.name === 'val1'}" >
                    <div class="col left">
                        <label for="rrr1" class="radio" style="max-width:80%">
                            <input type="radio" name="RL3" id="rrr1"  ng-model="color.name" value="val1"> 
                            <i class="skin"></i>
                            <div class="radio-label-text">
                                <span class="main-heading">AT&amp;T Next 24</span>  
                            </div>

                        </label>
                        <div class="tooltip" ddh-tooltip>
                            <a href="javascript:void(0)" class="icon-primary-tooltip"  data-placement="top" role="button" aria-label="Help"  aria-describedby="tooltiptext" ng-mouseover="">
                                <div class="arrow"></div>
                            </a>
                            <div class="tooltip-wrapper" role="tooltip" aria-live="polite" aria-atomic="false">
                                <div class="tooltip-size-control">
                                    <div class="helpertext" tabindex="-1" role="tooltip" id="tooltiptext">  
                                        <div class="popover-title">A tooltipsForForms is a text explanation to help identify what the user should enter in the text field. Keep it brief.</div>  
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="radio-label-text" >
                            <span class="sub-heading">$0 down with 30 monthly installment.</span>
                            <p class="p-small radio-product-link"><a href="#">Stand-alone legal link</a></p>
                        </div>

                    </div>
                    <div class="col right" id="price1">
                        <div class="radio-label-text">
                            <span class="main-heading">$00.00</span>
                            <span class="sub-heading">Due date</span>
                        </div>
                        <div class="radio-label-text">
                            <span class="main-heading">$00.00</span>
                            <span class="sub-heading">Due date</span>
                        </div>
                    </div>
                </li>
                <li ng-class="{active:color.name === 'val2'}">
                    <div class="col left">
                        <label for="rrr12" class="radio">
                            <input type="radio" name="RL3" id="rrr12" ng-model="color.name" value="val2">
                            <i class="skin"></i>
                            <div class="radio-label-text">
                                <span class="main-heading">AT&amp;T Next with down payment </span>

                            </div>
                        </label>
                        <div class="radio-label-text">
                            <span class="sub-heading">$195 down with 28 monthly installments.Trade in at 12 month. </span>
                        </div>
                    </div>
                    <div class="col right">
                        <div class="radio-label-text">
                            <span class="main-heading">$00.00</span>
                            <span class="sub-heading">Due date</span>
                        </div>
                        <div class="radio-label-text">
                            <span class="main-heading">$00.00</span>
                            <span class="sub-heading">Due date</span>
                        </div>
                    </div>
                </li>
            
            </ul>
        </fieldset>
    </form>
   
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/radioProductSelect/docs/demo.html" />
            <file src="src/radioProductSelect/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.radioProductSelect', []);
   
/**
 * @ngdoc directive
 * @name Forms.att:radios
 *
 * @description
 *  <file src="src/radios/docs/readme.md" />
 *
 * @usage
 *  <form  name="myForm" novalidate>
      <div class="form-row">
         <label class="radio">
         <input type="radio" role="radio" ng-model ="selectedvalue.name" name="optionsRadios" value="option1"  
            required="">
         <i class="skin"></i>
         <span>Option one</span>
         </label>
     </div>
    </form>
 *
 * @example
    <section id="code">
               <b>HTML + AngularJS</b>
               <example module="ddh.att">
                <file src="src/radios/docs/demo.html" />
                <file src="src/radios/docs/demo.js" />
               </example>
            </section>
 */
angular.module('ddh.att.radios', []);

/**
 * @ngdoc directive
 * @name Forms.att:searchField
 *
 * @description
 *  <file src="src/searchField/docs/readme.md" />
 *
 * @usage
 *  <div ddh-search-field dropdown-list="listdata" on-click-callback="clickFn(value)" class="span12" input-model='typedString' config-obj='keyConfigObj'></div>
 *
 * @example
 <section id="code">
    <b>HTML + AngularJS</b>
    <example module="ddh.att">
    <file src="src/searchField/docs/demo.html" />
    <file src="src/searchField/docs/demo.js" />
</example>
</section>
 */

angular.module('ddh.att.searchField', ['ddh.att.utilities', 'ddh.att.position']).filter('ddhFilterInput', [function () {
        return function (list, str, keyArray, displayListKey,isContainsSearch) {
            var res = [];
            var searchLabel;
            var searchCondition;
            
            angular.forEach(list, function (listItem) {
                for (var i = 0; i < keyArray.length; i++) {
                    searchLabel = keyArray[i];
                    
                    if(isContainsSearch){
                        searchCondition = listItem[searchLabel].toLowerCase().indexOf(str.toLowerCase())>-1;
                    }
                    else{
                        searchCondition = listItem[searchLabel].match(new RegExp('^' + str, 'gi'));
                    }
                    
                    if (searchCondition) {
                        res.push({
                            'title': listItem[displayListKey],
                            'valueObj': listItem
                        });
                        break;
                }
            }
        });
            return res;
        };
}]).directive('ddhSearchField', ['$filter', 'ddhFilterInputFilter', 'keymap', '$documentBind', '$isElement', '$document', 'events', '$timeout', function ($filter, ddhFilterInput, keymap, $documentBind, $isElement, $document, events, $timeout) {
        return {
            restrict: 'A',
            scope: {
                dataList: '=dropdownList',
                onClickCallback: '&',
                inputModel: '=',
                configObj: '=',
                objModel: '=',
                inputDeny: '=?'
            },
            replace: true,
            templateUrl: 'template/searchField/searchField.html',
            controller: ['$scope', function ($scope) {
                this.searchKeyArray = [];
                if ($scope.configObj.searchKeys) {
                    this.searchKeyArray = $scope.configObj.searchKeys;
                }
                this.triggerInput = function (searchString) {
                    $scope.originalInputModel = searchString;
                    if (searchString === '') {
                        $scope.currentIndex = -1;
                        $scope.filterList = [];
                        $scope.showListFlag = false;
                    } else if (searchString !== '' && !$scope.isFilterEnabled) {
                        $scope.filterList = $filter('ddhFilterInput')($scope.dataList, searchString, this.searchKeyArray, $scope.configObj.displayListDataKey,$scope.configObj.isContainsSearch);
                        $scope.showListFlag = true;
                    }
                };
                this.denyRegex = function() {
                    return $scope.inputDeny;
                };
        }],
            link: function (scope, elem) {
                scope.isFilterEnabled = false;
                scope.showListFlag = false;
                scope.currentIndex = -1;
                console.log('input deny: ', scope.inputDeny);

                scope.setCurrentIdx = function (idx) {
                    scope.currentIndex = idx;
                    if (idx > -1) {
                        scope.inputModel = scope.filterList[idx].title;
                        scope.objModel = scope.filterList[idx];
                    }
                };
                scope.isActive = function (index, dropdownLength) {
                    scope.dropdownLength = dropdownLength;
                    return scope.currentIndex === index;
                };
                scope.selectItem = function (idx) {
                    scope.setCurrentIdx(idx);
                    scope.onClickCallback({
                        value: scope.inputModel,
                        objValue: scope.objModel
                    });
                    scope.showListFlag = false;
                };
                scope.startSearch = function () {
                    scope.onClickCallback({
                        value: scope.inputModel,
                        objValue: scope.objModel
                    });
                };
                scope.selectPrev = function () {
                    if (scope.currentIndex > 0 && scope.filterList.length > 0) {
                        scope.currentIndex = scope.currentIndex - 1;
                        scope.setCurrentIdx(scope.currentIndex);
                    } else if (scope.currentIndex === 0) {
                        scope.currentIndex = scope.currentIndex - 1;
                        scope.inputModel = scope.originalInputModel;
                        scope.isFilterEnabled = true;
                    }
                };
                scope.selectNext = function () {
                    if (scope.currentIndex < scope.filterList.length - 1) {
                        scope.currentIndex = scope.currentIndex + 1;
                        scope.setCurrentIdx(scope.currentIndex);
                    }
                };
                scope.selectCurrent = function () {
                    scope.selectItem(scope.currentIndex);
                };
                scope.selectionIndex = function (e) {
                    switch (e.keyCode) {
                    case keymap.KEY.DOWN:
                        events.preventDefault(e);
                        scope.isFilterEnabled = true;
                        scope.selectNext();
                        break;
                    case keymap.KEY.UP:
                        events.preventDefault(e);
                        scope.isFilterEnabled = true;
                        scope.selectPrev();
                        break;
                    case keymap.KEY.ENTER:
                        events.preventDefault(e);
                        scope.isFilterEnabled = true;
                        scope.selectCurrent();
                        break;
                    case keymap.KEY.ESC:
                        events.preventDefault(e);
                        scope.isFilterEnabled = false;
                        scope.showListFlag = false;
                        scope.inputModel = '';
                        break;
                    default:
                        scope.isFilterEnabled = false;
                        break;
                    }
                    if (elem[0].querySelector('.filtercontainer')) {
                        elem[0].querySelector('.filtercontainer').scrollTop = (scope.currentIndex - 1) * 35;
                    }
                };
                scope.$watch('filterList', function (newVal, oldVal) {
                    if (newVal !== oldVal) {
                        scope.currentIndex = -1;
                    }
                });
                scope.blurInput = function () {
                    $timeout(function () {
                        scope.showListFlag = false;
                    }, 150);
                };
                var outsideClick = function (e) {
                    var isElement = $isElement(angular.element(e.target), elem.find('ul').eq(0), $document);
                    if (!isElement && document.activeElement.tagName.toLowerCase() !== 'input') {
                        scope.showListFlag = false;
                        scope.$apply();
                    }
                };
                $documentBind.click('showListFlag', outsideClick, scope);
            }
        };
}])
.directive('ddhSearchInput', [function () {
    return {
        restrict: 'A',
        require: ['ngModel', '^ddhSearchField'],
        link: function (scope, elem, attr, ctrl) {
            var ngModelCtrl = ctrl[0];
            var attSearchBarCtrl = ctrl[1];
            var REGEX = ctrl[1].denyRegex();
            var parser = function (viewValue) {
                attSearchBarCtrl.triggerInput(viewValue);
                return viewValue;
            };
            ngModelCtrl.$parsers.push(parser);

            if (REGEX !== undefined || REGEX !== '') {
                elem.bind('input', function () {
                    var inputString = ngModelCtrl.$viewValue && ngModelCtrl.$viewValue.replace(REGEX, '');
                    if (inputString !== ngModelCtrl.$viewValue) {
                        ngModelCtrl.$setViewValue(inputString);
                        ngModelCtrl.$render();
                        scope.$apply();
                    }
                });
            }
        }
    };
}]);
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:socialFollowUs
 *
 * @description
 *  <file src="src/socialFollowUs/docs/readme.md" />
 *
 * @usage
 *  <!-- Use Markup mentioned -->
 *
 * @example
   <section id="code">
              <b>HTML + AngularJS</b>
              <example module="ddh.att">
                <file src="src/socialFollowUs/docs/demo.html" />
                <file src="src/socialFollowUs/docs/demo.js" />
               </example>
            </section>
 */
angular.module('ddh.att.socialFollowUs', []);
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:socialShareThis
 *
 * @description
 *  <file src="src/socialShareThis/docs/readme.md" />
 *
 * @usage
 *  <!-- For code Check Demo.html -->
 *
 * @example
   <section id="code">
      <b>HTML + AngularJS</b>
      <example module="ddh.att">
         <file src="src/socialShareThis/docs/demo.html" />
         <file src="src/socialShareThis/docs/demo.js" />
      </example>
   </section>
 */
angular.module('ddh.att.socialShareThis', [])
    .directive('ddhSocialShare', [function () {
        return {
            restrict: 'EA',
            transclude: true,
            replace: true,
            scope: {
                ddhSocialShare: '@',
                moveLeft: '='
            },
            templateUrl: 'template/socialShareThis/socialShareThis.html',
            link: function (scope) {
                scope.showIt = false;
                scope.toggleSocialShare = function(){
                    scope.showIt = !scope.showIt;
                };
            }
        };
}]);
/**
 * @ngdoc directive
 * @name Buttons, links & UI controls.att:switches
 *
 * @description
 *  <file src="src/switches/docs/readme.md" />
 *
 * @usage
 *  
 *  <!-- On / Off Toggle switch -->
 *  <label for="switch1" class="controlled-text-wrap"> This is ON
 *      <input type="checkbox" role="checkbox" ddh-switches id="switch1" ng-model="switch1.value">
 *  </label>
 *
 *  <!-- On / Off Toggle switch and DISABLED -->
 *  <label for="switch2" class="controlled-text-wrap"> This is ON (disabled)
 *      <input type="checkbox" role="checkbox" ddh-switches id="switch2" ng-model="switch1.value" ng-disabled="true" >
 *  </label> 
 *
 *
 * @example
 *  <section id="code">
        <b>HTML + AngularJS</b>
        <example module="ddh.att">
            <file src="src/switches/docs/demo.js" />
            <file src="src/switches/docs/demo.html" />
        </example>
    </section>
 */
angular.module('ddh.att.switches', ['ddh.att.utilities'])
    .directive('ddhSwitches', ['$compile', '$templateCache', 'keymap', 'events', function ($compile, $templateCache, keymap, events) {
        return {
            restrict: 'EA',
            require: ['ngModel'],
            link: function (scope, element, attrs, ctrl) {
                var ngModelController = ctrl[0];
        
                element.parent().bind("keydown", function (e) {
                    if (!attrs.disabled && (e.keyCode === keymap.KEY.ENTER || e.keyCode === keymap.KEY.SPACE)) {
                        events.preventDefault(e);
                        ngModelController.$setViewValue(!ngModelController.$viewValue);
                        element.prop("checked", ngModelController.$viewValue);
                    }
                });

                element.wrap('<div class="btn-switch">');
                element.attr("tabindex", -1);

                var templateSwitch = angular.element($templateCache.get("template/switches/switches.html"));
                if (angular.isDefined(attrs.typeSpanish)) {
                    templateSwitch = angular.element($templateCache.get("template/switches/switches-spanish.html"));
                }

                templateSwitch = $compile(templateSwitch)(scope);
                element.parent().append(templateSwitch);
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Tabs, tables & accordions.att:tables
 *
 * @description
 *  <file src="src/tables/docs/readme.md" />
 *
 * @usage
 *   
 Table
 <table ddh-table table-data="tableData" search-string="searchString">
    <thead ddh-table-row type="header">
        <tr>
            <th ddh-table-header key="requestId" default-sort="a" id="col1">Header 1</th>
            <th ddh-table-header key="requestType" sortable="false" id="col2">Header 2</th>
        </tr>
    </thead>
    <tbody ddh-table-row type="body" row-repeat="rowData in tableData">
        <tr>
            <td ddh-table-body id="rowheader{{$index}}" headers="col1" ng-bind="rowData['requestId']"> </td>
            <td ddh-table-body ng-bind="rowData['requestType']"></td>
        </tr>
    </tbody>
</table>

Accordion Table
<ddh-accordion class="tiny-accordion iconleft accordion-table-layout" close-others="false" scroll-top="false">
    <ddh-accordion-table column1="Account changes" column2="$100.00">
        <div>
            <div class="faux-table-cell">
                <strong>Monthly plan charges</strong>
            </div>
            <div class="faux-table-cell">

            </div>
        </div>
        <div role="option">
            <div class="faux-table-cell">
                - Mobile Share Value iPhone on 4G LTE w/VVM
            </div>
            <div class="faux-table-cell">
                $100.00
            </div>
        </div>
    </ddh-accordion-table>
    <ddh-accordion-table column1="Daniel Blackman <br>404-231-7479" column2="$100.00">
        <div>
            <div class="faux-table-cell">
                <strong>Monthly plan charges</strong>
            </div>
            <div class="faux-table-cell">
            </div>
        </div>
        <div role="option">
            <div class="faux-table-cell">
                - Mobile Share Value iPhone on 4G LTE w/VVM
            </div>
            <div class="faux-table-cell">
                $100.00
            </div>
        </div>
    </ddh-accordion-table>
    <div class="toggle-header closed" id="tab6" aria-expanded="false" aria-selected="false">
        <div class="faux-table-cell">
            <strong>Total for 404-231-7479</strong>
        </div>
        <div class="faux-table-cell">
            $1000.00
        </div>
    </div>
</ddh-accordion>
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/tables/docs/demo.html" />
            <file src="src/tables/docs/demo.js" />
       </example>
        </section>
 *
 */
angular.module('ddh.att.tables', ['ddh.att.utilities'])
    .constant('tableConfig', {
        defaultSortPattern: false, //true for descending & false for ascending
        highlightSearchStringClass: 'tablesorter-search-highlight'
    })
    .directive('ddhTable', ['$filter', function ($filter) {
        return {
            restrict: 'EA',
            replace: true,
            transclude: true,
            scope: {
                tableData: "=",
                viewPerPage: "=",
                currentPage: "=",
                totalPage: "=",
                searchCategory: "=",
                searchString: "=",
                nextSort:'='
            },
            require: 'ddhTable',
            templateUrl: 'template/table/ddhTable.html',
            controller: ['$scope', function ($scope) {
                this.headers = [];
                this.currentSortIndex = null;
                this.setIndex = function (headerScope) {
                    this.headers.push(headerScope);
                };
                this.getIndex = function (headerName) {
                    for (var i = 0; i < this.headers.length; i++) {
                        if (this.headers[i].headerName === headerName) {
                            return this.headers[i].index;
                        }
                    }
                    return null;
                };
                this.sortData = function (columnIndex, reverse,externalSort) {
                    if($scope.$parent && $scope.$parent !== undefined) {
						$scope.$parent.columnIndex = columnIndex;
						$scope.$parent.reverse = reverse;  
					}
                    this.currentSortIndex = columnIndex;
					if(externalSort===true) {
                        if(reverse==false){
                            $scope.nextSort='d'
                        } else {
                            $scope.nextSort='a'
                        } 
                    }
                    $scope.currentPage = 1;
                    this.resetSortPattern();
                };
                this.getSearchString = function () {
                    return $scope.searchString;
                };
                this.resetSortPattern = function () {
                    for (var i = 0; i < this.headers.length; i++) {
                        var currentScope = this.headers[i];
                        if (currentScope.index !== this.currentSortIndex) {
                            currentScope.resetSortPattern();
                        }
                    }
                };
				
				$scope.$watch('nextSort', function (val) {
                    if($scope.$parent && $scope.$parent !== undefined) {
                        $scope.$parent.nextSort = val;
                    }
                    
                });
				
            }],
            link: function (scope, elem, attr, ctrl) {
                scope.searchCriteria = {};
                scope.$watchCollection('tableData', function (value) {
                    if (value && !isNaN(value.length)) {
                        scope.totalRows = value.length;
                    }
                });
                scope.$watch('currentPage', function (val) {
                    if(scope.$parent && scope.$parent !== undefined) {
                        scope.$parent.currentPage = val;
                    }
                });
                scope.$watch('viewPerPage', function (val) {
                    if(scope.$parent && scope.$parent !== undefined) {
                        scope.$parent.viewPerPage = val;
                    }
                });
                scope.$watch(function () {
                    return scope.totalRows / scope.viewPerPage;
                }, function (value) {
                    if (!isNaN(value)) {
                        scope.totalPage = Math.ceil(value);
                        scope.currentPage = 1;
                    }
                });
                var searchValCheck = function (val) {
                    if (angular.isDefined(val) && val !== null && val !== "") {
                        return true;
                    }
                };
                var setSearchCriteria = function (v1, v2) {
                    if (searchValCheck(v1) && searchValCheck(v2)) {
                        var index = ctrl.getIndex(v2);
                        scope.searchCriteria = {};
                        if (index !== null) {
                            scope.searchCriteria[index] = v1;
                        }
                    } else if (searchValCheck(v1) && (!angular.isDefined(v2) || v2 === null || v2 === "")) {
                        scope.searchCriteria = {
                            $: v1
                        };
                    } else {
                        scope.searchCriteria = {};
                    }
                };
                scope.$watch('searchCategory', function (newVal, oldVal) {
                    if (newVal !== oldVal) {
                        setSearchCriteria(scope.searchString, newVal);
                    }
                });
                scope.$watch('searchString', function (newVal, oldVal) {
                    if (newVal !== oldVal) {
                        setSearchCriteria(newVal, scope.searchCategory);
                    }
                });
                scope.$watchCollection('searchCriteria', function (val) {
                    if(scope.$parent && scope.$parent !== undefined) {
                        scope.$parent.searchCriteria = val;
                    }
                    scope.totalRows = scope.tableData && ($filter('filter')(scope.tableData, val, false)).length || 0;
                    scope.currentPage = 1;
                });
            }
        };
    }])
    .directive('ddhTableRow', [function () {
        return {
            restrict: 'EA',
            compile: function (elem, attr) {
                if (attr.type === 'header') {
                    elem.find('tr').eq(0).addClass('tablesorter-headerRow');
                } else if (attr.type === 'body') {
                    var html = elem.children();
                    if (attr.rowRepeat) {
                        html.attr('ng-repeat', attr.rowRepeat.concat(" | orderBy : columnIndex : reverse | filter : searchCriteria : false "));
                    }
                    html.attr('ng-class', "{'alt-row': $even,'normal-row': $odd}");
                    elem.append(html);
                }
            }
        };
    }])
    .directive('ddhTableHeader', ['tableConfig', function (tableConfig) {
        return {
            restrict: 'EA',
            replace: true,
            transclude: true,
            scope: {
                sortable: '@',
                defaultSort: '@',
                index: '@key'
            },
            require: '^ddhTable',
            templateUrl: 'template/table/ddhTableHeader.html',
            link: function (scope, elem, attr, ctrl) {
                var reverse = tableConfig.defaultSortPattern;
                scope.headerName = elem.text();
                scope.sortPattern = null;
                ctrl.setIndex(scope);

                scope.$watch(function () {
                    return elem.text();
                }, function (value) {
                    scope.headerName = value;
                });
                scope.sort = function (sortType) {
                    if (typeof sortType === 'boolean') {
                        reverse = sortType;
                    }
                    ctrl.sortData(scope.index, reverse, false);
                    scope.sortPattern = reverse ? 'descending' : 'ascending';
                    reverse = !reverse;
                };
                scope.$watch(function () {
                    return ctrl.currentSortIndex;
                }, function (value) {
                    if (value !== scope.index) {
                        scope.sortPattern = null;
                    }
                });

                if (scope.sortable === undefined || scope.sortable === 'true' || scope.sortable === true) {
                    scope.sortable = 'true';
                } else if (scope.sortable === false || scope.sortable === 'false') {
                    scope.sortable = 'false';
                }

                if (scope.sortable !== 'false') {
                    if (scope.defaultSort === 'A' || scope.defaultSort === 'a') {
                        scope.sort(false);
                    } else if (scope.defaultSort === 'D' || scope.defaultSort === 'd') {
                        scope.sort(true);
                    }
                }
                scope.resetSortPattern = function () {
                    reverse = tableConfig.defaultSortPattern;
                };
            }
        };
    }])
    .directive('ddhTableBody', ['$filter', '$timeout', 'tableConfig', function ($filter, $timeout, tableConfig) {
        return {
            restrict: 'EA',
            require: '^ddhTable',
            replace: true,
            transclude: true,
            templateUrl: 'template/table/ddhTableBody.html',
            link: function (scope, elem, attr, ctrl) {
                var highlightSearchStringClass = tableConfig.highlightSearchStringClass;
                var searchString = "";
                var wrapElement = function (elem) {
                    var text = elem.text();
                    elem.html($filter('ddhHighlight')(text, searchString, highlightSearchStringClass));
                };
                var traverse = function (elem) {
                    var innerHtml = elem.children();
                    if (innerHtml.length > 0) {
                        for (var i = 0; i < innerHtml.length; i++) {
                            traverse(innerHtml.eq(i));
                        }
                    } else {
                        wrapElement(elem);
                        return;
                    }
                };
                var clearWrap = function (elem) {
                    var elems = elem.find('*');
                    for (var i = 0; i < elems.length; i++) {
                        if (elems.eq(i).attr('class') && elems.eq(i).attr('class').indexOf(highlightSearchStringClass) !== -1) {
                            var text = elems.eq(i).text();
                            elems.eq(i).replaceWith(text);
                        }
                    }
                };
                $timeout(function () {
                    var actualHtml = elem.children();
                    scope.$watch(function () {
                        return ctrl.getSearchString();
                    }, function (val) {
                        searchString = val;
                        clearWrap(elem);
                        if (actualHtml.length > 0) {
                            traverse(elem);
                        } else {
                            wrapElement(elem);
                        }
                    });
                }, 50);
            }
        };
		
    }])
    
    .directive('ddhTableSort',['tableConfig',function(tableConfig){

         return {
            restrict: 'EA',
            replace: true,
            require: '^ddhTable',
            link: function (scope, elem, attr, ctrl) {
                var initialSort='',
                nextSort='';
                initialSort = attr.initialSort;

                scope.sortTable = function(msg) {  
                    if(nextSort.length>0){

                        if(nextSort==='d'|| nextSort==='D'){
                            ctrl.sortData(msg, true, true);
                            nextSort='a';
                        } else {
                            ctrl.sortData(msg, false, true);
                            nextSort='d';
                        }
                   }else if(initialSort.length>0) {

                            if(initialSort==='d' || initialSort==='D'){
                            ctrl.sortData(msg, true, true);
                            nextSort = 'a';
                        } else {
                            ctrl.sortData(msg, false, true);
                            nextSort = 'd';
                        }
                    }
                    
                };
            }
        };

    }]);   
/**
 * @ngdoc directive
 * @name Tabs, tables & accordions.att:tabs
 *
 * @description
 *  <file src="src/tabs/docs/readme.md" />
 *
 * @usage
 *  <ddh-tabset tab-id-selected="activeTabsId">
        <ddh-tab ng-repeat="tab in gTabs" tab-item="tab" 
                 id="{{tab.uniqueId}}" aria-controls="{{tab.tabPanelId}}"
                 ng-disabled="tab.disabled">
            {{tab.title}}
        </ddh-tab>
    </ddh-tabset>
 *
 * @example
 *  <section id="code">
        <example module="ddh.att">
            <file src="src/tabs/docs/demo.html" />
            <file src="src/tabs/docs/demo.js" />
        </example>
    </section>
 *
 */

angular.module('ddh.att.tabs', ['ddh.att.utilities'])
    .directive('ddhTabset', function () {
        return {
            restrict: 'EA',
            transclude: true,
            replace: true,
            scope: {
                tabIdSelected: '='
            },
            templateUrl: 'template/tabs/ddhTabset.html',
            controller: ['$scope', function ($scope) {

                this.setTabIdSelected = function (tab) {
                    $scope.tabIdSelected = tab.id;
                };

                this.getTabIdSelected = function () {
                    return $scope.tabIdSelected;
                };
            }]
        };
    })
    .directive('ddhTab', ['keymap', function (keymap) {
        return {
            restrict: 'EA',
            transclude: true,
            replace: true,
            require: '^ddhTabset',
            scope: {
                tabItem: "="
            },
            templateUrl: 'template/tabs/ddhTab.html',
            controller: [function(){}],
            link: function (scope, element, attr, ddhTabsetCtrl) {

                if (scope.tabItem && !scope.tabItem.disabled) {
                    scope.tabItem.disabled = false;
                }

                scope.isTabActive = function () {
                    return (scope.tabItem.id === ddhTabsetCtrl.getTabIdSelected());
                };

                scope.clickTab = function () {
                    if (attr.disabled) {
                        return;
                    }
                    ddhTabsetCtrl.setTabIdSelected(scope.tabItem);
                };

                scope.nextKey = function () {
                    return; // JM520E - 508 Hack 
                    /*var el = angular.element(element[0])[0]; 
                    var elementToFocus = null;
                    while (el && el.nextElementSibling) {
                        el = el.nextElementSibling;
                        if (!el.querySelector('a').disabled) {
                            elementToFocus = el.querySelector('a');
                            break;
                        }
                    }

                    if (!elementToFocus) {
                        var childTabs = element.parent().children();
                        for (var i = 0; i < childTabs.length; i++) {
                            if (!childTabs[i].querySelector('a').disabled) {
                                elementToFocus = childTabs[i].querySelector('a');
                                break;
                            }
                        }
                    }

                    if (elementToFocus) {
                        elementToFocus.focus();
                    }*/
                };

                scope.previousKey = function () {
                    return; // JM520E - 508 Hack 
                    /*var el = angular.element(element[0])[0]; 
                    var elementToFocus = null;

                    while (el && el.previousElementSibling) {
                        el = el.previousElementSibling;
                        if (!el.querySelector('a').disabled) {
                            elementToFocus = el.querySelector('a');
                            break;
                        }
                    }

                    if (!elementToFocus) {
                        var childTabs = element.parent().children();
                        for (var i = childTabs.length - 1; i > 0; i--) {
                            if (!childTabs[i].querySelector('a').disabled) {
                                elementToFocus = childTabs[i].querySelector('a');
                                break;
                            }
                        }
                    }

                    if (elementToFocus) {
                        elementToFocus.focus();
                    }*/
                };

                /*angular.element(element[0].querySelector('a')).bind('keydown', function (evt) { 
                    return; // JM520E - 508 Hack 
                    if (!(evt.keyCode)) {
                        evt.keyCode = evt.which;
                    }

                    switch (evt.keyCode) {
                        case keymap.KEY.RIGHT:
                            evt.preventDefault();
                            scope.nextKey();
                            break;

                        case keymap.KEY.LEFT:
                            evt.preventDefault();
                            scope.previousKey();
                            break;

                        default:;
                    }
                });*/
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Forms.att:termsAndConditions
 *
 * @description
 *  <file src="src/termsAndConditions/docs/readme.md" />
 *
 * @usage
 *      <div>
            <form>
                <h3 class="heading-sub-section" style="font-size: 24px">Legal aggrements</h3>
                <div class="span12" style="font-size: 16px">This sentence lets user knows the benefits of agreeing</div>
                <ul>
                    <li style="margin-top: 20px" ddh-modal="template/modalsAndAlerts/modal-dynamic.html" window-class="modal-docked" modal-ok="ok()" modal-cancel="cancel()"><span style="color:#0574ac;font-size:20px">&#8226;</span> <a href="#" >Read agreement 1 here</a></li>
                    <li style="margin-top: 20px" ddh-modal="template/modalsAndAlerts/modal-dynamic.html" window-class="modal-docked" modal-ok="ok()" modal-cancel="cancel()"><span style="color:#0574ac;font-size:20px">&#8226;</span> <a href="#" >Read agreement 2 here</a></li>
                </ul>
                <label for="terms1" class="checkbox">
                    <input id="terms1" type="checkbox" value="">
                    <i class="skin"></i>
                    <span>I have read and agree to the AT&amp;T Terms of Service</span>
                </label>
            </form>
        </div>

 * @example
 *  <section id="code">   
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/termsAndConditions/docs/demo.html" />
 <file src="src/termsAndConditions/docs/demo.js" />
 </example>
 </section>
 *
 */
angular.module('ddh.att.termsAndConditions',[]);
      
/**
 * @ngdoc directive
 * @name Forms.att:textArea
 *
 * @description
 *  <file src="src/textArea/docs/readme.md" />
 *
 * @usage
 *  <textarea ddh-reset ddh-reset-textarea ng-model="textAreaModel" ng-disabled="disabled" ng-trim="false" placeholder="{{placeholderText}}" rows="{{textAreaRows}}" maxlength="{{textAreaMaxlength}}" role="textarea"></textarea>
 *
 * @example
    <section id="code">
        <b>HTML + AngularJS</b>
        <example module="ddh.att">
            <file src="src/textArea/docs/demo.html" />
            <file src="src/textArea/docs/demo.js" />
        </example>
    </section>
 */
angular.module('ddh.att.textArea', ['ddh.att.utilities'])

.directive('ddhResetTextarea', [ function () {
    return {
        restrict: 'A',
        require: 'ddhReset',
        link: function (scope, element, attrs, ctrl) {

            var resetButton = ctrl.getResetButton();
            
            var computeScrollbarAndAddClass = function () {
                if (element.prop('scrollHeight') > element[0].clientHeight) {
                    element.addClass('hasScrollbar');
                } else {
                    element.removeClass('hasScrollbar');
                }
            };
            
            computeScrollbarAndAddClass();

            element.on('focus keyup', function(){
                computeScrollbarAndAddClass();
            });
        }
    };
}]);

/**
 * @ngdoc directive
 * @name Forms.att:tooltipsForForms
 *
 * @description
 *  <file src="src/tooltipsForForms/docs/readme.md" />
 *
 * @usage
 * <div class="tooltip" ddh-tooltip>
    <a href="javascript:void(0)" class="icon-primary-tooltip"  data-placement="top" role="button" aria-label="Help">
        <div class="arrow"></div>
    </a>
    <div class="tooltip-wrapper" role="tooltip" aria-live="polite" aria-atomic="false">
        <div class="tooltip-size-control" style="left: 220px;">
            <div class="helpertext" tabindex="-1" aria-hidden="true" role="tooltip">  
                <div class="popover-title">This is an optional heading:</div>  
                <div class="popover-content">
                    <p>This unfolds only when user taps the help button. This is just plain ol' text that should wrap nicely - but keep it short please.</p>
                    <strong class="hidden-spoken">Double tap to close help message.</strong>
                </div>
            </div>
        </div>
    </div>
    </div> 
 ** On-Focus ** 
    <div class="field-group tooltip-onfocus"  ddh-tooltip show-tooltip="showTooltipOnInput1">
        <input type="text" class="span12" id="onfocustip1" placeholder="Example: you@att.net" ng-focus="showTooltipOnInput1 = true" ng-blur="showTooltipOnInput1 = false">
        <button class="reset-field" type="button" aria-label="Reset text field" tabindex="-1" aria-hidden="true"></button>
        <div class="helpertext" id="tooltipContent1" role="tooltip" tabindex="-1">
            <div class="popover-title">What is this?</div>
            <div class="popover-content">
                <p>This is a text explanation to help identify what the user should enter in the text field. Keep it brief.</p>
                <p>This could also be an image or text/image combo. This unfolds automatically when the user taps into the field.</p>
            </div>
        </div>                  
    </div>
 ** On-click ** 
    <div class="field-group tooltip-onclick"  ddh-tooltip show-tooltip="showTooltipOnclicktip2">
        <input type="text" class="span12" id="onclicktip2" placeholder="Example: you@att.net">
        <button class="reset-field" type="button" aria-label="Reset text field" tabindex="-1" aria-hidden="true"></button>
        <button id="tooltipButton2" class="btn icon-primary-tooltip" data-toggle="button" aria-label="Email help" aria-expanded="showTooltipOnclicktip2" aria-describedby="tooltipContent2" ng-click="showTooltipOnclick()" ng-blur="showTooltipOnclicktip2 = false"></button>
        <div class="helpertext" id="tooltipContent2" role="tooltip" aria-live="polite">
            <div class="popover-title">Please enter your email address.</div>
            <div class="popover-content">
                <p>This is a text explanation to help identify what the user should enter in the text field.</p>
            </div>
        </div>
    </div>
 
 
 *
 * @example
 <example module="ddh.att">
 <file src="src/tooltipsForForms/docs/demo.html" />
 <file src="src/tooltipsForForms/docs/demo.js" />
 </example>
 */
angular.module('ddh.att.tooltipsForForms', ['ddh.att.utilities'])
    .directive('ddhTooltip', [function () {
        return  {
            restrict: 'A',
            scope: {
                showTooltip: "="
            },
            link: function (scope, elem, attr, ctrl) {
                scope.$watch('showTooltip', function (val) {
                    if (val) {
                        elem.addClass('active');
                    } else {
                        elem.removeClass('active');
                    }
                });
                if (elem.hasClass('tooltip')) {
                    var tooltipWrapper = elem[0].querySelector('.tooltip-size-control');
                    var tooltipText = elem[0].querySelector('.helpertext');
                    var icon = elem[0].querySelector('[data-placement]');
                    var objPos = {};
                    var flag = false;

                    angular.element(icon).on('mouseover', function (ev) {
                        elem.addClass('active');
                        showOnHover(ev);
                    });
                    angular.element(icon).on('mouseleave', function () {
                        elem.removeClass('active');
                    });

                    angular.element(icon).on('click', function (ev) {
                        flag = !flag;
                        if (flag) {
                            elem.addClass('active activeClick');
                            angular.element(icon).off('mouseleave');
                        } else {
                            elem.removeClass('active activeClick');
                            angular.element(icon).on('mouseleave', function () {
                                elem.removeClass('active');
                            });
                        }
                        showOnClick(ev);
                    });
                }

                var showOnHover = function (evt) {
                    var clientRect = tooltipWrapper.getBoundingClientRect();
                    objPos = icon.getBoundingClientRect();
                    var left = objPos.left + (parseInt(objPos.width)/2) - (parseInt(clientRect.width) / 2);
                    angular.element(tooltipWrapper).css({left: left + 'px'});
                };

                var showOnClick = function (evt) {
                    showOnHover(evt);
                    var tooltipTop = -(tooltipText.offsetTop);
                    var top = objPos.top - tooltipTop;
                    if (objPos.top <= tooltipTop) {
                        document.documentElement.scrollTop = parseInt(document.documentElement.scrollTop) + top;
                    }
                };
            }
        };
    }]);
/**
 * @ngdoc directive
 * @name Progress & usage indicators.att:usageBar
 *
 * @description
 *  <file src="src/usageBar/docs/readme.md" />
 *
 * @usage
 * <div class="progress progress-success" ddh-usage-bar="{{usageBarVal3}}" role="progressbar" aria-valuetext="40% usage"> </div>
 * 
    <div class="progress" ddh-usage-bar="{{usageBarVal2}}" is-animated='true' role="progressbar" aria-valuetext="20% usage"> </div>
        <span class="hidden-spoken">20% usage with animation</span> 

        <div class="usage-bar">
            <div class="usage-text"><i class="cssIcon-error valign-top"></i> 1/2 increments</div>
            <div class="usage-text text-right"><b>{{usageBarIncrementedVal1}}%</b> used</div>
        </div>
        <div class="progress increment progress-thin" ddh-usage-bar="{{usageBarIncrementedVal1}}" no-of-segments='2' bar-type-class='bar-success' ms-delay='5000' is-animated="true" role="progressbar" aria-valuetext="100% of the first half. 10% of the second half. 60% total used."></div>
        <div class="usage-bar">
            <div class="usage-text">Additional description text</div>
        </div>

 * @example
 *  <section id="code">   
 <b>HTML + AngularJS</b>
 <example module="ddh.att">
 <file src="src/usageBar/docs/demo.html" />
 <file src="src/usageBar/docs/demo.js" />
 </example>
 </section>
 *
 */
angular.module('ddh.att.usageBar', ['ddh.att.utilities']).
        directive('ddhUsageBar', ['$window', '$timeout', function($window, $timeout) {
                return {
                    restrict: 'A',
                    scope: {
                        noOfSegments: '=',
                        barTypeClass: '@?',
                        msDelay: '=?'
                    },
                    templateUrl: 'template/usageBar/usageBar.html',
                    link: function(scope, ele, attr) {

                        if (!(angular.isNumber(scope.noOfSegments) && scope.noOfSegments > 0)) {
                            scope.noOfSegments = 1;
                        }

                        scope.barsArray = [];

                        for (var i=0; i < scope.noOfSegments; i++) {
                            scope.barsArray.push({showItem : true});
                        }

                        if (!angular.isNumber(scope.msDelay)) {
                            scope.msDelay = 5000;
                        }
                        
                        var transition = 'width ' + scope.msDelay + 'ms';

                        var animateStyle = {
                            '-webkit-transition': transition,
                            '-moz-transition': transition,
                            '-ms-transition': transition,
                            '-o-transition': transition,
                            transition: transition
                        };

                        if (attr.isAnimated) {
                            for (var i = 0; i < scope.barsArray.length; i++) {
                                angular.forEach(animateStyle, function(value, key) {
                                    scope.barsArray[i][key] = value;
                                });
                            }
                        }

                        $timeout(function() {                            
                            var percentDivEach = 100/scope.noOfSegments;
                            
                            var fullBars = Math.floor(attr.ddhUsageBar / percentDivEach);
                            var cutOffBarWidth = attr.ddhUsageBar % percentDivEach;
                            var i=0;
                            while(i < fullBars) {
                                scope.barsArray[i].width = percentDivEach + '%';
                                scope.barsArray[i].showItem = true;
                                i++;
                            }
                            if(cutOffBarWidth > 0) {
                                scope.barsArray[i].width = cutOffBarWidth + '%';
                                scope.barsArray[i].showItem = true;
                                i++;
                            }
                            while(i < scope.barsArray.length) {
                                scope.barsArray[i].showItem = false;
                                i++;
                            }
                        });
                    }

                };

            }]);
     
angular.module("template/calendar/datepicker-popup.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/calendar/datepicker-popup.html",
    "<div id=\"datepicker\" class=\"datepicker dropdown-menu\" ng-class=\"{'datepicker-dropdown datepicker-orient-top': !inline, 'datepicker-orient-left': !inline && orientation === 'left', 'datepicker-orient-right': !inline && orientation === 'right'}\" ng-style=\"{position: inline && 'relative', 'z-index': inline && '0', top: !inline && position.top + 'px', left: !inline && position.left + 'px'}\" style=\"display: block;\" aria-hidden=\"false\" role=\"dialog presentation\" tabindex=\"0\">\n" +
    "    <div class=\"datepicker-days\" style=\"display: block;\">\n" +
    "        <table class=\"table-condensed\" role=\"presentation\">\n" +
    "            <thead>\n" +
    "                <tr ng-repeat=\"header in headers\">\n" +
    "                    <th colspan=\"7\" class=\"text-left\" style=\"width: 278px;\" ddh-append-element=\"header\"></th>\n" +
    "                </tr>\n" +
    "                <tr>\n" +
    "                    <th id=\"prev\" tabindex=\"0\" ddh-accessibility-click=\"13\" ddh-element-focus=\"!disablePrev && getFocus\" role=\"option\" ng-class=\"{'prev': !disablePrev}\" ng-style=\"{visibility: visibilityPrev}\" ng-click=\"!disablePrev && move(-1)\"><i class=\"icon-primary-left\"></i>Previous Month</th>\n" +
    "                    <th id=\"month\" tabindex=\"0\" ddh-element-focus=\"disablePrev && getFocus\" class=\"datepicker-switch\" colspan=\"{{rows[0].length - 2}}\">{{title}}</th>\n" +
    "                    <th id=\"next\" tabindex=\"0\" ddh-accessibility-click=\"13\" role=\"option\" ng-class=\"{'next': !disableNext}\" ng-style=\"{visibility: visibilityNext}\" ng-click=\"!disableNext && move(1)\"><i class=\"icon-primary-right\"></i>Next Month</th>\n" +
    "                </tr>\n" +
    "                <tr ng-show=\"labels.length > 0\">\n" +
    "                    <th id=\"{{label.post}}\" class=\"dow\" ng-repeat=\"label in labels\" aria-hidden=\"true\"><span aria-hidden=\"true\">{{label.pre}}</span></th>\n" +
    "                </tr>\n" +
    "            </thead>\n" +
    "            <tbody ddh-key type=\"table\" columns=\"7\">\n" +
    "                <tr ng-repeat=\"row in rows\">\n" +
    "                    <td headers=\"{{dt.header}}\" ddh-key-item=\"dt.focusable\" ddh-accessibility-click=\"13\" tabindex=\"{{(!dt.focusable && -1) || 0}}\" aria-hidden=\"{{(!dt.focusable && true) || false}}\" role=\"{{(dt.focusable && 'option') || ''}}\" aria-label=\"{{(dt.focusable && dt.date | date : 'EEEE, MMMM d') || ''}}\" aria-selected=\"{{(dt.focusable && (dt.selected || dt.dateRange) && true) || false}}\" ng-repeat=\"dt in row\" class=\"day magic\" ng-class=\"{'active': dt.focusable && dt.selected && !dt.dateRange, 'start-date': dt.focusable && dt.fromDate && dt.dateRange, 'between-date': dt.focusable && !dt.fromDate && !dt.selected && dt.dateRange, 'end-date': dt.focusable && dt.selected && dt.dateRange, 'old': dt.oldMonth, 'new': dt.nextMonth, 'disabled': dt.disabled, 'due-date': dt.dueDate, 'late-fee': dt.pastDue}\" title=\"{{(dt.focusable && dt.pastDue && legendMessage) || ''}}\" ng-click=\"!ngDisabled && !dt.disabled && select(dt.date)\">\n" +
    "                        <div aria-hidden=\"true\" tabindex=\"-1\" class=\"show-date\" ng-if=\"!(dt.oldMonth || dt.nextMonth)\">{{dt.label}}</div>{{(dt.focusable && dt.date | date : 'EEEE, MMMM d') || ''}}{{(dt.focusable && dt.dueDate && '. Bill-due-date.. ') || ''}}{{(dt.focusable && dt.pastDue && legendMessage) || ''}}</td>\n" +
    "                </tr>\n" +
    "            </tbody>\n" +
    "            <tfoot>\n" +
    "                <tr ng-repeat=\"footer in footers\">\n" +
    "                    <th colspan=\"7\" class=\"text-left\" style=\"width: 278px;\" ddh-append-element=\"footer\"></th>\n" +
    "                </tr>\n" +
    "            </tfoot>\n" +
    "        </table>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/calendar/datepicker.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/calendar/datepicker.html",
    "<div>\n" +
    "    <span class=\"icon-primary-calendar span12\" ng-class=\"{'disabled': ngDisabled}\" ng-transclude></span>\n" +
    "</div>");
}]);

angular.module("template/creditCard/creditCardImage.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/creditCard/creditCardImage.html",
    "<label for=\"ccForm.card\" class=\"pull-left\">Card number</label>");
}]);

angular.module("template/creditCard/cvcSecurityImg.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/creditCard/cvcSecurityImg.html",
    "<div>              \n" +
    "   <button id=\"tooltipButton2\" class=\"btn icon-primary-tooltip lH26\" aria-label=\"Security Code help\" aria-expanded=\"{{showTooltipOnInput1}}\" aria-describedby=\"tooltipContent2\" ng-click=\"showTooltipOnclick()\" ng-blur=\"showTooltipOnInput1 = false\"></button>\n" +
    "   <div class=\"helpertext ABS\" id=\"tooltipContent2\" role=\"tooltip\" aria-live=\"polite\">\n" +
    "       <div class=\"popover-title\"></div>\n" +
    "       <div class=\"popover-content\">\n" +
    "            <p>\n" +
    "           <img ng-src=\"style/images/ccv2-security.png\" alt=\"The CVC security code is on the back of your card right after the credit card number.\" class=\"img_Inline mR25Em\">\n" +
    "            <!-- </p>\n" +
    "            <p> -->\n" +
    "           <img ng-src=\"style/images/ccv2-security-amex.png\" alt=\"The 4 digit CVC security code is on the front of the card.\" class=\"img_Inline\">\n" +
    "            </p>\n" +
    "       </div>\n" +
    "   </div>  \n" +
    "</div>\n" +
    "");
}]);

angular.module("template/creditCard/tooltipCardImage.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/creditCard/tooltipCardImage.html",
    "<div>              \n" +
    "   <button id=\"tooltipButtonCC\" class=\"btn icon-primary-tooltip lH26\" aria-label=\"Credit Card type\" aria-expanded=\"{{showTooltipOnInputCC}}\" aria-describedby=\"tooltipContentCC\" ng-click=\"showTooltipCCOnclick()\" ng-blur=\"showTooltipOnInputCC = false\"></button>\n" +
    "   <div class=\"helpertext\" id=\"tooltipContentCC\" role=\"tooltip\" aria-live=\"polite\">\n" +
    "       <div class=\"popover-title\">Accepted Cards</div>\n" +
    "       <div class=\"popover-content popover-content_IB\">\n" +
    "            <span class=\"css-sprite pull-right\">\n" +
    "            <span class=\"hidden-spoken\">We accept</span>\n" +
    "            <ul class=\"show-all mT10\">\n" +
    "                    <li class=\"css-sprite-mc\"><span class=\"hidden-spoken\">MasterCard</span></li>\n" +
    "                    <li class=\"css-sprite-visa\"><span class=\"hidden-spoken\">Visa</span></li>\n" +
    "                    <li class=\"css-sprite-amex\"><span class=\"hidden-spoken\">American Express</span></li>\n" +
    "                    <li class=\"css-sprite-discover\"><span class=\"hidden-spoken\">Discover</span></li>\n" +
    "                    <li class=\"css-sprite-diners\"><span class=\"hidden-spoken\">Diners</span></li>\n" +
    "            </ul>\n" +
    "            </span>\n" +
    "       </div>\n" +
    "   </div>  \n" +
    "</div>\n" +
    "");
}]);

angular.module("template/dropdownVariations/ddhDropdownVariations.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/dropdownVariations/ddhDropdownVariations.html",
    "<div class=\"form-row\">\n" +
    "    <label for=\"select3cc\">Dropdown with optgroup</label>\n" +
    "    <span class=\"selectWrap\"><input type=\"hidden\" id=\"select3cc-value\" class=\"awd-select-value\" value=\"a1xxxc\">\n" +
    "       <button id=\"select3cc\" class=\"awd-select isWrapped closed\" aria-expanded=\"false\" aria-haspopup=\"true\" role=\"combobox\" value=\"a1xxxc\">Bank account ending 1234</button><div id=\"select3cc-wrapper\" class=\"selectWrapper\"><ul class=\"awd-select-list\" id=\"select3cc-dropdown\" tabindex=\"-1\" aria-labeledby=\"select3cc-button\" style=\"display: none;\">\n" +
    "       <li class=\"optgroup-wrapper\" aria-selected=\"false\">Late payment arrangements\n" +
    "          <ul class=\"optgroup\"><li value=\"6xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">Mail payment by 12/15</li><li value=\"7xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">In-store by 12/19</li><li value=\"8xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">Call by 12/19</li></ul>\n" +
    "        </li>\n" +
    "       <li class=\"optgroup-wrapper\" aria-selected=\"false\">Online payment options\n" +
    "          <ul class=\"optgroup\"><li value=\"1xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"true\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\" selected=\"selected\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">Bank account ending 1234</li><li value=\"2xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">Card ending 4567</li><li value=\"3xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">New bank account</li><li value=\"4xxxc\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/retail/autopay.svg\">New credit/debit card</li></ul>\n" +
    "       </li>\n" +
    "       <li value=\"a1xxxc\" aria-selected=\"true\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\" selected=\"selected\">Bank account ending 1234</li>\n" +
    "       <li value=\"a2xxxc\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\">Card ending 4567</li>\n" +
    "    </ul></div></span>\n" +
    " </div>\n" +
    "<div class=\"form-row\">\n" +
    "    <label for=\"big1\">Dropdown with images</label>\n" +
    "    <span class=\"selectWrap large\"><input type=\"hidden\" id=\"big1-value\" class=\"awd-select-value\"><button class=\"awd-select large isWrapped closed\" id=\"big1\" aria-expanded=\"false\" aria-haspopup=\"true\" role=\"combobox\">Select</button><div id=\"big1-wrapper\" class=\"selectWrapper\"><ul class=\"awd-select-list\" id=\"big1-dropdown\" tabindex=\"-1\" aria-labeledby=\"big1-button\" style=\"display: none;\">\n" +
    "\n" +
    "       <li value=\"big1a\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/directv.svg\" aria-selected=\"true\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\" selected=\"selected\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/directv.svg\">Lynksys-WRT54G Wireless-G</li>\n" +
    "       <li value=\"big1b\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/cctv.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/cctv.svg\">DLink-DI-624 Wireless-G Broadband</li>\n" +
    "       <li value=\"big1c\" data-img-src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/gps.svg\" aria-selected=\"false\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\"><img src=\"../styles/att/rwd/1.0.0/css/images/svg/master/devices/gps.svg\">Cisco-RV110 W Wireless</li>\n" +
    "    </ul></div></span>\n" +
    " </div>");
}]);

angular.module("template/dropdowns/ddhDropdownDesktop.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/dropdowns/ddhDropdownDesktop.html",
    "<span ddh-key prev=\"38\" next=\"40\" enable-search class=\"selectWrap\" ng-class=\"{'large': (stype === 'large')}\">\n" +
    "    <input ddh-dropdown-validation type=\"hidden\" id=\"{{dropdownId}}\" name=\"{{dropdownName}}\" class=\"awd-select-value\" ng-required=\"dropdownRequired\" ng-model=\"currentSelected.value\">\n" +
    "		<button type=\"button\" ddh-dropdown-button ng-disabled=\"disabled\" class=\"awd-select isWrapped\" role=\"combobox\" aria-describedby=\"{{ariaDescribedbyId}}\" aria-label=\"{{labelText}} {{currentSelected.text}} is selected: press enter key then use up or down arrow keys to select option.\" aria-expanded=\"{{toggleFlag}}\" aria-haspopup=\"true\" aria-live=\"{{(!toggleFlag && 'polite') || ''}}\" aria-atomic=\"{{(!toggleFlag && true) || ''}}\" ng-click=\"toggleDropdown()\" ng-class=\"{'active': toggleFlag, 'closed': !toggleFlag, 'large': (stype === 'large')}\" ng-bind-html=\"currentSelected.text\"></button>\n" +
    "    <div class=\"selectWrapper\">\n" +
    "        <ul role=\"listbox\" class=\"awd-select-list\" tabindex=\"-1\" ng-show=\"toggleFlag\"></ul>\n" +
    "    </div>\n" +
    "</span>\n" +
    "");
}]);

angular.module("template/dropdowns/ddhDropdownGroupDesktop.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/dropdowns/ddhDropdownGroupDesktop.html",
    "<li ddh-dropdown-group-desktop class=\"optgroup-wrapper\">{{groupHeader}}\n" +
    "    <ul class=\"optgroup\" role=\"group\" aria-label=\"{{groupHeader}}\"></ul>\n" +
    "</li>");
}]);

angular.module("template/dropdowns/ddhDropdownListDesktop.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/dropdowns/ddhDropdownListDesktop.html",
    "<li ddh-dropdown-list-desktop ddh-key-item ddh-accessibility-click=\"13,9,27\" aria-selected=\"{{currentSelected.value === dropdownListValue}}\" class=\"awd-select-list-item\" tabindex=\"0\" role=\"option\" ng-click=\"selectDropdownItem()\" ng-focus=\"updateDropdownValue()\"></li>");
}]);

angular.module("template/expandCollapse/expandCollapse-group.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/expandCollapse/expandCollapse-group.html",
    "<div>\n" +
    "    <div id=\"tab{{index}}\" class=\"toggle-header\" aria-selected=\"{{focused}}\" aria-expanded=\"{{isOpen}}\" aria-controls=\"panel{{index}}\" ng-class=\"{opened: isOpen, closed: !isOpen}\" role=\"tab\" tabindex=\"0\" ng-click=\"toggle()\" ddh-accordion-transclude=\"heading\">\n" +
    "        <div ng-switch on=\"isOpen\">\n" +
    "            <i class=\"icon-primary-accordion-plus\" ng-switch-when=\"false\"></i>\n" +
    "            <i class=\"icon-primary-accordion-minus\" ng-switch-when=\"true\"></i>\n" +
    "        </div>\n" +
    "        <span>{{heading}} <span class=\"hidden-spoken\" ng-if=\"isOpen && isMobile\">Expanded</span><span class=\"hidden-spoken\" ng-if=\"!isOpen && isMobile\">Collapsed</span></span>\n" +
    "    </div>\n" +
    "    <div class=\"accordion-content clearfix\" aria-labelledby=\"tab{{index}}\" role=\"tabpanel\" id=\"panel{{index}}\" aria-hidden=\"{{!isOpen}}\" aria-expanded=\"{{isOpen}}\" ng-class=\"{'accordion-content-hack--visible': isOpen, 'accordion-content-hack--hidden': !isOpen}\" tabindex=\"{{isOpen}}\" role=\"tabpanel\" ddh-collapse=\"!isOpen\" style=\"z-index:1\" ng-transclude>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/expandCollapse/expandCollapse-table.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/expandCollapse/expandCollapse-table.html",
    "<div>\n" +
    "    <div id=\"tab{{index}}\" class=\"toggle-header\" aria-selected=\"{{focused}}\" aria-expanded=\"{{isOpen}}\" aria-controls=\"panel{{index}}\" ng-class=\"{opened: isOpen, closed: !isOpen}\"  role=\"tab\" tabindex=\"0\" ng-click=\"toggle()\">\n" +
    "        <div class=\"faux-table-cell\" >\n" +
    "           <div ng-switch on=\"isOpen\">\n" +
    "               <i class=\"icon-primary-accordion-plus\" ng-switch-when=\"false\"></i>\n" +
    "               <i class=\"icon-primary-accordion-minus\" ng-switch-when=\"true\"></i>\n" +
    "           </div>\n" +
    "           <span ng-bind-html=\"column1 | ddhUnsafe\"></span>\n" +
    "        </div>\n" +
    "        <div class=\"faux-table-cell\">\n" +
    "           {{column2}}\n" +
    "        </div>                        \n" +
    "    </div>\n" +
    "    <div class=\"accordion-content\" id=\"panel{{index}}\" aria-hidden=\"{{!isOpen}}\" aria-expanded=\"{{isOpen}}\" tabindex=\"{{isOpen}}\" role=\"tabpanel\" ddh-collapse=\"!isOpen\" ng-transclude>                                 \n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/expandCollapse/expandCollapse.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/expandCollapse/expandCollapse.html",
    "<div ng-transclude></div>");
}]);

angular.module("template/filmstrip/ddhFilmstrip.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/filmstrip/ddhFilmstrip.html",
    "<div class=\"filmstrip\" ng-class=\"{'filmstrip-icons': (filmstripType==='icon'), 'filmstrip-videos-small': (filmstripType==='videoSmall'), 'filmstrip-videos-large': (filmstripType==='videoLarge'), 'no-header': !filmstripHeading}\">\n" +
    "    <h3 class=\"filmstrip-header\" ng-if=\"filmstripHeading\" ng-bind=\"filmstripHeading\"></h3>\n" +
    "    <ul class=\"filmstrip-strip\" role=\"listbox\" ng-class=\"{'row-fluid-nowrap list no-arrow': (filmstripType=='videoSmall') || (filmstripType=='videoLarge')}\" ng-transclude></ul>\n" +
    "</div>\n" +
    "");
}]);

angular.module("template/filmstrip/ddhFilmstripContent.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/filmstrip/ddhFilmstripContent.html",
    "<li role=\"option\" ng-transclude></li>\n" +
    "");
}]);

angular.module("template/footer/footer_column_switch_tpl.html", []).run(["$templateCache", function($templateCache) {
    $templateCache.put("template/footer/footer_column_switch_tpl.html",
        "<div class=\"footer-columns \" ng-class=\"{'five-column':!threeColumnsbasedFooter, 'three-column':threeColumnsbasedFooter}\" ng-repeat=\"item in footerLinkItems\">\n" +
        "    <label tab-index=\"0\">{{item.categoryName}}</label>\n" +
        "    <ul>\n" +
        "        <li>{{scope.threeColumnsbasedFooter}}</li>\n" +
        "        <li ng-repeat=\"i in item.values\">\n" +
        "            <a href=\"{{i.href}}\">{{i.displayLink}}</a>  \n" +
        "        </li>\n" +
        "    </ul>\n" +
        "\n" +
        "</div>\n" +
        "\n" +
        "<ng-transclude></ng-transclude>");
}]);

angular.module("template/leftNavigation/leftNavigation.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/leftNavigation/leftNavigation.html",
    "<div class=\"ddh-nav-menu\" ng-init=\"(showmenu = true)\" ng-class=\"{false: 'left-menu-collapsed'}[showmenu]\">\n" +
    "    <div class=\"ddh-subnav-container\">\n" +
    "        <ul class=\"ddh-subnav-content\">\n" +
    "			 <li><a ng-click=\"(showmenu = !showmenu)\" class=\"text-right\"><i ng-class=\"{true: 'icon-primary-left', false: 'icon-primary-right'}[showmenu]\">&nbsp;</i></a></li>" +
    "            <li ng-repeat=\"menu in menuData\" ng-click=\"toggleNav($index)\">" +
    "				 <span class=\"menu-icon\"><i class=\"{{menu.imageSrc}}\"></i>&nbsp;&nbsp;</span>" +
    "				 <a ng-class=\"{expand: idx==$index}\" aria-label=\"{{menu.name}}\" title=\" \" aria-expanded=\"{{(idx==$index)?true:false;}}\" ng-click=\"navigate(menu.href)\" ng-show=\"(showmenu === true)\">{{menu.name}}<i ng-if=\"(menu.menuItems.length > 0)\" ng-class=\"{false:'icon-primary-accordion-plus', true:'icon-primary-accordion-minus'}[idx==$index]\"></i></a>\n" +
    "                <ul ng-class=\"{expand: idx==$index}\">\n" +
    "                    <li ng-repeat=\"menuItem in menu.menuItems\" ng-click=\"liveLink($event, $index, $parent.$index)\"><a aria-hidden=\"{{!(idx==$parent.$index)}}\" aria-label=\"{{menuItem.name}}\" title=\" \" href=\"{{menuItem.href}}\" tabindex=\"{{(idx==$parent.$index)?0:-1;}}\" ng-class=\"{active: itemIdx==$index && navIdx==$parent.$index}\">{{menuItem.name}}</a></li>\n" +
    "                </ul>\n" +
    "            </li>\n" +
    "        </ul>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/loaderAnimation/modalLoader.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/loaderAnimation/modalLoader.html",
    "<div class=\"modal-header modal-header-landscape\">\n" +
    "    <h2 id=\"myModalLabel\" tabindex=\"0\">Modal header text</h2>\n" +
    "    <div class=\"corner-button\">\n" +
    "        <button type=\"button\" class=\"close\" data-dismiss=\"modal\" aria-hidden=\"true\" aria-label=\"Close\" tabindex=\"-1\" ng-click=\"$dismiss('cancel')\"></button>\n" +
    "    </div>\n" +
    "</div>\n" +
    "<div ng-hide=\"loadingTracker.active()\">Content loaded!</div>\n" +
    "<div class=\"modal-body modal-body-landscape\">\n" +
    "    <ddh-progress-tracker text=\"loading...\" class=\"span4\" style=\"padding-top: 42%;margin: 0 auto;\"></ddh-progress-tracker>\n" +
    "</div>");
}]);

angular.module("template/modalsAndAlerts/backdrop.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/modalsAndAlerts/backdrop.html",
    "<div class=\"ddh-modal-backdrop fade in hidden-by-modal\" aria-hidden=\"true\" tabindex=\"-1\"></div>");
}]);

angular.module("template/modalsAndAlerts/window.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/modalsAndAlerts/window.html",
    "<div class=\"modalwrapper active {{windowClass}}\" role=\"dialog\" tabindex=\"-1\" aria-labelledby=\"{{title}}\" aria-describedby=\"{{content}}\">\n" +
    "    <div class=\"modal fade in {{sizeClass}}\" ng-class=\"{'modal-landscape': isModalLandscape}\" ng-transclude></div>\n" +
    "</div>");
}]);

angular.module("template/pagination/pagination.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/pagination/pagination.html",
    "<div class=\"pager\">\n" +
    "    <div ng-if=\"notMobile\">\n" +
    "        <a tabindex=\"0\" href=\"javascript:void(0)\" class=\"pager__item--prev\" ddh-accessibility-click=\"13,32\" title=\"previous\" ng-click=\"prev($event)\" ng-if=\"totalPages > 10\" ng-class=\"currentPage <= 1 ? 'disabled': '' \">\n" +
    "            <i class=\"icon-primary-left\"></i>\n" +
    "        </a>\n" +
    "        <a tabindex=\"{{currentPage === 1 ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" ng-class=\"{'pager__item--noclick': currentPage === 1}\" title=\"Page 1\" ng-if=\"totalPages > 10 && currentPage > 6\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(1, $event)\">1 <span class=\"offscreen-text\" ng-if=\"currentPage === 1\"> is selected</span></a>\n" + 
    "        <a tabindex=\"{{currentPage === 2 ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" ng-class=\"{'pager__item--noclick': currentPage === 2}\" title=\"Page 2\" ng-if=\"totalPages > 10 && currentPage > 6\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(2, $event)\">2 <span class=\"offscreen-text\" ng-if=\"currentPage === 2\"> is selected</span></a>\n" + 
    "        <span class=\"pager__item\" ng-if=\"totalPages > 10 && currentPage > 6\">...</span>\n" +
    "        <a tabindex=\"{{checkSelectedPage(page) ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" title=\"Page {{page}}\" ddh-element-focus=\"isFocused(page)\" ng-repeat=\"page in pages\" ng-class=\"{'pager__item--active': checkSelectedPage(page), 'pager__item--noclick': checkSelectedPage(page)}\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(page, $event)\">{{page}} <span class=\"offscreen-text\" ng-if=\"currentPage === page\"> is selected</span></a>\n" + 
    "        <span class=\"pager__item\" ng-if=\"totalPages > 10 && currentPage <= totalPages - 5\">...</span>\n" +
    "        <a tabindex=\"{{checkSelectedPage(page) ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" ng-class=\"{'pager__item--noclick': checkSelectedPage(page)}\" title=\"Page {{totalPages-1}}\" ng-if=\"totalPages > 10 && currentPage <= totalPages - 5\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(totalPages-1, $event)\">{{totalPages-1}} <span class=\"offscreen-text\" ng-if=\"currentPage === totalPages-1\"> is selected</span></a>\n" + 
    "        <a tabindex=\"{{currentPage === totalPages ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" ng-class=\"{'pager__item--noclick': currentPage === totalPages}\" title=\"Page {{totalPages}}\" ng-if=\"totalPages > 10 && currentPage <= totalPages - 5\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(totalPages, $event)\">{{totalPages}} <span class=\"offscreen-text\" ng-if=\"currentPage === totalPages\"> is selected</span></a>\n" + 
    "        <a tabindex=\"0\" href=\"javascript:void(0)\" class=\"pager__item--next\" ddh-accessibility-click=\"13,32\" title=\"Next\" ng-click=\"next($event)\" ng-if=\"totalPages > 10\" ng-class=\"currentPage >= totalPages ? 'disabled' :'' \">\n" +
    "            <i class=\"icon-primary-right\"></i>\n" +
    "        </a>\n" +
    "        <span class=\"fieldLabel\" ng-show=\"totalPages > 10 && showInput === true\">    \n" +
    "           Go to Page:\n" +
    "           <input class=\"pager__item--input\" type=\"number\" only-digits=\"\" placeholder=\"\" maxlength=\"2\" ng-model=\"$parent.currentPage\" aria-label=\"Go to Page {{currentPage}}\"/>\n" +
    "       </span>\n" +
    "    </div>\n" +
    "    <div ng-if=\"isMobile\" ng-swipe-right=\"prev()\" ng-swipe-left=\"next()\" ng-class=\"isMobile ? 'mobile-view': '' \">\n" +
    "        <a tabindex=\"{{checkSelectedPage(page) ? -1 : 0}}\" href=\"javascript:void(0)\" class=\"pager__item pager__item--link\" title=\"Page {{page}}\" ddh-element-focus=\"isFocused(page)\" ng-repeat=\"page in pages\" ng-class=\"{'pager__item--active': checkSelectedPage(page), fade1: ($index == 0 && currentPage > meanVal+1),  fade2: ($index == 1 && currentPage > meanVal+1), fadesl: ($index == pages.length-2 && currentPage < totalPages - meanVal),  fadel: ($last && currentPage < totalPages - meanVal), 'pager__item--noclick': checkSelectedPage(page)}\" ddh-accessibility-click=\"13,32\" ng-click=\"selectPage(page, $event)\">{{page}}</a>\n" + 
    "    </div>\n" +
    "</div>\n" +
    "");
}]);

angular.module("template/searchField/searchField.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/searchField/searchField.html",
    "<div class=\"search-bar\">\n" +
    "    <div class='input-container'>\n" +
    "        <input type=\"text\" class=\"innershadow\" id=\"{{configObj.labelId}}\" ddh-search-input ng-model=\"inputModel\" ddh-reset ng-keydown=\"selectionIndex($event)\" tabindex=\"0\" placeholder=\"{{configObj.ghostText}}\" ng-click=\"showListFlag = inputModel == '' ? false: true;\" ng-focus=\"showListFlag = inputModel == '' ? false: true;\" ng-blur=\"blurInput()\" style=\"width:100%\" maxlength=\"{{configObj.maxLength}}\" title=\"{{inputModel}}\" aria-label=\"{{inputModel.length>0?inputModel:configObj.ghostText}}\" aria-live=\"assertive\"/>\n" +
    "<!--        <div class=\"icon-primary-wrapper\" tabindex=\"\" role=\"button\" ng-click=\"startSearch()\" ddh-accessibility-click=\"13,32\" aria-label=\"Search\" ng-focus=\"showListFlag = false\">-->\n" +
    "            <button class=\"btn-search\" ng-click=\"startSearch()\" ddh-accessibility-click=\"13,32\" aria-label=\"Search\" tabindex=\"0\" ng-focus=\"showListFlag = false\" type=\"button\"></button>\n" +
    "<!--        </div>-->\n" +
    "    </div>\n" +
    "    <div class=\"search-suggestion-wrapper\" ng-if=\"filterList.length >=0\" ng-show=\"showListFlag\">\n" +
    "        <ul class=\"search-suggestion-list\" role=\"listbox\">\n" +
    "            <li class=\"no-result\" ng-if=\"filterList.length == 0 && configObj.display\">{{configObj.resultText}}</li>\n" +
    "            <li class=\"search-suggestion-item\" ng-if=\"filterList.length\" ng-repeat=\"item in filterList | limitTo:configObj.noOfItemsDisplay track by $index\" ng-class=\"{active:isActive($index,filterList.length)}\" ng-click=\"selectItem($index,item.title)\">\n" +
    "                {{item.title}}\n" +
    "            </li>\n" +
    "        </ul>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/socialShareThis/socialShareThis.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/socialShareThis/socialShareThis.html",
    "<div class=\"social-share-this\" ng-class=\"{'move-left':moveLeft}\">\n" +
    "    <a href=\"javascript:void(0)\" ng-click=\"toggleSocialShare()\" role=\"button\" class=\"share-this standalone-link small\" aria-describedby=\"shareIcons_id_{{$id}}\" ng-class=\"{active: showIt}\">\n" +
    "        <i class=\"icon-primary-social-share-this\"></i>\n" +
    "        {{ddhSocialShare}}\n" +
    "    </a>\n" +
    "    <div id=\"shareIcons_id_{{$id}}\" aria-live=\"polite\" aria-atomic=\"false\" class=\"share-icons clearfix\" aria-hidden=\"{{!showIt}}\" ng-class=\"{active: showIt}\">\n" +
    "        <div class=\"social-arrow\"></div>\n" +
    "        <div ng-transclude></div>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("template/stepIndicator/stepIndicator.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/stepIndicator/stepIndicator.html",
    "<div class=\"step-indicator\">\n" +
    "    <h1 class=\"step-heading\"> \n" +
    "        {{stepIndicatorHeading}}\n" +
    "    </h1>\n" +
    "\n" +
    "    <ul class=\"steps\">\n" +
    "        <li ng-class=\"{'step-done':$index < currentIndex - 1 ,'step-on':$index == currentIndex - 1}\" \n" +
    "            ng-repeat=\"stepsItem in stepsItemsObject\"><p class=\"step-text\" data-sm-text=\"{{stepsItem.dataMobile}}\" \n" +
    "                                                     data-large-text=\"{{stepsItem.dataDesktop}}\">{{stepsItem.text}}</p>\n" +
    "            <span class=\"hidden-spoken\">\n" +
    "                {{($index < currentIndex - 1)? 'Complete. '+stepsItem.text+' '+stepsItem.dataMobile:''}} \n" +
    "                {{($index == currentIndex - 1)? 'In Progress. '+stepsItem.text+' '+stepsItem.dataMobile:''}} \n" +
    "                {{($index > currentIndex - 1)? 'Incomplete. '+stepsItem.text+' '+stepsItem.dataMobile:''}}\n" +
    "            </span>\n" +
    "        </li>\n" +
    "\n" +
    "    </ul>\n" +
    "</div>\n" +
    "\n" +
    "<ng-transclude></ng-transclude>");
}]);

angular.module("template/switches/switches-spanish.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/switches/switches-spanish.html",
    "<div class=\"switch-overlay\" aria-hidden=\"true\">\n" +
    "    <span class=\"btn-slider-on\"><span class=\"hidden-spoken\">seleccione para hacer </span><i class=\"activo\">activo</i></span>\n" +
    "    <span class=\"switch-handle\"></span>\n" +
    "    <span class=\"btn-slider-off\"><span class=\"hidden-spoken\">seleccione para hacer </span><i class=\"inactivo\">inactivo</i></span>\n" +
    "</div>");
}]);

angular.module("template/switches/switches.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/switches/switches.html",
    "<div class=\"switch-overlay\" aria-hidden=\"true\">\n" +
    "    <span class=\"btn-slider-on\">On</span>\n" +
    "    <span class=\"switch-handle\"></span>\n" +
    "    <span class=\"btn-slider-off\">Off</span>\n" +
    "</div>");
}]);

angular.module("template/table/ddhTable.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/table/ddhTable.html",
    "<table class=\"tablesorter tablesorter-default\" ng-transclude></table>\n" +
    "");
}]);

angular.module("template/table/ddhTableBody.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/table/ddhTableBody.html",
    "<td ng-transclude></td>\n" +
    "");
}]);

angular.module("template/table/ddhTableHeader.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/table/ddhTableHeader.html",
        "<th role=\"columnheader\" scope=\"col\" aria-live=\"polite\" aria-sort=\"{{sortPattern !== 'null' && '' || sortPattern}}\" aria-label=\"{{headerName}} {{sortable !== 'false' && ': activate to sort' || ' '}} {{sortPattern !== 'null' && '' || sortPattern}}\" tabindex=\"{{sortable !== 'false' && '0' || '-1'}}\" class=\"tablesorter-header\" ng-class=\"{'tablesorter-headerAsc': sortPattern === 'ascending', 'tablesorter-headerDesc': sortPattern === 'descending', 'tablesort-sortable': sortable !== 'false', 'sorter-false': sortable === 'false'}\" ddh-accessibility-click=\"13,32\" ng-click=\"(sortable !== 'false') && sort();\"><div class=\"tablesorter-header-inner\" ng-transclude></div></th>");
}]);

angular.module("template/tabs/ddhTab.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/tabs/ddhTab.html",
        "<li aria-selected=\"{{isTabActive()}}\" aria-controls=\"{{tabPanelId}}\" class=\"tab\" \n" +
    "    ng-class=\"{'active': isTabActive()}\" ng-click=\"clickTab()\" ng-hide=\"tabItem.disabled\">\n" +
        "    <a href=\"javascript:void(0)\" aria-expanded=\"{{isTabActive() && !tabItem.disabled}}\" tabindex=\"{{!tabItem.disabled?0:-1}}\"\n" +
        "       ng-disabled=\"tabItem.disabled\" \n" +
    "       ddh-accessibility-click=\"32,13\" ng-transclude></a>\n" +
    "       <span class=\"hidden-spoken\" ng-if=\"isTabActive()\">Active tab</span>\n" + 
    "</li>");
}]);

angular.module("template/tabs/ddhTabset.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/tabs/ddhTabset.html",
        "<ul class=\"tabs promo-tabs\" ng-transclude></ul>");
}]);

angular.module("template/usageBar/usageBar.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("template/usageBar/usageBar.html",
    "<div class=\"bar {{barTypeClass}}\" ng-style=\"item\" ng-repeat=\"item in barsArray\" ng-if=\"item.showItem\"></div>");
}]);
